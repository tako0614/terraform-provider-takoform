import{_ as a,o,c as i,a2 as t}from"./chunks/framework.DzNHXt93.js";const m=JSON.parse('{"title":"Data-only Form Package","description":"","frontmatter":{},"headers":[],"relativePath":"spec/form-package/index.md","filePath":"spec/form-package/index.md"}'),n={name:"spec/form-package/index.md"};function r(c,e,s,d,l,p){return o(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="data-only-form-package" tabindex="-1">Data-only Form Package <a class="header-anchor" href="#data-only-form-package" aria-label="Permalink to &quot;Data-only Form Package&quot;">​</a></h1><p>A Form Package is a closed local directory with a root <code>package-index.json</code> and exactly the payload files listed by that index. Requirement keywords are used as described in <a href="./../conformance.html"><code>../conformance.md</code></a>. The normative Draft 2020-12 index schemas are the current family <a href="/schemas/v1alpha4/package-index.schema.json"><code>v1alpha4</code></a> profile carrying namespaced-group <code>forms.takoform.com</code> family FormRefs, the retained provider-v2 <a href="../../formpackage/schemas/package-index-v1alpha3.schema.json"><code>v1alpha3</code></a> profile for v1alpha2 FormRefs, and the retained Legacy <a href="../../formpackage/schemas/package-index-v1alpha2.schema.json"><code>v1alpha2</code></a> and <a href="../../formpackage/schemas/package-index.schema.json"><code>v1alpha1</code></a> profiles. The v1alpha2 envelope is immutable and content-addressed, but it refers to a v1alpha1 FormRef; it cannot carry a current v1alpha2 Form. The v1alpha3 envelope likewise carries only <code>forms.takoform.com/v1alpha2</code> FormRefs and cannot carry a family Form.</p><p>One package MUST contain exactly one Form Definition and therefore exactly one FormRef. There is no <code>packageId</code> and no multi-form <code>definitions</code> collection in this contract. A compatibility set, catalog, or host migration map is an external data object that points to multiple exact <code>(FormRef, packageDigest)</code> pairs; it is not a wider Form Package. Each Form is distributed independently; one package never carries a catalog-wide version or multiple definitions.</p><p>A Form Package carries a Form Definition and nothing else. There is no Interface Package and no Binding Package: those contracts are digest-bound documents distributed with this repository, and this envelope cannot carry one — the index is closed, fixes <code>kind</code> to <code>FormPackage</code>, and requires a <code>formRef</code> (<a href="./../decisions/0021-third-party-forms-and-contract-distribution.html">decision 0021</a>, <a href="./../interface-contract/"><code>../interface-contract/</code></a>).</p><h2 id="index-and-identity" tabindex="-1">Index and identity <a class="header-anchor" href="#index-and-identity" aria-label="Permalink to &quot;Index and identity&quot;">​</a></h2><p>The current family index has the fixed identity <code>packages.forms.takoform.com/v1alpha4</code> / <code>FormPackage</code> with an exact namespaced-group <code>FormRef</code>; the retained provider-v2 index has the identity <code>packages.forms.takoform.com/v1alpha3</code> / <code>FormPackage</code> with an exact <code>forms.takoform.com/v1alpha2</code> <code>FormRef</code>. Both carry one <code>definitionPath</code> and a lexicographically sorted <code>files</code> array. Every file entry records a canonical relative slash path, an allowlisted data media type, its byte length, and a lowercase <code>sha256:</code> digest over the exact payload bytes.</p><p>The package&#39;s semantic identity is SHA-256 over the RFC 8785 canonical index. The index does not list itself. The <code>FormRef.schemaDigest</code> separately covers the RFC 8785 canonical Form Definition. An archive is only transport: its headers and compression MUST NOT contribute to either identity.</p><p><code>packageDigest</code> is the verifier result used by an external catalog or mapping; it is not a self-referential field inside <code>package-index.json</code>.</p><p>The content-addressed v1alpha2 and v1alpha3 profiles deliberately have no <code>packageVersion</code>. Their publication locators use the full Package Digest as <code>sha256-&lt;hex&gt;</code> while <code>FormRef.definitionVersion</code> remains the only Form compatibility SemVer. The v1alpha2 profile remains accepted only for exact Legacy v1alpha1 FormRefs. The v1alpha1 profile and its required <code>packageVersion</code> likewise remain accepted only for exact Legacy read, verification, recovery, and migration.</p><h2 id="local-verifier" tabindex="-1">Local verifier <a class="header-anchor" href="#local-verifier" aria-label="Permalink to &quot;Local verifier&quot;">​</a></h2><p><code>../../formpackage/</code> and <code>../../cmd/form-package/</code> implement a library and CLI verifier. Verification performs no network access and executes no package content. It rejects:</p><ul><li>duplicate JSON names, invalid UTF-8/Unicode, non-finite numbers, and negative zero before RFC 8785 canonicalization;</li><li>a missing, duplicate, unsorted, unlisted, or extra payload;</li><li>digest, byte-size, media-type, FormRef, or definition identity mismatches;</li><li>absolute, traversal, backslash, volume/URI-like, or non-canonical paths;</li><li>symlinks, executable mode bits, executable-code extensions, devices, sockets, and pipes;</li><li>non-UTF-8/NUL text and all forbidden Form Definition content classes, including boundary-delimited singular and plural sensitive field forms;</li><li>portable-schema object admission that cannot be proven closed, cyclic or non-local references, and proofs exceeding 64 graph edges or the combined 4096 schema-node/local-reference operation budget;</li><li>portable schemas whose saturating worst-case validation-work estimate exceeds 16,384 schema evaluations, including repeated expansion through shared local <code>$ref</code> DAG edges;</li><li><code>contentEncoding</code>, <code>contentMediaType</code>, or <code>contentSchema</code>, because portable Forms do not decode or transform an embedded second document;</li><li>the legacy <code>dependencies</code> applicator (use <code>dependentRequired</code> or <code>dependentSchemas</code>); and</li><li>Form Definitions with more than 32 positive conformance fixtures or more than 32 negative conformance fixtures. These are independent per-class limits, not one combined 32-fixture budget.</li></ul><p>Local <code>$ref</code> targets are admitted once per canonical JSON Pointer with explicit <code>visiting</code>/<code>done</code> states. Shared acyclic schema graphs therefore cost linear proof work. The separate validation-work estimate still charges every local reference occurrence because a fixture validator may revisit the same target through every branch. Before calling the real validator, a second instance-aware estimate charges <code>items</code>, <code>contains</code>, <code>additionalProperties</code>, <code>propertyNames</code>, and the corresponding unevaluated keywords once per concrete fixture element or property. Both estimates saturate at the same 16,384-evaluation limit. Desired and observed schemas are each compiled once before the bounded fixture loop. Cycles and resource-exhaustion inputs fail closed.</p><p>The published JSON Schemas are normative structural minima. The semantic identity, filesystem closure, canonicalization, reference-proof, validation-work, fixture, and portable-content rules enforced here are also normative. Schema validity alone is therefore necessary but not sufficient for Form Package conformance.</p><p>Allowed payload media types are the Form Definition type, JSON Schema, generic JSON fixture data, Markdown, and plain text. The verifier limits index, file, and file-count sizes before reading content.</p><p>On Darwin, DragonFly BSD, FreeBSD, Linux, NetBSD, and OpenBSD, the verifier holds the package root directory descriptor and resolves every payload path component relative to it. Intermediate components are opened as directories with <code>O_NOFOLLOW</code>; the final component uses <code>O_NOFOLLOW | O_NONBLOCK</code> and is then required to be the same inventoried regular, non-executable file. This contains payload reads beneath the held root and avoids blocking on a file swapped to a pipe. Inventory and final metadata fences detect ordinary mutation, but the verifier does not claim to create an atomic filesystem snapshot against a malicious concurrent writer.</p><p>On other operating systems, callers must copy or extract into an immutable, private staging directory, close the writer, and only then call the verifier. The pathname, identity, and metadata fences on those systems are defense in depth and do not replace that immutable-staging precondition.</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/form-package verify PATH</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/form-package canonicalize FILE.json</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/form-package digest FILE.json</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/form-package validate-revocation STATEMENT.json</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/form-package validate-revocation-checkpoint CHECKPOINT.json</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/form-package conformance</span></span></code></pre></div><h2 id="release-boundary" tabindex="-1">Release boundary <a class="header-anchor" href="#release-boundary" aria-label="Permalink to &quot;Release boundary&quot;">​</a></h2><p>The repository-owned release tooling and protected workflows are documented in <a href="./../../release/form-packages.html"><code>../../release/form-packages.md</code></a>. A package source under <code>forms/releases/&lt;release-id&gt;/sha256-&lt;package-digest-hex&gt;/</code> is re-verified, canonicalized, deterministically archived, described by SPDX 2.3 and SLSA v1 evidence, and signed through a keyless Sigstore v0.3 bundle whose identity is bound to the exact repository and protected-main workflow. The reversible release ID is <code>k-</code> plus lowercase unpadded base32 of the exact FormRef Kind; the dispatcher separately verifies the exact <code>forms/&lt;release-id&gt;/sha256-&lt;package-digest-hex&gt;</code> source tag and approved commit before signing. Legacy v1alpha1 locators retain their existing <code>&lt;packageVersion&gt;</code> directory and <code>v&lt;packageVersion&gt;</code> tag unchanged. Legacy content-addressed v1alpha2 packages and retained provider-v2 v1alpha3 packages use their full digest locator, but only v1alpha3 may carry a retained v1alpha2 FormRef. The canonical index bytes—not archive metadata—remain the signed semantic subject.</p><h2 id="deliberate-non-goals-of-the-local-verifier" tabindex="-1">Deliberate non-goals of the local verifier <a class="header-anchor" href="#deliberate-non-goals-of-the-local-verifier" aria-label="Permalink to &quot;Deliberate non-goals of the local verifier&quot;">​</a></h2><p>The local verifier does not extract archives, fetch/install remote packages, verify Sigstore publisher identity, consume revocation feeds, activate a Form, publish a provider, or execute adapters. Those trust operations stay in the release or host layer. A package is not publishable merely because this local verifier accepts it, and a checked-in workflow is not proof that a live release exists.</p>`,22)])])}const f=a(n,[["render",r]]);export{m as __pageData,f as default};
