import{_ as t,o as i,c as a,a2 as o}from"./chunks/framework.LJP3gfxI.js";const u=JSON.parse(`{"title":"0022 — A host answers for an exact Form, and a relation pins its target's contract","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0022-relations-pin-the-target-contract.md","filePath":"spec/decisions/0022-relations-pin-the-target-contract.md"}`),n={name:"spec/decisions/0022-relations-pin-the-target-contract.md"};function s(r,e,d,h,c,l){return i(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="_0022-—-a-host-answers-for-an-exact-form-and-a-relation-pins-its-target-s-contract" tabindex="-1">0022 — A host answers for an exact Form, and a relation pins its target&#39;s contract <a class="header-anchor" href="#_0022-—-a-host-answers-for-an-exact-form-and-a-relation-pins-its-target-s-contract" aria-label="Permalink to &quot;0022 — A host answers for an exact Form, and a relation pins its target&#39;s contract&quot;">​</a></h1><ul><li>Status: accepted; rule 2 was amended on 2026-08-09 to say what a resolved lookup must ANSWER with (see &quot;An exact identity answers with the Definition&#39;s own bytes&quot; below), because resolving the right Definition and serving its contract turned out to be two promises and the lane only measured the first</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Two rules in this lane were written as if a Form line held one contract at a time, and a resource named one thing forever. Neither is true, and the lane had no way to notice.</p><p><strong>The exact ref stopped at the provider.</strong> Decision <a href="./0011-resource-identity-generation-and-revision.html">0011</a> settled that a resource&#39;s Form semantic identity is its exact FormRef, and decision <a href="./0017-provider-state-survives-form-evolution-and-interruption.html">0017</a> reworked the provider registry around that: a <code>Supported</code> map keyed by the whole <code>{apiVersion, kind, definitionVersion, schemaDigest}</code> tuple, separate from the one <code>DefaultCreates</code> entry a NEW resource is created under. The host side of the same promise was never built. The reference host&#39;s catalog was keyed by group and kind, so <code>ModuleWorker@0.1.0</code> and <code>ModuleWorker@0.2.0</code> could not be installed at once, and a stored resource carried a group and a kind rather than the ref it was created under. <code>exact-form-ref-fails-closed-on-unknown-definition</code> proved the host refuses a definition version it does not have — the only case a one-contract-per-kind host CAN get right. The case the provider actually depends on, a host holding two contracts of one line and answering each request about the right one, was unrepresentable in the lane, so no check could prove it and a host that matched by kind would have passed.</p><p><strong>A relation pinned an incarnation but not a contract.</strong> Decision <a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a> made a reference the closed object <code>{apiVersion, kind, name}</code> and made a host store the resolved target&#39;s <code>metadata.uid</code>, so a target deleted and re-created is reported rather than silently adopted. What a host verifies about the target, though, is only that its Form has the referenced group and kind. Nothing says what that target must still SATISFY. A <code>WorkerDeployment</code> reads its weighted version&#39;s <code>handlers</code>; a <code>WorkerVersion</code>&#39;s bundle reference is resolved through the bundle&#39;s <code>manifestDigest</code>; a <code>WorkerVersion</code>&#39;s worker reference is only meaningful against the ES Module Worker ABI that identity fixes (<a href="./0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>). A target whose Definition moved to a version that renamed, retyped, or withdrew any of that would keep satisfying every reference to it, and every source would keep reporting itself healthy.</p><p>Decision <a href="./0014-published-schemas-are-structural-minima.html">0014</a> settled where a new invariant may live: data the published schema already admits, then Definition content, then code plus conformance. It authorises putting the missing statement on the reference node itself, exactly as <code>x-takoform-binding</code> already names the Binding contract governing a binding list. It does not decide what a host is keyed by, what a relation requires of its target, what a host stores, or which stable errors either produces. Those are normative contract decisions, and this repository&#39;s <code>AGENTS.md</code> requires a decision record for them.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>A host answers for one EXACT Form identity at a time, and a relation states — and a host verifies — what contract its target must satisfy. The rules below are normative; the operative text lives in <a href="./../host-api/v1alpha3.html"><code>../host-api/v1alpha3.md</code></a> and <a href="./../binding-contract/"><code>../binding-contract/README.md</code></a>.</p><ol><li><p><strong>The installed catalog is keyed by the whole identity.</strong> A host&#39;s installed set MUST be keyed by <code>{apiVersion, kind, definitionVersion, schemaDigest}</code>. The package digest stays out of the key: it is distribution provenance and never identity (decision 0011). Two definition versions of one group and kind MUST be installable at once and MUST answer independently on availability, the Form Definition surface, and the support profile. A host MUST NOT install two Definitions that agree on group, kind, and <code>definitionVersion</code> while differing on <code>schemaDigest</code>; a definition version names one set of bytes.</p></li><li><p><strong>Every lookup resolves by that key, and fails closed.</strong> Availability, Form Definition, <code>validate</code>, <code>prepare</code>, <code>apply</code>, <code>read</code>, <code>import</code>, <code>observe</code>, <code>delete</code>, and the support profile MUST resolve the whole identity or fail <code>form_unknown</code> (404). A host MUST NOT resolve the group and kind first and then compare the rest; it MUST NOT fall back to another definition of the same kind. A <code>definitionVersion</code> from one contract combined with a <code>schemaDigest</code> from another names no installed Definition and is <code>form_unknown</code> like any other.</p></li><li><p><strong>A resource records the exact ref it was created under.</strong> That ref is written at create, carried forward unchanged by every update and import, and is the ONLY identity the resource is answered about. A request naming any other exact ref addresses no resource: <code>resource_not_found</code> (404), never a response about the stored one. A response MUST NOT rewrite an older resource&#39;s recorded ref to a newer one — that would be the host asserting, in a representation, that two contracts are the same, which is precisely what decision 0017 forbids a provider to assert on its own state.</p></li><li><p><strong>A resource NAME stays unique per space, group, and kind.</strong> The definition version decides what a request is ANSWERED about, never where a resource lives. A reference is <code>{apiVersion, kind, name}</code> and carries no definition version, so a host holding two same-named resources of one kind under different contracts could resolve a reference to neither. A create therefore still conflicts with a name taken under another contract of the same kind.</p></li><li><p><strong>Every reference-shaped node states one target contract.</strong> Each reference in a Form&#39;s <code>desiredSchema</code> MUST carry exactly one of</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-takoform-target-formrefs&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;definitionVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } ]</span></span></code></pre></div><p>when the relation depends on the target&#39;s exact desired contract, or</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-takoform-required-interface&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;interfaces.takoform.com/v1alpha1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>when it depends only on an Interface the target provides. Both are data the published Form Definition schema already admits, like <code>x-takoform-binding</code> (decision 0014). Neither is a new Definition member. A reference carrying both, or neither, is refused at derivation: a host that cannot say what a relation requires cannot verify it.</p><p>The choice is a statement about the dependency. <code>x-takoform-target-formrefs</code> is correct when the source — or the host acting for it — reads a member of the target&#39;s desired spec, or enforces a rule stated over the target Form itself. <code>x-takoform-required-interface</code> is correct when what the source needs is behavior a contract fixes and any Form providing it would serve; pinning the Form there refuses a legitimate target for a requirement the source does not have.</p></li><li><p><strong>A binding&#39;s annotation is its contract&#39;s <code>targetInterface</code>.</strong> A binding-list reference MUST require exactly the Interface its Binding Definition names as <code>targetInterface</code>. The Binding Definition stays the authority and the annotation is its projection onto the reference, so the two cannot become two sources of truth (decision 0014). A binding relation is verified against its Binding Definition first, in the published order of decision 0015 rule 8, and against the annotation second.</p></li><li><p><strong>Resolution verifies the annotation before mutation, and pins both.</strong> On <code>apply</code> and on <code>import</code>, before ANY mutation, a host MUST verify the resolved target against the annotation: recorded under one of the listed exact identities, or declaring the required Interface in its Definition&#39;s <code>providedInterfaces</code> at the exact digest. A target that does not satisfy it fails <code>invalid_argument</code> (400) — the request is well formed and states something untrue about the resource it points at, exactly like the binding rules 3 through 5 — and the message MUST name the relation pointer and what was required. The stored relation record MUST then carry BOTH the target&#39;s <code>metadata.uid</code> and the target&#39;s exact FormRef. The UID pins which incarnation; the ref pins what contract that incarnation satisfies.</p></li><li><p><strong>A moved contract is reported the way a moved incarnation already is.</strong> When a stored relation&#39;s target no longer matches its recorded ref, the source reports <code>Ready=False</code> with <code>ExternalChange</code> and a <code>hostReason</code> naming the relation pointer, the recorded ref, and the current one — the same condition, the same closed reason, and the same remedy as a changed UID (decision 0015 rule 7). A host MUST NOT re-bind, and a read MUST NOT heal it. There is no second drift mechanism and no new error code or condition reason: both published enums are closed.</p></li></ol><h3 id="an-exact-identity-answers-with-the-definition-s-own-bytes" tabindex="-1">An exact identity answers with the Definition&#39;s own bytes <a class="header-anchor" href="#an-exact-identity-answers-with-the-definition-s-own-bytes" aria-label="Permalink to &quot;An exact identity answers with the Definition&#39;s own bytes&quot;">​</a></h3><p><em>Amended 2026-08-09.</em> Rule 2 says every lookup RESOLVES the whole identity or fails closed. It does not say what a resolved lookup answers with, and the two are different promises. A host MUST serve, for an exact FormRef, the <code>desiredSchema</code> of the installed Definition whose canonical bytes that <code>schemaDigest</code> addresses — every declared default, bound, pattern, and enum, unchanged.</p><p>The gap this closes is in the oracle rather than in any host. The conformance runner read each Form Definition from the host under test and materialized every probe spec against what it was served. A host publishing a default of its own therefore agreed with itself about every byte the runner then sent, stored, digested, and compared: self-consistent for an entire run, and passing all 112 required checks. The client this lane exists to protect materializes the NORMATIVE default instead, computes a different <code>specDigest</code>, and has every <code>prepare</code> bound to a spec that host does not recognise — a failure with no local symptom, at the surface a practitioner cannot inspect. An oracle that adopts the subject&#39;s answer as the standard it measures against is the defect this repository keeps finding, and it was here in its purest form.</p><p>It cannot be closed by re-derivation. <code>schemaDigest</code> addresses the canonical bytes of the WHOLE Definition, while the wire serves a subset of it — identity, display name, description, and <code>desiredSchema</code> — so no party holding the digest can reconstruct the served document and compare. That was read once as &quot;so the lane cannot check this&quot;, and it is the wrong conclusion: decision <a href="./0025-declared-outputs-are-a-typed-contract.html">0025</a> had already answered the same problem for the output contract by pinning it IN THE CORPUS, with a repository test binding those bytes to the installed Definition at the exact pinned FormRef. This does the same for the desired schema.</p><ul><li>The corpus pins the desired schema of every Form its probes materialize a spec against — the ten Edge Family probes — as byte-digested corpus files, and <code>TestCorpusPinsTheFormsOwnDesiredSchema</code> recomputes them from the installed Definition at that exact FormRef, so the pin cannot drift into a private contract of the corpus&#39;s own.</li><li><code>form-definition-exact</code> compares what a host serves against those bytes, for every one of them, and for the synthetic second definition version out of the Definition the corpus already pins whole. It previously drove two Forms and compared nothing but the echoed identity.</li><li>The runner materializes probe specs from the PINNED schema. It is the half that matters: a comparison alone would name the divergence while the run continued to measure the host against its own defaults.</li></ul><p>Exactly the Forms the runner materializes against are pinned, and no others. A pin nothing uses is maintenance with no oracle value, and the two synthetic Forms this lane installs — the second-group <code>EdgeKVNamespace</code> and the second <code>ModuleWorker</code> definition version — are driven with a literal empty desired spec the runner writes itself, so no served schema can influence what is sent for them. The rule is the one that generalises: a Form whose spec the runner materializes MUST have a pin, which contract validation requires of every probe, so a probe added later cannot fall back to believing the subject.</p><p>These are proven by the required conformance checks <code>two-definition-versions-answer-independently</code>, <code>resource-answers-only-under-its-recorded-form-ref</code>, <code>relation-target-form-ref-verified</code>, <code>relation-target-interface-verified</code>, and <code>relation-pin-records-target-form-ref</code>, against a corpus that pins a SECOND Form Definition of one kind as exact bytes.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><em>(2026-08-09)</em> The corpus pins ten more artifacts — one desired schema per probe Form, byte-digested under <code>fixtures/</code> — and <code>form-definition-exact</code> becomes a comparison rather than an echo. Widening it to every probe also found a stale pin nothing else was looking at: the corpus&#39;s <code>WorkerEndpoint</code><code>packageDigest</code> had drifted from the generated registry, because the test that binds probe identities to that registry enumerated eight of the ten probes by hand. Both the pin and the enumeration are fixed, and the enumeration is now the one list the contract keeps.</li><li>The conformance corpus grows to 84 required checks and pins one more artifact: a byte-digested second Form Definition of the <code>ModuleWorker</code> line. It differs from the first in exactly two places, and both are load-bearing — the <code>definitionVersion</code>, which makes it a second contract at all, and the absent <code>providedInterfaces</code>, which gives a required-interface relation a live target of exactly the right group and kind that truthfully does not satisfy it. Inventing that second contract at runtime would have proven only that the host agrees with whatever the runner made up.</li><li>The Edge Platform Family&#39;s fourteen relations split eleven Interface requirements to three exact-Form ones. The three are the relations that read their target: a Worker Version&#39;s <code>/bundle</code> (the host resolves the bundle&#39;s <code>manifestDigest</code> to learn what the version runs), a deployment&#39;s <code>/versions/*/workerVersion</code> (the host reads the version&#39;s <code>handlers</code> and its own <code>/worker</code> relation), and a deployment&#39;s <code>/worker</code> — the one reference in the family that WRITES to its target, because a deployment is what renders a Module Worker&#39;s readiness and the aggregate rules of decision 0016 are stated over that Form rather than over any Interface it provides.</li><li>A host may now advance a Form line without a migration on its own side, which is what makes decision 0017&#39;s promise real from both ends: the provider keeps every recorded ref addressable, and the host keeps answering for each of them.</li><li>The host&#39;s &quot;which runtime ABI do I implement&quot; question can no longer be answered by looking up a kind. It is the one contract every installed Form providing <code>worker.runtime</code> agrees on; zero providers, or two different ones, fail closed rather than pick.</li><li><code>formpackage</code>&#39;s sensitive-field-name policy gains a closed four-entry exemption for the reviewed <code>x-takoform-*</code> annotations, because <code>x-takoform-target-formrefs</code> uses <code>target</code> in its portable sense — the resource a relation points at — while the policy exists to keep author-declared fields from naming backend placement authority.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Key the host catalog by group and kind.</strong> This is what the reference host did, and its cost is the whole gap. One Form line holds one contract, so the day a definition version moves, every resource already recorded under the previous one becomes unaddressable at once — and worse, a host that answered such a request by kind would answer it SUCCESSFULLY, describing the resource under a contract it was never applied under, with a spec validated against a schema it was never written for and a client&#39;s state unable to detect any of it. The lane could not even express the failure: with one contract per kind installed, &quot;answers for the exact ref&quot; and &quot;answers for the kind&quot; are the same behavior, so the corpus was structurally incapable of failing a host that got this wrong. That is the same defect decision 0017 rejected on the provider side, left standing on the host side.</li><li><strong>Pin only the target UID.</strong> This is what decision 0015 stored, and it catches exactly one thing: a target that was deleted and re-created. A target that MOVED — same resource, same incarnation, same UID, a Definition that advanced to a version with a different field set — passes every check, because group and kind still match and the UID never changed. The source keeps reporting itself healthy while the host reads a member of the target&#39;s spec that may no longer mean what it did, or projects an Interface the target no longer provides. The UID answers &quot;is this the same thing&quot;; nothing answered &quot;is it still the thing I needed&quot;.</li><li><strong>Declare target contracts as a new Form Definition member.</strong> Blocked and wrong. The published Form Definition schema admits no such member and its bytes are immutable (decision 0014), so it would require minting a replacement identity for a correction to unpublished Forms. And it would put the declared list and the desired schema in disagreement-capable positions for the same facts: the schema would still decide what a valid reference looks like, while a sibling member decided what its target must be, with nothing keeping a reference and its entry aligned. The annotation lives ON the reference it describes, so there is exactly one place to look and no way to describe a reference that is not there.</li><li><strong>Let a name hold two resources, one per definition version.</strong> Rejected because a reference is <code>{apiVersion, kind, name}</code> (decision 0015) and carries no definition version, so a host holding both could resolve a reference to neither. Ambiguity would have to become a new failure mode on the one path that must stay decidable, in exchange for a store shape nothing needs.</li><li><strong>Report a moved target contract with its own condition or error code.</strong> Rejected because the closed condition-reason and error-code vocabularies are published, and because the fact is the same one <code>ExternalChange</code> already names: the source is pinned to something that is no longer there, and the remedy is the same re-apply. A parallel mechanism would make a client that already renders relation drift wrong by omission.</li><li><strong>Verify the annotation after the mutation, on read.</strong> Rejected for the reason every pre-mutation rule in this lane exists: a host that stored the relation first would have written a record whose every later dependency and drift decision is unsound, and the remedy would be to delete something the client believed it had created.</li></ul><p>Rejected in the 2026-08-09 amendment:</p><ul><li><strong>Serve the desired schema&#39;s own digest on the Form Definition surface and have the client compare.</strong> This is the shape that would need no corpus pin, and it is unavailable: the published form-definition response is a closed object and its bytes are immutable (decision 0014), so a <code>desiredSchemaDigest</code> member is rung 4 of that ladder — a new schema generation, minted for one comparison the corpus can make today.</li><li><strong>Pin the WHOLE Form Definition per probe instead of its desired schema.</strong> Attractive, because <code>schemaDigest</code> would then be re-derivable from the pinned bytes and the corpus would verify itself without a repository checkout. It is the right shape and the wrong change set: the corpus already pins each Form&#39;s <code>outputSchema</code> inline under decision 0025, and pinning the whole Definition beside it would state that contract twice with nothing keeping the two agreeing. Folding both into one pinned Definition is a coherent follow-up and belongs to its own change.</li><li><strong>Pin a desired schema for every installed Form rather than every probed one.</strong> Rejected as maintenance without oracle value. What a pin buys is a comparison and a materialization source; a Form no probe drives supplies neither, and the two synthetic Forms this lane installs are driven with a literal spec the runner writes itself. The invariant kept instead is the one that cannot rot: every Form the runner materializes against has a pin, checked when the contract loads.</li><li><strong>Keep reading the served schema and merely warn on divergence.</strong> Rejected because the run would continue measuring the host against defaults the host supplied. A divergence named in a report that still says <code>passed</code> is the same oracle with a footnote.</li></ul>',23)])])}const f=t(n,[["render",s]]);export{u as __pageData,f as default};
