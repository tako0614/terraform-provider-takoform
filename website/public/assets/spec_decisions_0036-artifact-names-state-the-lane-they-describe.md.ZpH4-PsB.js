import{_ as a,o as t,c as o,ag as n}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse(`{"title":"0036 — An artifact's name states the lane it describes, not its place in a sequence","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0036-artifact-names-state-the-lane-they-describe.md","filePath":"spec/decisions/0036-artifact-names-state-the-lane-they-describe.md"}`),s={name:"spec/decisions/0036-artifact-names-state-the-lane-they-describe.md"};function r(c,e,i,d,h,l){return t(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="_0036-—-an-artifact-s-name-states-the-lane-it-describes-not-its-place-in-a-sequence" tabindex="-1">0036 — An artifact&#39;s name states the lane it describes, not its place in a sequence <a class="header-anchor" href="#_0036-—-an-artifact-s-name-states-the-lane-it-describes-not-its-place-in-a-sequence" aria-label="Permalink to &quot;0036 — An artifact&#39;s name states the lane it describes, not its place in a sequence&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-21</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Takoform&#39;s version axes are independent on purpose, and <a href="./../versioning.html"><code>versioning.md</code></a> says their numbers MUST NOT be aligned to imply a shared release. Independence is correct and it is not the problem this record answers.</p><p>The problem is that some names are <strong>absolute</strong> and some are <strong>relative</strong>. <code>forms.takoform.com/v1beta1</code> names one lane on the day it is written and afterwards. <code>current</code>, and the Nth of a sequence, name a position: they are true only until the thing they point at moves, nothing announces the move, and the correction has to be made by hand.</p><p>The correction was missed. The conformance corpora were numbered by generation, and for three generations the counter and the lane agreed — <code>portable-host-v1</code> measured <code>v1alpha1</code>, <code>-v2</code> measured <code>v1alpha2</code>, <code>-v3</code> measured <code>v1alpha3</code>. When <a href="./0035-beta-contracts-ship-in-stable-provider-v2-1.html">decision 0035</a> moved the current lane to <code>forms.takoform.com/v1beta1</code>, the absolute names were minted correctly: <code>host-api/v1beta1.md</code>, <code>host-api-wire-v1beta1.schema.json</code>, <code>host-discovery-v1beta1.schema.json</code>, and the rest arrived beside their retained v1alpha3 counterparts, exactly as 0035 requires. <code>conformance/portable-host-v3</code> was rewritten in place instead.</p><p>That made one published address answer about a different contract than it had answered about the day before:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://takoform.com/conformance/portable-host-v3/contract.json</span></span>
<span class="line"><span>  before: forms.takoform.com/v1alpha3, 114 required checks</span></span>
<span class="line"><span>  after:  forms.takoform.com/v1beta1,  116 required checks</span></span></code></pre></div><p>which is precisely what 0035 forbids — every <code>v1alpha3</code> schema, specification, operation table, public URL, and byte remains retained history. Comparing the 827 published JSON documents that existed on both sides of that change finds exactly two whose declared lane moved, and both are inside that corpus. The retention rule held everywhere a name was absolute and failed at the one place a name was relative.</p><p>No gate saw it, because every corpus check read the corpus and compared it against itself. A corpus that changes lanes agrees with itself perfectly.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A new artifact whose name carries a version word names the lane it describes.</strong><code>conformance/portable-host-v1beta1</code> rather than a fourth generation number; a Go package, script, or directory minted for a lane says which lane.</p><p><strong>Already-published relative names are retained history and stay.</strong><code>portable-host-v1</code>, <code>-v2</code>, and <code>-v3</code> are published addresses. Renaming them would be the same defect in the other direction. <code>-v3</code> holds the v1alpha3 bytes again, restored byte-for-byte, so the address that always named them names them once more.</p><p><strong>A retained corpus is retained bytes, not a runnable corpus.</strong> The v1alpha3 runner became the v1beta1 runner rather than being copied, so no runner in this repository loads <code>portable-host-v3</code>, and reviving one would prove nothing about a lane no host is being measured against. What is guaranteed is that the bytes do not change.</p><p><strong><code>current</code> is not one word, and documents say which one they mean.</strong> The current lane and family are <code>v1beta1</code>. The retained central Form epoch is <code>forms.takoform.com/v1alpha2</code>, and that is what <code>formpackage.CurrentFormAPIVersion</code>, <code>forms/lifecycle.json</code>&#39;s <code>currentEpoch</code>, and <code>internal/currentform*</code> name. <code>forms/lifecycle.schema.json</code> pins that value as a <code>const</code> and <code>internal/standardforms</code> enforces it, so it is deliberate; the word alone does not say so.</p><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p>Two checks in <code>bun run check</code>, because the defect had two halves.</p><p><code>release/published-document-lanes.json</code> records the lane each published document declares, and <code>check:published-document-lanes</code> refuses a change to one and refuses one disappearing. It reads the built site rather than the sources, because the thing that must not move is the address a reader fetches.</p><p><code>checkCorpusNamesStateTheirLane</code> in <code>scripts/check-public-surfaces.mjs</code> refuses a corpus directory whose name disagrees with the lane its contract declares. The three published generation-named corpora are listed one by one rather than matched by a pattern, so a fourth is an edit somebody has to justify.</p><p>Both were driven backwards before being trusted: on a replay of the original defect, on a published address disappearing, on a new corpus misnamed by generation, and on a retained address made to serve another lane.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The rule is about naming and never about values. Every published value stays exactly where it is: the lane, the family, the fifteen exact FormRefs and their digests are frozen by Registry-published provider <code>v2.1.1</code>; every <code>spec/schemas/</code> filename is frozen by the append-only public schema identity ledger; <code>packages.forms.takoform.com/v1alpha4</code> is frozen inside published schema bytes. Nothing in this decision moves any of them.</p><p>Internal package and identifier names that are still relative — <code>internal/portableconformancev3</code>, <code>internal/clientv3</code>, <code>internal/currentform*</code>, <code>internal/provider/v3_*.go</code> — are free to be made absolute and are not changed here. This record is what a later change of that kind cites.</p><p>Earlier decisions name <code>conformance/portable-host-v3</code> because that path was the v1alpha3 corpus when they were written. Restoring those bytes makes those references true again. A decision written during the Beta lane that names the same path means what is now <code>conformance/portable-host-v1beta1</code>; decision records are history and are not rewritten to match a later layout.</p>`,24)])])}const u=a(s,[["render",r]]);export{m as __pageData,u as default};
