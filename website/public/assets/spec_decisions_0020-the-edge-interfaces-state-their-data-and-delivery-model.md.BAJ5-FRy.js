import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.BTLSZHNB.js";const m=JSON.parse('{"title":"0020 — The edge Interfaces state their data and delivery model","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0020-the-edge-interfaces-state-their-data-and-delivery-model.md","filePath":"spec/decisions/0020-the-edge-interfaces-state-their-data-and-delivery-model.md"}'),n={name:"spec/decisions/0020-the-edge-interfaces-state-their-data-and-delivery-model.md"};function d(i,e,r,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="_0020-—-the-edge-interfaces-state-their-data-and-delivery-model" tabindex="-1">0020 — The edge Interfaces state their data and delivery model <a class="header-anchor" href="#_0020-—-the-edge-interfaces-state-their-data-and-delivery-model" aria-label="Permalink to &quot;0020 — The edge Interfaces state their data and delivery model&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><a href="./0010-exact-interface-and-binding-contracts.html">Decision 0010</a> made an Interface an exact, digest-bound contract, and <a href="./0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a> showed what that costs to do properly by writing the runtime ABI out in full. The four data-plane contracts of the Edge Platform Family were not held to the same standard, and each of them left something an application depends on either unsaid or said twice, differently.</p><p><strong><code>edge.kv</code> contradicted itself.</strong> Its <code>semantics</code> declared <code>consistency: eventual</code> while its behavior fixtures required a put to be visible to the very next get and a delete to make the next get miss. Both cannot be normative: the fixtures are exactly the trace a correct eventually consistent store is allowed to fail, so the deterministic set demanded read-your-writes from a contract that promised none. Its value model measured two quantities at once — <code>maxValueBytes</code> counts bytes, a JSON Schema <code>maxLength</code> counts string length — so no host could tell which one bounded a value, and the answers diverge for every value outside ASCII.</p><p><strong><code>edge.objects</code> promised an API it did not have.</strong> The description promised range reads while <code>get</code>&#39;s input took only a key, there were no conditional requests despite the description fencing on etags, there was no multipart path, and the body was a JSON string while <code>maxObjectBytes</code> was 5 GiB. No host can produce a 5 GiB JSON string and no client can consume one.</p><p><strong><code>edge.sql</code> could not carry SQLite.</strong> Its value model was <code>boolean|null|number|string</code>. A 64-bit INTEGER does not survive an IEEE-754 double — <code>9223372036854775807</code> arrives as <code>9223372036854775808</code> — and a BLOB has nowhere to go at all. <code>lastInsertRowId</code> was a JSON integer with the same defect, and a transaction result could report only a write count, so a <code>SELECT</code> inside a transaction had nowhere to put its rows: the atomic path was strictly weaker than the non-atomic one.</p><p><strong><code>edge.queue</code> left the message model open.</strong> Nothing said what a message body was, what a message id meant across redeliveries, what the timestamp measured, how an attempt was counted, how a message was settled, what an uncaught handler exception did to the rest of the batch, whether the first delivery counted toward <code>maxRetries</code>, what the attempt count was after a dead-letter transfer, or whether one queue could have two consumers. Every one of those is a thing two conforming hosts could answer differently.</p><p><strong>The cron grammar rejected the schedules the Form exists for.</strong> The enforced pattern was five single-value fields, so <code>* * * * *</code>, <code>0 * * * *</code>, and <code>*/5 * * * *</code> were all refused and the most frequent representable schedule was once a day. A Form whose entire purpose is periodic invocation could not express an hourly one.</p><p>All five are corrections to unpublished Forms and unpublished Interface candidates, so they are made under <a href="./0014-published-schemas-are-structural-minima.html">decision 0014</a>: everything below is expressed within what the already-published Interface Definition meta-schema admits — operations with input and output schemas, closed error vocabularies, <code>semantics</code>, <code>limits</code>, fixtures, and normative descriptions — and no schema identity is minted.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="one-encoded-bytes-shape-and-edge-kv-values-are-bytes" tabindex="-1">One encoded-bytes shape, and <code>edge.kv</code> values are bytes <a class="header-anchor" href="#one-encoded-bytes-shape-and-edge-kv-values-are-bytes" aria-label="Permalink to &quot;One encoded-bytes shape, and `edge.kv` values are bytes&quot;">​</a></h3><p>A value that is bytes travels as <code>{&quot;encoding&quot;: &quot;base64&quot;, &quot;data&quot;: &quot;…&quot;}</code>, with <code>data</code> RFC 4648 §4 base64, padded and unwrapped. The declared byte limit bounds the DECODED length; the <code>maxLength</code> on <code>data</code> is the structural ceiling of the encoding and is not the limit. The shape is the same in <code>edge.kv</code> values and <code>edge.queue</code> message bodies, so a value moving from a queue into a namespace does not change representation on the way. A key stays a UTF-8 string, and <code>maxKeyBytes</code> bounds its UTF-8 encoded length, which is stated rather than implied.</p><p>Because the values are bytes, the name <code>edge.kv</code> continues to describe what the contract is. No rename is needed.</p><h3 id="edge-kv-stays-eventually-consistent-and-says-so-where-it-matters" tabindex="-1"><code>edge.kv</code> stays eventually consistent, and says so where it matters <a class="header-anchor" href="#edge-kv-stays-eventually-consistent-and-says-so-where-it-matters" aria-label="Permalink to &quot;`edge.kv` stays eventually consistent, and says so where it matters&quot;">​</a></h3><p>Consistency remains <code>eventual</code>, and the contract now states the consequence outright: a read that follows a write may return the previous value, or <code>not_found</code>, until replication converges — including a read the same client issues immediately after its own write, on the same connection, from the same location. There is no read-your-writes guarantee, no session in which one holds, and no bound on convergence time; a host states its own convergence target in its Host Support Profile.</p><p>The immediate-visibility fixtures are removed from the deterministic set. What remains asserts only facts that no write has to converge for: a put is accepted, a never-written key is <code>not_found</code>, deleting an absent key succeeds, and the listing of an empty scope is complete. Convergence becomes a host obligation, listed as one in <a href="./../host-api/v1alpha3.html#what-the-lane-proves-and-what-stays-a-host-obligation"><code>../host-api/v1alpha3.md</code></a>.</p><h3 id="edge-objects-becomes-a-real-object-api-with-streaming-bodies" tabindex="-1"><code>edge.objects</code> becomes a real object API with streaming bodies <a class="header-anchor" href="#edge-objects-becomes-a-real-object-api-with-streaming-bodies" aria-label="Permalink to &quot;`edge.objects` becomes a real object API with streaming bodies&quot;">​</a></h3><p><code>head</code>, ranged and conditional <code>get</code>, conditional <code>put</code>, <code>delete</code>, <code>list</code> with delimiter roll-up, and <code>createMultipartUpload</code> / <code>uploadPart</code> / <code>completeMultipartUpload</code> / <code>abortMultipartUpload</code>. Bodies are STREAMS: <code>get</code> and <code>put</code> declare <code>bodyStream</code> and <code>contentLength</code>, and the bytes move beside the operation document rather than inside it, which is what makes the 5 GiB ceiling meaningful. Consistency stays <code>read_after_write</code> and is stated exactly: a <code>get</code>, <code>head</code>, or <code>list</code> issued after a <code>put</code> or <code>delete</code> that already resolved observes it; writes to one key are last-writer-wins; there is no cross-key atomicity and no versioning.</p><p>The fixtures are consistent with that model and prove what a single client can: put-then-head, a ranged get returning the declared subrange, a range past the end refused, a conditional put refused against an existing key, delete-then-head missing, and a listing truncating at the requested limit. A trace declaring <code>bodyStream</code> sends exactly <code>contentLength</code> bytes whose value at index <em>i</em> is <em>i</em> mod 256, so every trace is byte-for-byte reproducible. Multipart assembly is a host obligation: its steps depend on part etags the host mints during the trace, which a static trace cannot carry.</p><h3 id="edge-sql-values-are-tagged-by-storage-class" tabindex="-1"><code>edge.sql</code> values are tagged by storage class <a class="header-anchor" href="#edge-sql-values-are-tagged-by-storage-class" aria-label="Permalink to &quot;`edge.sql` values are tagged by storage class&quot;">​</a></h3><p>Every bound parameter and every returned column value is exactly one of <code>{&quot;type&quot;:&quot;null&quot;}</code>, <code>{&quot;type&quot;:&quot;integer&quot;,&quot;value&quot;:&quot;&lt;decimal&gt;&quot;}</code>, <code>{&quot;type&quot;:&quot;real&quot;,&quot;value&quot;:&lt;number&gt;}</code>, <code>{&quot;type&quot;:&quot;text&quot;,&quot;value&quot;:&quot;&lt;string&gt;&quot;}</code>, and <code>{&quot;type&quot;:&quot;blob&quot;,&quot;base64&quot;:&quot;&lt;base64&gt;&quot;}</code> — SQLite&#39;s five storage classes and nothing else. There is no boolean member, because SQLite has no boolean storage class. An INTEGER travels as canonical decimal text, and so does <code>lastInsertRowId</code>; a host refuses one outside the 64-bit two&#39;s-complement range with <code>sql_error</code>. Every statement, alone or inside a transaction, reports the same result — <code>rows</code>, <code>rowsWritten</code>, and <code>lastInsertRowId</code> when it inserted — so a <code>SELECT</code> inside a transaction returns its rows. <code>query</code> is declared idempotent, so a host refuses a writing statement submitted through it.</p><h3 id="edge-queue-states-its-message-model-and-a-queue-has-one-consumer" tabindex="-1"><code>edge.queue</code> states its message model, and a queue has one consumer <a class="header-anchor" href="#edge-queue-states-its-message-model-and-a-queue-has-one-consumer" aria-label="Permalink to &quot;`edge.queue` states its message model, and a queue has one consumer&quot;">​</a></h3><p>A body is opaque bytes in the shared encoded-bytes shape. A <code>messageId</code> is host-issued, opaque, unique within the queue, and stable across redeliveries. <code>timestampMillis</code> is the UTC instant of acceptance and does not change across redeliveries. <code>attempts</code> is 1 on first delivery and increments on each redelivery. Settlement is explicit: <code>acknowledge</code> and <code>retry</code> per message, <code>acknowledgeAll</code> and <code>retryAll</code> per batch, all scoped by the <code>batchId</code> the delivered batch carried. A handler that returns without settling anything acknowledges the whole batch; a handler that throws retries every message it had not already acknowledged. <code>maxRetries</code> counts REDELIVERIES only, so a message is delivered at most <code>1 + maxRetries</code> times and <code>maxRetries: 0</code> means one delivery. A message that exhausts its retries becomes a NEW message on the dead-letter queue — new id, new acceptance timestamp, <code>attempts</code> starting again at 1 — or is dropped when no dead-letter queue is declared. Because the transfer resets the attempt count, a dead-letter destination that leads back to the origin is a loop <code>maxRetries</code> cannot bound; refusing that cycle is <a href="./0026-attachment-claims-are-canonical-and-acyclic.html">decision 0026</a>.</p><p>One queue has AT MOST ONE consumer. <code>maxRetries</code>, <code>retryDelaySeconds</code>, <code>maxConcurrency</code>, and the dead-letter destination are properties of how that queue is drained; two consumers would give one queue two of each with no rule deciding which message got which, which leaves the queue&#39;s own behavior unstatable.</p><p>The queue-producer binding still projects only <code>send</code> and <code>sendBatch</code>: the settlement operations exist on the batch the module&#39;s <code>queue</code> handler receives, never on a binding, because settling a message you were not delivered is not an operation. <code>worker.runtime</code>&#39;s <code>queue</code> operation carries the same <code>batchId</code>, <code>timestampMillis</code>, and encoded-bytes body, so the two contracts describe one message.</p><h3 id="the-cron-grammar-is-parsed-not-matched" tabindex="-1">The cron grammar is parsed, not matched <a class="header-anchor" href="#the-cron-grammar-is-parsed-not-matched" aria-label="Permalink to &quot;The cron grammar is parsed, not matched&quot;">​</a></h3><p>The Worker Cron Trigger accepts five fields where each field is a comma-separated list of <code>*</code>, a literal, a range <code>low-high</code>, <code>*/step</code>, or <code>low-high/step</code>. Names and a step on a bare literal are refused. The regular expression carried inside the Form Definition is the STRUCTURAL half — it is what a host with only the Definition can enforce — and a parser is the semantic half: it builds an AST, holds each field to its own domain, refuses an inverted range and a step outside <code>1..span</code>, and produces a canonical form so two spellings of one schedule are comparable. The provider runs that same parser at plan time, so a configuration that plans is one that applies.</p><p>Schedules are UTC only. When both day-of-month and day-of-week are restricted, a match on EITHER fires the trigger; when one is restricted, only that one constrains the day. A missed run is not made up. Delivery of a match is at-least-once, so a handler may run twice for one matched minute and must be idempotent. An uncaught handler exception is a failed invocation reported to host diagnostics, not retried within the matched minute.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Every Interface Definition, the six Forms that reference them, and the four binding descriptions that project them change bytes, so the whole digest chain is regenerated: Interface digests, Form Definition digests, package digests, the provider v3 registry, and the conformance corpus pins.</li><li>Three required conformance checks are added — <code>edge-interface-contracts-advertised</code>, <code>cron-grammar-enforced</code>, and <code>queue-single-consumer-enforced</code> — bringing the closed list to 79. They are exactly what a black-box Host API runner can prove; everything else these contracts state is listed as a host obligation.</li><li>The catalog gains an authoring-time proof for EVERY Interface, not only the runtime ABI: operation names are unique and well formed, fixtures exercise declared operations, and a fixture expecting a failure names an error that operation declares. A fixture expecting an error outside the closed vocabulary is a trace no conforming host could pass, which is the same defect class as a required check no correct host can complete.</li><li><code>KindDateString</code> lost its last user when <code>compatibilityDate</code> was removed in decision 0019, and its validator only ever checked the <code>YYYY-MM-DD</code> shape, so <code>2026-99-99</code> passed. Rather than leave a dead field kind with a weak validator, the kind and its pattern are removed; a future calendar-date field mints one with a real calendar check.</li><li>The Host API lane&#39;s &quot;what the lane proves, and what stays a host obligation&quot; section grows a second half covering the data-plane contracts, so a reader of a passing report still knows which half it covers.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep the immediate-visibility fixtures under an eventual claim.</strong> Rejected because they are the one trace a correct eventually consistent store is allowed to fail: the deterministic set would have excluded exactly the systems the contract describes.</li><li><strong>Promise read-your-writes within a session.</strong> Rejected because it would need &quot;session&quot; defined precisely — a connection, an isolate, a location, a principal — and no definition of it is true of a globally replicated store served from the nearest replica. Stating a guarantee no implementation keeps is worse than stating none.</li><li><strong>Bound the KV value with <code>maxLength</code> alone and drop <code>maxValueBytes</code>.</strong> Rejected because a host stores bytes, bills bytes, and rejects on bytes; a code-unit ceiling would be a limit nobody enforces.</li><li><strong>JSON-string object bodies.</strong> Rejected because the object ceiling is 5 GiB. Either the body model or the ceiling had to go, and shrinking an object store to what fits in a JSON string makes it not an object store.</li><li><strong>Narrow <code>edge.objects</code> to a small-text store and rename it.</strong> Rejected because decision 0008 names the strongly consistent object bucket as one of the family&#39;s proven service shapes; narrowing it would remove the shape rather than fix the contract, and every application that needs an object store would be pushed off the family.</li><li><strong>Untagged SQL values, with the integer widened to a JSON number.</strong> Rejected because IEEE-754 double loses INTEGER precision above 2^53 and there is no JSON scalar that carries a BLOB at all; a value model that silently changes what was stored is worse than one that is verbose.</li><li><strong>A single <code>bytes</code> scalar instead of a tagged union for SQL.</strong> Rejected because SQLite&#39;s storage class is observable — <code>typeof()</code>, comparison order, and affinity all depend on it — so collapsing five classes into two would make the same query answer differently on two conforming hosts.</li><li><strong>Let a queue have several consumers and say the split is unspecified.</strong> Rejected because &quot;unspecified&quot; is exactly the free-token failure decision 0008 forbids: retry counts, delays, and dead-letter destinations would differ per message with no rule an author could predict.</li><li><strong>Count the first delivery toward <code>maxRetries</code>.</strong> Rejected because <code>maxRetries: 0</code> would then mean &quot;never deliver&quot;, which is not a queue.</li><li><strong>A daily-only cron under a general name.</strong> Rejected because the Form&#39;s whole purpose is periodic invocation, and a name that says &quot;cron&quot; while refusing <code>*/5 * * * *</code> misleads every author who has ever written a crontab. Renaming it to something honest about daily-only granularity would have preserved the defect under better labelling.</li><li><strong>Widen the cron regular expression and stop there.</strong> Rejected because a regex that admits lists, ranges, and steps also admits <code>0 24 * * *</code>, <code>5-1 * * * *</code>, and <code>*/0 * * * *</code>, which name no schedule; a host would store a trigger it can never fire, and two hosts would disagree about which ones they accepted.</li><li><strong>Canonicalise the stored cron expression.</strong> Rejected because the desired state is the bytes an author wrote: rewriting them would make every plan drift against the host&#39;s copy. The canonical form is a comparison form only.</li></ul>',33)])])}const g=t(n,[["render",d]]);export{m as __pageData,g as default};
