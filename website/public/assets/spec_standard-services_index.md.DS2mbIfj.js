import{_ as t,o as a,c as s,ag as o}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"External standard services (standards.takoform.com/v1alpha1)","description":"","frontmatter":{},"headers":[],"relativePath":"spec/standard-services/index.md","filePath":"spec/standard-services/index.md"}'),i={name:"spec/standard-services/index.md"};function r(n,e,d,c,l,h){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="external-standard-services-standards-takoform-com-v1alpha1" tabindex="-1">External standard services (standards.takoform.com/v1alpha1) <a class="header-anchor" href="#external-standard-services-standards-takoform-com-v1alpha1" aria-label="Permalink to &quot;External standard services (standards.takoform.com/v1alpha1)&quot;">​</a></h1><p>Some service categories already have the portability layer Takoform exists to provide: object storage has the S3-compatible API, relational databases have the PostgreSQL wire protocol, caches have the Redis protocol, mail submission has SMTP. <a href="./../decisions/0043-forms-target-popular-vendor-locked-primitives.html">Decision 0043</a> forbids respecifying them as Forms. This contract is the other half of that decision: how a Form&#39;s runtime <strong>reaches</strong> such a service.</p><p>Requirement keywords are used as described in <a href="./../conformance.html"><code>../conformance.md</code></a>.</p><h2 id="the-model-a-sealed-slot-with-a-protocol-tag" tabindex="-1">The model: a sealed slot with a protocol tag <a class="header-anchor" href="#the-model-a-sealed-slot-with-a-protocol-tag" aria-label="Permalink to &quot;The model: a sealed slot with a protocol tag&quot;">​</a></h2><p>An external standard service is declared exactly the way host-supplied sealed values already are (<a href="./../form-families.html"><code>../form-families.md</code></a>): <strong>portable state carries only a name and a protocol, never an endpoint and never a credential.</strong> The declaration a consuming Form&#39;s desired schema embeds is:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;MEDIA&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;service&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;standards.takoform.com/v1alpha1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;protocol&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;s3-compatible&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;required&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><code>name</code> matches <code>^[A-Z][A-Z0-9_]*$</code> (64 characters at most) — the SCREAMING_SNAKE grammar sealed-value slots already use, because the projection mints <code>NAME_</code>-prefixed members — and shares the consuming Form&#39;s one runtime namespace. Uniqueness is enforced over the PROJECTED closure: the union of vars keys, sensitive-value names, binding names, and every member each slot projects must be collision-free, and a host refuses a collision before mutation (<code>MEDIA_ENDPOINT</code> as a var beside an <code>s3-compatible</code> slot named <code>MEDIA</code> is <code>invalid_argument</code>).</li><li><code>required</code> is an optional boolean defaulting to true. A REQUIRED slot the host cannot satisfy blocks readiness; an optional one the host does not satisfy projects nothing and blocks nothing.</li><li>The array property embedding the slots carries the <code>x-takoform-standard-services</code> annotation so a host holding only the Definition derives every slot it must enforce (<a href="./../host-api/v1beta2.html"><code>../host-api/v1beta2.md</code></a>).</li><li><code>service</code> validates against <a href="/schemas/standards/v1alpha1/standard-service-ref.schema.json"><code>standard-service-ref-v1alpha1.schema.json</code></a>. The protocol vocabulary is closed: <code>s3-compatible</code>, <code>postgresql</code>, <code>redis</code>, <code>smtp</code>. Widening it is a reviewed spec change held to decision 0043&#39;s test — the protocol must be a de-facto standard, or the category belongs to a Form Family instead. Decision 0043&#39;s landscape survey records the growth roadmap (<code>kafka</code>, <code>amqp</code>, <code>mysql</code>, <code>mongodb</code>, <code>elasticsearch-compatible</code>, <code>openai-compatible</code>, <code>otlp</code>).</li></ul><p>The host or operator <strong>satisfies</strong> a slot by supplying connection material for a service that actually speaks the declared protocol. Where that service runs — a cloud provider, the host&#39;s own infrastructure, a box under a desk — is invisible to portable state, exactly as host placement already is. Which slots a host satisfies, and with what, is host/operator policy; a REQUIRED slot a host cannot or will not satisfy makes the Resource not Ready (<code>DependencyMissing</code>), and a host that knows it cannot satisfy one refuses at <code>prepare</code> with <code>unsupported_capability</code> rather than at runtime — <code>validate</code> never depends on the tenant, and satisfiability is tenant wiring. A host advertises per-protocol satisfiability through the <code>StandardServiceSupport</code> profile kind on the v1beta2 support surface, never with what would satisfy it.</p><h2 id="what-each-protocol-projects" tabindex="-1">What each protocol projects <a class="header-anchor" href="#what-each-protocol-projects" aria-label="Permalink to &quot;What each protocol projects&quot;">​</a></h2><p>The slot&#39;s runtime projection is defined per protocol, so a consumer&#39;s code is portable across hosts. Projected member names below are <strong>runtime names</strong>, not portable state; they never appear in a Form Package or a Resource document. For an environment-style runtime namespace (the Worker family&#39;s <code>env</code>, a container revision&#39;s environment), a slot named <code>NAME</code> projects:</p><table tabindex="0"><thead><tr><th>Protocol</th><th>Projected members</th><th>Content</th></tr></thead><tbody><tr><td><code>postgresql</code></td><td><code>NAME_URL</code></td><td>one libpq-style <code>postgresql://</code> connection URI carrying host, port, database, and authentication</td></tr><tr><td><code>redis</code></td><td><code>NAME_URL</code></td><td>one <code>redis://</code> or <code>rediss://</code> URI carrying host, port, and authentication</td></tr><tr><td><code>smtp</code></td><td><code>NAME_URL</code></td><td>one <code>smtps://</code> or <code>smtp://</code> submission URI carrying host, port, and authentication; <code>smtp://</code> implies STARTTLS on the standard submission port</td></tr><tr><td><code>s3-compatible</code></td><td><code>NAME_ENDPOINT</code>, <code>NAME_REGION</code>, <code>NAME_BUCKET</code>, <code>NAME_ACCESS_KEY_ID</code>, <code>NAME_SECRET_ACCESS_KEY</code></td><td>the five values every S3-compatible SDK takes; <code>NAME_REGION</code> MAY be the literal <code>auto</code> where the service ignores regions</td></tr></tbody></table><p>Every projected value is sealed exactly like a sensitive variable: it MUST NOT appear in desired state, observed state, outputs, diagnostics, logs the host returns, or provider state. The observed document MAY state that a slot is satisfied; it MUST NOT state with what.</p><h2 id="what-this-contract-does-not-do" tabindex="-1">What this contract does not do <a class="header-anchor" href="#what-this-contract-does-not-do" aria-label="Permalink to &quot;What this contract does not do&quot;">​</a></h2><ul><li>It grants no lifecycle authority over the external service. Takoform never creates, migrates, or deletes a PostgreSQL database or an S3 bucket through this contract; the slot reaches a service somebody else provisioned. A category graduating from &quot;reached&quot; to &quot;provisioned&quot; is a Form Family decision under decision 0043&#39;s test, not a widening of this contract.</li><li>It does not verify protocol conformance of the satisfied service. The host vouches for what it wires; measuring it is host-side quality, not portable conformance.</li><li>It adds nothing to the published v1beta1 Edge schemas, which are frozen identities. Families minted after this contract carry the declaration from birth; the Edge family adopts it when a graduation mints its next identities (<a href="./../decisions/0037-immutability-begins-at-stable.html">decision 0037</a>).</li></ul><h2 id="conformance" tabindex="-1">Conformance <a class="header-anchor" href="#conformance" aria-label="Permalink to &quot;Conformance&quot;">​</a></h2><p>The declaration shape is normative now; executable conformance arrives with the first family whose desired schemas embed it, as part of that family&#39;s corpus (plan-time <code>unsupported_capability</code> refusal, readiness gating on an unsatisfied REQUIRED slot, namespace-collision refusal, and the sealing of projected values). Until such a corpus exists, no host can claim measured support for this contract.</p>`,16)])])}const u=t(i,[["render",r]]);export{m as __pageData,u as default};
