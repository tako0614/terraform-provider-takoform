import{_ as t,o as a,c as o,ag as s}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"0050 — A standard-backed Form provisions an instance; it never respecifies the protocol","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0050-a-standard-backed-form-provisions-an-instance-not-a-protocol.md","filePath":"spec/decisions/0050-a-standard-backed-form-provisions-an-instance-not-a-protocol.md"}'),n={name:"spec/decisions/0050-a-standard-backed-form-provisions-an-instance-not-a-protocol.md"};function r(i,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="_0050-—-a-standard-backed-form-provisions-an-instance-it-never-respecifies-the-protocol" tabindex="-1">0050 — A standard-backed Form provisions an instance; it never respecifies the protocol <a class="header-anchor" href="#_0050-—-a-standard-backed-form-provisions-an-instance-it-never-respecifies-the-protocol" aria-label="Permalink to &quot;0050 — A standard-backed Form provisions an instance; it never respecifies the protocol&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-23</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The maintainer asked how the industry-standard endpoints — S3, an OpenAI-compatible API — are used from Takoform without Takoform specifying them, given that takoserver wants to implement them too.</p><p><a href="./0043-forms-target-popular-vendor-locked-primitives.html">Decision 0043</a> already answers half of it: a category with a de-facto standard API is never respecified as a Form, and a Form&#39;s runtime reaches one through a sealed slot (<a href="./0045-external-standard-services-are-sealed-slots.html">decision 0045</a>). What 0043 does not say is how an instance of such a service comes to EXIST, and that is the half the question is about.</p><h3 id="the-repository-already-answers-it-twice-and-one-answer-contradicts-0043" tabindex="-1">The repository already answers it twice, and one answer contradicts 0043 <a class="header-anchor" href="#the-repository-already-answers-it-twice-and-one-answer-contradicts-0043" aria-label="Permalink to &quot;The repository already answers it twice, and one answer contradicts 0043&quot;">​</a></h3><p><code>ObjectBucket</code> is a Form whose desired schema has <strong>no properties at all</strong>. Its whole portable desired state is its identity: a bucket exists. On takoserver its data plane is reached by an ordinary S3 client, through a credential route (<code>/v1/organizations/{org}/resources/{uid}/s3-credentials</code>) that vends a short-lived, bucket-scoped key. Nothing about the endpoint or the credential is in portable state. That is exactly the shape the question is looking for.</p><p><code>ObjectBucket</code> also declares <code>edge.objects@1.0.0</code>, and it is worth being exact about what that is, because the obvious reading is wrong. <code>edge.objects</code> is not an HTTP wire protocol competing with S3: it fixes the storage&#39;s SEMANTICS — strong read-after-write, streaming bodies, last-writer-wins per key, no cross-key atomicity, no versioning, a 5 GiB object ceiling — and a Binding projects those operations into the consumer&#39;s own runtime, where the code writes <code>env.MEDIA.get(...)</code> rather than signing a request. The two live at different layers, and the binding is a surface S3 does not provide at all.</p><p>Where they DO overlap is the semantics, and 0043&#39;s test is about the category rather than the layer: object storage has a de-facto standard API, so a contract specifying object-storage semantics is one 0043 would question today. What follows from that is not that <code>edge.objects</code> should be withdrawn — its bytes are published and <a href="./0037-immutability-begins-at-stable.html">decision 0037</a> keeps them — but that a bucket needs no SECOND Form to be reachable by an ordinary S3 client.</p><p>takoserver&#39;s <code>/v1/ai</code> is the second answer, and a different shape: nothing is provisioned per tenant, an organization key is simply granted <code>ai:invoke</code>. That is a service you call, not a resource you own.</p><h3 id="what-is-missing" tabindex="-1">What is missing <a class="header-anchor" href="#what-is-missing" aria-label="Permalink to &quot;What is missing&quot;">​</a></h3><p>A sealed slot&#39;s <code>service</code> member is <code>{apiVersion, protocol}</code>. It names a PROTOCOL, so a slot can only ever mean &quot;some service, resolved out of band, that speaks this&quot;. It cannot name a bucket the same desired document just asked for. Provision-and-wire is therefore not expressible in one portable document, which is the gap.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A standard-backed Form provisions an INSTANCE and names the standard as its data plane. It declares no Interface that respecifies that standard, and its desired state carries no member the standard already defines.</strong></p><p>0043 forbids a Form that describes what a protocol MEANS. It does not forbid a Form that says an instance of one EXISTS — and existence is portable desired state in a way the protocol&#39;s semantics are not: <em>&quot;I need a bucket&quot;</em> is the same sentence on every host, while how large, where, and on whose hardware are offering policy and never portable (<a href="./0007-current-forms-exclude-substrate-operation.html">decision 0007</a>).</p><p>The rule has a mechanical test: a standard-backed Form&#39;s desired schema adds nothing the standard already carries. <code>ObjectBucket</code>&#39;s empty desired schema is what that looks like when it is done right.</p><p><strong>A sealed slot may name a resource, not only an out-of-band service.</strong> A slot resolves to either the service an operator wired out of band — unchanged — or a resource in the same desired state that provides the named protocol. Either way the host resolves the endpoint and the credential, and neither enters portable state; the difference is only whether the thing on the other end is something the same document asked for.</p><p><strong>One resource, two projections — never two Forms for one thing.</strong> A bucket already exists as a Form, so a standard-backed bucket is not minted beside it. What is added is the second way to reach the same resource: a typed binding projects the Interface into code running on the host&#39;s own runtime, and a sealed slot hands credentials to everything else — a laptop, a CI job, a process on another cloud. Splitting the resource in two would make an author choose between them at provisioning time for a difference that only shows up at consumption time.</p><p><strong>A call-only standard gets no Form.</strong> Inference, mail submission, telemetry export: nothing is provisioned per tenant, so there is no instance to own and nothing for a lifecycle to hold. The slot is the whole mechanism, and what such a standard needs from this project is an entry in the protocol vocabulary that 0043 governs — <code>openai-compatible</code> and <code>otlp</code> are on that roadmap and are not yet minted.</p><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p>Slot resolution refuses a reference to a resource that does not provide the named protocol, at prepare time, exactly as it refuses an unsatisfiable slot today (decision 0045). The protocol vocabulary stays closed and every widening is held to 0043&#39;s table. A proposed standard-backed Form is a reviewed change whose review asks one question: does its desired state state anything the standard already states? If yes, it is a respecification wearing a lifecycle.</p><p><code>edge.objects</code> and <code>ObjectBucket</code> are retained as published. What this record stops is not their existence but the reflex to mint a parallel Form whenever a standard is involved: the question a review asks is whether the resource already exists, and only then whether a new one would state anything the standard already states.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>takoserver&#39;s two-sided role becomes constructible rather than aspirational: it serves the standard endpoints itself, and as a Takoform host it provisions instances of them — and an author writes both halves in one desired document, provisioning a bucket and wiring it into a worker as an <code>s3-compatible</code> slot, with no endpoint and no credential anywhere in that document.</p><p>The cost is that Takoform gains Forms in categories 0043 sent to the integrate column. That is not a reversal of 0043: those Forms carry no semantics, and the moment one of them grows a member the standard already defines, this record says it has become the thing 0043 refused.</p><p>This record also corrects itself on one point, because the first draft asserted more than the evidence carried: it called <code>edge.objects</code> a respecification of the S3 data plane. It is not — it is a semantic contract with a runtime projection, and S3 is a wire protocol. The overlap is real and worth naming, and the overstatement was not.</p>',26)])])}const u=t(n,[["render",r]]);export{m as __pageData,u as default};
