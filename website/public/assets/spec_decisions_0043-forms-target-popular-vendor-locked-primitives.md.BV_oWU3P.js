import{_ as t,o as a,c as o,ag as r}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"0043 — A Form Family targets a popular primitive whose APIs are vendor-locked; de-facto standards are integrated, not respecified","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0043-forms-target-popular-vendor-locked-primitives.md","filePath":"spec/decisions/0043-forms-target-popular-vendor-locked-primitives.md"}'),d={name:"spec/decisions/0043-forms-target-popular-vendor-locked-primitives.md"};function s(i,e,n,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="_0043-—-a-form-family-targets-a-popular-primitive-whose-apis-are-vendor-locked-de-facto-standards-are-integrated-not-respecified" tabindex="-1">0043 — A Form Family targets a popular primitive whose APIs are vendor-locked; de-facto standards are integrated, not respecified <a class="header-anchor" href="#_0043-—-a-form-family-targets-a-popular-primitive-whose-apis-are-vendor-locked-de-facto-standards-are-integrated-not-respecified" aria-label="Permalink to &quot;0043 — A Form Family targets a popular primitive whose APIs are vendor-locked; de-facto standards are integrated, not respecified&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-23</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>One family exists, and its shapes all come from one platform: the Edge Platform Family fixes the Workers model. The withdrawn v1alpha2 epoch had covered more ground — relational databases, container services, key/value stores, vector indexes — but as least-common-denominator contracts, which <a href="./0008-forms-preserve-service-shape.html">decision 0008</a> rejected: a Form fixes the complete shape of one proven primitive, and what is exchangeable is the host, never the meaning.</p><p>Widening the lineup again therefore needs a selection rule, or the same drift recurs: either every cloud product grows a Form (unbounded, and most would be LCD contracts again), or Forms accrete wherever the maintainer happens to work (which is how one platform&#39;s shapes became the whole catalog).</p><p>The rule falls out of asking where a host-neutral desired-state contract adds value at all. A service category can be popular and portable already — object storage has the S3-compatible API, relational databases have the PostgreSQL wire protocol, caches have the Redis protocol. There a portability layer already exists, an ecosystem of clients speaks it, and a Takoform respecification would compete with a standard instead of adding one. The categories that actually lock applications in are the popular managed services whose provisioning and service APIs are vendor-specific — offered by every major cloud, portable to none of them.</p><h2 id="the-landscape-partitioned" tabindex="-1">The landscape, partitioned <a class="header-anchor" href="#the-landscape-partitioned" aria-label="Permalink to &quot;The landscape, partitioned&quot;">​</a></h2><p>The rule is only as good as the survey behind it, so the survey is recorded. Sweeping the popular managed-service categories of the major clouds and splitting them by &quot;does a de-facto standard API exist&quot;:</p><table tabindex="0"><thead><tr><th>Category</th><th>De-facto standard</th><th>Side</th></tr></thead><tbody><tr><td>Object storage</td><td>S3-compatible API</td><td>integrate</td></tr><tr><td>Relational database</td><td>PostgreSQL / MySQL wire</td><td>integrate</td></tr><tr><td>Cache</td><td>Redis protocol</td><td>integrate</td></tr><tr><td>Message broker</td><td>Kafka protocol, AMQP</td><td>integrate</td></tr><tr><td>Document store (Mongo-flavored)</td><td>MongoDB wire (Cosmos/DocumentDB emulate it)</td><td>integrate</td></tr><tr><td>Search</td><td>Elasticsearch API (OpenSearch emulates it)</td><td>integrate</td></tr><tr><td>Mail submission</td><td>SMTP</td><td>integrate</td></tr><tr><td>Container orchestration</td><td>Kubernetes API</td><td>integrate</td></tr><tr><td>Auth flow</td><td>OIDC / OAuth2</td><td>integrate</td></tr><tr><td>Telemetry export</td><td>OTLP (OpenTelemetry)</td><td>integrate</td></tr><tr><td>LLM inference serving</td><td>OpenAI-compatible API (standardizing)</td><td>integrate</td></tr><tr><td>Browser push</td><td>RFC 8030 Web Push</td><td>integrate</td></tr><tr><td>Regional FaaS (Lambda, Cloud Functions)</td><td>none</td><td><strong>Form Family</strong></td></tr><tr><td>Serverless containers (Cloud Run, Fargate, Container Apps)</td><td>none</td><td><strong>Form Family</strong></td></tr><tr><td>Document/KV table (DynamoDB, Firestore, Cosmos native)</td><td>none</td><td><strong>Form Family</strong></td></tr><tr><td>Pull queue (SQS, Cloud Tasks)</td><td>none</td><td><strong>Form Family</strong></td></tr><tr><td>Pub/sub fanout (SNS, EventGrid)</td><td>none</td><td><strong>Form Family</strong></td></tr><tr><td>Scheduler (Cloud Scheduler, EventBridge Scheduler)</td><td>none</td><td><strong>Form Family</strong></td></tr><tr><td>Vector index (Pinecone, Vertex, serverless vector stores)</td><td>none</td><td><strong>Form Family</strong></td></tr><tr><td>Workflow engine (Step Functions, Durable Functions)</td><td>none</td><td>family candidate</td></tr><tr><td>Event bus/router (EventBridge)</td><td>none</td><td>family candidate</td></tr><tr><td>Realtime client push (managed WebSocket)</td><td>none</td><td>family candidate</td></tr><tr><td>Identity pool management (Cognito, Firebase Auth admin)</td><td>none</td><td>family candidate</td></tr><tr><td>Batch compute</td><td>none</td><td>family candidate</td></tr><tr><td>Addressable actors (Durable Objects)</td><td>none</td><td>family candidate</td></tr><tr><td>Edge workers (Workers, Fastly Compute)</td><td>none</td><td><strong>Form Family</strong> (edge, existing)</td></tr></tbody></table><p>The split is not &quot;clouds have no originality&quot; — it is an asymmetry: <strong>data planes have largely standardized while the serverless control planes have not</strong>, and the unstandardized half is exactly where a host-neutral contract is the only portability there is. One platform sits almost entirely on the locked side — the Workers platform speaks essentially no external standard except R2&#39;s S3 compatibility — which is why the first family filled itself with one platform&#39;s shapes and why the catalog must not stop there.</p><p>Virtual machines and other substrate stay out on separate grounds: decision <a href="./0007-current-forms-exclude-substrate-operation.html">0007</a> excludes substrate operation from portable desired state regardless of standards.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A Form Family is minted only for a service category that is (a) popularly offered as a managed service across major providers and (b) has no de-facto standard API — the category where a host-neutral contract is the only portability there is.</strong></p><p><strong>A category with a de-facto standard API is never respecified as a Form. Takoform integrates with it instead</strong>: a Form&#39;s runtime reaches the standard service as an external standard-service binding (<a href="./0045-external-standard-services-are-sealed-slots.html">decision 0045</a>) — the standard protocol is the contract, and the host resolves the endpoint and credential the way it already resolves sensitive slots, so neither ever enters portable desired state. The protocol vocabulary starts at <code>s3-compatible</code>, <code>postgresql</code>, <code>redis</code>, <code>smtp</code>; the survey above is its growth roadmap (<code>kafka</code>, <code>amqp</code>, <code>mongodb</code>, <code>elasticsearch-compatible</code>, <code>openai-compatible</code>, <code>otlp</code> are the recorded candidates), and every widening is a reviewed change held to this record&#39;s test.</p><p>Under this rule the v1 lineup is <strong>eight families</strong>:</p><table tabindex="0"><thead><tr><th>Family group</th><th>Fixed shape</th><th>Replaces (withdrawn)</th></tr></thead><tbody><tr><td><code>edge.forms.takoform.com</code></td><td>the Workers platform model (existing, 15 Forms)</td><td>—</td></tr><tr><td><code>function.forms.takoform.com</code></td><td>regional FaaS: artifact + handler + event invocation, concurrency, timeout (the Lambda shape)</td><td><code>EdgeWorker</code> in part</td></tr><tr><td><code>container.forms.takoform.com</code></td><td>OCI-image service with immutable revisions and traffic splitting (the Cloud Run/Knative shape)</td><td><code>ContainerService</code></td></tr><tr><td><code>table.forms.takoform.com</code></td><td>document/KV table with declared keys and secondary indexes (the DynamoDB shape)</td><td><code>KeyValueStore</code>, <code>StatefulEntity</code> in part</td></tr><tr><td><code>queue.forms.takoform.com</code></td><td>pull-based at-least-once queue with visibility timeout and dead-lettering (the SQS shape)</td><td><code>Queue</code></td></tr><tr><td><code>topic.forms.takoform.com</code></td><td>fanout topic with push subscriptions and filter policies (the SNS shape)</td><td>—</td></tr><tr><td><code>schedule.forms.takoform.com</code></td><td>standalone cron invoking a declared target with a retry policy (the Cloud Scheduler shape)</td><td><code>Schedule</code></td></tr><tr><td><code>vector.forms.takoform.com</code></td><td>fixed-dimension vector index with declared metric and namespaces</td><td><code>VectorIndex</code></td></tr></tbody></table><p>The Edge family&#39;s <code>AtLeastOnceQueue</code> is not the same Form as <code>queue</code>&#39;s: one is push-delivery into a worker consumer, the other is pull with visibility semantics — two proven shapes, two contracts, per decision 0008.</p><p><code>workflow</code>, <code>eventbus</code>, <code>realtime</code>, <code>identity</code>, <code>batch</code>, and <code>actor</code> are recorded as family candidates: each satisfies the rule, and each is deferred for its own reason (state-language size, coupling to <code>workflow</code>, protocol churn, security surface, narrower demand, single-vendor shape) rather than by the rule.</p><p>What is deliberately <strong>excluded</strong>: every integrate-side row above. The earlier <code>postgres</code>-family idea dies here — PostgreSQL is a standard, so it is an integration target.</p><p>The same principle is adopted ecosystem-wide: takoserver builds on de-facto standards where they exist and defines contracts only where the category is vendor-locked. That obligation lives with takoserver; this record states the shared principle.</p><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p>Minting a family is a reviewed spec change, and this record is what the review holds it against: a proposed family must name the popular category, show the absence of a de-facto standard API, and name the one proven shape it fixes (decision 0008). Widening the standard-service protocol vocabulary is held to the same table. The external standard-service binding contract is <a href="./0045-external-standard-services-are-sealed-slots.html">decision 0045</a> with its own conformance obligations.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The lineup grows to eight families with six recorded candidates, every one justified by the same sentence, and the catalog stops being one platform&#39;s silhouette. Where the industry already solved portability, Takoform rides the standard instead of fighting it — which is also what keeps the spec small enough to graduate.</p>',24)])])}const u=t(d,[["render",s]]);export{m as __pageData,u as default};
