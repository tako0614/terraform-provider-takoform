import{_ as t,o,c as a,ag as n}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"0029 — An immutable revision is named by its content","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0029-immutable-revisions-are-named-by-their-content.md","filePath":"spec/decisions/0029-immutable-revisions-are-named-by-their-content.md"}'),r={name:"spec/decisions/0029-immutable-revisions-are-named-by-their-content.md"};function s(i,e,d,l,h,c){return o(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="_0029-—-an-immutable-revision-is-named-by-its-content" tabindex="-1">0029 — An immutable revision is named by its content <a class="header-anchor" href="#_0029-—-an-immutable-revision-is-named-by-its-content" aria-label="Permalink to &quot;0029 — An immutable revision is named by its content&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-09</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Edge Platform Family splits one running worker across five Forms, and two of them — <code>WorkerBundle</code> and <code>WorkerVersion</code> — carry the <code>revision</code> role (<a href="./0009-form-families-and-namespaced-api-versions.html">0009</a>). A host refuses every update to a revision-role resource, so any desired change to one is a REPLACEMENT rather than a mutation. In Terraform, a replacement of a resource whose <code>name</code> the author pinned lands under the name the old resource still holds.</p><p>That was believed to deadlock. It was an inference from the spec, so it was driven end to end against the deterministic v1alpha3 reference host with the real <code>tofu</code> (1.12.3) and <code>terraform</code> (1.15.8) CLIs, over a live worker aggregate — a bundle a version executes, a version a deployment weights — with only the module bytes changed between applies. What actually happens:</p><ul><li><strong>Destroy-then-create</strong>, Terraform&#39;s default replacement order, fails on the FIRST step. <code>takoform_worker_bundle.app: Destroying...</code> returns <code>dependency_in_use</code> (409): <em>resource is the target of 1 live relation holder(s) held by WorkerVersion counter-version</em>. Nothing is mutated and the apply exits non-zero.</li><li><strong><code>create_before_destroy = true</code></strong> fails on the first step too, from the other side: <code>takoform_worker_bundle.app: Creating...</code> returns <code>invalid_argument</code> (400) — <em>prepare on an existing resource requires the Takoform-Expected- Generation fence</em>. The name is still occupied, a create carries no generation fence, and the host refuses before the mutation. Nothing is mutated.</li><li>A third fact the run exposed is worse than the deadlock and was not part of the hypothesis. The plan did not replace the <code>WorkerVersion</code> at all — only the bundle. A version references its bundle by NAME, the pinned name did not change, so the version had no diff. Even if the bundle replacement had somehow completed, the deployment would still serve the old version, pinned by UID to the old bundle incarnation (<a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a>), and the new code would never reach traffic.</li></ul><p>So the deadlock is real, it is symmetric, and the configuration that produces it is the obvious one. There is also no plan that repairs it: the destroy is refused while the holder lives, and the holder is not being replaced.</p><p>A name derived from content alone then turned out to have the opposite problem, found by driving TWO <code>worker-app</code> instances in one space from byte-identical build output. They derive one manifest digest, therefore one <code>bundle-dbdf1aacff23</code>, and what happens next is a coin toss. Sometimes the deterministic create replays the same UID for the same principal and both module instances silently manage one host address; sometimes the second create loses its <code>If-None-Match: *</code> and the apply fails with <em>prepare on an existing resource requires the Takoform-Expected-Generation fence</em> (<code>invalid_argument</code>, 400). Either way the first destroy is wrong: it is refused because the peer&#39;s version still holds the bundle, or it succeeds and takes the peer&#39;s bundle with it. A digest is a name and not an ownership claim (<a href="./0012-artifacts-use-content-addressed-upload.html">0012</a>); Terraform requires each managed address to have exactly one owner. Nothing in the content, the space, or the framework says who that owner is — a provider is never shown its own resource address — so the owner has to be DECLARED.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>A revision&#39;s host name is derived from its content and its declared owner, and the provider refuses at plan time the replacement that cannot complete.</p><ol><li><p><strong>Derived names.</strong> On a <code>revision</code>-role Form the provider&#39;s <code>name</code> attribute is <code>Optional+Computed</code>. When the configuration omits it, the provider derives <code>&lt;prefix&gt;-&lt;first 12 hex of the content digest&gt;-&lt;first 12 hex of the owner digest&gt;</code>, where the prefix is the Form&#39;s slug with the family&#39;s <code>worker-</code> qualifier removed — so <code>bundle-&lt;manifest digest prefix&gt;-&lt;owner digest prefix&gt;</code> and <code>version-&lt;spec digest prefix&gt;-&lt;owner digest prefix&gt;</code>. The content digest is the manifest digest when the Form&#39;s whole desired state is a content address, and the RFC 8785 canonical digest of the desired spec otherwise. Changed content is therefore a different resource NAME, created beside the old one, rather than a replacement of the same one.</p></li><li><p><strong>The owner is declared, and required.</strong> A new provider-side attribute, <code>revision_owner</code>, names the stable thing this revision belongs to — the <code>ModuleWorker</code> of the aggregate, in every ordinary case. It is authoring input only: no wire member carries it, the host never sees it, and the one thing it decides is the derived name. It is REQUIRED whenever the provider derives that name, and the provider refuses at plan time to derive a name without it (<code>takoform.provider/revision-owner-missing</code>) rather than mint one two owners can both reach. Pinning <code>name</code> and setting <code>revision_owner</code> together is also refused (<code>takoform.provider/revision-owner-ignored</code>): the pinned name settles the question and the owner would decide nothing. The owner travels as a digest rather than as a literal prefix so the derived name has a fixed width; a literal owner would push the longest legal owner name past the 63-character portable grammar, and the only way back inside it would be to refuse owner names the provider itself accepts.</p></li><li><p><strong>The plan computes it, and apply agrees byte for byte.</strong> The name is derived in <code>ModifyPlan</code> from values the plan already knows, and <code>name</code> is added to <code>RequiresReplace</code> there, because attribute plan modifiers have already run by the time <code>ModifyPlan</code> changes a value. Where the plan cannot resolve the content — a build output that does not exist yet — the planned value stays unknown on a create and untouched on a replacement, and apply derives it from the same function over the same spec.</p></li><li><p><strong>A same-name replacement is a plan error.</strong> When a revision-role resource already exists, a plan replaces it for any reason other than the relation-drift recovery marker of <a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a>, and the planned name equals the recorded one, the provider raises</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>This immutable revision cannot be safely replaced under the same host name.</span></span>
<span class="line"><span>Use a new revision name or the official worker-app module.</span></span></code></pre></div><p>naming both refusals above, the exact FormRef, and the pointer <code>/metadata/name</code>.</p></li><li><p><strong>The official <code>worker-app</code> module is the ordinary surface.</strong> The raw Forms stay the low-level one. The module writes no <code>name</code> on either revision, sets <code>revision_owner = var.name</code> on both, and declares <code>create_before_destroy</code> on both — derived names alone are not enough, because Terraform&#39;s default order still destroys the old revision first and the destroy is still refused. The module&#39;s <code>name</code> is the right owner because it is already the one name an author chooses and it is already unique in the space: two instances of the module that collide on it collide on the <code>ModuleWorker</code> first, where the host refuses. The identity and the deployment deliberately do NOT carry <code>create_before_destroy</code>: they are the stable things, and the deployment is updated in place.</p></li><li><p><strong>The sequence is measured, not assumed.</strong> A conformance command (<code>cmd/worker-authoring-conformance</code>) drives both CLIs against the reference host and asserts the exact mutation timeline of a code change —</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PUT WorkerBundle bundle-&lt;new&gt;-&lt;owner&gt; 201</span></span>
<span class="line"><span>PUT WorkerVersion version-&lt;new&gt;-&lt;owner&gt; 201</span></span>
<span class="line"><span>PUT WorkerDeployment &lt;name&gt;-deployment 200</span></span>
<span class="line"><span>DELETE WorkerVersion version-&lt;old&gt;-&lt;owner&gt; 204</span></span>
<span class="line"><span>DELETE WorkerBundle bundle-&lt;old&gt;-&lt;owner&gt; 204</span></span></code></pre></div><p>— while sampling the <code>ModuleWorker</code>&#39;s Ready condition throughout the apply. Ready is a claim about SERVICE (<a href="./0016-the-worker-aggregate-has-one-active-deployment.html">0016</a>), so an observation of <code>Ready=False</code> during the apply would be exactly the window where nothing serves. The run must record zero.</p></li><li><p><strong>Two owners are measured too.</strong> The same command stands up two <code>worker-app</code> instances in one space from byte-identical build output, asserts that the two bundles and the two versions are four distinct names, then moves ONE owner forward — the apply that deletes that owner&#39;s old bundle and version — while sampling the other owner&#39;s Ready condition. The untouched owner must still hold exactly the revisions it held before, and must never be observed unserved.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A revision&#39;s name is no longer something an author chooses, and plans now show <code>bundle-…</code>/<code>version-…</code> names that change on every code change. That is the honest rendering of what a revision is; the stable handle an author reads and references is the <code>ModuleWorker</code> identity, whose name they still choose.</li><li>Two revisions of ONE owner with identical content derive one name. That is correct — they are the same revision — and a genuine 48-bit collision between different contents fails on the host&#39;s own create fence rather than overwriting anything. Two DIFFERENT owners no longer do, which is the whole point of the owner half.</li><li>A revision name is now 32 or 33 characters rather than 19, and reading it takes two glances instead of one: the content half still answers &quot;which bytes&quot;, and the owner half answers &quot;whose&quot;. The content half is unchanged and still equals the manifest digest prefix, so an operator comparing a bundle name against an artifact manifest reads the same twelve characters as before.</li><li>The raw surface gained a required-in-practice argument. Omitting both <code>name</code> and <code>revision_owner</code> used to work and now fails at plan with a diagnostic naming the repair. That is a deliberate trade: what it used to do was mint a name a second configuration could reach, and the failure mode of that is a destroy that breaks somebody else&#39;s worker.</li><li>An author who pins a name keeps it, and gets the plan error instead of a failed apply. Pinning remains legitimate: an imported revision has a name the host already assigned, and the raw surface must be able to express it.</li><li>Deleting the old revisions is now part of the ordinary apply rather than a separate cleanup, so a long-lived worker does not accumulate them.</li><li><code>create_before_destroy</code> is a Terraform meta-argument, invisible to a provider. The plan error therefore fires for both orders, because both fail; the provider cannot tell them apart and does not need to.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Let the author keep pinning names and document the ordering.</strong> This is the status quo, and the measurement above is what it costs: the first apply after a code change fails, in either order, with an error that names a dependency rather than the naming decision that caused it. No amount of documentation makes a deadlocked apply recoverable from inside Terraform.</li><li><strong>Make the provider delete the holder and re-create it.</strong> A provider that deletes a resource it was not asked to delete is a provider that can lose a deployment. It would also have to sequence three resources&#39; lifecycles from inside one resource&#39;s apply, which Terraform&#39;s graph exists to do.</li><li><strong>Model shared bundle ownership explicitly, so two versions may legitimately reference one bundle.</strong> This is the other honest reading of the collision: two owners with identical bytes really do want the same artifact, and a bundle really is just content. It was rejected on what it costs. The provider would have to ADOPT a revision it did not create — the create fence (<code>If-None-Match: *</code>) exists precisely to refuse that, and a create that silently adopts cannot tell &quot;my own bytes&quot; from &quot;somebody else&#39;s resource that happens to match&quot;. Deletion would then need reference counting the host does not expose and Terraform cannot hold, because each state file knows only its own references; the first <code>terraform destroy</code> would either leak the bundle forever or break the other owner, which is the defect it was meant to fix. And it would put two Terraform addresses on one resource permanently, which is the one thing Terraform&#39;s model does not admit. A discriminator costs one attribute and thirteen characters of name.</li><li><strong>Suffix the name with a serial (<code>-1</code>, <code>-2</code>).</strong> A serial is state the provider would have to keep and reconcile, it is not reproducible from the configuration, and two workspaces applying the same bytes would produce different names. A content digest is a function of the thing itself.</li><li><strong>Derive the bundle name from the spec digest rather than the manifest digest.</strong> Rejected because the manifest digest IS the bundle&#39;s content address; hashing a one-field document that already contains it adds a level of indirection and makes the same bytes derive different names under different spec spellings.</li><li><strong>Fire the plan error for the relation-drift replacement too.</strong> A revision whose relation target moved is replaced under the same name by design (<a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a> rule 7), and that path already states its own remedy. Refusing it here would remove a recovery without offering a better one.</li><li><strong>Prefix the derived name with the owner literally (<code>counter-bundle-&lt;digest&gt;</code>).</strong> More readable, and rejected on length. The portable grammar admits 63 characters, so a literal owner would leave 42 or 43 for the owner name, and the only way to keep the derivation inside the grammar would be for the module — and the raw surface — to refuse owner names <code>takoform_module_worker</code> accepts. Narrowing the provider&#39;s own grammar is a defect this same review found elsewhere in this change, and reproducing it here to buy legibility is not a trade worth making.</li><li><strong>Derive the owner from something the provider already knows.</strong> There is nothing. Two colliding resources share a space, share a Form, and share their content by construction; a <code>WorkerVersion</code>&#39;s spec happens to carry its <code>worker</code>, but a <code>WorkerBundle</code>&#39;s carries nothing at all, and two versions of one worker with an identical spec still collide. The Terraform resource address would settle it, and the plugin framework never shows a provider its own address.</li><li><strong>Mark the name unknown when the plan cannot resolve the content.</strong> Unknown differs from the recorded value, so it would force a replacement on every plan of a resource whose build output happened to be absent — a perpetual diff produced by the provider rather than by any change. Leaving the planned value alone hands the case to the plan error, which states the real problem.</li></ul>`,15)])])}const u=t(r,[["render",s]]);export{m as __pageData,u as default};
