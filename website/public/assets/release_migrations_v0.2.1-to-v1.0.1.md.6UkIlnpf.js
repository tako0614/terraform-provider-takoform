import{_ as a,o as t,c as o,ag as s}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"Provider v0.2.1 to v1.0.1","description":"","frontmatter":{},"headers":[],"relativePath":"release/migrations/v0.2.1-to-v1.0.1.md","filePath":"release/migrations/v0.2.1-to-v1.0.1.md"}'),i={name:"release/migrations/v0.2.1-to-v1.0.1.md"};function r(n,e,d,c,l,p){return t(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="provider-v0-2-1-to-v1-0-1" tabindex="-1">Provider v0.2.1 to v1.0.1 <a class="header-anchor" href="#provider-v0-2-1-to-v1-0-1" aria-label="Permalink to &quot;Provider v0.2.1 to v1.0.1&quot;">​</a></h1><p>This is an explicit resource cutover, not an in-place provider upgrade. Provider <code>v1.0.1</code> is published and its authenticated Terraform/OpenTofu readback from the canonical Registry is retained. These instructions are now the operator procedure for that exact signed provider.</p><p>This guide applies only when the state was written by provider <code>v0.2.1</code>. Provider <code>v0.1.x</code> state must remain pinned to the exact <code>v0.1.x</code> release that wrote it; it cannot be safely refreshed through <code>v0.2.1</code> as an intermediate migration step.</p><h2 id="why-a-direct-upgrade-is-unsafe" tabindex="-1">Why a direct upgrade is unsafe <a class="header-anchor" href="#why-a-direct-upgrade-is-unsafe" aria-label="Permalink to &quot;Why a direct upgrade is unsafe&quot;">​</a></h2><p>Provider <code>v0.2.1</code> and published <code>v1.0.1</code> each compile 34 exact Form identities, but none of the old identities is unchanged:</p><ul><li>33 kinds keep a Terraform resource type while changing their exact definition and/or package identity;</li><li><code>HttpService</code> is removed and <code>EdgeWorker</code> is added;</li><li>the writable <code>takoform_interface</code> resource is removed;</li><li>common naming, connection, artifact, and semantic field contracts changed.</li></ul><p>An exact Resource GET includes the build-pinned <code>FormRef</code> and package digest. Without a migration fence, refreshing old state through provider v1 could query the new exact identity, receive <code>404</code> while the old exact Resource still exists, and remove that Terraform state entry as if the Resource had been deleted.</p><p>Provider v1 therefore gives every Form resource schema version <code>1</code>, records the exact identity in five computed state attributes, and gives schema version <code>0</code> only a diagnostic-only rejection handler. It returns no transformed state and makes no Resource lifecycle request. OpenTofu or Terraform must therefore reject v0.2.1 Form state before resource lifecycle code can query an exact Resource identity. A missing or mismatched exact identity also produces a diagnostic before any Resource lifecycle request and retains state. Provider configuration may already have performed host discovery; this fence is about Create, Read, Update, Delete, and Import requests, not every possible HTTP request. There is no portable transformation that could honestly infer backend data migration, rename Resources, or translate all changed desired schemas.</p><p>The machine-readable audit is <a href="v0.2.1-to-v1.0.1.json"><code>v0.2.1-to-v1.0.1.json</code></a>.</p><h2 id="_1-inventory-and-back-up-under-v0-2-1" tabindex="-1">1. Inventory and back up under v0.2.1 <a class="header-anchor" href="#_1-inventory-and-back-up-under-v0-2-1" aria-label="Permalink to &quot;1. Inventory and back up under v0.2.1&quot;">​</a></h2><p>Keep the old provider and source configuration pinned while inspecting the old state:</p><div class="language-hcl vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">hcl</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">terraform</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  required_providers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    takoform</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      source  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;registry.terraform.io/tako0614/takoform&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      version </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;= 0.2.1&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">tofu providers</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">tofu state list</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">tofu state pull &gt; takoform-v0.2.1-state.json</span></span></code></pre></div><p><code>state pull</code> output can contain sensitive values. Store it according to the backend recovery policy and never commit it. For each Form Resource, record its Space, name, exact host identity, consumers, public outputs, data backup, and rollback owner. A stateful Resource needs a host/backend-specific data migration and recovery proof; Takoform cannot synthesize one from HCL state.</p><h2 id="_2-normalize-an-old-opentofu-provider-address-when-necessary" tabindex="-1">2. Normalize an old OpenTofu provider address when necessary <a class="header-anchor" href="#_2-normalize-an-old-opentofu-provider-address-when-necessary" aria-label="Permalink to &quot;2. Normalize an old OpenTofu provider address when necessary&quot;">​</a></h2><p>First inspect <code>tofu providers</code>. Many v0.2.1 configurations already used the canonical Terraform Registry address. Only state that actually names <code>registry.opentofu.org/tako0614/takoform</code> needs replacement.</p><p>Keep version <code>0.2.1</code> pinned, change <code>required_providers.takoform.source</code> to the canonical address shown above, back up state, and run the command without <code>-auto-approve</code> so the proposed state identities can be reviewed:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">tofu state replace-provider \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  registry.opentofu.org/tako0614/takoform \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  registry.terraform.io/tako0614/takoform</span></span></code></pre></div><p>Then run <code>tofu providers</code> again and take another state backup. This operation changes only the provider address stored in state. It does not convert a v0.2.1 Form identity, desired schema, remote Resource, or host data into its v1 successor, and it does not make an in-place provider upgrade safe.</p><h2 id="_3-cut-over-resources-explicitly" tabindex="-1">3. Cut over Resources explicitly <a class="header-anchor" href="#_3-cut-over-resources-explicitly" aria-label="Permalink to &quot;3. Cut over Resources explicitly&quot;">​</a></h2><p>Use a separate state/work directory for candidate v1 Resources. Provider constraints select one version of one source address for a configuration, so the old management state and new cutover state must not be mixed.</p><p>For each Resource:</p><ol><li>Choose a new Space/name when old and new Resources must coexist.</li><li>Translate the HCL deliberately to the current resource schema.</li><li>Create the new exact Resource with provider v1.</li><li>Verify host observation, public outputs, runtime behavior, and any data copy.</li><li>Move consumers to the new Resource and prove rollback while the old one still exists.</li><li>Delete the old Resource through its v0.2.1 state only after cutover is accepted.</li></ol><p><code>takoform_http_service</code> requires a semantic migration to <code>takoform_edge_worker</code>; a state rename is not a migration. A writable <code>takoform_interface</code> may point at an Interface record the host has already adopted. Destroying that resource through v0.2.1 sends the host delete request and can delete the adopted record. After explicitly verifying host ownership, binding continuity, and a rollback record, remove only the Terraform state entry for an adopted Interface. Destroy it through v0.2.1 only when the operator intentionally wants to retire the remote Interface record. No <code>state mv</code>, <code>replace-provider</code>, or v1 import can change either old remote identity into the new one.</p><p>Destroy-and-recreate in one name/Space is an alternative only when the operator has explicitly accepted the outage and data-loss boundary. Takoform does not declare that safe by default.</p><h2 id="_4-start-the-v1-state-line" tabindex="-1">4. Start the v1 state line <a class="header-anchor" href="#_4-start-the-v1-state-line" aria-label="Permalink to &quot;4. Start the v1 state line&quot;">​</a></h2><p>Only a clean v1 state, or a Resource that the host already exposes under the current exact v1 Form identity, may be created or imported with provider v1. Every successful create, read, and import records:</p><ul><li><code>form_api_version</code>;</li><li><code>form_kind</code>;</li><li><code>form_definition_version</code>;</li><li><code>form_schema_digest</code>;</li><li><code>form_package_digest</code>.</li></ul><p>Future provider builds must fail closed when those values differ from the build-pinned exact identity. A genuine <code>404</code> removes state only after this identity check proves that the state and provider are talking about the same exact Form.</p><p>Do not run <code>plan</code>, <code>apply</code>, <code>refresh</code>, or <code>import</code> with provider v1 against a v0.2.1 state snapshot. The schema-version and exact-identity diagnostics are a last-resort loss-prevention fence, not an automatic migration mechanism.</p>`,30)])])}const v=a(i,[["render",r]]);export{u as __pageData,v as default};
