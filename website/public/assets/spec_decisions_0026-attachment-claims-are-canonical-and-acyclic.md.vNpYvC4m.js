import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.LJP3gfxI.js";const u=JSON.parse(`{"title":"0026 — An attachment's claim is decided on canonical, resolved identity","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0026-attachment-claims-are-canonical-and-acyclic.md","filePath":"spec/decisions/0026-attachment-claims-are-canonical-and-acyclic.md"}`),s={name:"spec/decisions/0026-attachment-claims-are-canonical-and-acyclic.md"};function i(r,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="_0026-—-an-attachment-s-claim-is-decided-on-canonical-resolved-identity" tabindex="-1">0026 — An attachment&#39;s claim is decided on canonical, resolved identity <a class="header-anchor" href="#_0026-—-an-attachment-s-claim-is-decided-on-canonical-resolved-identity" aria-label="Permalink to &quot;0026 — An attachment&#39;s claim is decided on canonical, resolved identity&quot;">​</a></h1><ul><li>Status: accepted; amended 2026-08-09 — the tenant half of the claim scope is now MEASURED, the acyclic half of the dead-letter rule is measured for a shape other than one chain, and the two SURFACES this record always named besides <code>apply</code> — <code>import</code>, and the commit of an accepted <code>202</code> — are measured too, along with the U-label refusal rule 1 states</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Two Edge Platform Family attachments claim something outside the resource that declares them, and a host could decide both claims wrongly while passing every check the lane had. Neither claim is visible to a desired-state schema: both documents are valid on their own, and what makes one wrong is the store.</p><p><strong>A hostname was compared as written.</strong> <code>WorkerCustomDomain.hostname</code> is a DNS name, and DNS is case-insensitive with a trailing dot meaning the fully qualified form of the same name. <code>API.Example.com</code>, <code>api.example.com.</code> and <code>api.example.com</code> are one hostname to DNS and were three to the host. Worse, nothing compared them at all: the lane had no uniqueness rule for <code>hostname</code>, so two attachments could serve one name and the host would hold two answers to one request with no rule choosing between them — the incompleteness <a href="./0008-forms-preserve-service-shape.html">decision 0008</a> forbids, at the one place where the world outside the host decides who is right.</p><p><strong>A dead-letter reference could point home.</strong> <code>QueueConsumer.deadLetterQueue</code> could name the consumer&#39;s own queue, or close a longer cycle through another consumer&#39;s. <a href="./0020-the-edge-interfaces-state-their-data-and-delivery-model.html">Decision 0020</a> says a message that exhausts its retries arrives at the dead-letter queue as a NEW message, with a new identity and its attempt count starting again at 1. That is the right rule, and it is exactly what makes a cycle unbounded: <code>maxRetries</code> bounds one message&#39;s deliveries and nothing bounds the loop. The platform would build an infinite redelivery on the author&#39;s behalf and report success.</p><p>The two are one failure. A host was deciding an attachment&#39;s claim on the bytes a client wrote — a string, and one reference read one hop deep — instead of on the identity those bytes name.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The rules below are normative; the operative text lives in <a href="./../host-api/v1alpha3.html#an-attachment-s-claim-is-decided-on-canonical-resolved-identity"><code>../host-api/v1alpha3.md</code></a>. Both fail <code>invalid_argument</code> (400) before any mutation, on <code>apply</code> and on <code>import</code> alike, and are <strong>re-verified when an accepted <code>202</code> commits</strong> — the precedent of <a href="./0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a> rule 9 and <a href="./0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a> rule 10: a claim is a statement about the store, and the store moves between accept and commit.</p><h3 id="_1-a-hostname-is-canonicalized-before-it-is-compared-and-before-it-is-stored" tabindex="-1">1. A hostname is canonicalized before it is compared and before it is stored <a class="header-anchor" href="#_1-a-hostname-is-canonicalized-before-it-is-compared-and-before-it-is-stored" aria-label="Permalink to &quot;1. A hostname is canonicalized before it is compared and before it is stored&quot;">​</a></h3><p>The canonical form of <code>WorkerCustomDomain.hostname</code> is the trailing root dot removed and every ASCII letter lowercased. Canonicalization happens at the host&#39;s single materialization entry point — the same place declared defaults are applied, before validation, before the spec digest, before storage and echo — so three spellings of one name produce byte-identical desired state, the same <code>specDigest</code>, and the same <code>generation</code>. That placement is the rule, not an implementation detail: canonicalizing at the comparison instead would leave the store holding a spelling, and every later question would be answered about the spelling rather than about the name.</p><p>An internationalized name travels as its <strong>A-label</strong>. This is a refusal, not a conversion: performing IDNA/UTS-46 mapping inside the host would make the host&#39;s Unicode table version part of the portable contract, so two conforming hosts on different tables could canonicalize one U-label to two different names — the same class of privately-decided meaning decision 0019 rejected the compatibility date for. The Form&#39;s <code>hostname</code> pattern admits no non-ASCII byte, so a U-label is refused by the Form&#39;s own grammar and the conversion belongs in the author&#39;s tooling, where its Unicode version is the author&#39;s choice.</p><p>The pattern DOES admit an uppercase letter and a trailing dot, because refusing a legitimate DNS spelling is a different rule from agreeing on one identity for it, and only the second is what the lane needs.</p><h3 id="_2-one-canonical-hostname-is-claimed-by-at-most-one-attachment-per-tenant" tabindex="-1">2. One canonical hostname is claimed by at most one attachment, per tenant <a class="header-anchor" href="#_2-one-canonical-hostname-is-claimed-by-at-most-one-attachment-per-tenant" aria-label="Permalink to &quot;2. One canonical hostname is claimed by at most one attachment, per tenant&quot;">​</a></h3><p>A <code>WorkerCustomDomain</code> whose canonical hostname a live <code>WorkerCustomDomain</code> already serves is refused. Releasing the holder makes the claim representable.</p><p>Scope is the <strong>tenant</strong>, not the space. Spaces partition one tenant&#39;s resources; DNS does not partition with them, so two spaces claiming one hostname is the same collision as two resources in one space. Scope is not wider than the tenant either: what one tenant may claim is a question about who controls the name, which is authority a Form cannot answer and this contract does not pretend to.</p><p>The tenant is the <strong>authenticated tenant of the request</strong>, and it has to be, because nothing in the document names one: a reference carries <code>{apiVersion, kind, name}</code>, metadata carries a space, and neither is a boundary. A host compares the proposed hostname against the live claims of that tenant in every one of its spaces and against no others — including at commit, where the tenant is the one the accepted mutation was admitted from rather than whoever polls the operation.</p><p>That the scope must be SUPPLIED is what separates this rule from every other cross-resource rule in the lane. One deployment per worker, one consumer per queue, and the dead-letter walk below are all decided on a host-issued uid, and a uid already names one resource inside one boundary, so those scans cannot reach past it however the store is arranged. A hostname is a name DNS owns: the comparison carries no boundary of its own, so a host that answered it out of an unpartitioned store would enforce the rule host-wide and refuse a name this decision says another tenant may hold — while naming, in the refusal, a resource the caller may not read.</p><h3 id="_3-a-dead-letter-destination-never-leads-back" tabindex="-1">3. A dead-letter destination never leads back <a class="header-anchor" href="#_3-a-dead-letter-destination-never-leads-back" aria-label="Permalink to &quot;3. A dead-letter destination never leads back&quot;">​</a></h3><p>A <code>QueueConsumer</code> whose <code>deadLetterQueue</code> resolves to the UID of the queue it drains is refused, and so is one closing a cycle of any length through the dead-letter graph.</p><p>The graph is over QUEUES: the edge <code>Q -&gt; D</code> exists when the consumer of <code>Q</code> declares <code>D</code> as its dead-letter destination. Because a queue has at most one consumer (decision 0020), a queue has at most one outgoing edge, so a host follows a single path from the proposed destination and asks whether it returns to the origin. <strong>The walk terminates on any graph shape</strong> for two independent reasons: it admits each queue UID at most once, and the number of stored consumers is finite and does not grow while it runs. A cycle a laxer earlier state left behind therefore ends the walk instead of running it forever.</p><p>&quot;Any length&quot; is the load-bearing half. A host that asked only whether the DESTINATION&#39;s consumer points back at the origin refuses the self-reference and <code>A -&gt; B -&gt; A</code> and admits <code>A -&gt; B -&gt; C -&gt; A</code>, which is the same infinite circulation reached one hop later — and it is the shape an author actually builds, one consumer at a time. Nothing short of following the path decides this, so the required check drives a three-queue cycle: a corpus that stopped at two would be passed by the one-hop test.</p><h3 id="the-error-code" tabindex="-1">The error code <a class="header-anchor" href="#the-error-code" aria-label="Permalink to &quot;The error code&quot;">​</a></h3><p>Both refusals are <code>invalid_argument</code> (400), and deliberately not a 409.</p><p>The lane&#39;s closed taxonomy has no <code>already_exists</code>, and every 409 in it names a different fact: <code>resource_busy</code> is a concurrent mutation, <code>import_conflict</code> is adoption, <code>form_identity_conflict</code> is Form identity, <code>dependency_in_use</code> and <code>deletion_protected</code> are deletion edges, <code>uid_mismatch</code> is a fence. Reusing one would make a code mean two things, which is the one thing a closed taxonomy must not permit.</p><p><code>invalid_argument</code> is also the right answer on its own terms, and the lane already gives it to exactly this shape: one active deployment per worker (decision 0016 rule 1) and one consumer per queue (decision 0020) are both &quot;this request is well formed and states something untrue about what will run&quot;, and both are 400. A hostname another attachment serves and a dead-letter queue that leads home are the same sentence. The message names the holder, or the cycle, so the author can act on it without a second request.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A <code>WorkerCustomDomain</code> is now a claim on a name rather than a record about one. Applying the same hostname twice fails where it used to succeed, which is a behavior change for any configuration that was already broken.</li><li>The Terraform provider REFUSES a non-canonical hostname at plan time rather than rewriting it. A plan modifier cannot replace a Required attribute&#39;s configured value, and a Terraform attribute holds the literal string an author wrote, so a configuration the host would rewrite would plan one value and read back another forever. The diagnostic names the canonical spelling. This is the same three-places split decision 0016 rule 6 already uses: the client refuses what it cannot represent, the host canonicalizes because it is the only party that sees every client.</li><li>Hostname canonicalization is the lane&#39;s first host-side rewrite of desired state other than declared defaults, and it reuses that machinery exactly, so the &quot;effective spec IS the wire spec&quot; property already stated for defaults extends to it unchanged.</li><li>A dead-letter chain is now a DAG a host can walk, which is what makes &quot;exhausted messages come to rest&quot; a checkable claim rather than an intention. Building a two-queue mutual dead-letter pair is unrepresentable; a chain A -&gt; B -&gt; C is not.</li><li>Seven required conformance checks join the v1alpha3 runner list: <code>custom-domain-hostname-canonicalized</code>, <code>custom-domain-hostname-claim-unique</code>, <code>custom-domain-hostname-claim-stops-at-the-tenant</code>, <code>dead-letter-cycle-rejected</code>, <code>attachment-claim-decided-on-import</code>, <code>attachment-claim-revalidated-at-commit</code>, and <code>custom-domain-u-label-refused</code>. Each drives a configuration a laxer host accepts and proves nothing was stored, and each also drives the accepting case so a host cannot pass by refusing everything. Three of them drive the SCOPE of the rule and not only the rule: the claim check builds a second space with its own worker, version and deployment and collides from there while the first space&#39;s holder is live, in both directions, so a host enforcing uniqueness inside one space fails; the tenant check does the opposite and is described below; the cycle check closes a three-queue cycle, so a host testing the destination for an immediate back edge fails.</li><li><strong>The tenant half of the claim scope is measured too</strong> <em>(amended 2026-08-09)</em>. The first version of this record proved the SPACE half and declined the other, on the reasoning that requiring a host to ACCEPT a hostname another tenant serves would decide who controls that name. That reasoning was wrong about its own rule. Rule 2 above does not say a host may choose; it says the scope &quot;is not wider than the tenant&quot; and that a host answering the claim out of an unpartitioned store &quot;would refuse a name this decision says another tenant may hold&quot;. Leaving the only normative half a corpus cannot see to each host&#39;s own tests is how a boundary stops being one — and this boundary&#39;s failure is not an internal detail, it is one tenant denying another a DNS name it has every right to claim, on nothing but who asked first. Nor does measuring it decide who controls a name. Every hostname this corpus writes is under <code>.invalid</code>, which RFC 2606 reserves precisely so that no registry, host, or tenant has any claim to it. There is no control question for a host to be answering about such a name, so the only thing a refusal can be is a claim scan that reached past the tenant. A host with a control policy of its own — domain verification, an allow list — answers a different question about different names and is untouched. The check therefore drives BOTH polarities against one live pair, because the accepting half alone would be satisfied by a host with no claim rule at all: a second tenant&#39;s claim on the name the first tenant serves must SUCCEED, both claims must read back live under two host-issued uids, releasing one must leave the other exactly where it was — and a THIRD claim inside the second tenant must still be refused. It also reads the refusal, because the message names the holder: a host-wide scan would name a resource in another tenant, which is the membership oracle the tenant-isolation checks of <a href="./0028-the-resource-plane-is-tenant-isolated.html">decision 0028</a> spend a whole check removing. So the refusal must name the caller&#39;s own holder and must not name anyone else&#39;s.</li><li><strong>The acyclic half of the dead-letter rule is measured as a graph, not as a chain</strong> <em>(amended 2026-08-09)</em>. The cycle check drove three refusals and one accepted path, and every destination it ever accepted had in-degree zero and no consumer. A host that never walks the graph — refusing only the self-reference and any destination that already has a consumer — passed the whole check, including the three-queue cycle the check exists for, and would then refuse legitimate configurations this decision permits. In-degree is unbounded here: a queue has at most one CONSUMER and therefore at most one outgoing edge (decision 0020), and nothing bounds how many chains end at one queue. The check now closes a diamond as well, so two acyclic chains meeting is accepted and the shortcut fails.</li><li><strong>Both rules are measured on all three surfaces this record names</strong> <em>(amended 2026-08-09)</em>. &quot;On <code>apply</code> and on <code>import</code> alike, and re-verified when an accepted <code>202</code> commits&quot; was stated here from the beginning and driven on <code>apply</code> alone, so a host validating the whole gauntlet on apply and treating the other two as &quot;write what was already agreed&quot; passed everything. Neither is a formality. <code>import</code> is the surface that mints a resource for something already running, which is exactly where nobody is watching: adoption is how a second attachment lands on a hostname the host already answers, or a consumer that closes a dead-letter loop enters the store. And a claim is a statement about the STORE, which is why the precedent cited above exists at all — between accept and commit the store moves, and each of the two requests that produce the collision was correct when it was made. <code>attachment-claim-decided-on-import</code> drives both rules through adoption in both polarities, so a host cannot pass by refusing every adoption; <code>attachment-claim-revalidated-at-commit</code> accepts a <code>202</code> while the store makes it legal, moves the store with a synchronous request that is itself legal, and requires the commit to terminate <code>invalid_argument</code>, commit nothing, and leave the synchronous resource alive.</li><li><strong>The U-label refusal of rule 1 is measured</strong> <em>(amended 2026-08-09)</em>. Rule 1 says an internationalized name travels as its A-label and that this is a refusal rather than a conversion, and nothing drove it — so a host performing IDNA/UTS-46 mapping, which is one library call and looks like a kindness, conformed. What that imports is the host&#39;s Unicode table version into a portable contract: two conforming hosts on two tables canonicalize one U-label to two different names, so one desired state would claim two different hostnames depending on where it was applied. <code>custom-domain-u-label-refused</code> drives the refusal while the name is FREE, so nothing but the Form&#39;s own grammar can produce it, at both pre-mutation gates; and it drives the same name written as its A-label, which must be accepted and stored byte-for-byte, so a host that refuses anything unusual — or that rewrites an A-label into something else — fails instead.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Refuse a non-canonical hostname at the host too.</strong> Simpler, and it makes uniqueness trivially safe — but it refuses spellings DNS calls correct, so an author who pasted a fully-qualified name from a zone file gets an error about a dot. Canonicalizing costs one function and answers the same question.</li><li><strong>Compare hostnames case-insensitively without storing the canonical form.</strong> Rejected because the stored value would still be a spelling: two clients reading the resource would see different names for one attachment depending on who applied last, and <code>generation</code> would move on a re-apply that changed nothing.</li><li><strong>Convert U-labels to A-labels in the host.</strong> Rejected above: it imports a Unicode table version into a portable contract. It is also strictly larger than the problem, since the conversion is deterministic in the author&#39;s tooling and the result is what the Form&#39;s grammar already admits.</li><li><strong>Make hostname uniqueness per space.</strong> Rejected because it is a rule about the host&#39;s own bookkeeping rather than about the name. Two spaces are one tenant&#39;s; DNS answers one of them.</li><li><strong>Refuse only the self-referencing dead-letter queue.</strong> Rejected because it is the case an author is least likely to write and most likely to notice. The loop that actually happens is built one consumer at a time, by two people or on two days, and only a graph walk sees it.</li><li><strong>Bound redelivery with a hop counter instead of forbidding cycles.</strong> Rejected because it would put a number nobody chose into the delivery model, and because a message that legitimately traverses a long chain and one caught in a loop would be cut off by the same rule. Refusing the cycle keeps the delivery model exactly as decision 0020 states it.</li></ul>',30)])])}const p=t(s,[["render",i]]);export{u as __pageData,p as default};
