import{_ as i,o as t,c as o,ag as a}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"Historical Specification, Provider, and publication evidence policy","description":"","frontmatter":{},"headers":[],"relativePath":"spec/publication-freeze.md","filePath":"spec/publication-freeze.md"}'),n={name:"spec/publication-freeze.md"};function r(c,e,s,d,l,p){return t(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="historical-specification-provider-and-publication-evidence-policy" tabindex="-1">Historical Specification, Provider, and publication evidence policy <a class="header-anchor" href="#historical-specification-provider-and-publication-evidence-policy" aria-label="Permalink to &quot;Historical Specification, Provider, and publication evidence policy&quot;">​</a></h1><p>Takoform has independent release and publication axes. Evidence from one axis MUST NOT be treated as authority for another.</p><h2 id="historical-specification-1-1-evidence" tabindex="-1">Historical Specification 1.1 evidence <a class="header-anchor" href="#historical-specification-1-1-evidence" aria-label="Permalink to &quot;Historical Specification 1.1 evidence&quot;">​</a></h2><p>The Specification 1.1 track freezes the portable specification source and is retained as historical evidence. The current Host API <code>forms.takoform.com/v1</code> is a stable protocol identity independent of this receipt. One complete, exact committed snapshot of the normative <code>spec/</code> tree satisfied the historical receipt&#39;s readiness; the append-only numbered ledger records that publication state. Candidate Forms, packages, conformance corpora, and reference implementations remain quality and adoption evidence; they are not current Host API or Provider authority.</p><p>Identity <code>1.0</code> was never published, is withdrawn, and is never reused. The separately generated five-class <a href="../release/specification-compatibility.json">compatibility report</a> records raw source digests, owning ledgers, migration dispositions, and the historical Host API source pin. It is compatibility evidence only, not publication evidence, a release asset, or a prerequisite. The historical Specification receipt has no Host API, Form publication, or Provider effect and does not mint a <code>/v1.1</code> or v2 lane/schema/tag/receipt.</p><p>The canonical machine policy is <a href="publication-evidence.json"><code>publication-evidence.json</code></a>, and the numbered release ledger is <a href="../release/specification-releases.json"><code>../release/specification-releases.json</code></a>. Decision <a href="./decisions/0057-specification-1-1-compatibility-and-independent-identities.html">0057</a> amends decisions <a href="./decisions/0052-the-specification-is-released-on-its-own-line.html">0052</a> and <a href="./decisions/0053-specification-and-provider-release-evidence.html">0053</a>, and <a href="./decisions/0055-specification-release-needs-only-normative-source.html">0055</a> define the boundary.</p><h2 id="c1-c2-c3-and-c4" tabindex="-1">C1, C2, C3, and C4 <a class="header-anchor" href="#c1-c2-c3-and-c4" aria-label="Permalink to &quot;C1, C2, C3, and C4&quot;">​</a></h2><p>The W09 workflow keeps the freeze, evidence, receipt, and derived-public boundaries separate:</p><ul><li><p><strong>C1 — normative freeze and executable tooling:</strong> the normative <code>spec/</code> tree and its validation tooling are frozen, while every field in the publication evidence record remains <code>null</code>.</p></li><li><p><strong>C2 — evidence-only source snapshot:</strong> one exact committed Specification source snapshot is recorded, with only the allowed evidence projections; candidate corpus, reference conformance, Provider, Host, and compatibility report data remain outside the release evidence record.</p></li><li><p><strong>C3 — authoritative publication receipt:</strong> one immutable Specification 1.1 receipt is appended to the numbered ledger and its static projections. The receipt carries only the source-snapshot prerequisite and publication readback.</p></li><li><p><strong>C4 — deterministic derived-public refresh:</strong> the first ancestry commit after C3 is its direct single-parent child and changes only the explicit generated compatibility, site-status, README, and website outputs. C4 is not publication authority. It MUST NOT change <code>spec/**</code>, publication evidence, any ledger or ledger projection, release authority/tooling, Provider/Form/Host source, or an unrelated file. Later descendants are permitted after this fixed point.</p><p>Concretely, that bounded write set is <code>README.md</code>; the canonical/static/public compatibility report; the static/public site status; every generated VitePress <code>website/public/**/*.html</code> page (including <code>404.html</code>); and only the content-hashed <code>website/public/assets/**</code> closure changed by the build. The immutable runtime-ABI fixture <code>website/public/conformance/runtime-abi-v1/bundles/unsupported-media-type/page.html</code>, <code>website/docs/reference.md</code>, and <code>website/public/hashmap.json</code> do not change in this transition and are not allowed C4 paths.</p></li></ul><p>The compatibility report is generated and checked independently and is never a C2/C3 asset, prerequisite, or release identity. C3 and C4 remain separate, linear commits and are never squashed.</p><p>The current generator index contains eight versionless families and 31 exact Experimental <code>0.x</code> FormRefs. Edge contains 16 and has no current <code>ObjectBucket</code>, <code>edge.objects</code>, or <code>module-worker.object-bucket</code>. The stable standard-service contract is <code>standards.takoform.com/v1</code>; protocol identifiers are opaque reverse-DNS strings, not a Takoform-owned enum.</p><p>Publishing Specification 1.1 does not publish or promote the separate Host API v1 candidate, and it does not:</p><ul><li>relabel any current Form as <code>1.0.0</code> or Stable;</li><li>publish a Form Package, Interface, or Binding;</li><li>release Provider 3;</li><li>prove any Host, backend, runtime, or product supports the contract; or</li><li>authorize a commit, tag, publication, deployment, signing action, or production change.</li></ul><p>A future Stable Form begins at <code>1.0.0</code> only through an explicit per-Form decision. Form maturity remains independent from the Host API and numbered Specification.</p><h2 id="official-provider" tabindex="-1">Official Provider <a class="header-anchor" href="#official-provider" aria-label="Permalink to &quot;Official Provider&quot;">​</a></h2><p>The Terraform Provider is an independent implementation stream. Provider 2.1.1 and its exact Host v1beta1/versioned Edge 15-Form identities remain immutable Registry history.</p><p>Provider 3.0.0 is a Registry-published but non-normative reference implementation. Its evidence track binds the exact current Form projection, Provider identity ledger, retained codecs, state continuity, and migration behavior. Its publication cannot close the Specification track.</p><h2 id="adoption-evidence" tabindex="-1">Adoption evidence <a class="header-anchor" href="#adoption-evidence" aria-label="Permalink to &quot;Adoption evidence&quot;">​</a></h2><p>Independent Hosts, Takoserver, Takosumi, other backends, runtime ABI reports, production consumers, compatibility windows, package publishers, signers, and operators may record adoption evidence in their owning repositories. Those facts are useful for support, implementation confidence, and product readiness. They are not Takoform release authorities and can neither add a Specification prerequisite nor substitute for one.</p><p>Decisions 0044 and 0046 are retained as adoption-program history. Decision 0053 supersedes them as Specification and Provider release gates.</p><h2 id="retained-publication-history" tabindex="-1">Retained publication history <a class="header-anchor" href="#retained-publication-history" aria-label="Permalink to &quot;Retained publication history&quot;">​</a></h2><p><a href="publication-blockers.json"><code>publication-blockers.json</code></a> records an earlier v1beta1 Form Package/public-service attempt. Its bytes and open statuses remain immutable historical truth. The new Specification assertion does not rewrite or require closure of those rows.</p><p>Likewise:</p><ul><li>published schema identities remain append-only in <a href="../release/public-schema-identities.json"><code>../release/public-schema-identities.json</code></a>;</li><li>occupied document lanes remain recorded in <a href="../release/published-document-lanes.json"><code>../release/published-document-lanes.json</code></a>;</li><li>Provider release and FormRef identities remain append-only in the release ledgers; and</li><li>retired or retained bytes are never republished under a different meaning.</li></ul><p>The historical <code>assert:publishable</code> command remains the assertion for that old Form Package/public-service obligation set. It is not a Specification 1.1 or Provider 3 gate.</p><h2 id="fail-closed-checks" tabindex="-1">Fail-closed checks <a class="header-anchor" href="#fail-closed-checks" aria-label="Permalink to &quot;Fail-closed checks&quot;">​</a></h2><ul><li><code>bun run check:publication-evidence</code> validates both independent track shapes and reports their open/ready state.</li><li><code>bun run check:specification-releases</code> validates the numbered ledger, protects committed release entries append-only, and enforces the exact C3-to-C4 history fixed point after a 1.1 receipt exists.</li><li><code>bun run check:specification-1-1-release</code> asserts only the normative source snapshot.</li><li><code>bun run check:provider-v3-release</code> asserts only the non-normative Provider milestone.</li><li><code>bun run check:takoform-milestones</code> asserts both as an optional project checkpoint.</li></ul><p>When Specification source evidence is <code>null</code>, readiness remains open; when it records one clean reachable commit, its exact normative bytes must match. Candidate and reference evidence may stay <code>null</code> without blocking it. A dirty, staged, unreachable, or digest-mismatched normative snapshot fails.</p><p>There is no <code>stable-mint</code> alias. The readiness assertion does not silently promote Forms or packages.</p>',29)])])}const m=i(n,[["render",r]]);export{u as __pageData,m as default};
