import{_ as i,o as a,c as s,ag as t}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"Form Definition profiles","description":"","frontmatter":{},"headers":[],"relativePath":"spec/form-definition/index.md","filePath":"spec/form-definition/index.md"}'),o={name:"spec/form-definition/index.md"};function n(r,e,d,c,l,h){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="form-definition-profiles" tabindex="-1">Form Definition profiles <a class="header-anchor" href="#form-definition-profiles" aria-label="Permalink to &quot;Form Definition profiles&quot;">​</a></h1><p>A Form Definition is a deterministic, data-only description of one portable service shape. Requirement keywords are used as described in <a href="./../conformance.html"><code>../conformance.md</code></a>.</p><p>The current authoring profile is <a href="/schemas/v1/form-definition.schema.json"><code>form-definition-v1.schema.json</code></a> with <a href="/schemas/v1/form-ref.schema.json"><code>form-ref-v1.schema.json</code></a>. The Form Definition profile moves independently of the exact FormRef shape. Stable v1 carries the closed authoring vocabulary used only by versionless Forms that declare the stable <code>forms.takoform.com/v1</code> Host lane; it does not reinterpret the occupied Beta Host lane or a retained Form package. The <a href="/schemas/v1beta1/form-definition.schema.json"><code>form-definition-v1beta1.schema.json</code></a> profile is retained unchanged and still validates the <code>edge.forms.takoform.com/v1beta1</code> package bytes embedded by retained Provider 2.1.1. The unpublished v1beta2 Form Definition schema remains as predecessor history, while <a href="/schemas/v1beta1/form-ref.schema.json"><code>form-ref-v1beta1.schema.json</code></a> remains the exact reference profile of those retained v1beta1 packages; no published identity is rewritten. Across current and retained profiles: its <code>apiVersion</code> is a namespaced Form Family group (<a href="./../decisions/0009-form-families-and-namespaced-api-versions.html">decision 0009</a>), it declares a required closed <code>role</code> (identity/revision/deployment/attachment/policy), it references exact Interface and Binding contracts (<code>providedInterfaces</code>, <code>acceptedBindings</code>), its desired schema never contains a <code>name</code> property (the wire envelope owns <code>metadata.name</code> — <a href="./../decisions/0011-resource-identity-generation-and-revision.html">decision 0011</a>), and free semantic tokens are prohibited (<a href="./../decisions/0008-forms-preserve-service-shape.html">decision 0008</a>).</p><p>The retained <code>forms.takoform.com/v1alpha2</code> Draft 2020-12 schemas are <a href="../../formpackage/schemas/form-definition-v1alpha2.schema.json"><code>form-definition-v1alpha2.schema.json</code></a> and <a href="../../formpackage/schemas/form-ref-v1alpha2.schema.json"><code>form-ref-v1alpha2.schema.json</code></a>. The unversioned filenames <a href="../../formpackage/schemas/form-definition.schema.json"><code>form-definition.schema.json</code></a> and <a href="../../formpackage/schemas/form-ref.schema.json"><code>form-ref.schema.json</code></a> are retained v1alpha1 Legacy profiles, not aliases for the retained v1alpha2 or current family profiles.</p><h2 id="exact-formref" tabindex="-1">Exact FormRef <a class="header-anchor" href="#exact-formref" aria-label="Permalink to &quot;Exact FormRef&quot;">​</a></h2><p>The immutable reference to a definition is exactly these four fields, with no extensions:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;function.forms.takoform.com&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Function&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;definitionVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sha256:&lt;64 lowercase hexadecimal characters&gt;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>kind</code> MUST be a PascalCase portable kind, <code>definitionVersion</code> MUST be SemVer, and <code>schemaDigest</code> MUST be SHA-256 over the definition&#39;s RFC 8785 canonical bytes. The definition MUST repeat the first three identity fields, and a verifier MUST reject any mismatch. A current group is versionless: the exact identity is <code>(group, kind, definitionVersion, schemaDigest)</code>. Resolution never substitutes a family latest, a sibling kind, or a Definition with a different version or digest.</p><h2 id="definition-fields" tabindex="-1">Definition fields <a class="header-anchor" href="#definition-fields" aria-label="Permalink to &quot;Definition fields&quot;">​</a></h2><p>A definition contains:</p><ul><li>the three non-digest identity fields;</li><li>a title and optional description;</li><li>inline Draft 2020-12 desired and observed schemas, plus an optional output schema;</li><li>optional immutable JSON Pointer fields;</li><li>an explicit subset of <code>create</code>, <code>read</code>, <code>update</code>, <code>delete</code>, <code>import</code>, and <code>observe</code> — the current capability vocabulary. <code>refresh</code> and <code>drift</code> belong to the withdrawn lanes. The current profile (<code>v1/form-definition.schema.json</code>) admits neither; its predecessor <code>v1beta1</code> still admits <code>refresh</code> and is retained unchanged for the packages published against it, because narrowing a served identity would change what it meant when it was published; the open <code>(name, version)</code> Interface descriptors of the withdrawn v1alpha2 profile are replaced by exact digest-bound <code>providedInterfaces</code> (<a href="./../interface-contract/"><code>../interface-contract/</code></a>);</li><li>optional references to data-only positive desired/observed/output fixtures and negative schema fixtures in the same package, with independent maxima of 32 positive fixtures and 32 negative fixtures.</li></ul><p>The current family structure deliberately has no maturity or deprecation field. Proposal, Experimental, Stable, and Legacy state is owned outside the immutable definition as defined in <a href="./../project-lifecycle.html"><code>../project-lifecycle.md</code></a>. This keeps one mutable authority for a fact that can change while immutable Definition bytes cannot.</p><p>Published v1alpha1 Definitions retain their historical document-local <code>compatibility-candidate</code>, <code>standard</code>, or <code>deprecated</code> field. A verifier MUST preserve those bytes, but current tooling MUST NOT infer maturity from them or copy the field into a current family definition.</p><p>Negative fixtures name their validation stage. Host Support evidence executes <code>desired</code> negatives in the host role; provider compatibility evidence executes <code>desired</code> and <code>observed</code> negatives before state projection. <code>output</code> remains a valid package-structure validation stage, but a conformance claim fails closed on it until a portable runner contract defines how its role executes that case.</p><p>The published Form Definition JSON Schema is a normative structural minimum. The semantic verifier rules below are also normative: document-local reference closure, closed-object proof, validation-work limits, fixture semantics, and the portable data-only vocabulary can reject a document that satisfies the outer JSON Schema.</p><h2 id="closed-recursive-values-and-defaults" tabindex="-1">Closed recursive values and defaults <a class="header-anchor" href="#closed-recursive-values-and-defaults" aria-label="Permalink to &quot;Closed recursive values and defaults&quot;">​</a></h2><p>The stable-v1 authoring model adds only the concrete data shapes current Forms need; it does not add a generic JSON, bytes, message, or graph-expression type.</p><ul><li>An ordered string list preserves item order and duplicate values. It is not interchangeable with a string set.</li><li>A string map and string-set map both declare <code>maxProperties</code>, use the portable map-key grammar below, and bound their string values. Every set value also declares <code>maxItems</code> and uses <code>uniqueItems: true</code>.</li><li>RFC 8785 canonical encoding supplies deterministic object-key order. Defaults and conformance examples additionally sort each string-set-map value lexically; ordered string lists are never sorted or deduplicated.</li><li>Closed objects and object lists recurse through the same vocabulary. Defaults materialize inside a present object and every present list element. An optional object whose absence is itself semantic remains absent; nested defaults do not synthesize that parent.</li><li>A tagged object is a closed <code>oneOf</code>. Every branch requires the same string discriminator with one branch-specific <code>const</code>, forbids unknown members, and contains all and only that variant&#39;s fields. Reference traversal follows only the selected valid branch.</li></ul><h2 id="structural-and-resolved-uid-constraints" tabindex="-1">Structural and resolved-UID constraints <a class="header-anchor" href="#structural-and-resolved-uid-constraints" aria-label="Permalink to &quot;Structural and resolved-UID constraints&quot;">​</a></h2><p>The stable-v1 <code>constraints</code> list adds exactly two desired-structure variants and four resolved-UID variants. The structural variants compare already validated desired values without coercion:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orderedPair&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;references&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/minInstances&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/maxInstances&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;uniqueBy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;list&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/secondaryIndexes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;member&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>orderedPair</code> carries exactly two distinct non-wildcard pointers, both naming required numeric properties, and requires the first value to be less than or equal to the second. <code>uniqueBy</code> names an object-list property plus one direct, required scalar member of every item; no two items in one list may carry the same JSON scalar value. Equality is typed JSON equality, so implementations do not stringify or otherwise coerce values. The list itself may be optional when its schema gives omission a portable meaning.</p><p>The four resolved-UID variants compare host-resolved immutable resource UIDs only: a reused name with a replacement UID is a different target, and group, kind, name, or desired bytes are never a substitute for resolution.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acyclic&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;reference&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/deadLetter/queue&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;distinctPair&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;references&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/target&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/deadLetter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;uniquePair&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;references&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/topic&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/target&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sameResolvedTarget&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;anchor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/function&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;members&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/versions/*/functionVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;through&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/function&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>acyclic</code> rejects an edge that would close a cycle through the declared relation. <code>distinctPair</code> requires its two local relations to resolve to different UIDs. <code>uniquePair</code> allows at most one live resource of the same exact Form to hold the ordered pair of resolved UIDs. <code>sameResolvedTarget</code> resolves the local <code>anchor</code>, resolves every local relation selected by <code>members</code>, then follows <code>through</code> on each member target; every resulting UID MUST equal the anchor UID.</p><p>The vocabulary is closed. <code>orderedPair.references</code>, <code>uniqueBy.list</code>, <code>acyclic.reference</code>, both resolved-pair members, and <code>sameResolvedTarget.anchor</code> carry no array wildcard. <code>members</code> carries exactly one <code>*</code> array token. <code>through</code> is an RFC 6901 pointer in the resolved member target&#39;s desired document, not a pointer in the declaring Form. A Definition with another kind, a foreign member, the wrong pointer cardinality, or a local pointer that is not a declared relation is invalid. The comparison and uniqueness scope are fixed by these variants; there is no universal graph DSL.</p><p><code>requiresHostApi</code> is checked against the mechanisms a Form actually declares, not against its kind or family. Resolved-UID constraints, tagged-branch relation traversal, sealed external-service slots, and a new Form-neutral required entrypoint require <code>forms.takoform.com/v1</code>. Ordinary JSON Schema validation does not raise the bound by itself. Retained v1beta1 declarations keep their published lower bound and meaning; this profile does not reinterpret or re-identify them.</p><h2 id="connections-were-the-withdrawn-profile-s-surface" tabindex="-1">Connections were the withdrawn profile&#39;s surface <a class="header-anchor" href="#connections-were-the-withdrawn-profile-s-surface" aria-label="Permalink to &quot;Connections were the withdrawn profile&#39;s surface&quot;">​</a></h2><p>The <code>connections</code> map — request-only permission/projection tokens naming another Resource — was the withdrawn v1alpha2 profile&#39;s capability surface. The current family replaces it end to end with exact digest-bound typed Bindings (<a href="./../binding-contract/"><code>../binding-contract/</code></a>) and uid-pinned relations (<a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a>, <a href="./../decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>); no current Definition declares <code>connections</code>, and the withdrawn semantics stay in git history with their profile.</p><h2 id="digest-bound-artifact-sources" tabindex="-1">Digest-bound artifact sources <a class="header-anchor" href="#digest-bound-artifact-sources" aria-label="Permalink to &quot;Digest-bound artifact sources&quot;">​</a></h2><p>An artifact-backed Form of the current family declares its bytes as ONE member: the content-addressed manifest digest committed through the artifact-transport upload API (<a href="./../decisions/0012-artifacts-use-content-addressed-upload.html">decision 0012</a>, <a href="./../artifact-transport/"><code>../artifact-transport/</code></a>). No URL enters desired state — where bytes are fetched from is a host fact, and the digest alone says which bytes. The withdrawn v1alpha2 profile&#39;s <code>artifactUrl</code>/<code>artifactSha256</code>/<code>artifactMediaType</code> source object and its credential-free-HTTPS grammar stay in git history with that profile.</p><p>Object schemas are closed by default and MUST set <code>&quot;additionalProperties&quot;: false</code>. A pure typed map is the only open-key escape. It must explicitly use <code>&quot;type&quot;: &quot;object&quot;</code>, must not mix fixed or dependent properties, must reject <code>patternProperties</code>, and must use a schema for <code>additionalProperties</code> plus this exact key policy:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;propertyNames&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;string&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;pattern&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;^[A-Za-z][A-Za-z0-9._-]{0,63}$&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;x-takoform-fieldPolicy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;portable-data-only-v1&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The marker is a host conformance requirement, not an annotation to ignore: map keys are checked with the same portable data-only forbidden-field policy as declared fields. <code>additionalProperties: true</code>, an omitted <code>additionalProperties</code>, a permissive or unmarked <code>propertyNames</code>, and <code>patternProperties</code> are rejected.</p><p>This rule applies at every nested schema node, not only at the desired or observed root. Boolean <code>false</code> is safe because it accepts no value; boolean <code>true</code>, <code>{}</code>, an implicit schema such as <code>{&quot;not&quot;:{&quot;type&quot;:&quot;string&quot;}}</code>, and object keywords such as <code>minProperties</code> without an explicit closed <code>type: object</code> are rejected because each can admit arbitrary objects. <code>allOf</code>/<code>anyOf</code>/<code>oneOf</code> and local <code>$ref</code> remain usable only when every relevant branch or resolved target proves that objects are excluded or closed. A non-object <code>type</code> is the normal proof for primitive and array schemas. Arrays must additionally declare <code>items</code> (a safe schema or <code>false</code>) so omitted item constraints cannot reintroduce arbitrary nested objects; tuple <code>prefixItems</code> do not remove that requirement for trailing items.</p><h2 id="hard-boundary" tabindex="-1">Hard boundary <a class="header-anchor" href="#hard-boundary" aria-label="Permalink to &quot;Hard boundary&quot;">​</a></h2><p>Definitions and every JSON payload are recursively checked for credential, secret, token, account, operator, target/pool, capacity, backend manager, provider config, price, SKU, billing, quota, SLA/support policy, executable, command, script, source/adapter/validation/runtime code, WebAssembly, and plugin fields. This policy is intentionally fail-closed. A host-owned implementation, placement, commercial configuration, or executable extension is not portable Form Definition data.</p><p>The check is structural: normalized exact names, exact camelCase, snake_case, or kebab-case tokens, and reviewed token sequences such as <code>api</code> + <code>key</code>, <code>private</code> + <code>key</code>, <code>service</code> + <code>offering</code>, and <code>manager</code> + <code>identifier</code> are compared with a forbidden vocabulary. Glued lowercase spellings are limited by exact reviewed compound-base and qualifier pairs such as <code>apikey</code> + <code>material</code>; the policy does not use arbitrary substring matching. Standard schema keys such as <code>description</code>, and prose values that discuss authentication, API keys, service offerings, or billing, remain valid; fields such as <code>authorization</code>, <code>oauthClient</code>, <code>sessionCookie</code>, <code>apiKeyValue</code>, <code>privateKeyPem</code>, <code>invoice</code>, <code>paymentMethod</code>, <code>currency</code>, <code>taxCode</code>, <code>serviceOfferingId</code>, <code>managerIdentifier</code>, and <code>region</code> do not.</p><p>Three desired-property spellings have shape-dependent exceptions; their names alone never bypass the boundary. <code>command</code> is permitted only as a bounded ordered list whose string items have both a non-empty pattern and an explicit maximum length. <code>concurrencyTarget</code> is permitted only as an integer with explicit minimum and maximum bounds. <code>target</code> is permitted only when its child schema proves one portable relation contract:</p><ul><li>a closed object with exactly <code>apiVersion</code>, <code>kind</code>, and <code>name</code>, a bounded name grammar, and exactly one of <code>x-takoform-target-formrefs</code> or <code>x-takoform-required-interface</code>; or</li><li>a closed, discriminator-selected <code>oneOf</code> with two through sixteen distinct branches, where every branch contains such an annotated relation object.</li></ul><p>Exact-Form annotations contain only complete group, kind, definition-version, and canonical SHA-256 identities. Required-Interface annotations contain only complete API version, name, version, and canonical SHA-256 identity. A bare string, an open object, an unannotated or ambiguously annotated reference, a tagged branch without a reviewed relation, and lookalike names such as <code>backendTarget</code> remain forbidden. These are schema proofs consumed equally by every implementation; they do not grant a provider or host authority to widen Form semantics.</p><p>Every package is an exact one-definition package, never one multi-definition package. The current generated catalog and package set predate decision <a href="./../decisions/0004-takoform-is-an-experimental-specification.html"><code>0004</code></a>: their <code>status: standard</code>, <code>structural-candidate</code>, and <code>portable-standard</code> values are retained Legacy document facts, not a current maturity hierarchy. Host Support and provider compatibility evidence are supplied by their owning implementations and are not synthesized by the package generator.</p>`,42)])])}const k=i(o,[["render",n]]);export{u as __pageData,k as default};
