import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.DzNHXt93.js";const p=JSON.parse('{"title":"Form Families","description":"","frontmatter":{},"headers":[],"relativePath":"spec/form-families.md","filePath":"spec/form-families.md"}'),i={name:"spec/form-families.md"};function n(r,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="form-families" tabindex="-1">Form Families <a class="header-anchor" href="#form-families" aria-label="Permalink to &quot;Form Families&quot;">​</a></h1><p>A Form Family is a named group of Forms that share one platform model and are designed to compose. A family is an API namespace and a catalog grouping; it is not a package unit, a maturity state, or a compatibility promise. The family model is decided in <a href="./decisions/0009-form-families-and-namespaced-api-versions.html">decision 0009</a>; the semantic rule every member Form must satisfy is <a href="./portability-boundary.html"><code>portability-boundary.md</code></a>.</p><h2 id="groups" tabindex="-1">Groups <a class="header-anchor" href="#groups" aria-label="Permalink to &quot;Groups&quot;">​</a></h2><p>Each family owns a DNS-like API group with its own version:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>edge.forms.takoform.com/v1alpha1</span></span>
<span class="line"><span>containers.forms.takoform.com/v1alpha1   (future)</span></span>
<span class="line"><span>forms.example.com/v1alpha1               (third party)</span></span></code></pre></div><p>A FormRef <code>apiVersion</code> is validated as <code>&lt;dns-like group&gt;/&lt;version&gt;</code>, not as a fixed constant. Official families use subdomains of <code>forms.takoform.com</code>. The groups <code>forms.takoform.com/v1alpha1</code> (Legacy) and <code>forms.takoform.com/v1alpha2</code> (retained provider-v2 preview) are frozen and never reused by a family. Family groups version independently; a new group version is a new namespace, and occupied FormRefs are never rebound.</p><p>Publisher identity never enters the FormRef. Semantic identity (FormRef), distribution bytes (package digest), publisher trust (signature policy), and implementation (Host Support) remain four independent facts.</p><h2 id="resource-roles" tabindex="-1">Resource roles <a class="header-anchor" href="#resource-roles" aria-label="Permalink to &quot;Resource roles&quot;">​</a></h2><p>Every current Form Definition declares one <code>role</code> from a closed enum. Roles let tooling enforce lifecycle rules mechanically.</p><table tabindex="0"><thead><tr><th>Role</th><th>Meaning</th><th>Rules</th></tr></thead><tbody><tr><td><code>identity</code></td><td>long-lived logical resource</td><td>stable name; carries no implementation snapshot</td></tr><tr><td><code>revision</code></td><td>immutable implementation snapshot</td><td>never updated in place; changes create a new resource</td></tr><tr><td><code>deployment</code></td><td>selects which revisions are active</td><td>the only mutable path for traffic movement and rollback</td></tr><tr><td><code>attachment</code></td><td>connects a parent to external events or endpoints</td><td>deleting an attachment never deletes the parent</td></tr><tr><td><code>policy</code></td><td>operating rules changed independently of the parent</td><td>never migrates into the parent identity</td></tr></tbody></table><p>Outward capability use (a worker using KV, buckets, databases, queues, services) is a typed Binding held by a revision resource (<a href="./decisions/0010-exact-interface-and-binding-contracts.html">decision 0010</a>). Inward activation (HTTP routes, custom domains, cron triggers, queue consumption) is an attachment resource. The two are never merged.</p><p>A family that splits one running thing across these roles owes a statement of what holds them together. For the Edge Platform Family that statement is <a href="./decisions/0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a>, normatively stated in <a href="./host-api/v1alpha3.html#the-worker-aggregate"><code>host-api/v1alpha3.md</code></a>: an identity has at most ONE deployment resource; that deployment selects revisions of its own identity, each named once, with weights summing to exactly 10000; every attachment is admitted against the deployment rather than against any stored revision, and refused when the deployment is absent or does not serve the handler the attachment invokes; a deployment change that would leave a live attachment or inbound binding unserved is refused, as is deleting the deployment while one lives; and the identity reports itself Ready only while its deployment actually serves. Because that last one is a representation rendered from another resource, a deployment change also moves the identity&#39;s revision and therefore its ETag, while leaving its generation alone.</p><h2 id="edge-platform-family" tabindex="-1">Edge Platform Family <a class="header-anchor" href="#edge-platform-family" aria-label="Permalink to &quot;Edge Platform Family&quot;">​</a></h2><p><code>edge.forms.takoform.com/v1alpha1</code> is the first official family. Its members fix the shape of a proven edge developer platform without naming its vendor. The authored first-milestone members are:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Compute      ModuleWorker, WorkerBundle, WorkerVersion, WorkerDeployment,</span></span>
<span class="line"><span>             WorkerCustomDomain, WorkerEndpoint, WorkerCronTrigger</span></span>
<span class="line"><span>Data         EdgeKVNamespace, ObjectBucket, SQLiteDatabase</span></span>
<span class="line"><span>Messaging    AtLeastOnceQueue, QueueConsumer</span></span></code></pre></div><p>Later milestones add further members through their own proposals — <code>StaticAssetBundle</code>, <code>WorkerRoute</code>, <code>SQLiteMigrationSet</code>, <code>SQLiteMigrationApplication</code>, <code>DenseVectorIndex</code>, <code>VectorMetadataIndex</code>, and the bucket policy resources (<code>BucketCorsPolicy</code>, <code>BucketLifecyclePolicy</code>, <code>BucketLockPolicy</code>). Listing a planned member here reserves nothing: a Form exists only when its proposal, catalog declaration, and candidate package exist.</p><p><code>ModuleWorker</code> fixes the ES Module Worker ABI by identity, and states what that ABI is: the exact Interface contract <code>worker.runtime@1.0.0</code> in its <code>providedInterfaces</code> (<a href="./decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>). That contract fixes the module&#39;s default-export shape, the <code>fetch</code>, <code>scheduled</code>, and <code>queue</code> signatures and what each event carries, the <code>env</code> object, <code>ctx.waitUntil</code>, exception handling, request and response body streaming, the minimum Web API surface, and module loading. Its handler vocabulary is those three and nothing else: a handler no attachment in this family can activate is a member no run can reach and no divergence between two hosts can be detected in, so it does not belong in the contract until the attachment that makes it observable ships beside it, in a new exact version (<a href="./decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>). A host that supports <code>ModuleWorker</code> implements that contract at its exact digest, and advertises it there; a Worker Version is the code that fills it.</p><p>Consequently <strong>a runtime revision is a new exact Interface version, and — if it changes what a Form desires — a new Form version. It is never a date.</strong><code>WorkerVersion</code> therefore declares no <code>compatibilityDate</code> and no <code>compatibilityFlags</code>. A compatibility date is meaningful only against a registry that states which behavior each date changes; this project publishes none, so two conforming hosts could read the same date differently, which is exactly the incompleteness <a href="./portability-boundary.html"><code>portability-boundary.md</code></a> forbids. The <code>handlers</code> vocabulary is the handler set the runtime contract defines, and a host refuses a handler that contract does not define before it mutates anything.</p><p>Two authored first-milestone decisions are recorded here so neither is read as an oversight:</p><ul><li><code>WorkerVersion</code> declares no <code>assets</code> field. Static assets served next to a worker belong to the separate <code>StaticAssetBundle</code> member above; <code>WorkerVersion</code> gains an <code>assets</code> reference to it when <code>StaticAssetBundle</code> lands in the next milestone. Until then a version fixes code, runtime behavior, configuration, and bindings only, and an asset-serving worker is not yet fully expressible.</li><li><code>WorkerVersion</code> names its sealed-value declaration <code>requiredSensitiveVars</code> rather than <code>secretRequirements</code>. The Form Package data-only policy rejects the token <code>secret</code> anywhere in a field name (<a href="./form-package/"><code>form-package/</code></a>), so the portable field carries the same fact — only the names of host-supplied sensitive values are portable state — in permitted vocabulary.</li><li><code>WorkerVersion</code> projects <code>vars</code> keys, <code>requiredSensitiveVars</code> entries, and every binding <code>name</code> into ONE runtime environment namespace, so their union must be unique (<a href="./decisions/0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a>). The desired schema cannot state it — <code>uniqueItems</code> compares whole objects, and no keyword relates one property&#39;s keys to a sibling array&#39;s element member — so a host refuses the collision before mutation and a client refuses it at plan time.</li><li>The family carries two inward activations for <code>fetch</code>, and they are two Forms rather than one Form with a mode. <code>WorkerCustomDomain</code> states which name the AUTHOR owns that reaches a worker; <code>WorkerEndpoint</code> states that the worker is reachable at all, at an address the HOST assigns and publishes as outputs (<a href="./decisions/0024-a-worker-is-reachable-at-a-host-assigned-address.html">decision 0024</a>). The desired states are disjoint — one carries a hostname, the other carries nothing but the worker — so a selector token between them would be a free semantic token of exactly the kind this family forbids. A worker may have both, and has at most one endpoint.</li><li><code>WorkerEndpoint</code> is the family&#39;s first member to declare an <code>outputSchema</code>, which makes its assigned address a typed contract rather than an untyped document a consumer decodes (<a href="./decisions/0025-declared-outputs-are-a-typed-contract.html">decision 0025</a>). A Form declaring one publishes exactly its members; a Form declaring none publishes no <code>status.outputs</code> at all.</li></ul><p>Semantics that differ from these shapes join other families instead of widening a member: <code>PostgresDatabase</code>, <code>FifoQueue</code>, <code>WasiFunction</code>, <code>ContainerService</code>, <code>TimezoneSchedule</code>, and durable-actor and addressable container designs are separate family work with their own proposals.</p><h2 id="what-a-family-does-not-do" tabindex="-1">What a family does not do <a class="header-anchor" href="#what-a-family-does-not-do" aria-label="Permalink to &quot;What a family does not do&quot;">​</a></h2><ul><li>It does not merge packages: one Form Package still contains exactly one Form Definition.</li><li>It does not grant maturity: family candidates are tracked in the family candidate set, and a member Form gains its own lifecycle record at its Experimental transition (no family member has made that transition yet).</li><li>It does not constrain hosts: a host may support any subset of a family and states that subset in its Host Support Profile.</li><li>It does not admit vendor identity: adapter profiles map family Forms to concrete backends outside the contract.</li></ul>`,23)])])}const u=t(i,[["render",n]]);export{p as __pageData,u as default};
