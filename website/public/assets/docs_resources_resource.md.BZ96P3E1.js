import{_ as o,o as r,c as t,a2 as a}from"./chunks/framework.BTLSZHNB.js";const m=JSON.parse('{"title":"takoform_resource","description":"Generic Host API v1alpha3 carrier for any Form, including third-party Forms, addressed by exact FormRef.","frontmatter":{"page_title":"takoform_resource Resource - takoform","subcategory":"Host API v1alpha3","description":"Generic Host API v1alpha3 carrier for any Form, including third-party Forms, addressed by exact FormRef."},"headers":[],"relativePath":"docs/resources/resource.md","filePath":"docs/resources/resource.md"}'),c={name:"docs/resources/resource.md"};function s(i,e,d,n,l,h){return r(),t("div",null,[...e[0]||(e[0]=[a('<h1 id="takoform-resource" tabindex="-1">takoform_resource <a class="header-anchor" href="#takoform-resource" aria-label="Permalink to &quot;takoform_resource&quot;">​</a></h1><p>Carries any Host API v1alpha3 Form by exact FormRef, including Forms published by third parties in their own API groups. It is not a Form and has no FormRef of its own: the configuration names the exact reference plus one JSON desired spec, so a Form this provider was never built against is still usable.</p><p>This resource speaks the Host API v1alpha3 lane and requires provider v2.1.0 or later (source candidate; not yet published). The configured host selects and operates the concrete backend; no attribute names a vendor, target, credential, price, or implementation. See the <a href="../../examples/resources/takoform_resource/resource.tf">complete example</a>.</p><p>Prefer a typed resource where one exists: the Edge Platform Family resources carry per-field validation, a typed plan, and import support. This carrier trades all three for reach.</p><h2 id="arguments" tabindex="-1">Arguments <a class="header-anchor" href="#arguments" aria-label="Permalink to &quot;Arguments&quot;">​</a></h2><p>The four <code>form_*</code> attributes are the exact FormRef. They, <code>name</code>, and <code>space</code> all force replacement, so existing state is never rebound to another identity.</p><ul><li><code>form_api_version</code> (String, required, forces replacement) — Exact namespaced Form group and version, for example <code>forms.example.com/v1alpha1</code>. The frozen <code>forms.takoform.com/v1alpha1</code> and <code>forms.takoform.com/v1alpha2</code> groups are rejected; retained-epoch Forms belong to the provider-v2 typed resources.</li><li><code>form_kind</code> (String, required, forces replacement) — Exact Form kind, matching <code>^[A-Z][A-Za-z0-9]{0,63}$</code>.</li><li><code>form_definition_version</code> (String, required, forces replacement) — Exact immutable Form definition version, a semantic version.</li><li><code>form_schema_digest</code> (String, required, forces replacement) — Exact immutable Form schema digest, <code>sha256:</code> followed by 64 lowercase hexadecimal characters.</li><li><code>name</code> (String, required, forces replacement) — Portable resource name (<code>metadata.name</code>), matching <code>^[a-z][a-z0-9-]{0,62}$</code>.</li><li><code>space</code> (String, optional, forces replacement) — Exact opaque SpaceID; overrides the provider default.</li><li><code>spec_json</code> (String, optional) — The portable desired spec as one JSON object string (for example <code>jsonencode({...})</code>); the provider sends the parsed object as <code>spec</code>. Omitting it means the empty spec <code>{}</code>.</li><li><code>create_timeout</code> / <code>update_timeout</code> / <code>delete_timeout</code> (String, optional) — Go durations bounding each operation (defaults <code>20m</code> / <code>20m</code> / <code>30m</code>).</li></ul><h2 id="read-only-attributes" tabindex="-1">Read-only attributes <a class="header-anchor" href="#read-only-attributes" aria-label="Permalink to &quot;Read-only attributes&quot;">​</a></h2><ul><li><code>uid</code> — host-issued immutable resource identity; delete and re-create yields a new UID.</li><li><code>generation</code> — desired-state generation; increments only when the portable desired spec changes. Updates fence on it together with <code>uid</code>.</li><li><code>revision</code> — representation revision; increments whenever the representation changes — a spec-changing update, new status, or new outputs. Deletes fence on it via <code>If-Match</code>.</li><li><code>ready</code> — true when the closed <code>Ready</code> condition reports <code>True</code>.</li><li><code>outputs_json</code> — JSON-serialized <code>status.outputs</code> document (<code>&quot;{}&quot;</code> when empty).</li></ul><p>State records the four FormRef fields and no package digest: the distribution a host installed is audit evidence, never resource identity.</p><h2 id="no-local-schema-compilation" tabindex="-1">No local schema compilation <a class="header-anchor" href="#no-local-schema-compilation" aria-label="Permalink to &quot;No local schema compilation&quot;">​</a></h2><p>The provider neither fetches nor compiles the Form&#39;s desired schema:</p><ul><li>at plan time it checks only that the four FormRef fields are wholly known and satisfy the FormRef grammar and that <code>spec_json</code> parses as one JSON object; it reads no Form Definition and makes no host call;</li><li>before each apply the client requires <code>GET {api}/forms</code> to return that exact FormRef as installed, executable, activated, available to the principal, and supporting the requested operation, then calls <code>prepare</code> and <code>apply</code>;</li><li>the host validates <code>spec</code> against that exact Form&#39;s <code>desiredSchema</code>, so a spec that violates the Form surfaces as a host <code>invalid_argument</code> error during apply rather than as a plan-time diagnostic.</li></ul><p>Reads compare <code>spec_json</code> semantically rather than textually: your formatting survives while the parsed document still equals the host&#39;s <code>spec</code>, and a real out-of-band change adopts the host&#39;s canonical serialization so the next plan shows the drift. When the host serves a different <code>uid</code> for the same name, the provider warns that the resource was replaced out of band and removes it from state so the next plan proposes re-creating it.</p><h2 id="import" tabindex="-1">Import <a class="header-anchor" href="#import" aria-label="Permalink to &quot;Import&quot;">​</a></h2><p><code>terraform import</code> is not supported for <code>takoform_resource</code>. An import ID carries only <code>NAME</code> or <code>SPACE/NAME</code>, which cannot supply the four exact FormRef fields this carrier requires, and the provider will not guess them. Adopt an existing resource through the typed family resource for its Form where one exists; otherwise the resource must be created through this provider, because create fences on <code>If-None-Match: *</code> and fails instead of silently adopting an existing host resource.</p>',16)])])}const u=o(c,[["render",s]]);export{m as __pageData,u as default};
