import{_ as a,o,c as t,ag as r}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"VectorIndex — takoform_dense_vector_index","description":"","frontmatter":{},"headers":[],"relativePath":"proposals/vector/vector-index.md","filePath":"proposals/vector/vector-index.md"}'),i={name:"proposals/vector/vector-index.md"};function d(n,e,s,c,l,h){return o(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="vectorindex-—-takoform-dense-vector-index" tabindex="-1">VectorIndex — <code>takoform_dense_vector_index</code> <a class="header-anchor" href="#vectorindex-—-takoform-dense-vector-index" aria-label="Permalink to &quot;VectorIndex — `takoform_dense_vector_index`&quot;">​</a></h1><p>The shipped resource type is <code>takoform_dense_vector_index</code>: the withdrawn v1alpha2 lane published <code>takoform_vector_index</code>, and a Terraform resource type is never reoccupied with a different contract (<a href="./../../spec/decisions/0030-a-form-line-moves-a-terraform-resource-type-may-not.html">decision 0030</a>). Dense is also the accurate word — this Form fixes the dense-vector shape, and a sparse or hybrid index is a different Form.</p><h2 id="workload-and-consumer" tabindex="-1">Workload and consumer <a class="header-anchor" href="#workload-and-consumer" aria-label="Permalink to &quot;Workload and consumer&quot;">​</a></h2><p>An application stores embeddings — document chunks, products, images — and asks for the k records nearest a query vector, optionally filtered by metadata. Workers will consume the index through <code>module-worker.vector</code> bindings; management is the ordinary resource lifecycle.</p><h2 id="role" tabindex="-1">Role <a class="header-anchor" href="#role" aria-label="Permalink to &quot;Role&quot;">​</a></h2><p><code>identity</code>. Desired fields are <code>dimension</code> and <code>metric</code>, both fixed at creation; everything else about the index is the <code>vector.index</code> Interface. <code>metric</code> is the closed enum <code>cosine | euclidean | dotproduct</code> — precisely the case decision 0008 admits as a closed enum, an algorithm choice within identical API shape, storage model, and lifecycle (&quot;for example a vector distance metric&quot;). <code>dimension</code> is a positive integer; a conforming host MUST accept any dimension from 1 through 1536 and states a higher ceiling in its Host Support Profile. A change to either field is refused before mutation and plans as replacement. Namespaces are runtime data reached through the Interface, never Resources.</p><h2 id="observable-semantics" tabindex="-1">Observable semantics <a class="header-anchor" href="#observable-semantics" aria-label="Permalink to &quot;Observable semantics&quot;">​</a></h2><p>Exactly the <code>vector.index@1.0.0</code> contract: <code>upsert</code>, <code>fetch</code>, <code>query</code>, and <code>delete</code>, each addressed to one namespace, with whole-record upsert, read-after-write fetch, approximate top-k query, closed errors, and portable minimum limits.</p><p>A record is <code>{id, values, metadata?}</code>. A record <code>id</code> follows the portable uid grammar (<code>^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$</code>), so UUIDs and content digests — the common embedding identities — are valid ids; a namespace name follows the portable resource-name grammar (<code>^[a-z][a-z0-9-]{0,62}$</code>). An omitted namespace means the default namespace, and a namespace exists exactly while records exist in it. <code>values</code> is an array of exactly <code>dimension</code> finite binary64 numbers; a wrong length, a non-finite member, or an all-zero vector on a <code>cosine</code> index fails <code>invalid_value</code>, and a malformed id fails <code>invalid_key</code>. Components travel as binary64 but the contract guarantees only IEEE binary32 precision: a host MAY round components to its storage precision, never below binary32, and <code>fetch</code> returns the stored values. Metadata is a flat string-keyed map of scalars — UTF-8 string, finite binary64 number, or boolean; no null, list, map, or bytes — round-tripped exactly, with <code>maxMetadataBytes</code> (40960) bounding the UTF-8 encoding of its canonical JSON; exceeding it fails <code>metadata_too_large</code>.</p><p><code>upsert</code> inserts or replaces up to <code>maxUpsertBatchVectors</code> (100) whole records per call — values AND metadata; there is no partial update. A larger batch fails <code>batch_too_large</code>. <code>fetch</code> reads up to <code>maxFetchIds</code> (100) records by id and omits absent ids from its result rather than failing: it is a batch read, and absence is an answer. A fetch after a resolved upsert or delete observes it — fetch is read-after-write. <code>query</code> takes a <code>dimension</code>-length vector (held to the same <code>invalid_value</code> rules as stored vectors), <code>topK</code> (1 through <code>maxTopK</code>, 256), an optional filter, and flags selecting whether values and metadata return with each match; it returns at most <code>topK</code> matches ordered most-similar first. The score is the declared metric&#39;s value computed at the host&#39;s storage precision: cosine similarity and dot product order descending, Euclidean distance ascending. Retrieval is APPROXIMATE: a host MAY return approximate nearest neighbors, recall targets are host quality stated in its Host Support Profile, and only the scores of returned matches are exact per the metric. Query freshness is eventual — a resolved upsert or delete may be missing from, or still present in, query results until the index converges. <code>delete</code> removes up to <code>maxDeleteIds</code> (1000) records by id and succeeds for ids already absent.</p><p>The filter grammar is closed equality and inclusion, nothing else: a filter is a string-keyed map of at most <code>maxFilterClauses</code> (16) members, each either a scalar (the metadata value must equal it) or <code>{&quot;in&quot;: [scalar, ...]}</code> with at most <code>maxInValues</code> (64) members (the value must equal one of them), and members conjoin. There is no range, negation, disjunction, or free query language. The grammar is structural, so a malformed filter never reaches the data plane; a clause naming an absent metadata key, or comparing across scalar types, matches nothing and is not an error.</p><h2 id="why-this-is-one-form" tabindex="-1">Why this is one Form <a class="header-anchor" href="#why-this-is-one-form" aria-label="Permalink to &quot;Why this is one Form&quot;">​</a></h2><p>Fixed dimension, one metric, approximate top-k, and the closed filter are the model consumer code and its embeddings are built against; re-embedding is what changing them means, which is why they are immutable identity rather than configuration. A metric or dimension &quot;update&quot;, an exactness selector, or an open filter language would hollow the contract out (decision 0008).</p><h2 id="what-would-require-a-separate-form" tabindex="-1">What would require a separate Form <a class="header-anchor" href="#what-would-require-a-separate-form" aria-label="Permalink to &quot;What would require a separate Form&quot;">​</a></h2><p>A sparse or hybrid keyword/vector index changes the data model and the query contract and is a different Form; so is an exact-kNN index whose recall is the contract rather than host quality. Range and negation filters, id listing and namespace enumeration, and partial metadata update are versioned <code>vector.index</code> additions, never selectors. A search engine with a de-facto standard API is an integration, never a Form (decision 0043).</p><h2 id="provided-interfaces" tabindex="-1">Provided Interfaces <a class="header-anchor" href="#provided-interfaces" aria-label="Permalink to &quot;Provided Interfaces&quot;">​</a></h2><p><code>vector.index@1.0.0</code>.</p><h2 id="accepted-bindings" tabindex="-1">Accepted Bindings <a class="header-anchor" href="#accepted-bindings" aria-label="Permalink to &quot;Accepted Bindings&quot;">​</a></h2><p>None; it is a binding target (<code>module-worker.vector</code>, a future binding Form mirroring <code>module-worker.edge-kv</code>). Consumers outside the worker family reach the same Interface only when the cross-family binding projection exists; that realization is its own work and is not defined here.</p><h2 id="lifecycle-risks" tabindex="-1">Lifecycle risks <a class="header-anchor" href="#lifecycle-risks" aria-label="Permalink to &quot;Lifecycle risks&quot;">​</a></h2><p>Deleting an index bound by any consumer revision must fail with <code>dependency_in_use</code> (refuse_while_bound). Delete destroys every namespace and record; there is no portable restore. Because <code>dimension</code> and <code>metric</code> plan as replacement, changing them destroys the data with the resource — re-embedding into a new index is the honest migration, and no portable path pretends otherwise.</p><h2 id="prior-art" tabindex="-1">Prior art <a class="header-anchor" href="#prior-art" aria-label="Permalink to &quot;Prior art&quot;">​</a></h2><p>The Pinecone-like serverless vector index category — offered by every major cloud and several independents, standardized by none (decision 0043). The withdrawn v1alpha2 <code>VectorIndex</code>, whose resource type this Form deliberately does not reoccupy. The Edge family&#39;s planned-member listing of <code>DenseVectorIndex</code> / <code>VectorMetadataIndex</code> reserved nothing (<a href="./../../spec/form-families.html">spec/form-families.md</a>); decision 0043 makes this family the home it assigns to vector search.</p>',23)])])}const p=a(i,[["render",d]]);export{u as __pageData,p as default};
