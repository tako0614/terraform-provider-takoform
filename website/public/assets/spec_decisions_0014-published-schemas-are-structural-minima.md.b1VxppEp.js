import{_ as t,o as a,c as i,ag as n}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"0014 — Published schemas are structural minima; closure lives in code and conformance","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0014-published-schemas-are-structural-minima.md","filePath":"spec/decisions/0014-published-schemas-are-structural-minima.md"}'),s={name:"spec/decisions/0014-published-schemas-are-structural-minima.md"};function r(o,e,c,l,d,h){return a(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="_0014-—-published-schemas-are-structural-minima-closure-lives-in-code-and-conformance" tabindex="-1">0014 — Published schemas are structural minima; closure lives in code and conformance <a class="header-anchor" href="#_0014-—-published-schemas-are-structural-minima-closure-lives-in-code-and-conformance" aria-label="Permalink to &quot;0014 — Published schemas are structural minima; closure lives in code and conformance&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-07</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The v1alpha3 lane&#39;s schema documents were published on 2026-08-06, before the lane&#39;s publication blockers were fixed. Publication is per artifact: no family Form, Interface, or Binding is published, but the seven meta-schemas that describe them are, and their bytes are immutable. The append-only ledger and the deploy no-overwrite guard both reject a byte change, so <code>bun run check</code> fails permanently on any attempt.</p><p>Several remaining blockers were designed as schema edits: closing the artifact manifest per kind with <code>oneOf</code>, adding a defaults map to the Form Definition, adding relation metadata, tightening Interface semantics. Each would now require minting a replacement identity, and minting one per blocker would leave a trail of superseded documents served forever.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>A published schema is the <strong>structural minimum</strong> for its document, not the complete acceptance contract — the rule <a href="./../conformance.html"><code>conformance.md</code></a> already states. Closure that a frozen schema cannot express is therefore placed in the semantic verifier, the host, and the conformance corpus, which is where the project&#39;s other fail-closed rules already live.</p><p>When a contract needs a new invariant, prefer the first option that works:</p><ol><li><strong>Data the published schema already admits.</strong> A JSON Schema <code>default</code> inside a Form&#39;s desired schema carries portable defaults; the canonical definition bytes cover it, so the exact FormRef digest protects it.</li><li><strong>Form Definition content.</strong> Definitions are unpublished, so their desired schema, required set, capability list, and referenced contracts may change freely; only the derived digests move.</li><li><strong>Code and conformance.</strong> The host rejects the shape, the authoring model rejects the declaration, and a required conformance check proves a laxer host fails the lane.</li><li><strong>A new schema identity.</strong> Only when the invariant is unexpressible by the first three. New identities are then minted <strong>together, once</strong>, as a coherent generation — never one per change set.</li></ol><p>Two consequences follow. A conforming implementation MUST NOT treat schema validity as sufficient; the semantic rules named in the owning specification are equally normative. And the repository MUST NOT publish again until the lane&#39;s contracts are settled, which the ledger enforces mechanically: a schema byte change turns the gate red rather than reaching production.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The artifact manifest&#39;s per-kind closure (a <code>WorkerBundle</code> manifest must not carry asset files; an asset or migration manifest must not carry modules) is enforced by the host and a required conformance check rather than by <code>oneOf</code>. The host already had to verify manifest-to-blob agreement, media types, and published limits in code, so the schema was never the whole contract for this document.</li><li>Relation metadata is derived from the desired schema, where each reference already encodes its target kind, instead of being duplicated into a new Definition member. Deriving it removes a second source of truth as well as the mint.</li><li>The seven published v1alpha3 meta-schemas stay served and retained. They describe the lane accurately; what they do not describe is stated in the owning specification and proven by conformance.</li><li>Publishing a schema before its contract is settled is expensive. Future lanes SHOULD keep meta-schemas unpublished until their first Form is ready for publication.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Mint a replacement identity per blocker.</strong> Rejected because it publishes a superseded document for every change set, and because the resulting version story teaches consumers to expect churn in exactly the surface that is meant to be immutable.</li><li><strong>Edit the published bytes and record an exception.</strong> Rejected because it is mechanically impossible without weakening the append-only ledger, and the ledger is the only thing making a published identity mean anything.</li><li><strong>Freeze the design until a new generation is minted.</strong> Rejected because the blockers are corrections to unpublished Forms; delaying them to batch a schema mint would publish the defects instead of the fixes.</li></ul>',14)])])}const p=t(s,[["render",r]]);export{u as __pageData,p as default};
