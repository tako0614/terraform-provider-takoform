import{_ as t,o as r,c as o,a2 as a}from"./chunks/framework.BTLSZHNB.js";const p=JSON.parse('{"title":"0017 — Provider state survives Form evolution and interruption","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0017-provider-state-survives-form-evolution-and-interruption.md","filePath":"spec/decisions/0017-provider-state-survives-form-evolution-and-interruption.md"}'),n={name:"spec/decisions/0017-provider-state-survives-form-evolution-and-interruption.md"};function i(s,e,d,c,h,l){return r(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="_0017-—-provider-state-survives-form-evolution-and-interruption" tabindex="-1">0017 — Provider state survives Form evolution and interruption <a class="header-anchor" href="#_0017-—-provider-state-survives-form-evolution-and-interruption" aria-label="Permalink to &quot;0017 — Provider state survives Form evolution and interruption&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Decision <a href="./0011-resource-identity-generation-and-revision.html">0011</a> settled that a resource&#39;s state identity is <code>space/apiVersion/kind/uid</code> and that its Form semantic identity is the exact FormRef, and decision <a href="./0013-v1alpha3-lane-ships-in-provider-v2-1.html">0013</a> promised provider v2.1 &quot;a multi-FormRef registry dispatching on the exact FormRef recorded in state&quot;. The provider could not honour either promise, because everything it dispatches on is keyed too coarsely or consulted too late.</p><p>The generated registry keys a FormRef by <code>apiVersion + &quot;/&quot; + kind</code>, so one Form line has exactly one identity at a time. The moment a Form&#39;s definition version moves, every resource already in state records a FormRef the build no longer holds, and the dispatch seam that was supposed to absorb that (<code>v3FormResource.supportedRefs</code>) was an unexported, test-only field that no production construction ever filled. A ref alone would not have been enough anyway: an older definition may have declared a different field set, so decoding state written under it needs that definition&#39;s declarations, not the current schema&#39;s.</p><p>Import made the same mistake in the other direction. The typed <code>ImportState</code> seeded the DEFAULT create ref, so importing a resource created under an earlier definition version silently rebound it to a contract it was never applied under. The generic carrier had no <code>ImportState</code> at all, on the stated grounds that a <code>NAME</code> / <code>SPACE/NAME</code> string cannot carry the four exact FormRef members — true of that string, and taken as if it were true of import itself.</p><p>Two more paths removed resources from Terraform&#39;s management without a way back. When a read found a different UID under the same name, it removed the resource from state; the host still held the new incarnation, so the next apply fenced on <code>If-None-Match: *</code>, failed, and left no plan that repaired anything. And <code>pending_operation_id</code> — written precisely so an accepted-but-unfinished mutation is not orphaned — was never consulted: a read went straight to the resource GET, so on a host where the resource does not exist until its operation commits, a truthful 404 during that window was read as deletion and the next apply created a duplicate.</p><p>Decision <a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a> already settled the shape of the answer for one neighbouring case — a relation whose target changed incarnation is reported, never re-bound, and never allowed to fail the refresh that computes its repair. This decision applies the same reasoning to the resource itself and to the operation that created it. These are MUST-level provider and host semantics, so this repository&#39;s <code>AGENTS.md</code> requires a decision record.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Provider state is bound to one EXACT Form identity and one host incarnation, and a refresh never silently changes either. The rules below are normative; the operative text lives in <a href="./../host-api/v1alpha3.html"><code>../host-api/v1alpha3.md</code></a>, <a href="./../versioning.html"><code>../versioning.md</code></a>, and the generated provider resource documents.</p><ol><li><p><strong>Dispatch on the recorded ref.</strong> Read, update, and delete address the resource under the exact FormRef recorded in state. Create — and only create — uses the default create ref. Membership in the supported set, not equality with the default, decides whether state can be served.</p></li><li><p><strong>The registry is keyed by the whole identity.</strong> The generated data is two maps: <code>Supported</code>, keyed by the complete <code>{apiVersion, kind, definitionVersion, schemaDigest}</code> tuple, holding every identity the build can read, observe, update, and delete; and <code>DefaultCreates</code>, keyed by <code>{apiVersion, kind}</code>, naming the one identity a NEW resource of that line is created under. The package digest stays inside the value as audit evidence and is excluded from the key, because it is distribution provenance and never identity (decision 0011). A registry snapshot is immutable; registering an identity returns a copy.</p></li><li><p><strong>Each supported ref carries a codec.</strong> A codec is the field set that exact definition declared. It decodes the state written under that ref and encodes the spec sent for it, so an update to a resource created under an earlier definition version is a mutation of that contract rather than a migration onto the current one. The compatibility rule is the Form compatibility rule of <a href="./../versioning.html"><code>../versioning.md</code></a>, read from the provider&#39;s side: an ADDITIVE Form minor may share one codec with the definitions before it, because every previously valid desired document stays valid with the same portable meaning; a BREAKING Form major needs its own codec, because a removed, retyped, or re-meant field cannot be encoded or decoded by the other definition&#39;s declarations.</p></li><li><p><strong>Fail closed on an identity the build cannot decode.</strong> State bound to a ref with no compiled codec produces a hard error naming the recorded ref and every ref of that kind the build knows. The provider MUST NOT read, update, or delete the resource under a different exact FormRef. Substituting one would reinterpret state written against one contract as another, and — worse — the substituted query&#39;s <code>resource_not_found</code> would then look like deletion.</p></li><li><p><strong>Import names the exact identity.</strong> The canonical import ID is one JSON object, <code>{&quot;space&quot;, &quot;apiVersion&quot;, &quot;kind&quot;, &quot;definitionVersion&quot;, &quot;schemaDigest&quot;, &quot;name&quot;}</code>, with <code>space</code> optional and the four FormRef members all-or-nothing. The short <code>NAME</code> and <code>SPACE/NAME</code> forms are retained and resolve to the default create ref. The generic carrier accepts the JSON form only, because it has no default create ref to resolve a short form against. An import naming an identity the build cannot decode is refused rather than rebound.</p></li><li><p><strong>A UID mismatch is an error that preserves state.</strong> When a read finds a different <code>metadata.uid</code> under the name state records, the provider emits a hard error naming the expected and current UIDs and the three remedies — import the new incarnation explicitly, restore the prior one, or delete the host-side replacement — and keeps the resource in state. It never re-binds and never removes. This is deliberately stricter than the relation drift of decision 0015, which warns and offers a repairing apply: there, the resource itself is still the one state names and an apply re-pins the reference; here, no apply the provider can send converts one incarnation into another, so the operator must choose.</p></li><li><p><strong>A read resumes a pending operation before it reads the resource.</strong> When state carries <code>pending_operation_id</code>, the operation is consulted first, and what the following resource read may conclude is:</p><table tabindex="0"><thead><tr><th>operation state</th><th>the read may then conclude</th></tr></thead><tbody><tr><td>still running</td><td>absence is NOT deletion; a readable representation settles state but the marker survives, because nothing has committed</td></tr><tr><td>terminal, success</td><td>the result resource is verified against the exact identity and its UID checked; the ordinary read settles and clears</td></tr><tr><td>terminal, error</td><td>the exact resource GET is the final word: absent means state may be removed, present means the UID decides</td></tr><tr><td><code>operation_not_found</code></td><td>the record expired; the exact resource GET is the final word, under the same UID rule</td></tr></tbody></table><p>No branch re-binds by name alone: a known UID is verified in every branch, and an unknown one is adopted only from a representation the host served under the exact FormRef state records.</p></li><li><p><strong>The host side is proven, not assumed.</strong> Two required conformance checks carry what rules 1 and 7 depend on: <code>operation-resumable-after-settlement</code> proves a recorded operation stays addressable and replays its terminal state byte-identically after the resource it created has been updated and a second operation has run; <code>exact-form-ref-fails-closed-on-unknown-definition</code> proves an exact FormRef query naming a definition version or schema digest the host does not have fails <code>form_unknown</code> on availability, on the Form Definition, and on a resource read, instead of matching by group and kind.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A Form line may advance without a state migration. Adding the new definition to <code>Supported</code> keeps every existing resource addressable as itself; moving <code>DefaultCreates</code> changes only what new resources are created under. The two are now separate acts rather than one entry that means both.</li><li>The exact-FormRef test seam is gone. Production and the multi-version tests run the same registry type through the same code path; the tests differ only in registering a synthetic second definition version, which is also what makes the eleven-entry generated data provably capable of more.</li><li>A refresh can now end in a hard error that leaves the resource in state. That is a deliberate reversal of the usual provider instinct to &quot;heal&quot; a refresh: a refresh that heals by forgetting produces an apply that cannot succeed, and the operator has no way to see why.</li><li><code>pending_operation_id</code> becomes load-bearing rather than informational, so it is cleared only by an operation that actually settled. A read taken while the operation is still running keeps it, which means an interrupted create is resumable across as many refreshes as the host needs.</li><li>The conformance corpus grows to 68 required checks, and hosts that pass the lane today must additionally keep operation records addressable past the next operation.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep the registry keyed by group+kind.</strong> This is the current shape, and its cost is exactly the blocker: one Form line can hold one identity, so the day a definition version moves, every existing resource&#39;s recorded FormRef becomes unknown to the build at once. The only recoveries would have been to reinterpret that state under the new ref (rejected below) or to require every user to destroy and re-create their resources — a migration forced by the provider&#39;s data structure rather than by any change in what the resources are.</li><li><strong>Rebind state to the default ref on read.</strong> Rejected because it is a silent claim, made by the party least entitled to make it, that two contracts are the same. The read would succeed, state would afterwards name a definition the resource was never applied under, and every later update would send that definition&#39;s field set. Where the substituted identity is not one the host serves for that resource, the failure is worse than silent: the query returns <code>resource_not_found</code>, the provider reads it as deletion, and the resource is dropped from management. Failing closed costs an error message; rebinding costs the ability to tell which contract a resource is under.</li><li><strong>Remove state on a UID mismatch.</strong> This is what the lane did, and it is the one remedy that removes its own path. The host still holds the replacement, so the next apply fences on <code>If-None-Match: *</code> and fails; the plan that would have repaired anything was computed from a state that no longer has the resource. The operator is left editing state by hand — outside Terraform, which is where a provider&#39;s error handling is supposed to keep them from going. Preserving state and naming the three remedies leaves every option open, including the removal, which the operator can still perform deliberately.</li><li><strong>A delimiter-joined import ID carrying the exact FormRef.</strong> Rejected because no delimiter is safe. A SpaceID is opaque, case-sensitive UTF-8 whose only forbidden character is <code>/</code> (decision 0003), so every separator a reader could type may legitimately appear inside the space, and any escaping convention has to survive copy-paste through a shell that also interprets it. A JSON object names each member, needs no positional convention, and can be copied straight out of a resource representation.</li><li><strong>Poll the operation to completion inside the read.</strong> Rejected because a read is Terraform&#39;s refresh: blocking it for the length of a provisioning operation makes <code>terraform plan</code> hang for as long as the host takes, and a cancelled plan then loses the very information the poll was gathering. Consulting the operation once, reporting what it says, and keeping the marker leaves the resource under management and lets the next refresh finish the job.</li><li><strong>Treat <code>operation_not_found</code> as &quot;the mutation failed&quot;.</strong> Rejected because the lane explicitly permits an operation record to expire while the resource it created lives on. Reading an expired record as failure would delete a healthy resource from state on nothing more than the passage of time.</li></ul>',15)])])}const m=t(n,[["render",i]]);export{p as __pageData,m as default};
