import{_ as t,o as a,c as o,ag as s}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"Form Families","description":"","frontmatter":{},"headers":[],"relativePath":"spec/form-families.md","filePath":"spec/form-families.md"}'),i={name:"spec/form-families.md"};function n(r,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="form-families" tabindex="-1">Form Families <a class="header-anchor" href="#form-families" aria-label="Permalink to &quot;Form Families&quot;">​</a></h1><p>A Form Family is a named group of Forms that share one platform model and are designed to compose. A family is an API namespace and a catalog grouping; it is not a package unit, a maturity state, or a compatibility promise. The family model is decided in <a href="./decisions/0009-form-families-and-namespaced-api-versions.html">decision 0009</a>; the semantic rule every member Form must satisfy is <a href="./portability-boundary.html"><code>portability-boundary.md</code></a>.</p><h2 id="groups" tabindex="-1">Groups <a class="header-anchor" href="#groups" aria-label="Permalink to &quot;Groups&quot;">​</a></h2><p>Each current family owns one versionless reverse-DNS API group:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>edge.forms.takoform.com</span></span>
<span class="line"><span>container.forms.takoform.com</span></span>
<span class="line"><span>forms.example.com                       (third party)</span></span></code></pre></div><p>A stable-v1 FormRef <code>apiVersion</code> is the whole versionless group, not a central constant and not <code>&lt;group&gt;/&lt;version&gt;</code>. <code>kind</code>, <code>definitionVersion</code>, and <code>schemaDigest</code> complete the exact Form identity; one Form changes without renumbering its siblings (<a href="./decisions/0049-a-form-versions-alone.html">decision 0049</a>). Official families use subdomains of <code>forms.takoform.com</code>, and a third-party reverse-DNS group is equally representable. Publisher trust remains a separate fact.</p><p>Versioned groups such as <code>edge.forms.takoform.com/v1beta1</code> are retained pre-v1 identities. The groups <code>forms.takoform.com/v1alpha1</code> (Legacy) and <code>forms.takoform.com/v1alpha2</code> (retained provider-v2 preview) are also frozen. None is accepted as a current v1 group or reused for new semantics.</p><p>The generated current-family index closes this repository&#39;s candidate corpus; the prose table is a readable projection, not a second registry:</p><table tabindex="0"><thead><tr><th>Family group</th><th style="text-align:right;">Forms</th></tr></thead><tbody><tr><td><code>container.forms.takoform.com</code></td><td style="text-align:right;">5</td></tr><tr><td><code>edge.forms.takoform.com</code></td><td style="text-align:right;">16</td></tr><tr><td><code>function.forms.takoform.com</code></td><td style="text-align:right;">4</td></tr><tr><td><code>queue.forms.takoform.com</code></td><td style="text-align:right;">1</td></tr><tr><td><code>schedule.forms.takoform.com</code></td><td style="text-align:right;">1</td></tr><tr><td><code>table.forms.takoform.com</code></td><td style="text-align:right;">1</td></tr><tr><td><code>topic.forms.takoform.com</code></td><td style="text-align:right;">2</td></tr><tr><td><code>vector.forms.takoform.com</code></td><td style="text-align:right;">1</td></tr></tbody></table><p>Those eight groups contain 31 exact Experimental <code>0.x</code> FormRefs. Specification 1.0 and Host API v1 do not promote any of them to Form <code>1.0.0</code>; a future Stable Form starts at <code>1.0.0</code> only by an explicit decision for that Form.</p><p>Publisher identity never enters the FormRef. Semantic identity (FormRef), distribution bytes (package digest), publisher trust (signature policy), and implementation (Host Support) remain four independent facts.</p><h2 id="resource-roles" tabindex="-1">Resource roles <a class="header-anchor" href="#resource-roles" aria-label="Permalink to &quot;Resource roles&quot;">​</a></h2><p>Every current Form Definition declares one <code>role</code> from a closed enum. Roles let tooling enforce lifecycle rules mechanically.</p><table tabindex="0"><thead><tr><th>Role</th><th>Meaning</th><th>Rules</th></tr></thead><tbody><tr><td><code>identity</code></td><td>long-lived logical resource</td><td>stable name; carries no implementation snapshot</td></tr><tr><td><code>revision</code></td><td>immutable implementation snapshot</td><td>never updated in place; changes create a new resource</td></tr><tr><td><code>deployment</code></td><td>selects which revisions are active</td><td>the only mutable path for traffic movement and rollback</td></tr><tr><td><code>attachment</code></td><td>connects a parent to external events or endpoints</td><td>deleting an attachment never deletes the parent</td></tr><tr><td><code>policy</code></td><td>operating rules changed independently of the parent</td><td>never migrates into the parent identity</td></tr></tbody></table><p>Outward capability use (a worker using KV, databases, queues, services, workflows, or actors) is a typed Binding held by a revision resource (<a href="./decisions/0010-exact-interface-and-binding-contracts.html">decision 0010</a>). Inward activation (HTTP routes, custom domains, cron triggers, queue consumption) is an attachment resource. The two are never merged.</p><p>A family that splits one running thing across these roles owes a statement of what holds them together. For the Edge Platform Family that statement is <a href="./decisions/0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a> and the current Edge Form Definitions: an identity has at most ONE deployment resource; that deployment selects revisions of its own identity, each named once, with weights summing to exactly 10000; every attachment is admitted against the deployment rather than against any stored revision, and refused when the deployment is absent or does not serve the handler the attachment invokes; a deployment change that would leave a live attachment or inbound binding unserved is refused, as is deleting the deployment while one lives; and the identity reports itself Ready only while its deployment actually serves. Because that last one is a representation rendered from another resource, a deployment change also moves the identity&#39;s revision and therefore its ETag, while leaving its generation alone.</p><h2 id="edge-platform-family" tabindex="-1">Edge Platform Family <a class="header-anchor" href="#edge-platform-family" aria-label="Permalink to &quot;Edge Platform Family&quot;">​</a></h2><p><code>edge.forms.takoform.com</code> is the first official family. Its members fix the shape of a proven edge developer platform without naming its vendor. Its 16 current members are:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Compute      ModuleWorker, WorkerBundle, WorkerVersion, WorkerDeployment,</span></span>
<span class="line"><span>             StaticAssetBundle, WorkerCustomDomain, WorkerEndpoint,</span></span>
<span class="line"><span>             WorkerCronTrigger</span></span>
<span class="line"><span>Data         EdgeKVNamespace, SQLiteDatabase,</span></span>
<span class="line"><span>             SQLiteMigrationSet, SQLiteMigrationApplication</span></span>
<span class="line"><span>Messaging    AtLeastOnceQueue, QueueConsumer</span></span>
<span class="line"><span>Stateful     DurableWorkflow, ActorNamespace</span></span></code></pre></div><p><code>ObjectBucket</code> is not a current Form. The related <code>edge.objects</code> Interface and <code>module-worker.object-bucket</code> Binding are likewise absent from the current candidate closure. Runtime access to an externally managed object service uses a sealed <code>standards.takoform.com/v1</code> slot with an opaque reverse-DNS protocol identifier such as <code>com.amazonaws.s3</code>; Takoform carries no central protocol enum and grants no portable lifecycle authority over that service. The exact retained v1beta1 ObjectBucket bytes keep their historical meaning.</p><p>Static files and SQLite migrations are artifact-backed rather than inline: <code>StaticAssetBundle</code> and <code>SQLiteMigrationSet</code> desired state is exactly one committed manifest digest, while <code>SQLiteMigrationApplication</code> attaches an ordered set to a database with append-only path+digest history (<a href="./decisions/0033-edge-app-assets-and-sqlite-migrations-are-content-addressed.html">decision 0033</a>).</p><p><code>ModuleWorker</code> fixes the ES Module Worker ABI by identity, and states what that ABI is: the exact Interface contract <code>worker.runtime@1.1.0</code> in its <code>providedInterfaces</code> (<a href="./decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>). That contract fixes the module&#39;s default-export shape, the <code>fetch</code>, <code>scheduled</code>, and <code>queue</code> signatures and what each event carries, the <code>env</code> object, <code>ctx.waitUntil</code>, exception handling, request and response body streaming, the minimum Web API surface, and module loading. Its handler vocabulary is those three and nothing else: a handler no attachment in this family can activate is a member no run can reach and no divergence between two hosts can be detected in, so it does not belong in the contract until the attachment that makes it observable ships beside it, in a new exact version (<a href="./decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>). A host that supports <code>ModuleWorker</code> implements that contract at its exact digest, and advertises it there; a Worker Version is the code that fills it.</p><p>Consequently <strong>a runtime revision is a new exact Interface version, and — if it changes what a Form desires — a new Form version. It is never a date.</strong><code>WorkerVersion</code> therefore declares no <code>compatibilityDate</code> and no <code>compatibilityFlags</code>. A compatibility date is meaningful only against a registry that states which behavior each date changes; this project publishes none, so two conforming hosts could read the same date differently, which is exactly the incompleteness <a href="./portability-boundary.html"><code>portability-boundary.md</code></a> forbids. The <code>handlers</code> vocabulary is the handler set the runtime contract defines, and a host refuses a handler that contract does not define before it mutates anything.</p><p>Three authored decisions are recorded here so none is read as an oversight:</p><ul><li><code>WorkerVersion.assets</code> is one optional closed object referring to the separate <code>StaticAssetBundle</code> member above. Absence means no asset lookup; presence fixes request order and not-found handling without granting a hidden runtime binding (<a href="./decisions/0033-edge-app-assets-and-sqlite-migrations-are-content-addressed.html">decision 0033</a>).</li><li><code>WorkerVersion</code> names its sealed-value declaration <code>requiredSensitiveVars</code> rather than <code>secretRequirements</code>. The Form Package data-only policy rejects the token <code>secret</code> anywhere in a field name (<a href="./form-package/"><code>form-package/</code></a>), so the portable field carries the same fact — only the names of host-supplied sensitive values are portable state — in permitted vocabulary.</li><li><code>WorkerVersion</code> projects <code>vars</code> keys, <code>requiredSensitiveVars</code> entries, and every binding <code>name</code> into ONE runtime environment namespace, so their union must be unique (<a href="./decisions/0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a>). The desired schema cannot state it — <code>uniqueItems</code> compares whole objects, and no keyword relates one property&#39;s keys to a sibling array&#39;s element member — so a host refuses the collision before mutation and a client refuses it at plan time.</li><li>The family carries two inward activations for <code>fetch</code>, and they are two Forms rather than one Form with a mode. <code>WorkerCustomDomain</code> states which name the AUTHOR owns that reaches a worker; <code>WorkerEndpoint</code> states that the worker is reachable at all, at an address the HOST assigns and publishes as outputs (<a href="./decisions/0024-a-worker-is-reachable-at-a-host-assigned-address.html">decision 0024</a>). The desired states are disjoint — one carries a hostname, the other carries nothing but the worker — so a selector token between them would be a free semantic token of exactly the kind this family forbids. A worker may have both, and has at most one endpoint.</li><li><code>WorkerEndpoint</code> is the family&#39;s first member to declare an <code>outputSchema</code>, which makes its assigned address a typed contract rather than an untyped document a consumer decodes (<a href="./decisions/0025-declared-outputs-are-a-typed-contract.html">decision 0025</a>). A Form declaring one publishes exactly its members; a Form declaring none publishes no <code>status.outputs</code> at all.</li></ul><p>Semantics that differ from these shapes join other families instead of widening an Edge member. The current Container, Function, Pull Queue, Schedule, Table, Topic, and Vector families therefore keep their own groups and exact FormRefs. Their presence does not turn the family list into a central kind enum: the generated index closes this candidate corpus, while a Host advertises the exact subset it actually supports.</p><h2 id="what-a-family-does-not-do" tabindex="-1">What a family does not do <a class="header-anchor" href="#what-a-family-does-not-do" aria-label="Permalink to &quot;What a family does not do&quot;">​</a></h2><ul><li>It does not merge packages: one Form Package still contains exactly one Form Definition.</li><li>It does not grant maturity: all 31 current FormRefs are exact Experimental <code>0.x</code> identities, including the Edge family&#39;s 16. The stable Host lane and Specification 1.0 do not make them Stable, and their package artifacts remain a separate unpublished fact.</li><li>It does not constrain hosts: a host may support any subset of a family and states that subset in its Host Support Profile.</li><li>It does not admit vendor identity: adapter profiles map family Forms to concrete backends outside the contract.</li></ul><h2 id="the-edge-platform-family-s-artifact-semantics" tabindex="-1">The Edge Platform Family&#39;s artifact semantics <a class="header-anchor" href="#the-edge-platform-family-s-artifact-semantics" aria-label="Permalink to &quot;The Edge Platform Family&#39;s artifact semantics&quot;">​</a></h2><p>These two rulebooks lived in the Host API document until <code>forms.takoform.com/v1beta3</code>, which states mechanisms rather than rules about particular Forms. They are family material: they say what an artifact MEANS to the Form that references it, which is exactly the knowledge a family owns. The protocol&#39;s own obligation — resolve the referenced manifest and hold it to the contract the referring Form states, before any mutation — is unchanged and stays there.</p><h3 id="static-assets-on-a-worker-version" tabindex="-1">Static assets on a Worker Version <a class="header-anchor" href="#static-assets-on-a-worker-version" aria-label="Permalink to &quot;Static assets on a Worker Version&quot;">​</a></h3><p>The rules here are decided by <a href="./decisions/0033-edge-app-assets-and-sqlite-migrations-are-content-addressed.html">decision 0033</a>. A <code>WorkerVersion</code> with no <code>assets</code> member performs no asset lookup. When the member is present it is one closed object with three required members:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;bundle&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;edge.forms.takoform.com&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;StaticAssetBundle&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;static-assets&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;runWorkerFirst&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;notFoundHandling&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;single_page_application&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The bundle relation requires the target&#39;s exact FormRef and is UID-pinned like every other relation. <code>notFoundHandling</code> is exactly <code>none</code> or <code>single_page_application</code>.</p><ul><li>With <code>runWorkerFirst=false</code>, the host performs asset lookup first and invokes <code>fetch</code> only when that stage produces no response.</li><li>With <code>runWorkerFirst=true</code>, it invokes <code>fetch</code> first and performs asset lookup only when the worker returns 404. An asset response wins; if asset lookup misses, the worker&#39;s 404 is preserved.</li><li><code>none</code> leaves a missing exact path as a miss.</li><li><code>single_page_application</code> answers a missing path with <code>index.html</code>. The host MUST resolve the exact referenced manifest and refuse the Worker Version with <code>invalid_argument</code> (400), before mutation, when it contains no <code>index.html</code>.</li></ul><p>Asset lookup maps the runtime URL <code>pathname</code> to a manifest path as one closed operation. Query strings and fragments are ignored; the escaped pathname is percent-decoded once as strict UTF-8, and exactly one leading <code>/</code> is removed. The host MUST reject encoded <code>/</code> or <code>\\\\</code>, repeated or empty interior segments, dot segments, backslashes, controls, Unicode noncharacters, malformed escapes, and invalid UTF-8. A valid path must still match the manifest&#39;s relative path grammar. Invalid paths fail closed and MUST NOT enter SPA fallback. A valid missing path is a miss under <code>none</code>, or resolves to <code>index.html</code> under <code>single_page_application</code>; the root pathname <code>/</code> is the canonical empty-path miss and follows that same fallback rule.</p><p>The attachment never grants a runtime binding and never changes the asset bundle. A provider may author the manifest from local files, but desired state and provider state carry no file bytes.</p><h3 id="sqlite-migration-history" tabindex="-1">SQLite migration history <a class="header-anchor" href="#sqlite-migration-history" aria-label="Permalink to &quot;SQLite migration history&quot;">​</a></h3><p>A <code>MigrationBundle</code> is an ordered non-empty <code>files</code> list and every entry MUST use <code>application/sql</code>; other media types are <code>artifact_invalid</code> (400). <code>SQLiteMigrationApplication</code> pins exact <code>SQLiteDatabase</code> and <code>SQLiteMigrationSet</code> relations. The host keeps a durable ordered ledger in the database, whose entry identity is <code>(path, digest)</code>.</p><p>Before mutation, and again when an accepted operation commits, the host MUST serialize against other migration applications for the database and prove the ledger is an exact prefix of the referenced manifest. An applied entry that is absent, moved, or has another digest is <code>migration_required</code> (409) and changes nothing. Only the unapplied suffix may execute. Each file&#39;s SQL execution and its ledger insertion are one SQLite transaction: failure rolls back both for that file, keeps earlier committed entries, and makes a retry resume at the same suffix boundary. Ready means the durable ledger equals the exact ordered set.</p><p>A database has AT MOST ONE live <code>SQLiteMigrationApplication</code>, over its UID exactly like the deployment and consumer rules; a second application against the same database incarnation fails <code>invalid_argument</code> (400) before any mutation. Advancing a database&#39;s schema is deleting the application and creating one referencing the longer set — the ledger check makes the new application resume at the unapplied suffix, so the handover replays nothing.</p><p>Deleting <code>SQLiteMigrationApplication</code> removes the attachment and its relation holders only. It MUST NOT execute down SQL, remove ledger entries, reinterpret the schema, or delete the database. The database and set are protected by ordinary <code>dependency_in_use</code> while the attachment lives; database deletion after it is gone is a separate database policy, not migration rollback.</p>`,42)])])}const u=t(i,[["render",n]]);export{m as __pageData,u as default};
