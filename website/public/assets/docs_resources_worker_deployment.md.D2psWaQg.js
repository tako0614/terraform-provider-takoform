import{_ as o,o as t,c as r,a2 as a}from"./chunks/framework.BTLSZHNB.js";const u=JSON.parse('{"title":"takoform_worker_deployment","description":"Worker Deployment (edge.forms.takoform.com/v1alpha1, role deployment).","frontmatter":{"page_title":"takoform_worker_deployment Resource - takoform","subcategory":"Edge Platform Family","description":"Worker Deployment (edge.forms.takoform.com/v1alpha1, role deployment)."},"headers":[],"relativePath":"docs/resources/worker_deployment.md","filePath":"docs/resources/worker_deployment.md"}'),i={name:"docs/resources/worker_deployment.md"};function s(n,e,d,c,l,m){return t(),r("div",null,[...e[0]||(e[0]=[a(`<h1 id="takoform-worker-deployment" tabindex="-1">takoform_worker_deployment <a class="header-anchor" href="#takoform-worker-deployment" aria-label="Permalink to &quot;takoform_worker_deployment&quot;">​</a></h1><p>Selects which Worker Versions of one Module Worker serve traffic and in what proportion. Weights are basis points and must sum to exactly 10000 across entries; the sum is host-validated semantics because a schema cannot add weights. Rollback is re-weighting, never mutating a revision.</p><p>This is a <code>deployment</code> resource: the only mutable path for traffic movement and rollback. It selects which revisions are active.</p><p>This resource speaks the Host API v1alpha3 lane and requires provider v2.1.0 or later (source candidate; not yet published). The configured host selects and operates the concrete backend; no attribute names a vendor, target, credential, price, or implementation. See the <a href="../../examples/resources/takoform_worker_deployment/resource.tf">complete example</a>.</p><h2 id="arguments" tabindex="-1">Arguments <a class="header-anchor" href="#arguments" aria-label="Permalink to &quot;Arguments&quot;">​</a></h2><ul><li><code>name</code> (String, required, forces replacement) — Portable resource name (<code>metadata.name</code>).</li><li><code>worker</code> (String, required, forces replacement) — Module Worker identity whose traffic this deployment governs. Set the name of the target <code>ModuleWorker</code> resource.</li><li><code>versions</code> (List of Object, required) — Active Worker Versions and their traffic weights in basis points. Weights must sum to exactly 10000. Each entry declares <code>worker_version</code>, <code>weight</code> (between 1 and 10000). The list must declare between 1 and 8 entries.</li><li><code>space</code> (String, optional, forces replacement) — Exact opaque SpaceID; overrides the provider default.</li><li><code>create_timeout</code> / <code>update_timeout</code> / <code>delete_timeout</code> (String, optional) — Go durations bounding each operation (defaults <code>20m</code> / <code>20m</code> / <code>30m</code>).</li></ul><h2 id="read-only-attributes" tabindex="-1">Read-only attributes <a class="header-anchor" href="#read-only-attributes" aria-label="Permalink to &quot;Read-only attributes&quot;">​</a></h2><ul><li><code>uid</code> — host-issued immutable resource identity; delete and re-create yields a new UID.</li><li><code>generation</code> — desired-state generation; increments only when the portable desired spec changes. Updates fence on it.</li><li><code>revision</code> — representation revision; increments whenever the representation changes — a spec-changing update, new status, or new outputs. Deletes fence on it via <code>If-Match</code>.</li><li><code>ready</code> — true when the closed <code>Ready</code> condition reports <code>True</code>.</li><li><code>outputs_json</code> — JSON-serialized <code>status.outputs</code> document (<code>&quot;{}&quot;</code> when empty).</li><li><code>form_api_version</code>, <code>form_kind</code>, <code>form_definition_version</code>, <code>form_schema_digest</code> — the exact immutable FormRef this state is bound to; reads dispatch on it.</li><li><code>form_package_digest</code> — audit-only package provenance; never part of resource identity, queries, or fences.</li></ul><h2 id="import" tabindex="-1">Import <a class="header-anchor" href="#import" aria-label="Permalink to &quot;Import&quot;">​</a></h2><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_worker_deployment.example NAME</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_worker_deployment.example SPACE/NAME</span></span></code></pre></div>`,10)])])}const h=o(i,[["render",s]]);export{u as __pageData,h as default};
