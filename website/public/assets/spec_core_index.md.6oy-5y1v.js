import{_ as i,o as a,c as t,ag as o}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"Takoform neutral Core and compiled Snapshot","description":"","frontmatter":{},"headers":[],"relativePath":"spec/core/index.md","filePath":"spec/core/index.md"}'),n={name:"spec/core/index.md"};function r(s,e,l,c,d,p){return a(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="takoform-neutral-core-and-compiled-snapshot" tabindex="-1">Takoform neutral Core and compiled Snapshot <a class="header-anchor" href="#takoform-neutral-core-and-compiled-snapshot" aria-label="Permalink to &quot;Takoform neutral Core and compiled Snapshot&quot;">​</a></h1><p>This document is normative source for Specification 1.1. It defines the family-neutral compiler boundary; it does not publish a Core SDK, release a Form Package, select a publisher, or mint a Host API lane. The future public Go module is <code>github.com/tako0614/takoform</code>; no SDK or CLI publication from that coordinate is prepared in C1.</p><h2 id="purpose" tabindex="-1">Purpose <a class="header-anchor" href="#purpose" aria-label="Permalink to &quot;Purpose&quot;">​</a></h2><p>The Core turns already acquired, data-only contract artifacts into one closed, immutable graph that a Host, client adapter, or conformance runner can consume. It owns the portable meaning that must be identical in all of those consumers:</p><ul><li>RFC 8785 and I-JSON canonicalization;</li><li>complete Form Package closure verification and exact identity checks;</li><li>Definition/schema validation and closed mechanism vocabulary;</li><li>exact cross-Form, Interface, and Binding reference closure;</li><li>closed Host API lower-bound compatibility;</li><li>recursive portable-default materialization;</li><li>explicit default-create selection;</li><li>deterministic ordering, diagnostics, and Snapshot identity.</li></ul><p>It owns no official family roster, Terraform resource type, Resource instance, Host implementation, publisher trust decision, executable extension, credential, target, activation, availability, price, or capacity.</p><h2 id="boundary-before-compilation" tabindex="-1">Boundary before compilation <a class="header-anchor" href="#boundary-before-compilation" aria-label="Permalink to &quot;Boundary before compilation&quot;">​</a></h2><p>Artifact acquisition precedes Core compilation. An acquisition Adapter fetches or reads an immutable package closure. The Core-owned package verifier proves that the package index names every payload, that byte sizes and digests match, that the closure is data-only, and that fixtures satisfy the Definition. An operator or Host admission Adapter then applies its selected publisher identity, signature, transparency, and revocation policy.</p><p>The verifier issues a non-forgeable immutable package capability only after the complete check succeeds. Core receives:</p><ul><li>a diagnostics-only origin;</li><li>the expected canonical package-index digest;</li><li>that verified package capability, which owns the validated index, canonical Definition, complete file inventory, and copies of every verified payload;</li><li>zero or more digest-pinned exact Interface Definitions; and</li><li>zero or more digest-pinned exact Binding Definitions.</li></ul><p>Callers cannot construct or deserialize a valid package capability. Core rechecks the caller&#39;s package digest pin and compiles the issued canonical Definition. Interface and Binding artifacts are independently checked against their embedded normative schemas and canonical digest pins. Core does not repeat publisher admission or execute a payload. Origin and publisher identity never participate in exact contract equality.</p><p>The current implementation tracer accepts this semantic closure in memory. Directory, embedded, OCI, and HTTP-by-digest acquisition are Adapters; they are not separate compiler paths.</p><h2 id="compile-input" tabindex="-1">Compile input <a class="header-anchor" href="#compile-input" aria-label="Permalink to &quot;Compile input&quot;">​</a></h2><p>One complete input contains:</p><ol><li>the exact Host API lane against which the Snapshot is selected;</li><li>zero or more verified package capabilities with explicit package digest pins;</li><li>zero or more exact Interface Definition artifacts;</li><li>zero or more exact Binding Definition artifacts; and</li><li>zero or one explicit default-create FormRef for every selected <code>(group, kind)</code>.</li></ol><p>Input order has no meaning. A family group is a reverse-DNS identity. The same Kind MAY occur in multiple groups; every lookup and reference is group-first. There is no Kind-only, newest-version, compatible-version, or latest fallback.</p><p>Zero packages and zero default pins are valid. This is the required proof that Core has no built-in official family.</p><h2 id="exact-graph-compilation" tabindex="-1">Exact graph compilation <a class="header-anchor" href="#exact-graph-compilation" aria-label="Permalink to &quot;Exact graph compilation&quot;">​</a></h2><p>Compilation performs these stages in order:</p><ol><li>validate that the Host API identity is a known served lane;</li><li>require a verifier-issued complete package capability and match its package digest to the explicit pin;</li><li>revalidate the canonical Definition and exact FormRef carried by the verified package;</li><li>validate and digest every Interface and Binding Definition against its normative schema;</li><li>reject duplicate exact identities, including duplicates whose origin or package provenance differs;</li><li>index the complete Form, Interface, and Binding exact-identity sets;</li><li>reject a Form whose <code>requiresHostApi</code> lower bound the selected lane cannot satisfy;</li><li>resolve every <code>x-takoform-target-formrefs</code>, provided/required Interface, and accepted/annotated Binding reference against the complete set;</li><li>verify that each Binding&#39;s exact target Interface and projected operations exist;</li><li>require exactly one explicit default-create pin per selected group+kind; and</li><li>freeze stable views ordered by exact contract identity.</li></ol><p>Compilation is not incremental. Any failed stage returns diagnostics and no Snapshot. A caller must never keep a partial result from the valid subset.</p><p>The stable diagnostic codes for this boundary are:</p><ul><li><code>invalid_input</code>;</li><li><code>invalid_artifact</code>;</li><li><code>digest_mismatch</code>;</li><li><code>duplicate_identity</code>;</li><li><code>unresolved_reference</code>;</li><li><code>ambiguous_default</code>; and</li><li><code>missing_default</code>; and</li><li><code>unsupported_host_api</code>.</li></ul><p>Diagnostics sort by subject, JSON Pointer, code, then message. Code is the automation contract; human-readable message text may improve without changing the code. Reordering otherwise identical input MUST produce byte-identical Snapshot identity and diagnostics.</p><h2 id="immutable-snapshot" tabindex="-1">Immutable Snapshot <a class="header-anchor" href="#immutable-snapshot" aria-label="Permalink to &quot;Immutable Snapshot&quot;">​</a></h2><p>A successful Snapshot owns copies of all bytes and exposes only copied views. It contains:</p><ul><li>exact FormRefs and selected package digests;</li><li>canonical Form Definition bytes and compiled desired schemas;</li><li>exact Interface and Binding refs plus canonical Definition bytes;</li><li>the closed exact-reference graph across all three contract types;</li><li>explicit default-create pins; and</li><li>the selected Host API lane.</li></ul><p>Its digest is calculated over a canonical normalized projection of those facts. Diagnostics-only origin and publisher identity are excluded. Package digest remains Snapshot selection/provenance even though it is not part of FormRef equality.</p><p>The minimum operations are:</p><ul><li>enumerate Forms in stable order;</li><li>enumerate Interfaces and Bindings in stable order;</li><li>retrieve canonical Definition bytes by exact FormRef;</li><li>resolve a create default by exact group+kind; and</li><li>validate desired I-JSON against the schema of an exact FormRef; and</li><li>recursively materialize schema defaults into canonical effective desired bytes before validation.</li></ul><p>A wrong group, version, or schema digest is a miss. No operation falls back to another FormRef.</p><h2 id="downstream-adapters" tabindex="-1">Downstream Adapters <a class="header-anchor" href="#downstream-adapters" aria-label="Permalink to &quot;Downstream Adapters&quot;">​</a></h2><ul><li>A Terraform Provider consumes a Snapshot plus a Provider-owned projection that names resource types, schemas, state versions, imports, codecs, and every readable historical FormRef. A new package never changes a running Provider&#39;s schema dynamically. Provider 3 remains typed and MUST NOT add an opaque generic JSON resource as a substitute for a compiled projection.</li><li>A conformance runner consumes the same Snapshot plus explicitly selected generic, family, Interface/Binding, and composition corpora. Family semantics stay outside generic Core.</li><li>A Host consumes a Snapshot only after its own package admission. It separately decides implementation support and activation and owns all Resources.</li></ul><p>Official Forms and external Forms use the same compile path. An official namespace is a publisher-policy fact, not a Core enum or import. An external author follows the same public path and imports no internal Provider or repository package.</p><h2 id="compatibility-and-release-boundary" tabindex="-1">Compatibility and release boundary <a class="header-anchor" href="#compatibility-and-release-boundary" aria-label="Permalink to &quot;Compatibility and release boundary&quot;">​</a></h2><p>This contract is compatible Specification-minor work because it fixes how existing exact package and Definition identities are compiled; it does not change their bytes or the closed Host API v1 wire. Specification 1.1 may record this boundary without minting a Form, package, Provider, Host adoption, or literal Host API identity.</p><p>Adding a required field to a published package or Definition envelope, changing FormRef equality, adding an executable family hook, or requiring independently installable Interface/Binding package kinds is a new identity decision and cannot be backfilled into a closed v1 contract.</p><p>During the one-repository stage the implementation is internal. No neutral SDK is released from <code>github.com/tako0614/terraform-provider-takoform</code>. A public SDK may be released only after the final <code>github.com/tako0614/takoform</code> repository, module, single Specification writer, signer, workflow, and history-transfer evidence are established.</p><h2 id="conformance-witnesses" tabindex="-1">Conformance witnesses <a class="header-anchor" href="#conformance-witnesses" aria-label="Permalink to &quot;Conformance witnesses&quot;">​</a></h2><p>The Core boundary is not complete until all of these remain green:</p><ul><li>zero-family compilation;</li><li>two synthetic external groups using the same Kind without collision;</li><li>cross-family exact-reference closure and wrong-digest rejection;</li><li>input-permutation equality for Snapshot and diagnostics;</li><li>no partial Snapshot on failure;</li><li>complete-package forgery resistance and defensive-copy ownership;</li><li>unknown and insufficient Host API refusal;</li><li>exact Interface/Binding closure and immutable Definition views;</li><li>omitted and explicitly written defaults producing identical canonical bytes;</li><li>the official publisher&#39;s 31 Forms, 13 Interfaces, and 6 Bindings compiled by an integration test outside neutral Core;</li><li>an external package compiled in the same Snapshot with those official artifacts, without a Core source change; and</li><li>Provider 3&#39;s independently locked public/state/history surface remains unchanged.</li></ul><p>The non-normative inventory of current sources, generated surfaces and their post-extraction owners is in <code>../../docs/source-boundary-inventory.md</code>.</p>',42)])])}const h=i(n,[["render",r]]);export{u as __pageData,h as default};
