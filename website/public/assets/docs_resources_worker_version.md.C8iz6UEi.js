import{_ as t,o,c as r,ag as i}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"takoform_worker_version","description":"Worker Version (edge.forms.takoform.com, role revision).","frontmatter":{"page_title":"takoform_worker_version Resource - takoform","subcategory":"tako0614 Forms","description":"Worker Version (edge.forms.takoform.com, role revision)."},"headers":[],"relativePath":"docs/resources/worker_version.md","filePath":"docs/resources/worker_version.md"}'),a={name:"docs/resources/worker_version.md"};function s(n,e,d,c,l,h){return o(),r("div",null,[...e[0]||(e[0]=[i(`<h1 id="takoform-worker-version" tabindex="-1">takoform_worker_version <a class="header-anchor" href="#takoform-worker-version" aria-label="Permalink to &quot;takoform_worker_version&quot;">​</a></h1><p>Immutable executable snapshot of one Module Worker: a bundle, the handlers its module exports, non-secret vars, and the typed capability bindings the code may use. A change is a new Worker Version; traffic moves only through Worker Deployments. The runtime this code runs on is not a field of this Form: it is fixed by the worker.runtime@1.1.0 contract the Module Worker identity provides, so a version carries no compatibility date and no compatibility flag (decision 0019).</p><p>This is a <code>revision</code> resource: an immutable snapshot. It is create-only — every desired attribute forces replacement, and rollback means pointing a deployment at an earlier revision, never editing this one.</p><p>This page documents the publisher-set next-major Provider mapping; its exact FormRef <code>edge.forms.takoform.com/WorkerVersion</code> is recorded below. The resource type is Provider metadata. The published Edge Form Definition is maintained in the <a href="https://github.com/tako0614/takoform-forms/blob/3231633605b737ce5279d7fc020b4780568e7091/forms/candidates/edge.forms.takoform.com/worker-version/definition.json" target="_blank" rel="noreferrer">takoform-forms source</a>. See the complete exact identity and the <a href="https://takoform.com/examples/resources/takoform_worker_version/resource.tf" target="_blank" rel="noreferrer">complete example</a>.</p><h2 id="exact-formref" tabindex="-1">Exact FormRef <a class="header-anchor" href="#exact-formref" aria-label="Permalink to &quot;Exact FormRef&quot;">​</a></h2><p>This Provider mapping carries the following exact four-field FormRef:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;edge.forms.takoform.com&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;WorkerVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;definitionVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.3.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sha256:65870343bfab512fe5e7ae6faea8b3dbc48f9c9de0d4d9349dcbfd819f06d365&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>packageDigest</code> — Form Package digest (separate from FormRef; embedded Provider provenance): <code>sha256:21adc2e4e677cd31e905483d38eff60c9eb61112f6c234a01d6a487154980891</code>.</p><h2 id="arguments" tabindex="-1">Arguments <a class="header-anchor" href="#arguments" aria-label="Permalink to &quot;Arguments&quot;">​</a></h2><ul><li><code>name</code> (String, optional, computed, forces replacement) — Portable resource name (<code>metadata.name</code>). Omit it and set <code>revision_owner</code> instead: this Form is an immutable revision, so the provider derives <code>version-&lt;content digest prefix&gt;-&lt;owner digest prefix&gt;</code> from this revision&#39;s own content and its declared owner. Changed content is then a NEW revision created beside the old one, which is the only way a code change applies at all — a host refuses every update to a revision, and replacing one under a name it still holds completes in neither apply order. Setting it pins the name, which an imported revision needs; the provider then refuses at plan time any change that would replace this revision under it.</li><li><code>revision_owner</code> (String, optional, forces replacement) — Stable name of the logical resource that owns this revision. When the Form carries an owner relation, use that target resource&#39;s name. Required whenever <code>name</code> is omitted. Two independent resources built from identical content derive identical content digests, so without an owner they would derive one name and two Terraform resources would manage one host address — where a destroy of either breaks the other. It is provider-side authoring input: no wire member carries it, the host never sees it, and it enters only the derived name. The repository <a href="https://github.com/tako0614/terraform-provider-takoform/tree/main/modules/worker-app" target="_blank" rel="noreferrer"><code>worker-app</code> module</a> sets it for you.</li><li><code>worker</code> (String, required, forces replacement) — Module Worker identity this version belongs to. Set the name of the target <code>ModuleWorker</code> resource.</li><li><code>bundle</code> (String, required, forces replacement) — Worker Bundle carrying the exact module bytes this version executes. Set the name of the target <code>WorkerBundle</code> resource.</li><li><code>assets</code> (Object, optional, forces replacement) — Optional static-asset attachment for this immutable version. Without it the host performs no asset lookup. When present, every member is required and the request order is closed: with runWorkerFirst=false the host tries the asset lookup before invoking fetch; with true it invokes fetch first and tries the asset lookup only when that invocation returns 404. An asset result wins; if both stages miss, the worker&#39;s 404 is preserved. The attachment never grants a hidden runtime binding and never mutates the referenced bundle. The object declares <code>bundle</code>, <code>run_worker_first</code>, <code>not_found_handling</code>; when the object is present, every member is required.</li><li><code>handlers</code> (Set of String, required, forces replacement) — Module event handlers this version exports, from the closed vocabulary the worker.runtime@1.1.0 contract defines. A host rejects a handler that contract does not define, and rejects an attachment whose event kind is not declared here. One of <code>fetch</code>, <code>scheduled</code>, <code>queue</code>.</li><li><code>vars_json</code> (String, optional, forces replacement) — Non-secret configuration values projected into the module environment. Sensitive material never enters portable state. Omitting it projects no variable. Authored as one JSON object string (for example <code>jsonencode({...})</code>); the provider sends the parsed object. Defaults to the empty object <code>{}</code>.</li><li><code>kv_bindings</code> (List of Object, optional, forces replacement) — Typed module-worker.edge-kv bindings projecting the edge.kv API under JavaScript identifier names. Omitting it declares no such binding. Each entry declares <code>name</code> (a JavaScript identifier) and <code>target_name</code> (the target <code>EdgeKVNamespace</code> resource name); the wire carries the typed <code>resource</code> reference. Defaults to the empty list <code>[]</code>.</li><li><code>bucket_bindings</code> (List of Object, optional, forces replacement) — Typed module-worker.object-bucket bindings projecting the edge.objects API. Omitting it declares no such binding. Each entry declares <code>name</code> (a JavaScript identifier) and <code>target_name</code> (the target <code>ObjectBucket</code> resource name); the wire carries the typed <code>resource</code> reference. Defaults to the empty list <code>[]</code>.</li><li><code>sqlite_bindings</code> (List of Object, optional, forces replacement) — Typed module-worker.sqlite bindings projecting the edge.sql API. Omitting it declares no such binding. Each entry declares <code>name</code> (a JavaScript identifier) and <code>target_name</code> (the target <code>SQLiteDatabase</code> resource name); the wire carries the typed <code>resource</code> reference. Defaults to the empty list <code>[]</code>.</li><li><code>queue_producer_bindings</code> (List of Object, optional, forces replacement) — Typed module-worker.queue-producer bindings projecting only send and sendBatch. Omitting it declares no such binding. Each entry declares <code>name</code> (a JavaScript identifier) and <code>target_name</code> (the target <code>AtLeastOnceQueue</code> resource name); the wire carries the typed <code>resource</code> reference. Defaults to the empty list <code>[]</code>.</li><li><code>service_bindings</code> (List of Object, optional, forces replacement) — Typed module-worker.service bindings projecting worker.service fetch toward another Module Worker. Omitting it declares no such binding. Each entry declares <code>name</code> (a JavaScript identifier) and <code>target_name</code> (the target <code>ModuleWorker</code> resource name); the wire carries the typed <code>resource</code> reference. Defaults to the empty list <code>[]</code>.</li><li><code>workflow_bindings</code> (List of Object, optional, forces replacement) — Typed module-worker.workflow bindings projecting the instance surface — create, get, status, sendEvent, terminate. Omitting it declares no such binding. Each entry declares <code>name</code> (a JavaScript identifier) and <code>target_name</code> (the target <code>DurableWorkflow</code> resource name); the wire carries the typed <code>resource</code> reference. Defaults to the empty list <code>[]</code>.</li><li><code>actor_bindings</code> (List of Object, optional, forces replacement) — Typed module-worker.actor bindings projecting addressing and invocation — idFromName, newUniqueId, get. Omitting it declares no such binding. Each entry declares <code>name</code> (a JavaScript identifier) and <code>target_name</code> (the target <code>ActorNamespace</code> resource name); the wire carries the typed <code>resource</code> reference. Defaults to the empty list <code>[]</code>.</li><li><code>required_sensitive_vars</code> (Set of String, optional, forces replacement) — Names of sensitive values this version requires the host to supply out-of-band. Only the names are portable state; values travel through each host&#39;s own sealed path. Omitting it requires no sensitive value. Defaults to the empty list <code>[]</code>.</li><li><code>external_services</code> (List of Object, optional, forces replacement) — External standard services this version speaks, each a sealed slot naming only a runtime binding NAME and an opaque namespaced protocol. The host resolves the integration out-of-band and supplies one sealed runtime-native binding under NAME; neither its entries nor the credential is portable state. A required slot the host cannot satisfy keeps the version from becoming Ready. Omitting it declares no external service. Each entry declares <code>name</code> (SCREAMING_SNAKE, the sealed binding slot), an opaque normalized reverse-DNS <code>protocol</code> identifier such as <code>com.amazonaws.s3</code>, and optional <code>required</code> (default true). Takoform carries no central protocol enum or protocol-specific members. The Host must fail closed unless its support profile exactly supports the identifier, then projects one sealed runtime-native binding under the slot name. Defaults to the empty list <code>[]</code>.</li><li><code>apply_idempotency_key</code> (candidate Provider 4.0.0, String, optional, computed, forces replacement) — Provider-only Host operation identity for this immutable version&#39;s apply. Released Provider 3.0.0 does not expose this attribute. On an ordinary run with no <code>runtime_input_nonce</code>, omitting it keeps the Provider&#39;s established deterministic operation key; an explicitly configured 1..255-byte visible-ASCII value retains the caller-selected behavior. When <code>runtime_input_nonce</code> is configured on this exact Provider instance, this argument must be omitted: the Provider computes it from that nonce and the exact value-free logical WorkerVersion apply identity and records only the opaque result in plan and state. Rotating a sensitive value under the same nonce does not change the key; rotating the nonce does, producing a new immutable WorkerVersion identity. The key is never included in the portable desired spec or read back from the Host.</li><li><code>space</code> (String, optional, forces replacement) — Exact opaque SpaceID; overrides the provider default.</li><li><code>create_timeout</code> / <code>delete_timeout</code> (String, optional) — Go durations bounding each operation (defaults <code>20m</code> / <code>30m</code>). There is no <code>update_timeout</code>: this Form declares no update capability. Changing only these provider-side timeouts is applied in place without any host call.</li></ul><h2 id="run-scoped-sensitive-inputs" tabindex="-1">Run-scoped sensitive inputs <a class="header-anchor" href="#run-scoped-sensitive-inputs" aria-label="Permalink to &quot;Run-scoped sensitive inputs&quot;">​</a></h2><p><code>required_sensitive_vars</code> declares names only. When it is non-empty, the exact Provider instance must receive a 22..128 character unpadded-base64url <code>runtime_input_nonce</code> during Plan while its sensitive <code>runtime_inputs</code> map remains empty. Apply must reuse that nonce and supply a map whose names exactly equal the declaration. Missing or extra names, a changed nonce, a value supplied during Plan, or a value supplied to a different Provider instance is refused before any Host mutation.</p><p>The runner supplies the Apply-only map through an ephemeral root variable and the selected Provider block. It writes the transient variable file to the OpenTofu process&#39;s standard input; it does not put values in command arguments, process environment, ordinary credential files, plan, or state. This Provider ignores <code>TAKOFORM_RUNTIME_INPUTS_FILE</code> and ambient environment variables named by the declaration. One module may use other Takoform or industry-standard Provider instances; none receives this map unless the root explicitly targets it.</p><p>The map is limited to 1..64 bindings, each value to 1..32768 bytes of UTF-8 text without NUL, and the runner dispatch to 1 MiB total. These limits are separate from the value-free public apply envelope: <code>publicApply.path</code> is limited to 8,192 UTF-8 bytes and <code>publicApply.body</code> is limited to 1,048,576 UTF-8 bytes. An overlong path, body, binding, or dispatch is refused before a commitment, private preparation, or public Host mutation.</p><p>Every run first reads the value-free private preparation by the deterministic operation key. Only an absent record permits one same-origin private PUT of the plaintext bindings over TLS. A prepared record continues with one ordinary public PUT; an accepted, dispatched, or consumed record polls its exact ordinary Host operation without resending bindings or replaying the public PUT. After any PUT acknowledgement failure, recovery uses a fresh bounded context and value-free private readback.</p><p>Values never enter the public apply body, Terraform plan or state, Provider logs or diagnostics, or the computed operation key. The Provider keeps accepted values in mutable buffers where practical, wipes those buffers and transport response buffers promptly on a best-effort basis, and drops references after the private PUT. This is not a guarantee of Go process-memory erasure: the runtime, compiler, HTTP/TLS stack, operating system, or a crash dump may retain copies outside those buffers. Runner operators must protect or disable crash dumps and process inspection as appropriate. The durable guarantee is absence from plan, state, logs, diagnostics, and public Host requests.</p><h2 id="read-only-attributes" tabindex="-1">Read-only attributes <a class="header-anchor" href="#read-only-attributes" aria-label="Permalink to &quot;Read-only attributes&quot;">​</a></h2><ul><li><code>uid</code> — host-issued immutable resource identity; delete and re-create yields a new UID.</li><li><code>generation</code> — desired-state generation; increments only when the portable desired spec changes; this Form declares no update capability — every desired attribute forces replacement instead. It is also the DELETE fence, because a delete withdraws desired state like any other desired-state mutation.</li><li><code>revision</code> — representation revision; increments whenever the representation changes — a spec-changing update, new status, new outputs, or a change to another resource this one is rendered from. It is the strong ETag, and it is deliberately NOT the delete fence: a teardown removes dependents first and would otherwise be refused by a revision it moved itself.</li><li><code>conditions</code> — the complete status condition list the host reports, in its order. Each entry carries <code>type</code> (the closed <code>Ready</code> / <code>Reconciling</code> / <code>Degraded</code> / <code>Drifted</code> / <code>Blocked</code> / <code>Deleting</code> vocabulary), <code>status</code> (<code>True</code> / <code>False</code> / <code>Unknown</code>), the closed portable <code>reason</code>, an optional <code>message</code>, an optional non-portable <code>host_reason</code> naming exactly what is wrong, the <code>observed_generation</code> the status reflects, and <code>last_transition_time</code>. Conditions are host-rendered state: they change when this resource changes AND when a resource it depends on changes, with no desired spec changing anywhere, so they are read-only and a configuration must not assert them.</li><li><code>ready</code> — derived convenience: true when <code>conditions</code> carries the closed <code>Ready</code> condition with status <code>True</code>. Read <code>conditions</code> for the reason it is not.</li><li><code>outputs_json</code> — the WHOLE <code>status.outputs</code> document, JSON-serialized. This Form declares no <code>outputSchema</code>, so a conforming host omits <code>status.outputs</code> entirely and this attribute is <code>&quot;{}&quot;</code>. It stays declared because a host may publish a value no contract describes, and an undescribed value must still be reachable rather than silently dropped.</li><li><code>form_api_version</code>, <code>form_kind</code>, <code>form_definition_version</code>, <code>form_schema_digest</code> — the exact immutable FormRef this state is bound to; reads dispatch on it.</li><li><code>form_package_digest</code> — audit-only package provenance; never part of resource identity, queries, or fences.</li><li><code>relation_drift_reason</code> — internal recovery only: <code>ExternalChange</code> or <code>DependencyMissing</code> while the host reports that a resource this one references was replaced or removed out of band, null otherwise. A refresh reports the break as a warning and keeps the resource in state; the next plan then proposes replacing this resource, because this Form declares no in-place update and a host refuses every apply to the existing one. It is provider-side recovery bookkeeping — no portable wire member carries it — and configurations must not depend on it.</li><li><code>pending_operation_id</code> — internal recovery only: the host operation id of a mutation the host accepted but that did not reach a terminal state before the operation deadline, null in steady state. A refresh consults it before it reads the resource, and it is cleared only once that operation settles. It is not resource identity and configurations must not depend on it.</li></ul><h2 id="state-continuity" tabindex="-1">State continuity <a class="header-anchor" href="#state-continuity" aria-label="Permalink to &quot;State continuity&quot;">​</a></h2><ul><li><strong>Reads dispatch on the recorded FormRef.</strong> <code>WorkerVersion</code> state is addressed under the exact <code>form_*</code> identity it records, not under this build&#39;s default create ref, so a resource created before the Form line advanced stays addressable as itself. An identity this provider build carries no codec for is a hard error naming that identity and the ones the build does carry; the provider never substitutes another exact FormRef, because a substituted query&#39;s &quot;not found&quot; is indistinguishable from deletion.</li><li><strong>A changed <code>uid</code> is an error, and state is kept.</strong> When the host serves a different <code>uid</code> under the recorded name, the resource this state was applied against is gone and something re-used its name. The provider reports a hard error naming both uids and keeps the resource in state. It does not re-bind — that would adopt a resource you never applied — and it does not remove state, which would make the next apply fail against the resource that does exist, with no plan left to repair it. Resolve it by importing the new incarnation explicitly, restoring the prior one, or deleting the host-side replacement.</li><li><strong>An unfinished mutation is resumed, not re-created.</strong> When <code>pending_operation_id</code> is set, a refresh asks the host about that operation before it reads the resource. While the operation is still running the resource may legitimately not exist yet, so its absence is not treated as deletion and the marker survives; a terminal success is verified against the exact identity and settles state; a terminal failure or an expired operation record defers to an exact read of the resource, which decides. Refresh again once the host settles.</li></ul><h2 id="accepted-bindings" tabindex="-1">Accepted bindings <a class="header-anchor" href="#accepted-bindings" aria-label="Permalink to &quot;Accepted bindings&quot;">​</a></h2><ul><li><code>module-worker.edge-kv@1.0.0</code></li><li><code>module-worker.object-bucket@1.1.0</code></li><li><code>module-worker.sqlite@1.0.0</code></li><li><code>module-worker.queue-producer@1.0.0</code></li><li><code>module-worker.service@1.0.0</code></li><li><code>module-worker.workflow@1.0.0</code></li><li><code>module-worker.actor@1.0.0</code></li></ul><p>Outward capability use is a typed binding held by this revision; inward activation (routes, domains, cron, queue consumption) is a separate attachment resource. The two are never merged.</p><h2 id="import" tabindex="-1">Import <a class="header-anchor" href="#import" aria-label="Permalink to &quot;Import&quot;">​</a></h2><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_worker_version.example NAME</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_worker_version.example SPACE/NAME</span></span></code></pre></div><p>Both short forms bind state to this provider build&#39;s default create FormRef. To adopt a resource created under an EARLIER definition version of this Form, name the exact identity instead. The import ID is then one JSON object — not a delimiter-joined string, because a SpaceID is opaque UTF-8 whose only forbidden character is <code>/</code>, so no separator can escape it safely:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_worker_version.example \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &#39;{&quot;space&quot;:&quot;prod&quot;,&quot;apiVersion&quot;:&quot;edge.forms.takoform.com&quot;,&quot;kind&quot;:&quot;WorkerVersion&quot;,&quot;definitionVersion&quot;:&quot;0.3.0&quot;,&quot;schemaDigest&quot;:&quot;sha256:…&quot;,&quot;name&quot;:&quot;…&quot;}&#39;</span></span></code></pre></div><p><code>space</code> is optional and falls back to the provider default; the four FormRef members are all-or-nothing. An identity this provider build carries no codec for is refused, naming the identities it does carry — it is never silently rebound to the default.</p>`,28)])])}const m=t(a,[["render",s]]);export{u as __pageData,m as default};
