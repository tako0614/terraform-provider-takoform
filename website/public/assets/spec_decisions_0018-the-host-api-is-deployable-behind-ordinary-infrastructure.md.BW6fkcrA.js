import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.DzNHXt93.js";const u=JSON.parse('{"title":"0018 — The Host API is deployable behind ordinary infrastructure","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.md","filePath":"spec/decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.md"}'),s={name:"spec/decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.md"};function i(r,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="_0018-—-the-host-api-is-deployable-behind-ordinary-infrastructure" tabindex="-1">0018 — The Host API is deployable behind ordinary infrastructure <a class="header-anchor" href="#_0018-—-the-host-api-is-deployable-behind-ordinary-infrastructure" aria-label="Permalink to &quot;0018 — The Host API is deployable behind ordinary infrastructure&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Decision <a href="./0013-v1alpha3-lane-ships-in-provider-v2-1.html">0013</a> opened the <code>forms.takoform.com/v1alpha3</code> lane, and decisions <a href="./0011-resource-identity-generation-and-revision.html">0011</a>, <a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a>, <a href="./0016-the-worker-aggregate-has-one-active-deployment.html">0016</a>, and <a href="./0017-provider-state-survives-form-evolution-and-interruption.html">0017</a> settled what the lane MEANS. None of them asked whether the lane can be put in front of a real network, in front of more than one caller, or in front of an operator who has to work out why something is not ready. Five defects made the answer no.</p><p><strong>The lane demanded a percent-encoded slash inside a path segment.</strong> Decision <a href="./0009-form-families-and-namespaced-api-versions.html">0009</a> made Form groups namespaced, so an apiVersion contains a slash. The client packed the whole apiVersion into ONE segment, producing <code>/resources/edge.forms.takoform.com%2Fv1alpha1/ModuleWorker/api</code>. Proxies, gateways, and web frameworks do not agree on what <code>%2F</code> inside a path segment is: some pass it through, some decode it before routing, some normalize it, and several reject the request outright. The lane&#39;s routing therefore depended on which intermediaries a deployment happened to have, and a host that worked directly could stop working the day it was placed behind a load balancer.</p><p><strong>Operation ids and upload ids were bearer capabilities.</strong> Anyone who presented an id could read an operation, cancel it, push bytes into an upload session, commit it, or abandon it. Nothing bound either handle to the caller that created it. Ids travel: they are persisted in Terraform state, logged, and passed between tools.</p><p><strong>A content address was treated as proof of entitlement.</strong><code>GET /artifacts/{manifestDigest}</code> and <code>HEAD /artifacts/blobs/{sha256}</code> answered anyone who named an existing digest. A manifest describes someone&#39;s deployable code, and a digest is a short string that appears in state files, CI logs, and error messages.</p><p><strong>Lane negotiation was serial and shared the resource-operation timeout.</strong> The provider discovered v1alpha2 and then v1alpha3 through one HTTP client whose timeout is twelve minutes, because that is what a resource operation may legitimately need. One unresponsive lane therefore blocked configuration entirely — and with it the OTHER lane&#39;s resources, which had nothing to do with the failure.</p><p><strong>Conditions were flattened to a boolean.</strong> The wire carries a closed condition list with reasons, <code>observedGeneration</code>, and transition times; the provider projected <code>ready</code> and dropped the rest. <code>Ready = false</code> with no reason is the state an operator most needs explained, and the only way to get the explanation was to leave Terraform.</p><p>These are MUST-level host, wire, and provider semantics, so this repository&#39;s <code>AGENTS.md</code> requires a decision record.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The lane is defined so that an ordinary deployment can serve it, an id is a handle rather than a key, and the state a client keeps is the state the host reports. The rules below are normative; the operative text lives in <a href="./../host-api/v1alpha3.html"><code>../host-api/v1alpha3.md</code></a>, <a href="../host-api/operations-v1alpha3.json"><code>../host-api/operations-v1alpha3.json</code></a>, and the generated provider resource documents.</p><ol><li><p><strong>A namespaced group travels as two ordinary path segments.</strong> Every URL template that names a group carries <code>{formGroup}/{formVersion}</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{api}/resources/{formGroup}/{formVersion}/{kind}/{name}</span></span>
<span class="line"><span>{api}/form-definitions/{formGroup}/{formVersion}/{kind}</span></span>
<span class="line"><span>{api}/support/forms/{formGroup}/{formVersion}/{kind}/{definitionVersion}</span></span></code></pre></div><p>No path segment ever percent-encodes a slash, and a host rejoins the two segments into the exact apiVersion. The FormRef <code>apiVersion</code> string is unchanged everywhere else — request bodies, responses, and the <code>group</code> query key still carry <code>edge.forms.takoform.com/v1alpha1</code> verbatim, so this changes the lane&#39;s URLs and nothing about its identities. A client also rejects a discovery document whose advertised endpoint paths carry any percent-encoding, comparing escaped paths so that <code>%2Fv1alpha3</code> cannot pass as <code>/v1alpha3</code>. The <code>%2F</code> convention is removed with no compatibility shim: the lane is unpublished, so there is nothing to be compatible with.</p></li><li><p><strong>Operations and upload sessions are owned.</strong> A host records the authenticated tenant and principal an operation or upload session was created by. Reading or cancelling an operation, and continuing, committing, or abandoning an upload, from a different tenant or a different principal fails with the surface&#39;s ordinary not-found outcome — <code>operation_not_found</code> (404) and <code>artifact_missing</code> (404) — never <code>permission_denied</code>. Abandoning an upload id the caller does not hold fails the same way whether the id never existed or belongs to someone else.</p></li><li><p><strong>A content address is not a capability.</strong> <code>GET /artifacts/{manifestDigest}</code> and <code>HEAD /artifacts/blobs/{sha256}</code> answer only a caller whose tenant already holds that address, acquired by uploading the bytes or by committing a manifest that names them; any other tenant is answered as if the address did not exist. <code>missingBlobs</code> is therefore answered per tenant rather than per byte store. The rule binds every resolution a host performs for a request, not only the read endpoints: a bundle-shaped desired state carries a manifest digest, so the referenced manifest is resolved against the caller&#39;s tenant too — before any mutation, on apply and import alike, and again when an accepted <code>202</code> commits — and a manifest the tenant does not hold fails <code>artifact_missing</code> indistinguishably from one nobody committed. Physical deduplication is explicitly still permitted: one stored copy per content address, one immutable identity for identical bytes, and no tenant&#39;s abandon or garbage collection may take bytes away from another.</p></li><li><p><strong>Lanes negotiate independently, under a short deadline of their own.</strong> A client discovers each lane concurrently, bounded by a discovery deadline separate from the resource-operation deadline. A lane that fails or hangs never prevents another lane&#39;s resources from working, and each resource type reports its own lane&#39;s negotiation error.</p></li><li><p><strong>Conditions are client-visible state.</strong> The provider projects the full condition list — type, status, reason, message, host reason, observed generation, and last transition time — as a typed read-only <code>conditions</code> attribute, and keeps <code>ready</code> as a convenience derived from the same list. Conditions are host-rendered state that changes when ANOTHER resource is mutated (decisions <a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a> and <a href="./0016-the-worker-aggregate-has-one-active-deployment.html">0016</a>), so the attribute is computed-only and carries no plan modifier that would hold a previous value the host has already contradicted.</p></li></ol><p>Rules 1 through 3 are proved by required conformance checks (<code>namespaced-group-travels-as-two-path-segments</code>, <code>operation-bound-to-its-creating-principal</code>, <code>upload-session-bound-to-its-creating-principal</code>, <code>artifact-digest-is-not-a-capability</code>, <code>manifest-reference-is-not-a-capability</code>); rules 4 and 5 are provider behavior proved by provider tests.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Every v1alpha3 URL that names a group changes shape. The client, the reference host, the conformance runner, the machine operation table, and the lane specification move together; no published schema or identity changes, because the apiVersion string itself is untouched.</li><li>A host must carry two more facts it previously did not: who created each operation and upload session, and which tenants hold each content address. Both are small per-record additions, and neither constrains how the bytes are stored.</li><li><code>missingBlobs</code> may now report a blob whose bytes the host already has. The cost is one re-upload the first time a tenant wants an address someone else introduced; the alternative is a dedup oracle that hands over content to whoever can name it.</li><li>Two tenants uploading identical bytes converge on one identity and one stored copy, so the dedup that motivated content addressing is preserved exactly.</li><li>Configuration gets faster and more predictable: both lanes are negotiated at once, and the worst case is one short discovery deadline rather than one long resource-operation timeout.</li><li><code>conditions</code> appears in state for every v1alpha3 resource. It is computed, so no configuration changes; operators gain the reason, the host detail, and the observed generation without leaving Terraform.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep <code>%2F</code> and document a proxy requirement.</strong> Rejected because it makes the lane&#39;s correctness a property of someone else&#39;s deployment. &quot;Configure every intermediary to pass encoded slashes through&quot; is not a requirement a portable specification can impose: the operator often does not control the intermediaries, several reject <code>%2F</code> with no setting to change, and the failure appears as a routing error far from the contract that caused it. Two ordinary segments cost nothing and are unambiguous everywhere.</li><li><strong>Split only the resource route and leave the others.</strong> Rejected because one shape is what makes the rule checkable. A lane where two of the four group-bearing routes are ordinary and two are not is harder to describe, and the two exceptions would be exactly the routes a new implementation gets wrong.</li><li><strong>Return <code>permission_denied</code> (403) for another principal&#39;s operation.</strong> Rejected because it answers the question the stranger asked. A 403 says &quot;this id names a real operation, and it is not yours&quot; — an existence oracle over ids that appear in state files and logs. The not-found outcome is also the honest one: the caller has no operation by that id.</li><li><strong>Treat a digest as proof of entitlement — &quot;you can only read it if you already know it&quot;.</strong> Rejected because knowing a digest is not knowing the content. Digests are short, copied into state files and CI logs, and derivable by anyone who can guess the bytes; a manifest is a description of deployable code, and a blob is the code. Content addressing is a naming scheme, and making it double as authorization means the only protection is that the name is hard to say.</li><li><strong>Scope artifact access per principal rather than per tenant.</strong> Rejected because an artifact is a tenant asset: the CI principal that uploads a bundle and the apply principal that references it are routinely different, and per-principal scoping would break the ordinary pipeline while adding no protection the tenant boundary does not already give.</li><li><strong>Refuse to store bytes a second tenant uploads, to keep one copy.</strong> Rejected as solving the wrong problem: deduplication is a storage decision and authorization is an access decision. Physical dedup stays; what changes is that holding an address is recorded rather than assumed.</li><li><strong>Bound discovery with the existing resource-operation timeout.</strong> Rejected because the two operations are nothing alike. Discovery is one small read of a static document; a resource operation may take minutes by design. Sharing the long bound made the provider&#39;s startup as slow as its slowest legitimate mutation.</li><li><strong>Project conditions as an opaque <code>conditions_json</code> string.</strong> Rejected because a typed list is representable in this framework version and a JSON blob would push parsing into every configuration that wants a reason, with no schema, no documentation, and no type checking.</li><li><strong>Make <code>conditions</code> optional so a configuration can assert an expected state.</strong> Rejected because conditions are host-owned and change without any desired spec changing; a configured expectation would diff forever.</li></ul>`,18)])])}const m=t(s,[["render",i]]);export{u as __pageData,m as default};
