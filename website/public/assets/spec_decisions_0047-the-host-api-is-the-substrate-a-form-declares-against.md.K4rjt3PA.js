import{_ as t,o as a,c as o,ag as n}from"./chunks/framework.cyF4XceO.js";const p=JSON.parse('{"title":"0047 — The Host API is a substrate a Form declares against, and the two version apart","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0047-the-host-api-is-the-substrate-a-form-declares-against.md","filePath":"spec/decisions/0047-the-host-api-is-the-substrate-a-form-declares-against.md"}'),s={name:"spec/decisions/0047-the-host-api-is-the-substrate-a-form-declares-against.md"};function i(r,e,h,c,d,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="_0047-—-the-host-api-is-a-substrate-a-form-declares-against-and-the-two-version-apart" tabindex="-1">0047 — The Host API is a substrate a Form declares against, and the two version apart <a class="header-anchor" href="#_0047-—-the-host-api-is-a-substrate-a-form-declares-against-and-the-two-version-apart" aria-label="Permalink to &quot;0047 — The Host API is a substrate a Form declares against, and the two version apart&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-23</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Three versions exist in this specification and each has its own line: the Host API lane (<code>forms.takoform.com/vN</code>), a Form Family group (<code>edge.forms.takoform.com/vN</code>), and a Form&#39;s own <code>definitionVersion</code>. They are separate on purpose — <a href="./0039-a-lane-is-minted-for-one-of-two-reasons.html">decision 0039</a> restricts when a lane may move at all, and <a href="./0038-a-generation-move-is-measured-not-assumed.html">decision 0038</a> measures what a family move actually re-identified.</p><p>They have nevertheless moved together every time. The Beta 2 generation moved the lane and the family in one change, as the Beta 1 generation had before it. The maintainer named the smell (2026-08-23): <em>why, when each specification has its own version, is this being done all at once?</em></p><p>The answer is that <strong>nothing in a Form&#39;s contract says which Host API it needs</strong>, so nothing ever licensed moving one without the other. A Definition declares the exact Interfaces it PROVIDES, the exact Bindings it ACCEPTS, and the exact contracts its relations REQUIRE of their targets — every dependency it has on another contract is exact and stated, except the one every Form has: the protocol a host must speak to serve it at all. That one was carried by convention, and convention has no version, so the only safe move was to move everything.</p><p>The same review found the family&#39;s published surface carrying something that is not the family&#39;s: <code>candidate-set.json</code>, served at takoform.com, names a <code>resourceType</code> — <code>takoform_module_worker</code> — for each Form. That is the Terraform provider&#39;s authoring name. A Form fixes the shape of a cloud primitive (<a href="./0008-forms-preserve-service-shape.html">decision 0008</a>); how an author WRITES it is the province of whichever client renders it, and a second client rendering the same Form differently is not a conformance question. The published Definition itself was already clean — no HCL names, no resource type, wire names throughout. Only the index beside it was not.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Host API lane is a SUBSTRATE. A Form Definition declares the minimum lane it requires, and the lane and the family version apart.</strong></p><ol><li><p><strong>A Definition carries <code>requiresHostApi</code></strong>: the exact identity of the earliest Host API lane whose rules its contract needs. A host installing a Form whose requirement its own lane does not satisfy refuses it with <code>unsupported_capability</code>, at install time, naming both identities. A Form that needs nothing newer than the first Beta lane says so and runs on every host that serves it, whatever generation its family is on.</p></li><li><p><strong>The requirement is a LOWER BOUND, not a pin</strong>, and the asymmetry with <a href="./0022-relations-pin-the-target-contract.html">decision 0022</a> is deliberate. A relation pins its target&#39;s exact contract because the target is another resource whose Definition may move underneath it and whose meaning is nobody&#39;s obligation to preserve. A lane is not another resource: it is the protocol the host serves, its compatibility across generations is the LANE&#39;s own obligation, and decision 0039 already forbids minting one for any reason but a protocol change or an evidenced graduation. Pinning it would say that every Form must be re-minted whenever the protocol moves — which is the lockstep this record exists to end, wearing a different name.</p></li><li><p><strong>A family generation and a lane generation are separate events.</strong> A family moves when its shapes change; a lane moves for decision 0039&#39;s two reasons. Neither implies the other, and the continuity ledger measures the family&#39;s move alone. Members of one generation MAY declare different lane requirements: what a Form needs from the substrate is a property of that Form&#39;s contract, not of the group it was minted in.</p></li><li><p><strong>A family&#39;s published surface carries no client&#39;s authoring vocabulary.</strong><code>resourceType</code> leaves the candidate set. A provider derives its own resource names from <code>kind</code>, and two providers rendering one Form differently are both conforming.</p></li></ol><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p>The Definition profile requires <code>requiresHostApi</code>, so a Form minted without one cannot be published. A host&#39;s install path compares it against the lane it serves and refuses a Form it cannot honour; the conformance corpus drives that refusal, because an unenforced requirement would be a comment. The generation gates stop treating a family move and a lane move as one event: each is checked against its own record.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The next family generation can move without touching the lane, and the next protocol change can move without re-minting seventeen Forms. The cost is one more member on every Definition and one more thing a host must check — which is exactly what the previous arrangement was hiding.</p><p>This record does not renumber anything already minted. The Beta 2 identities keep the numbers they have; what changes is that the NEXT move is not required to be simultaneous.</p>',15)])])}const u=t(s,[["render",i]]);export{p as __pageData,u as default};
