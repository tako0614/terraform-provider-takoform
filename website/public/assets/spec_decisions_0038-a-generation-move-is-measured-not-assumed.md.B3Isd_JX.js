import{_ as t,o as a,c as o,ag as n}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"0038 — A generation move is measured, and what it costs is recorded","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0038-a-generation-move-is-measured-not-assumed.md","filePath":"spec/decisions/0038-a-generation-move-is-measured-not-assumed.md"}'),i={name:"spec/decisions/0038-a-generation-move-is-measured-not-assumed.md"};function r(s,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="_0038-—-a-generation-move-is-measured-and-what-it-costs-is-recorded" tabindex="-1">0038 — A generation move is measured, and what it costs is recorded <a class="header-anchor" href="#_0038-—-a-generation-move-is-measured-and-what-it-costs-is-recorded" aria-label="Permalink to &quot;0038 — A generation move is measured, and what it costs is recorded&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-22</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The move that produced the current generation was measured after the fact, because a reader could not tell from the identities what it had changed.</p><p><strong>The Host API lane moved and the protocol did not.</strong><code>spec/schemas/host-api-wire-v1alpha3.schema.json</code> and <code>-v1beta1.schema.json</code> are 590 lines each and, with version words normalised, structurally identical; so are the two discovery schemas; and <code>spec/host-api/v1alpha3.md</code> and <code>v1beta1.md</code> differ by exactly one line that is not a version word — a link to <a href="./0035-beta-contracts-ship-in-stable-provider-v2-1.html">decision 0035</a>. Comparing all four lanes pairwise, the only structurally identical pair in six is v1alpha3 against v1beta1.</p><p><strong>The Form family moved and most of its contracts did not.</strong> The same fifteen kinds, all still <code>0.1.0</code>, and twelve of the fifteen definitions structurally unchanged. Three changed: <code>WorkerVersion</code>, <code>WorkerDeployment</code>, <code>SQLiteMigrationApplication</code>.</p><p><strong>The axis that should have carried those three did not move at all.</strong><code>definitionVersion</code> stayed <code>0.1.0</code> for every Form, while <a href="./../versioning.html"><code>../versioning.md</code></a> already says a breaking Experimental change increments the minor version.</p><p>So one event moved three identity axes, and the axis built for it stayed still. That is why the numbers read as coupled while the documents describe them as independent, and why &quot;which one is current&quot; stopped being answerable by inspection.</p><h2 id="the-part-this-record-does-not-decide" tabindex="-1">The part this record does not decide <a class="header-anchor" href="#the-part-this-record-does-not-decide" aria-label="Permalink to &quot;The part this record does not decide&quot;">​</a></h2><p>Whether the lane should have moved is genuinely open, because two normative documents in this repository answer it differently.</p><p><a href="./../versioning.html"><code>../versioning.md</code></a> says the lane does not move for Form reasons:</p><blockquote><p>a Host wire version never implies Form maturity.</p></blockquote><blockquote><p>The API group MUST NOT graduate based on a Form count, package publication, provider major, historical admission, or one host&#39;s conformance report.</p></blockquote><p>and names only one trigger — &quot;Breaking protocol changes require a new Host API group identity&quot; — as a sufficiency, never as the only permitted reason.</p><p><a href="./0035-beta-contracts-ship-in-stable-provider-v2-1.html">Decision 0035</a> and <a href="./../publication-freeze.html"><code>../publication-freeze.md</code></a> say a maturity graduation arrives as a new identity:</p><blockquote><p>Stable <code>v1</code> and Form <code>1.0.0</code> are new exact identities</p></blockquote><blockquote><p>Promotion adds identities and create defaults</p></blockquote><p>Under the first reading the v1alpha3 → v1beta1 move is hard to justify; under the second it is the design working. <strong>Both are current normative text, and this record does not pick one.</strong> Deciding requires answering whether the maturity word is part of what a lane identity MEANS — which runs directly into <a href="./0037-immutability-begins-at-stable.html">decision 0037</a>&#39;s rule that a served identity means what it meant when it was published. If it is part of the meaning, a graduation must mint; if it is not, minting on maturity is the empty migration this record measured.</p><p>That question binds only when a graduation is real, and graduation is gated on evidence this project cannot produce alone — <a href="./../project-lifecycle.html"><code>../project-lifecycle.md</code></a> requires two independently maintained host implementations. <code>versioning.md</code> already reserves the decision: &quot;Any graduation is a separate ADR and public migration plan.&quot; This is a <strong>conformance gap</strong> in the sense <code>AGENTS.md</code> names — a disagreement between two authorities, recorded rather than silently resolved by whichever one a reader happens to open first.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A generation move records what it changed, per Form.</strong><a href="../../release/form-contract-continuity.json"><code>../../release/form-contract-continuity.json</code></a> carries, for each move, which contracts changed and which were only re-identified. The classification is DERIVED — definitions compared before and after with version words normalised — so it cannot drift from the definitions and cannot be asserted by hand. It answers the question a client holding a recorded FormRef actually has: is this the same contract under a new name, or one I must re-read.</p><p><strong>A graduation must be executable before it is decided.</strong> Two places would have failed the moment the family advanced past Beta, on a path this repository has already committed to in <code>internal/currentformregistry/registry_v3_test.go</code> and <code>internal/provider/v3_continuity_test.go</code>. The site-status derivation had a bare-<code>/vN</code> branch for the Host API lane and none for the family, so a stable family threw; and <code>formpackage</code> selected the family schema by matching <code>/v1beta1$</code>, letting any other namespaced group fall through to the retained v1alpha3 closure without an error. Both are fixed: the two derivations now read the same way, the family channel is checked against the recognised set rather than against today&#39;s value, and the retained families are named one at a time so a new one gets the current schema by default.</p><p><strong>A maturity word is not checked by anything, and this record says so.</strong> The lane and the family each carry a channel word that no gate compares against the evidence — not against <code>spec/publication-blockers.json</code>, not against the lifecycle records. Where a channel is derived from a name, it reports what the name says, never whether the name is still true. That is the same class as <a href="./0036-artifact-names-state-the-lane-they-describe.html">decision 0036</a>&#39;s relative names, and it is unresolved here deliberately: making the word verifiable and deciding when it may move are one question, and it is the question deferred above.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The cost of the last generation move is now legible: three contracts changed, twelve identities were spent on the rename. A future move can be judged against that record rather than described.</p><p>Nothing about the current identities changes. <code>forms.takoform.com/v1beta1</code> and <code>edge.forms.takoform.com/v1beta1</code> are embedded in Registry-published provider <code>v2.1.1</code> and are not movable regardless of how the deferred question is answered.</p><p>What a later graduation ADR inherits from this one is a measurement and two working code paths, rather than an argument and a latent crash.</p>',27)])])}const p=t(i,[["render",r]]);export{u as __pageData,p as default};
