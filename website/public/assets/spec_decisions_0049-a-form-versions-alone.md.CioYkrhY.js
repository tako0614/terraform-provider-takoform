import{_ as t,o as a,c as o,ag as n}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"0049 — A Form versions alone: the family group carries no version, and constraints leave the schema","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0049-a-form-versions-alone.md","filePath":"spec/decisions/0049-a-form-versions-alone.md"}'),i={name:"spec/decisions/0049-a-form-versions-alone.md"};function s(r,e,d,h,c,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="_0049-—-a-form-versions-alone-the-family-group-carries-no-version-and-constraints-leave-the-schema" tabindex="-1">0049 — A Form versions alone: the family group carries no version, and constraints leave the schema <a class="header-anchor" href="#_0049-—-a-form-versions-alone-the-family-group-carries-no-version-and-constraints-leave-the-schema" aria-label="Permalink to &quot;0049 — A Form versions alone: the family group carries no version, and constraints leave the schema&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-23</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The maintainer directed two changes together (2026-08-23): <em>abolish the version on the whole, and make it each Form&#39;s own version</em>; and, of the mechanism annotations, <em>this does not feel like a good specification — why not a REST API that defines the paths and everything except the Form-specific parts?</em></p><p>Both are answered by measurement rather than taste.</p><h3 id="the-family-group-s-version-has-never-carried-information" tabindex="-1">The family group&#39;s version has never carried information <a class="header-anchor" href="#the-family-group-s-version-has-never-carried-information" aria-label="Permalink to &quot;The family group&#39;s version has never carried information&quot;">​</a></h3><p>A Form&#39;s exact identity is <code>(apiVersion, kind, definitionVersion, schemaDigest)</code>. The digest already distinguishes every contract from every other; <code>definitionVersion</code> already orders them; and since <a href="./0047-the-host-api-is-the-substrate-a-form-declares-against.html">decision 0047</a> the substrate dependency is stated by <code>requiresHostApi</code>. The version inside the GROUP adds nothing to that tuple.</p><p>What it did do is force generation moves. The continuity ledger measured both: <code>v1alpha1 → v1beta1</code> re-identified 15 Forms and changed <strong>3</strong>; <code>v1beta1 → v1beta2</code> recorded 15 changed and 0 re-identified, and that 15 is itself the evidence — the whole family counted as changed because one shared reference moved, not because fifteen contracts did. In neither move did the group version tell a reader something the exact FormRef had not.</p><p>A version that adds no information and forces every member to move together is ceremony with a cost.</p><h3 id="the-behavioural-annotations-are-a-constraint-language-in-a-schema-s-clothes" tabindex="-1">The behavioural annotations are a constraint language in a schema&#39;s clothes <a class="header-anchor" href="#the-behavioural-annotations-are-a-constraint-language-in-a-schema-s-clothes" aria-label="Permalink to &quot;The behavioural annotations are a constraint language in a schema&#39;s clothes&quot;">​</a></h3><p><a href="./0048-the-protocol-states-mechanisms-not-forms.html">Decision 0048</a> took the family out of the protocol by having Definitions declare mechanisms. Counted against the Definitions that actually instantiate them, those annotations are three different things:</p><table tabindex="0"><thead><tr><th>Kind</th><th>Annotations</th><th>Instantiations</th></tr></thead><tbody><tr><td>Reference resolution</td><td><code>target-formrefs</code>, <code>required-interface</code>, <code>binding</code>, <code>standard-services</code>, <code>required-entrypoint</code></td><td>3, 7, 1, 1, 4</td></tr><tr><td>Behavioural constraint</td><td><code>exclusive</code>, <code>sum</code>, <code>claim</code>, <code>host-assigned</code></td><td>5, 1, 1, 1</td></tr><tr><td>Declared and unused</td><td><code>relation-extras</code></td><td>0</td></tr></tbody></table><p>The first kind belongs where it is: it annotates the exact node that IS the reference, and moving it elsewhere would mean carrying a JSON Pointer back to that node. The second kind is not about the shape of a document at all — it is a rule about resources, riding in a schema&#39;s extension slots where no standard validator will ever see it. The third is vocabulary nobody instantiates.</p><p>An earlier draft of this record counted <code>equals</code> and <code>omittedWhen</code> in that third row. That was a measurement taken in the wrong place: both live in the WIRE schema, where they carry envelope mirroring and conditional members, and counting Form Definitions is not where a wire annotation would ever appear. They are used, they stay, and the correction is recorded rather than quietly applied — a count is only evidence if it counted the right thing.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong><code>forms.takoform.com/v1beta4</code> is minted, and in it a Form versions alone.</strong></p><ol><li><p><strong>A Form Family group carries no version.</strong> <code>edge.forms.takoform.com</code>, not <code>edge.forms.takoform.com/vN</code>. A Form&#39;s identity is its group, its kind, its own <code>definitionVersion</code>, and its <code>schemaDigest</code> — which is what it always was, minus a member that never varied independently. There is no such thing as a generation move, so nothing can be moved in lockstep by one.</p></li><li><p><strong>Behavioural constraints leave the desired schema</strong> and become a first-class <code>constraints</code> list on the Form Definition, with a closed grammar the lane states. Adding a constraint kind is then one reviewed change in one place, rather than a new <code>x-</code> key that may appear anywhere in a schema tree. The desired schema goes back to being plain JSON Schema that any validator reads completely.</p></li><li><p><strong>Reference-resolution annotations stay in the schema</strong>, for the reason they were put there: they qualify the node they sit on.</p></li><li><p><strong>The unused annotation is removed.</strong> <code>x-takoform-relation-extras</code> is emitted by nothing, read by nothing, and has no field on the model, while the lane document described it as the mechanism behind the class-selecting gate — which actually works through a plain sibling property. Vocabulary with no instantiation is a promise nobody has kept, and one described as load-bearing is worse than one merely unused.</p></li></ol><h2 id="what-this-does-not-do" tabindex="-1">What this does not do <a class="header-anchor" href="#what-this-does-not-do" aria-label="Permalink to &quot;What this does not do&quot;">​</a></h2><p>It does not renumber or withdraw anything already minted — but which generations that covers was measured after the first draft asserted it, and the answer is narrower. Against takoform.com: the v1beta1 candidate set and Definition profile answer <strong>200</strong>; the v1beta2 ones answer <strong>404</strong>. Only v1beta1 is published, so only v1beta1 has a meaning <a href="./0037-immutability-begins-at-stable.html">decision 0037</a> protects.</p><p><strong>The v1beta2 generation therefore BECOMES the versionless group</strong> rather than being frozen beside a third tree. Nothing is served under its name, so nothing is being changed out from under a consumer; minting a third family tree to avoid touching an unpublished one would have been ceremony of exactly the kind this record exists to remove.</p><p>It does not make the protocol smaller by pushing rules onto hosts. A second implementer still reads one closed constraint grammar rather than one family&#39;s prose per family — which is the property the annotations bought and this record keeps, while moving them somewhere a specification can be read.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>A Form moves when its own contract moves, and never because a sibling&#39;s did. The continuity ledger stops measuring generations and starts measuring Forms, which is what it was always deriving anyway.</p><p>The cost is one more lane and a re-identification of every Form that adopts the versionless group — paid once, against a version line that would otherwise force the same cost on every future move.</p>',24)])])}const g=t(i,[["render",s]]);export{u as __pageData,g as default};
