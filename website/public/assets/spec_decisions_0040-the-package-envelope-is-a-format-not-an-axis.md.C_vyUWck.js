import{_ as a,o,c as t,ag as n}from"./chunks/framework.cyF4XceO.js";const p=JSON.parse('{"title":"0040 — The package envelope names a manifest format, not a Form generation","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0040-the-package-envelope-is-a-format-not-an-axis.md","filePath":"spec/decisions/0040-the-package-envelope-is-a-format-not-an-axis.md"}'),s={name:"spec/decisions/0040-the-package-envelope-is-a-format-not-an-axis.md"};function r(i,e,c,d,h,l){return o(),t("div",null,[...e[0]||(e[0]=[n('<h1 id="_0040-—-the-package-envelope-names-a-manifest-format-not-a-form-generation" tabindex="-1">0040 — The package envelope names a manifest format, not a Form generation <a class="header-anchor" href="#_0040-—-the-package-envelope-names-a-manifest-format-not-a-form-generation" aria-label="Permalink to &quot;0040 — The package envelope names a manifest format, not a Form generation&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-22</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>packages.forms.takoform.com/vN</code> is the <code>apiVersion</code> of a package index: the manifest that binds one exact FormRef to a closed byte inventory — paths, media types, sizes, digests. Four values of it exist, which made it look like a fifth version axis a reader had to hold.</p><p>Measured, it is not one. Normalising version words, the <code>v1alpha2</code>, <code>v1alpha3</code> and <code>v1alpha4</code> schemas are <strong>structurally identical</strong> — the only differences are each schema&#39;s own <code>$id</code>, its own <code>apiVersion</code> const, and the <code>$ref</code> to the FormRef schema it wraps. The one real format change in the envelope&#39;s history is <code>v1alpha1</code> to <code>v1alpha2</code>, which introduced content addressing (<a href="./0005-current-form-packages-use-content-addressed-locators.html">decision 0005</a>). The other two mints followed the grammar of the FormRef they wrap: <code>v1alpha3</code> for the retained provider-v2 epoch, <code>v1alpha4</code> for namespaced families.</p><p>And the envelope has already stopped following. <code>v1alpha4</code>&#39;s FormRef grammar admits any namespaced group, so when the Edge family moved from <code>edge.forms.takoform.com/v1alpha1</code> to <code>/v1beta1</code> the envelope stayed put and carried both. An identifier that does not move when the thing it was named after moves is not tracking it — it just looks like it is, and the lingering <code>v1alpha4</code>-wraps-<code>v1beta1</code> mismatch is one of the things that made &quot;which number is current&quot; unanswerable by inspection.</p><p>The information the envelope version appeared to carry lives three lines below it in the same document: <code>formRef.apiVersion</code>. An axis whose value is derivable from the document it labels is not an axis.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A new envelope is minted only when the manifest format itself changes structurally.</strong> Never because a Form generation moved, a family was renamed, or the FormRef grammar it wraps advanced — <code>v1alpha4</code> already proves the grammar can move underneath a standing envelope.</p><p>This is <a href="./0039-a-lane-is-minted-for-one-of-two-reasons.html">decision 0039</a>&#39;s rule applied to the envelope, with one branch fewer: an envelope has no maturity channel of its own, so the graduation reason does not exist for it. Format change is the only minting reason there is.</p><p><strong>The four published values stay exactly as they are.</strong> They are constants inside published schema bytes, declared by 24 served package indexes, and <a href="./0037-immutability-begins-at-stable.html">decision 0037</a> forbids a served identity changing meaning. <code>v1alpha3</code> and <code>v1alpha4</code> are recorded as what they were — re-mints that carried a grammar move, not format changes — the same way <code>forms.takoform.com/v1beta1</code> is recorded in 0039&#39;s table as a graduation whose evidence was never stated. History is recorded, not restated.</p><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p>The same gate shape as the Host API lanes, in <code>scripts/check-public-surfaces.mjs</code>: every envelope is recorded with the reason it was minted. A <code>format</code> envelope&#39;s schema must differ structurally from every other <code>format</code> envelope&#39;s, version words normalised, so a rename cannot present itself as a format; a <code>carried</code> envelope must say why it exists, because that is not provable from bytes. Both refusals were watched failing first — claiming <code>v1alpha3</code> was a format change fails against <code>v1alpha2</code>&#39;s identical bytes, and deleting a <code>carried</code> entry&#39;s reason fails by name.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The version axes a user of this project actually holds are three: the Host API lane (the wire a provider speaks), the Form contract (group plus <code>definitionVersion</code> plus digest), and the provider SemVer (the artifact installed from the Registry). Interface and Binding contracts version with the Forms that reference them. The envelope is a format tag on the manifest, and the next Form generation will not mint one.</p>',15)])])}const f=a(s,[["render",r]]);export{p as __pageData,f as default};
