import{_ as a,o as t,c as i,a2 as o}from"./chunks/framework.BTLSZHNB.js";const u=JSON.parse('{"title":"Publication freeze: the v1alpha3 lane","description":"","frontmatter":{},"headers":[],"relativePath":"spec/publication-freeze.md","filePath":"spec/publication-freeze.md"}'),r={name:"spec/publication-freeze.md"};function n(s,e,d,c,l,h){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="publication-freeze-the-v1alpha3-lane" tabindex="-1">Publication freeze: the v1alpha3 lane <a class="header-anchor" href="#publication-freeze-the-v1alpha3-lane" aria-label="Permalink to &quot;Publication freeze: the v1alpha3 lane&quot;">​</a></h1><p>The Host API <code>forms.takoform.com/v1alpha3</code> lane and the <code>edge.forms.takoform.com/v1alpha1</code> Edge Platform Family are <strong>publication-frozen</strong>.</p><p>While this freeze holds, the project MUST NOT:</p><ul><li>publish a Form Package for any family Form;</li><li>publish an Interface Definition or Binding Definition, in any envelope. No Interface Package or Binding Package identity exists to publish one in; those contracts are documents distributed with this repository (<a href="./decisions/0021-third-party-forms-and-contract-distribution.html">decision 0021</a>);</li><li>release provider <code>v2.1.0</code>, or any provider version that exposes the family resource types, to the Terraform Registry;</li><li>transition any family Form to Experimental, Stable, or any other lifecycle state in <a href="../forms/lifecycle.json"><code>../forms/lifecycle.json</code></a>;</li><li>describe a family Form, Interface, Binding, or the provider v2.1 line as published, Experimental, Stable, standard, admitted, or approved.</li></ul><p>The freeze does not restrict repository work. Definitions, schemas, exact FormRef digests, provider surfaces, and conformance may still change incompatibly, because nothing in the lane has a published identity to preserve.</p><h2 id="why-the-lane-is-frozen" tabindex="-1">Why the lane is frozen <a class="header-anchor" href="#why-the-lane-is-frozen" aria-label="Permalink to &quot;Why the lane is frozen&quot;">​</a></h2><p>Every family Form is <code>publicationStatus: unpublished</code> (<a href="../forms/candidates/edge/v1alpha1/candidate-set.json"><code>../forms/candidates/edge/v1alpha1/candidate-set.json</code></a>), so today a defect can be corrected by regenerating a digest. After publication the same defect becomes a compatibility obligation. The publication blockers are exactly the invariants a consumer would otherwise have to live with forever.</p><h2 id="what-is-already-published-and-therefore-immutable" tabindex="-1">What is already published, and therefore immutable <a class="header-anchor" href="#what-is-already-published-and-therefore-immutable" aria-label="Permalink to &quot;What is already published, and therefore immutable&quot;">​</a></h2><p>Publication is per artifact, not per lane. The lane&#39;s <strong>schema documents</strong> are published and immutable even though no Form is:</p><table tabindex="0"><thead><tr><th>Published</th><th>Consequence</th></tr></thead><tbody><tr><td>the schema <code>$id</code> URLs recorded in <a href="../release/public-schema-identities.json"><code>../release/public-schema-identities.json</code></a></td><td>their bytes can never change; a contract change mints a <strong>new</strong> identity</td></tr><tr><td>provider <code>v1.0.3</code> and <code>v2.0.0</code></td><td>their Registry releases, tags, and state contracts are fixed</td></tr><tr><td>the 34 Legacy Form Packages and the retained admission evidence</td><td>retained byte-for-byte</td></tr></tbody></table><p>A change to the frozen lane therefore has two different costs. Changing a Form Definition, a generated candidate, or a provider surface is free. Changing a published schema document is impossible: the append-only ledger and the deploy no-overwrite guard both reject it, so the change must mint a new schema identity instead. Prefer expressing a contract addition through data the published schema already admits — for example a JSON Schema <code>default</code> inside a Form&#39;s desired schema — over minting a new identity.</p><h2 id="publication-blockers" tabindex="-1">Publication blockers <a class="header-anchor" href="#publication-blockers" aria-label="Permalink to &quot;Publication blockers&quot;">​</a></h2><p>A blocker is an invariant that is cheap to fix now and expensive to fix after publication. The machine authority is <a href="publication-blockers.json"><code>publication-blockers.json</code></a>; each entry is also tracked as an issue labelled <code>v1alpha3-publication-blocker</code>. The freeze lifts for a given Form only when every blocker that touches it is closed, its evidence exists under <a href="./project-lifecycle.html"><code>project-lifecycle.md</code></a>, and a real host has implemented it end to end.</p><p>Prose cannot stop a release, so the ledger is enforced rather than described. <code>bun run check</code> validates its shape on every change and, while any blocker is open, additionally requires the state the repository would not be in had the lane published: the family candidate set must still declare itself <code>unpublished</code>, and no family Form may hold a lifecycle record.</p><p>A blocker closes by recording evidence that exists. An entry marked closed with an empty evidence list fails, and so does one naming a path that is not in the repository, so the freeze cannot be lifted by editing a status field or by inventing a filename. <code>bun run assert:publishable</code> is the assertion a future lane-publishing path calls; today it refuses and names every open blocker.</p><p>The freeze is <strong>scoped to this lane&#39;s own artifacts</strong>. It is deliberately not wired into <code>check:release-owner-gate</code>, because that gate also serves the retained v1alpha1/v1alpha2 packages and the append-only security revocation path in <a href="./trust/"><code>trust/</code></a>. An urgent revocation for an already-published package must never wait on this lane&#39;s product readiness; freezing new publication and blocking a security withdrawal are opposite obligations.</p><p>The first Forms proposed for Experimental publication are the Worker and edge KV vertical slice: <code>ModuleWorker</code>, <code>WorkerBundle</code>, <code>WorkerVersion</code>, <code>WorkerDeployment</code>, <code>WorkerCustomDomain</code>, and <code>EdgeKVNamespace</code>. The queue and schedule Forms follow. <code>ObjectBucket</code> and <code>SQLiteDatabase</code> come last, because their Interface contracts still need the most work and publishing them early would create the largest compatibility surface.</p><p>Lifting the freeze is a separate reviewed decision. Passing local gates is not publication authority, and this document is not evidence that any blocker has been closed.</p>',18)])])}const b=a(r,[["render",n]]);export{u as __pageData,b as default};
