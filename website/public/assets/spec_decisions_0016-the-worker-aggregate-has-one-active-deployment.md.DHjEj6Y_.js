import{_ as t,o,c as n,a2 as r}from"./chunks/framework.Bbwa0s72.js";const u=JSON.parse('{"title":"0016 — The Worker aggregate has one active deployment","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0016-the-worker-aggregate-has-one-active-deployment.md","filePath":"spec/decisions/0016-the-worker-aggregate-has-one-active-deployment.md"}'),a={name:"spec/decisions/0016-the-worker-aggregate-has-one-active-deployment.md"};function i(s,e,d,c,h,l){return o(),n("div",null,[...e[0]||(e[0]=[r('<h1 id="_0016-—-the-worker-aggregate-has-one-active-deployment" tabindex="-1">0016 — The Worker aggregate has one active deployment <a class="header-anchor" href="#_0016-—-the-worker-aggregate-has-one-active-deployment" aria-label="Permalink to &quot;0016 — The Worker aggregate has one active deployment&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Edge Platform Family splits one running worker across five Forms: a <code>ModuleWorker</code> identity, immutable <code>WorkerVersion</code> revisions, a <code>WorkerDeployment</code> that selects which of those versions serve traffic, and the <code>WorkerCustomDomain</code>, <code>WorkerCronTrigger</code>, and <code>QueueConsumer</code> attachments that activate it inward. The split is deliberate — it is what makes rollback a re-weighting instead of a mutation — but the v1alpha3 lane never stated what holds the five together, so a conforming host accepted configurations that cannot serve traffic coherently.</p><p>Nothing stopped two differently-named <code>WorkerDeployment</code> resources from targeting one <code>ModuleWorker</code>, leaving &quot;which one serves&quot; undefined. A deployment could weight a version belonging to a different worker, name the same version twice under two shares, or weight a version that was not ready. The attachment gate read the wrong evidence: it admitted an attachment when <em>any</em> stored <code>WorkerVersion</code> of the worker declared the handler, including a version no deployment serves, so a cron trigger could exist while nothing scheduled was deployed. There was no reverse check at all: with that trigger live, the deployment could move to 100% of a version exporting no <code>scheduled</code> handler. Deleting the deployment was unguarded. And <code>vars</code> keys, <code>requiredSensitiveVars</code> entries, and the five binding lists all project into one runtime environment namespace, where <code>uniqueItems</code> rejects only a duplicated whole object — two bindings may share a name with different targets, and a binding may collide with a var.</p><p>Decision <a href="./0015-cross-resource-references-are-uid-pinned-relations.html">0015</a> made every reference a resolved, UID-pinned relation and gave a host a reverse index from target UID to holders. That is the machinery these rules are built on; it does not decide any of them. Decision <a href="./0014-published-schemas-are-structural-minima.html">0014</a> settled where they may live: none of them is expressible in a Draft 2020-12 desired schema — a schema cannot count the deployments pointing at one worker, read the <code>/worker</code> relation of a version it does not contain, add weights, know which handlers a referenced version exports, or reach across sibling properties — so they belong in the host, the authoring model, and the conformance corpus. They are new MUST-level host semantics, so this repository&#39;s <code>AGENTS.md</code> requires a decision record.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The <strong>Worker aggregate</strong> is one <code>ModuleWorker</code> incarnation together with the Worker Versions pinned to it, the one Worker Deployment that governs its traffic, and everything activated against it. The rules below are normative; the operative text lives in <a href="./../host-api/v1alpha3.html"><code>../host-api/v1alpha3.md</code></a> and <a href="./../form-families.html"><code>../form-families.md</code></a>.</p><ol><li><p><strong>One active deployment per worker.</strong> Creating or importing a <code>WorkerDeployment</code> whose resolved <code>/worker</code> UID already has one MUST fail <code>invalid_argument</code> (400) before any mutation. Re-applying the worker&#39;s own deployment is not a second one. Traffic moves by re-weighting the deployment a worker already has.</p></li><li><p><strong>Deployment integrity.</strong> Before any mutation a host MUST refuse a deployment whose <code>versions[]</code></p><ul><li>weights a <code>WorkerVersion</code> whose stored <code>/worker</code> relation targets a different worker UID;</li><li>names one <code>WorkerVersion</code> twice, by resolved UID;</li><li>carries weights that do not sum to exactly 10000 basis points;</li><li>weights a version that is not Ready, or that an accepted delete is already removing.</li></ul><p>Each failure is <code>invalid_argument</code> (400): the request is well formed but states something untrue about what will run.</p></li><li><p><strong>Attachments gate on the active deployment.</strong> A <code>WorkerCustomDomain</code> requires <code>fetch</code>, a <code>WorkerCronTrigger</code> requires <code>scheduled</code>, and a <code>QueueConsumer</code> requires <code>queue</code>. The evidence is the worker&#39;s active deployment, and EVERY version that deployment weights MUST export the handler — a request served by any weighted version has to find it. An absent deployment or a missing handler MUST fail <code>unsupported_capability</code> (422) before any mutation, naming what is missing.</p></li><li><p><strong>Reverse validation on deployment change.</strong> An apply that would leave a live dependent unserved MUST be refused before any mutation, with <code>unsupported_capability</code> (422). The dependents are a live <code>WorkerCustomDomain</code> (<code>fetch</code>), a live <code>WorkerCronTrigger</code> (<code>scheduled</code>), a live <code>QueueConsumer</code> (<code>queue</code>), and a live INBOUND service binding — another Form&#39;s <code>serviceBindings</code> entry targeting this worker — which requires <code>fetch</code>.</p></li><li><p><strong>Deletion is blocked by dependents.</strong> Deleting a <code>WorkerDeployment</code> while any of those four dependents lives MUST fail <code>dependency_in_use</code> (409). Nothing references a deployment, so this is not the relation rule of decision 0015; it is the same statement about a different edge.</p></li><li><p><strong>The environment namespace is single.</strong> Within one <code>WorkerVersion</code>, the union of <code>vars</code> keys, <code>requiredSensitiveVars</code> entries, and every binding <code>name</code> across every binding list MUST be unique. A collision MUST fail <code>invalid_argument</code> (400) before any mutation, and a client SHOULD refuse it at plan time so the author sees it without a round trip.</p></li><li><p><strong>Worker readiness follows the deployment.</strong> A <code>ModuleWorker</code> reports <code>Ready=True</code> only when it has an active deployment whose every weighted version exports <code>fetch</code>. Before that it reports <code>Ready=False</code> with <code>Provisioning</code> (no deployment) or <code>UnsupportedCapability</code> (a deployment that serves no <code>fetch</code>), and a <code>hostReason</code> naming the worker and what is missing. The worker exists; it serves nothing.</p></li><li><p><strong>An inbound service binding is refused at bind time.</strong> Because <code>worker.service</code> is provided by the worker IDENTITY and answered by whatever its deployment selects, a <code>module-worker.service</code> binding to a worker that is not serving <code>fetch</code> MUST fail <code>unsupported_capability</code> (422) before any mutation. It is refused rather than stored-and-reported: a stored binding that projects nothing is a declared capability no host can keep, and the first Forms should be simple to reason about.</p></li><li><p><strong>A rendering computed from other resources moves the revision.</strong> Rule 7 makes one resource&#39;s representation depend on another&#39;s, so a host that renders any part of a representation from OTHER resources MUST advance that resource&#39;s <code>metadata.revision</code> when the rendering changes, and MUST NOT move its <code>metadata.generation</code> — no desired spec changed. This is the general form of the rule decision 0011 states for a resource&#39;s own status, and it governs the relation drift of decision 0015 (rule 7 there) as much as the worker readiness of rule 7 here: a deleted or replaced target changes what every source pinned to it renders. After an accepted mutation a host MUST recompute the rendering of every resource that mutation can affect and advance only those that actually changed, in one bounded pass — an idempotent re-apply must move nothing anywhere, and the recomputation must not cascade, which it cannot, because a rendering is a function of stored specs, relations, and UIDs and never of a revision.</p><p>This rule is what forced the delete fence off the revision. A teardown removes dependents first, so removing a deployment re-renders the worker that is about to be deleted next, after the plan read it — and a delete fenced on the representation would then be refused by a change the teardown itself made, with no repair, because the next dependent moves it again. So the rule stands and the fence moved: a delete fences on the desired generation (<a href="./0011-resource-identity-generation-and-revision.html">decision 0011</a>, amended 2026-08-09), which this rule deliberately never touches.</p></li></ol><p>Rules 3, 4, and 8 are three views of one fact — an activated worker is what its deployment selects — so a host that implements any of them in terms of stored versions rather than the deployment has implemented none of them.</p><p>Every rule here is about the worker an attachment activates. What an attachment CLAIMS — the DNS hostname a <code>WorkerCustomDomain</code> serves, the queue a <code>QueueConsumer</code> dead-letters to — is a separate question with the same shape, decided against the store on canonical, resolved identity: <a href="./0026-attachment-claims-are-canonical-and-acyclic.html">decision 0026</a>.</p><p>Rule 6 is enforced in three places for three different reasons: the authoring model proves a Form&#39;s own canonical examples are collision-free, the provider refuses the configuration during plan against the attribute the author wrote, and the host refuses the spec before mutation because it is the only party that sees every client.</p><p>The rules are proven by the required conformance checks <code>deployment-single-active-per-worker</code>, <code>deployment-version-ownership</code>, <code>deployment-version-duplicate-rejected</code>, <code>attachment-requires-active-deployment</code>, <code>deployment-change-preserves-dependents</code>, <code>deployment-delete-blocked-by-dependent</code>, <code>deployment-delete-blocked-by-inbound-binding</code>, <code>dependent-revision-advances-with-rendering</code>, and <code>binding-name-collision-rejected</code>, alongside the existing <code>deployment-weight-sum-enforced</code> and <code>handler-gated-attachments</code>.</p><p>The fourth dependent of rule 5 is proved on its own rather than only alongside the three attachment Forms. A host that guards a custom domain, a cron trigger, and a queue consumer and forgets the inbound service binding passes every attachment case, so proving the three together would leave the one edge that has no Form of its own — a <code>serviceBindings</code> entry inside another worker&#39;s version — unfalsified.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A worker&#39;s Ready condition becomes a claim about SERVICE rather than about the existence of a record. That is the honest reading of an identity Form whose whole purpose is to be addressed, and it makes &quot;the worker is up&quot; answerable from one resource instead of by inspecting the aggregate.</li><li>Because that condition is rendered from another resource, a deployment mutation now moves the worker&#39;s revision and therefore its ETag (rule 9). A host keeps enough of the representation it last served to answer &quot;did this change&quot;, which is the same bookkeeping the relation re-pin of decision 0015 already required, generalized.</li><li>Two workers cannot service-bind to each other from scratch. Each needs the other serving <code>fetch</code> before its own version can be applied, so a mutually calling pair is built one direction at a time, or the second direction is added in a later version. Fail-closed costs this; storing a broken binding would cost correctness.</li><li>Removing a worker is now explicitly ordered: attachments and inbound callers first, then the deployment, then the versions, then the worker. Every step is a refusal with a named dependent rather than a silent degradation, so the order is discoverable from the errors. That the whole ordering completes through a real <code>terraform destroy</code> and <code>tofu destroy</code> of the official <code>worker-app</code> module, leaving nothing in the store, is measured by the authoring matrix (<code>cmd/worker-authoring-conformance</code>) rather than by this lane, because it is a claim about a client walking the order and not about a host answering one request.</li><li>A deployment is a claim about what runs, so the versions it weights are held to being runnable at the moment it is stored. A host with an asynchronous provisioning model refuses a version whose Ready is False for its own reasons too, not only for relation drift.</li><li>The environment namespace rule is stated for <code>WorkerVersion</code> because it is the one Form today that holds bindings alongside <code>vars</code> and <code>requiredSensitiveVars</code>. The binding lists are discovered from the <code>x-takoform-binding</code> annotation the desired schema already carries, so a Form that gains a sixth binding list is covered without a host edit.</li><li>Whether two declarations collide is a property of one instance, not of a Form. The authoring model therefore cannot forbid it — a Form declaring five binding lists is not wrong, its author may simply write one name in two of them — and what the model proves is the collision it owns: the Form&#39;s own canonical examples.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Allow several deployments per worker and pick one by a rule.</strong> Rejected because every candidate rule — newest, lowest name, highest total weight, most recently applied — is a fact about host bookkeeping rather than about what the author asked for, and none is predictable from the configuration. The author would have to know the rule to know what serves, and two hosts choosing differently would both be conforming.</li><li><strong>Gate attachments on any stored <code>WorkerVersion</code> that declares the handler.</strong> This is what the lane did before this decision, and it is wrong because a stored version is not a running one. Versions are immutable revisions kept precisely so that old ones remain addressable for rollback, so the set of stored versions is a history, not a description of the current service. The rule let a cron trigger be created against a <code>scheduled</code> handler that existed only in a version no deployment selected — a trigger that would fire against nothing — and it reported success while doing it. Worse, it made the gate unfalsifiable in the direction that matters: once admitted, no later deployment change was checked against it.</li><li><strong>Degrade dependents to not-Ready instead of refusing the change.</strong> Rejected because it converts one explicit action into a fan-out of broken resources. The author asked to move traffic; they would get a successful apply plus three attachments and an unknown number of callers silently reporting failure, with no statement of which change caused it and no order in which to repair it. Refusing names the dependent, keeps the system serving, and leaves the author&#39;s next action obvious: remove what depends on the handler, or keep exporting it. The degrade behavior is also strictly harder to reverse — the broken state is already stored — and reversibility is what the deployment Form exists to provide.</li><li><strong>Require every weighted version to export every handler any attachment could use.</strong> Rejected as a blunter version of rule 3 that forbids a worker with only a <code>fetch</code> handler from ever being deployed. The gate is per attachment because the requirement is per attachment.</li><li><strong>Express the environment namespace in the desired schema.</strong> Rejected because it is unexpressible. <code>uniqueItems</code> compares whole objects, so two bindings agreeing only on <code>name</code> are distinct; no keyword relates a property&#39;s keys to a sibling array&#39;s element member; and the published Form Definition schema is immutable anyway (decision 0014).</li><li><strong>Report a not-serving worker as Ready.</strong> Rejected because it makes the condition meaningless for the one Form whose purpose is to be addressed by others. A caller reading <code>Ready=True</code> on a worker that answers nothing has been told the opposite of the truth.</li></ul>',18)])])}const p=t(a,[["render",i]]);export{u as __pageData,p as default};
