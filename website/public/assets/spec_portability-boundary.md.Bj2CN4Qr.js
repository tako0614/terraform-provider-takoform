import{_ as t,o as a,c as r,ag as o}from"./chunks/framework.DCKC-kU_.js";const u=JSON.parse('{"title":"Portable Form boundary","description":"","frontmatter":{},"headers":[],"relativePath":"spec/portability-boundary.md","filePath":"spec/portability-boundary.md"}'),n={name:"spec/portability-boundary.md"};function i(d,e,s,l,c,p){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="portable-form-boundary" tabindex="-1">Portable Form boundary <a class="header-anchor" href="#portable-form-boundary" aria-label="Permalink to &quot;Portable Form boundary&quot;">​</a></h1><p>This document defines what may enter a current Takoform Form. A Form owns the smallest desired-state contract and observable outcomes that independent hosts can implement with the same meaning. It does not model a cloud product, deployment platform, operator policy, or commercial service catalogue.</p><p>Cloudflare-like concepts such as an edge application, key/value namespace, queue, scheduled invocation, or namespace of stateful entities are valid when their semantics stand on their own. Cloudflare product names, compatibility APIs, account topology, limits, placement controls, billing tiers, and runtime configuration are not portable Form state.</p><h2 id="ownership-layers" tabindex="-1">Ownership layers <a class="header-anchor" href="#ownership-layers" aria-label="Permalink to &quot;Ownership layers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Layer</th><th>Owns</th><th>Examples</th></tr></thead><tbody><tr><td>Form</td><td>workload semantics and exact portable lifecycle</td><td>immutable artifact or image, entrypoint, data semantics, declared Interface</td></tr><tr><td>Host support profile</td><td>whether and how a host supports an exact FormRef</td><td>supported runtime tokens, engine versions, metrics, timezone behavior</td></tr><tr><td>Adapter or compatibility profile</td><td>translation to an external ecosystem</td><td>S3-compatible access, Workers compatibility, Kubernetes projection</td></tr><tr><td>Operator policy</td><td>realized placement and operating policy</td><td>region, route, replicas, CPU, memory, health checks, retry and scaling policy</td></tr><tr><td>Service Offering</td><td>capacity, availability, price, and support</td><td>quotas, size classes, HA tiers, SLA, billing</td></tr></tbody></table><p>A field belongs in a Form only when all of the following are true:</p><ol><li>a consumer needs the value to distinguish workload meaning, not merely a preferred implementation;</li><li>two independent hosts can give the value the same observable semantics;</li><li>drift, update, replacement, import, and delete behavior can be tested from the portable lifecycle;</li><li>the field does not select a vendor product, account, target, credential, price, placement, or operator-private policy;</li><li>absence of the field cannot be silently interpreted as one particular provider&#39;s default.</li></ol><p>Open tokens such as <code>runtime</code>, <code>engine</code>, <code>metric</code>, or <code>persistence</code> are capability requirements, not a central allowlist. A host must advertise support for the exact token or fail closed. A token does not authorize a host to expose backend configuration through the Form.</p><p>Interfaces describe a portable operation surface. Endpoints, credentials, authorization, provider bindings, and protocol compatibility remain host-owned. For example, <code>object.storage@1</code> can be implemented through S3, R2, GCS, a filesystem, or another backend; requesting an S3-compatible endpoint is an adapter/profile concern rather than an <code>ObjectBucket</code> desired field.</p><h2 id="current-candidate-authoring-rule" tabindex="-1">Current-candidate authoring rule <a class="header-anchor" href="#current-candidate-authoring-rule" aria-label="Permalink to &quot;Current-candidate authoring rule&quot;">​</a></h2><p>The v1alpha1 Legacy catalog is prior art only. A current candidate MUST be authored from its Proposal and this boundary. A generator MUST NOT copy a Legacy Definition or fixture tree and change only API version, status, or SemVer. Reusing the shared schema vocabulary and retaining a semantic field after explicit review are allowed; inheriting the old field set is not.</p><p>Current candidate generation starts from [<code>internal/currentformcatalog</code>] and produces the exact packages under <a href="/forms/candidates/v1alpha2/candidate-set.json"><code>forms/candidates/v1alpha2</code></a>. The catalog test pins every reviewed field set and rejects known substrate and operator fields. Reproducibility checks prove the tracked package bytes still come from that source.</p>',12)])])}const m=t(n,[["render",i]]);export{u as __pageData,m as default};
