import{_ as a,o as t,c as o,ag as r}from"./chunks/framework.cyF4XceO.js";const p=JSON.parse('{"title":"ActorNamespace — takoform_actor_namespace","description":"","frontmatter":{},"headers":[],"relativePath":"proposals/edge/actor-namespace.md","filePath":"proposals/edge/actor-namespace.md"}'),s={name:"proposals/edge/actor-namespace.md"};function i(n,e,c,d,l,h){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="actornamespace-—-takoform-actor-namespace" tabindex="-1">ActorNamespace — <code>takoform_actor_namespace</code> <a class="header-anchor" href="#actornamespace-—-takoform-actor-namespace" aria-label="Permalink to &quot;ActorNamespace — `takoform_actor_namespace`&quot;">​</a></h1><h2 id="workload-and-consumer" tabindex="-1">Workload and consumer <a class="header-anchor" href="#workload-and-consumer" aria-label="Permalink to &quot;Workload and consumer&quot;">​</a></h2><p>An application team keeps per-entity coordination — a chat room, a game session, a booking ledger, a rate-limit bucket — behind one addressable actor per entity: a class exported by a worker&#39;s module, one live execution context per actor id, private durable storage, and one alarm. Workers reach actors through <code>module-worker.actor</code> bindings — mint an id from a name, get a stub, invoke; actors are runtime data, never Resources. The Form enters the family at the v1beta2 generation as a <code>0.1.0</code> Experimental member (<a href="./../../spec/decisions/0043-forms-target-popular-vendor-locked-primitives.html">decision 0043</a>, <a href="./../../spec/decisions/0046-stable-arrives-through-a-stable-grade-beta-2.html">decision 0046</a>).</p><h2 id="role" tabindex="-1">Role <a class="header-anchor" href="#role" aria-label="Permalink to &quot;Role&quot;">​</a></h2><p><code>identity</code>, a member of the worker aggregate. Desired state is a <code>worker</code> reference to one <a href="./module-worker.html">ModuleWorker</a> plus <code>className</code>, a class export name in the JavaScript identifier grammar; the reference pins the exact <code>ModuleWorker</code> contract (<a href="./../../spec/decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>). The Form carries no implementation snapshot: which code answers is whatever the worker&#39;s ACTIVE DEPLOYMENT selects (<a href="./../../spec/decisions/0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a>), so behavior upgrades and rollback ride <a href="./worker-deployment.html">WorkerDeployment</a>. One worker UID carries at most one ActorNamespace per class name — two namespaces over one class would give one class two disjoint id spaces — and a second fails <code>invalid_argument</code> (400) before any mutation, a count no schema can state, exactly like one-deployment-per-worker.</p><h2 id="observable-semantics" tabindex="-1">Observable semantics <a class="header-anchor" href="#observable-semantics" aria-label="Permalink to &quot;Observable semantics&quot;">​</a></h2><p>Exactly the <code>worker.actor@1.0.0</code> contract, declared the way <code>ModuleWorker</code> declares <code>worker.runtime@1.1.0</code> (<a href="./../../spec/decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>): the identity is what a host implements; the class the serving version exports is the code that fills it. One contract carries the class ABI, addressing, storage, and the alarm, because each is scoped by the same actor id.</p><p>Addressing: an actor is addressed by a NAME the author&#39;s code chooses or an id the host mints. <code>env.NAME.idFromName(name)</code> derives the same id for the same name on every call, with no host round trip; <code>env.NAME.newUniqueId()</code> mints an id no name ever derives; <code>env.NAME.get(id) -&gt; ActorStub</code> builds a stub without host work. An id is an opaque, stable string a configuration must not parse. Every id addresses an actor: there is no create call and no existence check — the first delivery to an id is the actor&#39;s creation, and an actor with no storage, no alarm, and no live context costs nothing.</p><p>Invocation: <code>stub.fetch(request) -&gt; Promise&lt;Response&gt;</code> with exactly the invocation semantics the family fixed for <code>worker.service</code>: bodies stream in both directions, the promise resolves with the actor&#39;s host-generated 500 when the class&#39;s <code>fetch</code> throws, and rejects only when the call could not be made. What is new is WHERE the call lands. The host guarantees AT MOST ONE live execution context per actor id — across locations, eviction, and process death — and delivers to it one invocation at a time, each running to completion before the next begins, so concurrent callers observe serialization, never interleaving. The class ABI is <code>fetch(request)</code> plus an optional <code>alarm()</code> handler, constructed by the host with the actor&#39;s id, storage, and alarm surfaces.</p><p>Storage: each actor holds PRIVATE durable storage with the family&#39;s SQLite semantics — the SQL dialect, <code>EdgeSqlValue</code> domain, and serializable atomicity <a href="./sqlite-database.html">SQLiteDatabase</a> fixes through <code>edge.sql@1.0.0</code> (<a href="./../../spec/decisions/0034-edge-sql-uses-safe-wire-values-and-rollback-only-queries.html">decision 0034</a>) — scoped to one actor id and reachable only from that actor&#39;s own execution context; the single context is what makes every store single-writer. Unlike <code>SQLiteDatabase</code>, the store admits schema statements from the actor&#39;s own code: the migration ledger exists because runtime SQL from many clients cannot own one shared schema history, an actor&#39;s store has exactly one writer, and unbounded actor cardinality makes a per-actor migration Resource the same category error as a per-message one. The contract&#39;s <code>limits</code> fix the portable per-actor storage ceiling.</p><p>Alarm: each actor has AT MOST ONE pending alarm. <code>alarm.set(at)</code> schedules a wake-up and REPLACES any pending one; <code>alarm.get()</code> reads it; <code>alarm.clear()</code> removes it. At or after its time the host invokes the class&#39;s <code>alarm()</code> handler in the actor&#39;s own execution context, serialized with messages like any other delivery. Firing is at-least-once — a handler that throws is re-invoked — and a completed handler run consumes the alarm unless the handler set a new one, so the handler carries the consumer-idempotency obligation the family&#39;s at-least-once vocabulary already states.</p><p>Between deliveries the host may evict the execution context; storage and the pending alarm survive, and the next invocation or the alarm revives the actor. The identity outlives every execution context. Actors have unbounded cardinality and runtime addressing — like a queue&#39;s messages, nothing about them enters desired state, the resource envelope, or a plan: no uid, no generation, nothing for a provider to manage.</p><p>Readiness follows the aggregate: <code>Ready=True</code> only while the active deployment exists and EVERY weighted version exports <code>className</code> satisfying the ABI; otherwise <code>Ready=False</code> with <code>Provisioning</code> or <code>UnsupportedCapability</code> and a <code>hostReason</code> naming the class — a stable readiness failure, never a silent fallback, rendered from other resources and so moving <code>metadata.revision</code>, never <code>metadata.generation</code> (decision 0016). Creating the Form while a live deployment visibly lacks the class fails <code>unsupported_capability</code> (422) before any mutation; before any deployment it stores <code>Provisioning</code>, and invoking a stub of an unserved namespace rejects — the call could not be made. Host capability is decided at plan time (<a href="./../../spec/decisions/0031-host-capability-is-decided-at-plan-time.html">decision 0031</a>), and a client SHOULD refuse a bundle that demonstrably lacks the export.</p><h2 id="why-this-is-one-form" tabindex="-1">Why this is one Form <a class="header-anchor" href="#why-this-is-one-form" aria-label="Permalink to &quot;Why this is one Form&quot;">​</a></h2><p>The at-most-one-execution-context guarantee is the contract: it is what lets per-actor storage be read and written without cross-actor transactions, and what makes an actor a correct serialization point for the entity it names. Addressing, serialization, storage, and the alarm travel as one Form because each is stated in the others&#39; terms — the storage is safe because delivery is serialized, the alarm fires into the same serialized context, and the id is the unit all three are scoped by. A consistency or concurrency selector would change what application code may assume (<a href="./../../spec/decisions/0008-forms-preserve-service-shape.html">decision 0008</a>).</p><h2 id="what-would-require-a-separate-form" tabindex="-1">What would require a separate Form <a class="header-anchor" href="#what-would-require-a-separate-form" aria-label="Permalink to &quot;What would require a separate Form&quot;">​</a></h2><p>A shared multi-writer database is <code>SQLiteDatabase</code>; a globally addressed document/KV table with declared keys is the <code>table</code> family of decision 0043; a placement or geo-pinning rule is a separate <code>policy</code>-role Form; a WebSocket session surface terminated in the actor is the <code>realtime</code> family candidate. An RPC stub projecting the class&#39;s own methods needs a value-serialization contract this generation does not fix, so it is a new exact contract version, never a quiet widening of this one.</p><h2 id="provided-interfaces" tabindex="-1">Provided Interfaces <a class="header-anchor" href="#provided-interfaces" aria-label="Permalink to &quot;Provided Interfaces&quot;">​</a></h2><p><code>worker.actor@1.0.0</code> — the class ABI a conforming host provides to actor code and the addressing/invocation surface the binding projects. It is held by the IDENTITY because the identity is what a host implements; the class a Worker Version exports is the code that fills it.</p><h2 id="accepted-bindings" tabindex="-1">Accepted Bindings <a class="header-anchor" href="#accepted-bindings" aria-label="Permalink to &quot;Accepted Bindings&quot;">​</a></h2><p>None; it is a binding target (<code>module-worker.actor</code>). The class&#39;s own code runs inside its worker&#39;s environment, so every capability it uses is a binding the serving <code>WorkerVersion</code> declares (<a href="./../../spec/decisions/0010-exact-interface-and-binding-contracts.html">decision 0010</a>); the v1beta2 <code>WorkerVersion</code> carries <code>actorBindings</code> as one more annotated list, whose names join the single environment namespace with no host edit. Like <code>module-worker.workflow</code> and unlike <code>module-worker.service</code>, the binding does not require its target <code>Ready</code> at bind time: a namespace&#39;s readiness follows its own worker&#39;s deployment, so a Ready gate would make the ordinary self-bound wiring unconstructible in one apply; the binding requires existence and the exact Interface, and the deployment that lands next is what makes the namespace serve.</p><h2 id="lifecycle-risks" tabindex="-1">Lifecycle risks <a class="header-anchor" href="#lifecycle-risks" aria-label="Permalink to &quot;Lifecycle risks&quot;">​</a></h2><p>Deleting an ActorNamespace destroys EVERY actor&#39;s storage and pending alarm — irreversible, like <code>SQLiteDatabase</code> delete; portable state never carries backups. The namespace does not wait for quiescence: an actor is a passive addressee, so deleting one is deleting data at rest — where deleting a <a href="./durable-workflow.html">DurableWorkflow</a> mid-instance would abandon an execution the host promised to finish, which is why the two Forms answer delete differently. Deletion while any Worker Version&#39;s <code>actorBindings</code> entry pins the namespace fails <code>dependency_in_use</code> (409), and deleting the referenced worker while the namespace lives fails the same way. The Form joins the deployment fence of decision 0016: an apply that would leave a live namespace&#39;s class unexported, and a deployment delete while one lives, are refused — pending alarms need code to fire into, and at-least-once firing is a promise only a served class can keep.</p><h2 id="prior-art" tabindex="-1">Prior art <a class="header-anchor" href="#prior-art" aria-label="Permalink to &quot;Prior art&quot;">​</a></h2><p>The addressable-actor primitive of a proven edge platform, retained without its vendor, account, or commercial surface. The withdrawn v1alpha2 <code>StatefulEntity</code> candidate is prior art this Form supersedes in part — decision 0043 splits its remainder into the <code>table</code> family and retires the standalone actor family candidate into this addition.</p>',25)])])}const u=a(s,[["render",i]]);export{p as __pageData,u as default};
