import{_ as t,o as a,c as r,a2 as i}from"./chunks/framework.LJP3gfxI.js";const h=JSON.parse('{"title":"0008 — Forms preserve proven service shape","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0008-forms-preserve-service-shape.md","filePath":"spec/decisions/0008-forms-preserve-service-shape.md"}'),o={name:"spec/decisions/0008-forms-preserve-service-shape.md"};function s(n,e,c,d,l,p){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="_0008-—-forms-preserve-proven-service-shape" tabindex="-1">0008 — Forms preserve proven service shape <a class="header-anchor" href="#_0008-—-forms-preserve-proven-service-shape" aria-label="Permalink to &quot;0008 — Forms preserve proven service shape&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-06</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The nine v1alpha2 candidates abstract by resource category: one <code>EdgeWorker</code>, one <code>RelationalDatabase</code>, one <code>KeyValueStore</code>, one <code>Queue</code>. Category-level generalization forced high-freedom value tokens — <code>runtime</code>, <code>engine</code>, <code>consistency</code>, <code>ordering</code>, <code>persistence</code>, <code>permissions</code>, <code>projection</code> — to absorb every difference between real services. Two conforming hosts reading the same desired document could legitimately produce observably different systems: a Postgres and a SQLite database, an eventually consistent and a linearizable store, a FIFO and an at-least-once queue. That is not abstraction; it is hollowing out the contract.</p><p>What Takoform actually needs to make exchangeable is not the meaning of a resource but the business entity implementing it. The value of a proven service primitive — its execution model, client API, consistency, delivery guarantees, binding surface, update units, and failure behavior — is exactly what an application depends on and exactly what a portable contract must preserve.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Takoform does not define least-common-denominator resources reduced from multiple clouds. A current Form fixes, completely, the application-visible semantics of one proven service primitive:</p><ul><li>execution ABI and client/runtime API;</li><li>data model, consistency, and persistence;</li><li>delivery guarantees, retry, and duplicate behavior;</li><li>transaction boundaries and addressing;</li><li>update, replace, delete, and migration units;</li><li>error semantics and the capabilities exposed through Bindings.</li></ul><p>Only provider- or host-specific facts stay outside the contract: accounts, regions, SKUs, prices, credentials, native IDs, and internal implementation.</p><p>Implementations with different semantics are separate Forms; they are never selected by a free token inside one Form. A host supports a Form by implementing that shape with the same meaning; no host is obligated to implement every Form.</p><p>A Form is the smallest <strong>semantically complete</strong> desired-state contract that independent hosts can implement with the same application-visible behavior. A field or semantic rule must not be removed merely because one host can choose it privately. If two conforming hosts could make observably different choices after a field or rule is omitted, the Form is incomplete.</p><p>Where a difference appears determines where it lives:</p><table tabindex="0"><thead><tr><th>Difference</th><th>Placement</th></tr></thead><tbody><tr><td>ABI, client API, consistency, delivery semantics</td><td>separate Form</td></tr><tr><td>lifecycle or identity unit</td><td>separate Form or separate Resource</td></tr><tr><td>inter-resource connection API</td><td>exact Binding contract</td></tr><tr><td>algorithm choice within identical semantics</td><td>closed enum</td></tr><tr><td>host capability ceilings</td><td>Host Support Profile</td></tr><tr><td>capacity, price, region, quota</td><td>Service Offering</td></tr></tbody></table><p>Free semantic tokens are prohibited in current Forms. A closed enum is permitted only when the API shape, storage model, and lifecycle are identical and the value selects an internal algorithm (for example a vector distance metric). Namespaced non-portable extensions must be explicitly marked and excluded from the portable core.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The v1alpha2 category-shaped candidate line is superseded for new design work. Its bytes, identities, and generator remain retained provider-v2 preview source and are not overwritten.</li><li>New Forms are authored per service shape: a JavaScript Module Worker, an eventually consistent edge KV namespace, a strongly consistent object bucket, a SQLite database, an at-least-once queue are each one Form; a WASI function, a linearizable KV store, a Postgres database, a FIFO queue are each a different Form.</li><li>The <code>portability-boundary.md</code> &quot;smallest contract&quot; rule is restated as smallest <strong>complete</strong> contract.</li><li>Host Support Profiles, not desired fields, express what a host can accept.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep open tokens with a central token registry.</strong> Rejected because a registry of tokens still lets one Form mean many things; the contract stays unverifiable and hosts still diverge on everything the token does not say.</li><li><strong>Model categories with optional feature flags.</strong> Rejected because optional semantics make every consumer program against the intersection, reproducing the least common denominator this decision rejects.</li><li><strong>One Form per provider product name.</strong> Rejected because product identity is a distribution fact; the Form fixes semantics and deliberately omits the vendor&#39;s name, account, and commercial surface.</li></ul>',18)])])}const u=t(o,[["render",s]]);export{h as __pageData,u as default};
