import{_ as a,o as t,c as o,ag as i}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"PullQueue — takoform_pull_queue","description":"","frontmatter":{},"headers":[],"relativePath":"proposals/queue/pull-queue.md","filePath":"proposals/queue/pull-queue.md"}'),r={name:"proposals/queue/pull-queue.md"};function s(n,e,d,c,l,u){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="pullqueue-—-takoform-pull-queue" tabindex="-1">PullQueue — <code>takoform_pull_queue</code> <a class="header-anchor" href="#pullqueue-—-takoform-pull-queue" aria-label="Permalink to &quot;PullQueue — `takoform_pull_queue`&quot;">​</a></h1><h2 id="workload-and-consumer" tabindex="-1">Workload and consumer <a class="header-anchor" href="#workload-and-consumer" aria-label="Permalink to &quot;Workload and consumer&quot;">​</a></h2><p>A team decouples producers from consumers with a queue the consumers DRAIN: anything that can call the <code>queue.pull</code> Interface receives messages, does the work, and deletes what it finished. Lifecycle is managed through the provider from birth. Edge workers get a producer binding later (Accepted Bindings below); runtime consumption from non-worker compute families awaits the cross-family projection realization and is not invented here.</p><h2 id="role" tabindex="-1">Role <a class="header-anchor" href="#role" aria-label="Permalink to &quot;Role&quot;">​</a></h2><p><code>identity</code>. Retention, visibility, dead-lettering, and the long-poll bound are the queue&#39;s small desired surface; everything else is fixed by the <code>queue.pull@1.0.0</code> contract.</p><h2 id="observable-semantics" tabindex="-1">Observable semantics <a class="header-anchor" href="#observable-semantics" aria-label="Permalink to &quot;Observable semantics&quot;">​</a></h2><p>Exactly the <code>queue.pull@1.0.0</code> contract: send, receive, delete, and changeVisibility against one unordered at-least-once queue. Delivery is at-least-once with no ordering guarantee; consumers must be idempotent.</p><p>A message body is a UTF-8 string or the common canonical <code>{&quot;encoding&quot;:&quot;base64&quot;,&quot;data&quot;:&quot;...&quot;}</code> bytes object, plus at most 10 string attributes (the vocabulary the topic family&#39;s filter policy matches over); body and attributes together are bounded at 262144 bytes. <code>send</code> accepts one message and returns a message identity that is stable across redeliveries; the acceptance timestamp never moves.</p><p><code>receive</code> returns up to 10 messages, each with its identity, body, attributes, acceptance timestamp, per-message receive count (1 on first delivery, incremented on each), and a fresh receipt handle. A received message becomes invisible for the visibility timeout — the call&#39;s own <code>visibilityTimeoutSeconds</code> when stated, otherwise the queue&#39;s <code>defaultVisibilityTimeoutSeconds</code> — and is redelivered after it unless deleted. <code>waitSeconds</code> (0 up to <code>receiveWaitBoundSeconds</code>; absent means 0) long-polls: the call returns as soon as messages are deliverable or the wait elapses, and an empty result is a normal response, not an error.</p><p>Receipt handles: each receive of a message invalidates every earlier handle for it; the newest handle stays valid until the message is next received, deleted, or expires. <code>delete</code> and <code>changeVisibility</code> with an invalidated or unknown handle fail with the Interface&#39;s stated stale-handle error and have no effect — a stated error, not a silent no-op, because &quot;settled&quot; and &quot;will be redelivered&quot; must be distinguishable. A delete with the newest handle removes the message permanently, even after its visibility timeout has lapsed, so long as it was not received again. <code>changeVisibility</code> sets the remaining invisibility to 0..43200 seconds from now; 0 makes the message immediately deliverable.</p><p>Desired fields: <code>messageRetentionSeconds</code> (60..1209600) bounds how long an undeleted message survives. <code>defaultVisibilityTimeoutSeconds</code> (0..43200) and <code>receiveWaitBoundSeconds</code> (0..20) are the defaults and bounds above. <code>deadLetter</code> is an optional closed pair <code>{queue, maxReceiveCount}</code> (1..1000): a receive that would raise a message&#39;s receive count past <code>maxReceiveCount</code> instead moves it to the named queue as a new message with its own identity, its own acceptance timestamp, and a receive count starting again — mirroring the edge QueueConsumer&#39;s dead-letter rule. Without the pair, only deletion or retention expiry removes a message. <code>queue</code> is the closed exact <code>{apiVersion, kind, name}</code> reference to another PullQueue, UID-pinned under the Host API v1 relation rules; a host rejects self-dead-lettering and any cycle through the dead-letter graph, because the move resets the receive count and nothing else bounds the loop (<a href="./../../spec/decisions/0026-attachment-claims-are-canonical-and-acyclic.html">decision 0026</a>).</p><h2 id="why-this-is-one-form" tabindex="-1">Why this is one Form <a class="header-anchor" href="#why-this-is-one-form" aria-label="Permalink to &quot;Why this is one Form&quot;">​</a></h2><p>Receive count, handle invalidation, and redelivery-on-timeout are what consumer code is written against. There is deliberately no ordering or delivery-mode field: either would change consumer correctness requirements invisibly (decision 0008).</p><h2 id="what-would-require-a-separate-form" tabindex="-1">What would require a separate Form <a class="header-anchor" href="#what-would-require-a-separate-form" aria-label="Permalink to &quot;What would require a separate Form&quot;">​</a></h2><p>A FIFO queue, an exactly-once-effect queue, a task queue with per-message delivery targets, and the edge family&#39;s push-delivery AtLeastOnceQueue are each a different delivery shape and a different Form.</p><h2 id="provided-interfaces" tabindex="-1">Provided Interfaces <a class="header-anchor" href="#provided-interfaces" aria-label="Permalink to &quot;Provided Interfaces&quot;">​</a></h2><p><code>queue.pull@1.0.0</code> (Interface candidate to be authored with the family&#39;s candidate set).</p><h2 id="accepted-bindings" tabindex="-1">Accepted Bindings <a class="header-anchor" href="#accepted-bindings" aria-label="Permalink to &quot;Accepted Bindings&quot;">​</a></h2><p>None. It is the intended target of a future <code>module-worker.queue-pull-producer</code> binding projecting the send half into a Worker Version; a binding instance&#39;s <code>resource</code> reference carries an explicit <code>apiVersion</code>, so a cross-family target is representable as-is (<a href="./../../spec/binding-contract/">binding contract</a>). Non-worker consumption awaits the cross-family projection realization.</p><h2 id="lifecycle-risks" tabindex="-1">Lifecycle risks <a class="header-anchor" href="#lifecycle-risks" aria-label="Permalink to &quot;Lifecycle risks&quot;">​</a></h2><p>Deleting a queue referenced as another queue&#39;s dead-letter target, as a TopicSubscription target or dead-letter, or as a Schedule target must fail with <code>dependency_in_use</code>. Retained messages are destroyed with the queue.</p><h2 id="prior-art" tabindex="-1">Prior art <a class="header-anchor" href="#prior-art" aria-label="Permalink to &quot;Prior art&quot;">​</a></h2><p>The visibility-timeout pull queue offered by every major provider — the &quot;Pull queue&quot; survey row decision 0043 minted this family from. The withdrawn v1alpha2 <code>Queue</code> kind, whose open <code>ordering</code> enum this Form replaces by fixing one shape. The Edge family&#39;s push AtLeastOnceQueue for contrast: there the host invokes a worker&#39;s <code>queue</code> handler with batches; here consumers pull with visibility semantics — two proven shapes, two Forms (decision 0043).</p>',23)])])}const p=a(r,[["render",s]]);export{m as __pageData,p as default};
