import{_ as t,o as i,c as o,ag as a}from"./chunks/framework.cyF4XceO.js";const h=JSON.parse('{"title":"0052 — The Specification is released on its own line","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0052-the-specification-is-released-on-its-own-line.md","filePath":"spec/decisions/0052-the-specification-is-released-on-its-own-line.md"}'),n={name:"spec/decisions/0052-the-specification-is-released-on-its-own-line.md"};function r(s,e,c,d,l,p){return i(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="_0052-—-the-specification-is-released-on-its-own-line" tabindex="-1">0052 — The Specification is released on its own line <a class="header-anchor" href="#_0052-—-the-specification-is-released-on-its-own-line" aria-label="Permalink to &quot;0052 — The Specification is released on its own line&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-23</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Takoform has several independent compatibility and maturity axes. The Host API lane, each exact FormRef, the Form Package envelope, Interface and Binding identities, and the Terraform Provider all version for different reasons. None of those numbers identifies the complete normative document set a reader implemented.</p><p>Earlier policy also made implementation adoption a precondition for the Specification itself: two Hosts, production traffic, a compatibility window, third-party publication, and Provider release were treated as inputs to the v1 decision. Those facts are useful adoption evidence, but their owners are not Takoform specification authorities. Takoserver, Takosumi, another backend, a production operator, or the official Provider must not be able to block or define the normative protocol.</p><p>The source now has a literal Host API v1 suite. Its closed generator-owned index names eight versionless Form families and 31 exact current FormRefs. The Edge family contains 16 Forms. <code>ObjectBucket</code>, <code>edge.objects</code>, and <code>module-worker.object-bucket</code> are not current identities. The 31 Forms keep their own current <code>0.x</code> Definition versions and Experimental maturity.</p><p>Provider 2.1.1, Host v1beta1, its versioned 15-Form Edge family, and their published bytes remain immutable history. The official Provider 3 work is a separate, non-normative sample implementation.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Takoform Specification 1.0 is a numbered normative document release with the literal Host API identity <code>forms.takoform.com/v1</code>. It releases only after three Takoform-owned prerequisites close.</strong></p><p>The three prerequisites, in order, are:</p><ol><li><code>specification-source-snapshot</code>: one reachable full commit and the exact path set and digest of every normative Specification source byte;</li><li><code>candidate-form-corpus</code>: the exact committed multi-family index reopened through every package, Definition, Interface, Binding, suite corpus, runner input, and transitively referenced artifact; and</li><li><code>reference-conformance</code>: execution of the exact runner argv declared by the committed suite manifest, producing its exact class-specific report.</li></ol><p>No external Host, backend, runtime, deployment, production consumer, Takoserver, Takosumi, signer, or operator fact may replace, add to, or close one of those rows. They remain independently recordable adoption evidence.</p><p>The release has no implicit effect on another axis:</p><ul><li>the 31 current FormRefs are not relabelled or promoted to Form <code>1.0.0</code>;</li><li>a future Stable Form begins at <code>1.0.0</code> only through an explicit per-Form decision, with its own compatibility and maturity evidence;</li><li>no Form Package is published by the Specification release;</li><li>no Interface or Binding identity changes merely because v1 releases;</li><li>Provider 3 may implement the current exact <code>0.x</code> FormRefs, but its completion is neither necessary nor sufficient for Specification 1.0; and</li><li>the retained Provider 2.1.1/v1beta1 identities are never rewritten.</li></ul><h2 id="release-record" tabindex="-1">Release record <a class="header-anchor" href="#release-record" aria-label="Permalink to &quot;Release record&quot;">​</a></h2><p><a href="../../release/specification-releases.json"><code>../../release/specification-releases.json</code></a> is the append-only numbered-release ledger. Its <code>candidate</code> object states the intended 1.0 identity, canonical index and suite paths, and no-promotion effects. An entry moves into <code>releases</code> only after all three exact publication evidence objects are committed and closed. Once committed, a numbered release entry cannot be edited, removed, or reordered.</p><p>The release record binds:</p><ul><li>the full source commit;</li><li><code>forms/candidates/current-family-index.json</code> and its exact digest;</li><li><code>conformance/takoform-v1/manifest.json</code> and its exact digest;</li><li>canonical digests of the source-snapshot, candidate/corpus, and reference conformance evidence objects; and</li><li>the explicit statements that Form maturity is unchanged and Provider 3 is independent.</li></ul><p>The release ledger is excluded from the source snapshot it records, avoiding a self-referential commit digest. Publication evidence is committed separately and must equal the record read from <code>HEAD</code> before a prerequisite can close.</p><h2 id="current-state" tabindex="-1">Current state <a class="header-anchor" href="#current-state" aria-label="Permalink to &quot;Current state&quot;">​</a></h2><p>The repository currently records a <strong>Specification 1.0 candidate</strong>, not a completed release. The evidence tuple remains absent in <a href="../publication-evidence.json"><code>../publication-evidence.json</code></a>, and the three Specification evidence objects are <code>null</code>. This is intentional: a dirty or uncommitted worktree cannot be the exact source snapshot and must remain red.</p><p>The v1 source generator and suite may be locally green without making this release claim true. Exact baseline commit and digests are recorded only after the integrated source bytes are committed. Provider 3 can remain open or close independently without changing that result.</p><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><ul><li><code>bun run check:publication-evidence</code> validates both independent track shapes and reports open rows without requiring a release.</li><li><code>bun run check:specification-releases</code> validates the candidate and protects every committed numbered release append-only.</li><li><code>bun run check:specification-v1-release</code> asserts only the three Specification prerequisites and fails while they are open.</li><li><code>bun run check:provider-v3-release</code> asserts only the non-normative Provider milestone.</li><li><code>bun run check:takoform-milestones</code> is an optional project checkpoint that asserts both tracks; it is not the Specification release gate.</li></ul><p>There is no <code>stable-mint</code> alias. A command that asserts Specification 1.0 must not imply that every Form or package becomes Stable.</p><p>None of these commands commits, tags, publishes, deploys, signs, enrolls a key, or changes production state.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>Readers can cite one exact Takoform-owned Specification 1.0 document set and literal Host API v1 contract without delegating normative authority to a product adoption schedule. The price is explicit separation: protocol release, Form maturity, Provider compatibility, package publication, Host support, and production availability must each state their own evidence and must never be inferred from another axis.</p>',28)])])}const u=t(n,[["render",r]]);export{h as __pageData,u as default};
