import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.BTLSZHNB.js";const g=JSON.parse('{"title":"Form Package publication, Legacy retention, and revocation","description":"","frontmatter":{},"headers":[],"relativePath":"release/form-packages.md","filePath":"release/form-packages.md"}'),n={name:"release/form-packages.md"};function o(r,e,c,l,d,p){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="form-package-publication-legacy-retention-and-revocation" tabindex="-1">Form Package publication, Legacy retention, and revocation <a class="header-anchor" href="#form-package-publication-legacy-retention-and-revocation" aria-label="Permalink to &quot;Form Package publication, Legacy retention, and revocation&quot;">​</a></h1><p>Form Package publication is independent of the Terraform provider&#39;s GPG key, <code>v*</code> tag namespace, and <code>provider-release</code> Environment. The current lane is driven only by Experimental or Stable records in <code>forms/lifecycle.json</code> and uses content-addressed package identities. The pre-reset 34-entry plan is a closed immutable <strong>Legacy</strong> inventory and cannot authorize a new release.</p><p>The responsibilities are:</p><ul><li>publish a lifecycle-authorized current package without inventing a second SemVer clock;</li><li>verify and retain the 34 exact historical publication identities;</li><li>publish an append-only security revocation when an exact Legacy package requires it;</li><li>preserve read, observe, delete, recovery, and migration for Legacy users;</li><li>introduce every new Form through Proposal and Experimental lifecycle evidence without reusing the closed <code>portable-v1</code> release plan.</li></ul><h2 id="current-content-addressed-publication-lane" tabindex="-1">Current content-addressed publication lane <a class="header-anchor" href="#current-content-addressed-publication-lane" aria-label="Permalink to &quot;Current content-addressed publication lane&quot;">​</a></h2><p><code>bun run deploy -- takoform-form-package-release plan</code> derives only publishable Experimental and Stable identities from <code>forms/lifecycle.json</code>. With the current empty lifecycle it prints no commands. For an authorized current Form, the prepare, publish, and verify phases use the exact derived tag:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release prepare \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/sha256-&lt;64-lowercase-hex&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;40-character-reviewed-commit&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release publish \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/sha256-&lt;same-64-lowercase-hex&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-40-character-reviewed-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;candidate-workflow-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;candidate-workflow-run-attempt&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release verify \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/sha256-&lt;same-64-lowercase-hex&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-40-character-reviewed-commit&gt;</span></span></code></pre></div><p>The artifact ID is the verified <code>packageDigest</code> with <code>:</code> replaced by <code>-</code>. Source path, tag, asset base, release manifest, and deep verification all use that same value. <code>FormRef.definitionVersion</code> remains the Form contract SemVer; provider versions remain independent.</p><h2 id="closed-legacy-publication-lane" tabindex="-1">Closed Legacy publication lane <a class="header-anchor" href="#closed-legacy-publication-lane" aria-label="Permalink to &quot;Closed Legacy publication lane&quot;">​</a></h2><p>The 34 Legacy Forms carried independent versions and were published on separate tags from separate reviewed sources. This remains an important identity rule, but it is a historical fact rather than an open release queue.</p><p><a href="../forms/release-plan.json"><code>forms/release-plan.json</code></a> is the immutable source plan bound by the historical publication ledger. It must not be regenerated, passed to the current <code>plan</code>/<code>prepare</code>/<code>publish</code> phases, or used to create tags. Only the explicit <code>verify-all</code> retention path consumes it.</p><p>The following prepare/publish/recovery commands describe how the historical lane worked. They are retained for forensic and recovery review only. Every canonical tag is already published, so the create-only entrypoint must refuse them; operators must not treat these examples as current instructions.</p><p>Historical prepare phase:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release prepare \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/v&lt;semver&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;40-character-reviewed-commit&gt;</span></span></code></pre></div><p>The prepare phase stopped after recording one exact workflow run and required a reviewer. The historical publish phase then consumed only that explicit run and attempt:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release publish \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/v&lt;semver&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-40-character-reviewed-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;candidate-workflow-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;candidate-workflow-run-attempt&gt;</span></span></code></pre></div><p>The publish phase verified the candidate before creating its tag or Release, then performed exact immutable readback. The historical verifier invocation was:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release verify \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/v&lt;semver&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;40-character-reviewed-commit&gt;</span></span></code></pre></div><p>The historical batch mode serialized several independently reviewed candidates from one protected <code>main</code> commit:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release publish-batch \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --input /absolute/path/outside/the/repository/form-publish-batch.json</span></span></code></pre></div><p>The input is a non-empty, canonical recursively key-sorted JSON array. It must be a regular non-symbolic-link file no larger than 1 MiB, and every object must contain exactly four string fields:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;expectedCommit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0123456789abcdef0123456789abcdef01234567&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;runAttempt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;runId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;123456789&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;tag&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;forms/k-ivsgozkxn5zgwzls/v3.0.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>Every tag must be one canonical release-plan identity, and tags and exact workflow run/attempt pairs may not repeat. <code>publish-batch</code> validates the whole operator input before running the complete owner check exactly once. That gate produces a process-local proof bound to the clean protected-main commit and tree. The ordinary <code>publish</code> flow is then reused synchronously for each entry: before tag push, Release draft creation, and final draft publication it freshly fetches protected <code>origin/main</code>, requires the same clean HEAD and tree, and re-runs the selected Form&#39;s release-authority path fence. GitHub writers are never parallel.</p><p>A batch was not atomic across independent public identities. It stopped at the first failure and reported completed tags plus the failed tag. Historical recovery required authoritative inspection and a new input containing only identities proven absent.</p><p><code>publish</code> was create-only and was never a retry mechanism after a tag-side partial failure. The narrow tag-only recovery invocation was:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release recover-tag-only \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/v&lt;semver&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;candidate-source-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-tag-object &lt;candidate-annotated-tag-object-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-recovery-commit &lt;current-reviewed-protected-main-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;original-candidate-workflow-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;original-candidate-workflow-run-attempt&gt;</span></span></code></pre></div><p>This phase re-downloaded and re-verified the original run/attempt, required source/tooling/recovery ancestry, and proved the release inputs had not changed. It never pushed, created, moved, or deleted a tag and failed closed on any identity ambiguity.</p><p>The corresponding exact-draft recovery invocation was:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release recover-draft \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/&lt;release-id&gt;/v&lt;semver&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;candidate-source-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-tag-object &lt;candidate-annotated-tag-object-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-recovery-commit &lt;current-reviewed-protected-main-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --release-id &lt;retained-github-release-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;original-candidate-workflow-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;original-candidate-workflow-run-attempt&gt;</span></span></code></pre></div><p><code>recover-draft</code> repeated the original candidate, tag, protected-main, stable recovery-path, and owner-gate proofs. It accepted only one exact draft identity with the original tag, name, body, upload endpoint, and candidate asset subset. Every existing asset had to carry a unique positive GitHub ID and the exact candidate name, state, size, and digest. Any extra, duplicate, unknown, drifted, public, or competing identity stopped recovery.</p><p>After all 34 independent releases existed, the historical lane produced one deterministic publication record outside the repository:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release verify-all \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --output-root /absolute/new/directory/outside/takoform</span></span></code></pre></div><p>The retained <code>form-package-publication-set.json</code> binds every public Release identity and digest in plan order. It proves exact historical bytes and their publisher identity only. The later <code>ga-core-v2</code> ten-Form classification is an immutable Legacy assertion, not a current admission decision. Today, Form lifecycle comes only from <code>forms/lifecycle.json</code>; Host Support and activation are independent host decisions.</p><h2 id="package-source-and-tag" tabindex="-1">Package source and tag <a class="header-anchor" href="#package-source-and-tag" aria-label="Permalink to &quot;Package source and tag&quot;">​</a></h2><p>A current release source is one already-valid closed v1alpha3 package carrying an exact v1alpha2 FormRef:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>forms/releases/&lt;release-id&gt;/sha256-&lt;64-lowercase-hex&gt;/</span></span>
<span class="line"><span>  package-index.json</span></span>
<span class="line"><span>  &lt;exact payload closure listed by the index&gt;</span></span></code></pre></div><p>The release ID is <code>k-</code> plus the lowercase, unpadded base32 encoding of the exact ASCII FormRef Kind bytes. It is reversible, filesystem-safe, and does not collapse case-distinct Kinds. The tag is <code>forms/&lt;release-id&gt;/sha256-&lt;64-lowercase-hex&gt;</code>. The builder derives that artifact ID from the verified canonical index digest, decodes the release ID back to the exact FormRef Kind, verifies the complete package, and requires the tag to point at a clean <code>HEAD</code>. Published Legacy v1alpha1 packages retain their original <code>&lt;packageVersion&gt;</code> directory and <code>v&lt;packageVersion&gt;</code> tag; neither may be reused or renamed. Local tests may use the explicit <code>--allow-untagged-candidate</code> switch; its manifest remains <code>publicationReady=false</code>.</p><p>Tag protection selects an immutable source commit; it is not the package&#39;s cryptographic signature. Trust in the package bytes comes from verifying the canonical-index Sigstore bundle against the exact workflow OIDC identity.</p><p>The release contains:</p><ul><li>the newline-free RFC 8785 canonical <code>package-index.json</code>, which is the exact Cosign signed subject and semantic package identity;</li><li>a deterministic <code>.tar.gz</code> transport whose root index has those same bytes and whose payload bytes match the index closure;</li><li>an RFC 8785 canonical SPDX 2.3 data-artifact SBOM that binds the exact FormRef, package digest, artifact identity, index/payload SHA-256 closure, and SPDX package verification code; the document <code>DESCRIBES</code> the package and that package has one deterministic <code>CONTAINS</code> relationship for the index and every payload file;</li><li>an RFC 8785 canonical in-toto Statement v1 with SLSA Provenance v1 that binds the exact index/archive digests, source repository and tag commit, the distinct protected-main release-tooling commit, protected workflow, and canonicalization mode; its builder ID is versioned by that tooling commit;</li><li>a Sigstore v0.3 bundle containing the ephemeral certificate, signature, and transparency-log inclusion evidence;</li><li>a release manifest and <code>SHA256SUMS</code> for the exact final asset inventory.</li></ul><p><code>.github/workflows/form-package-release.yml</code> is dispatched only from current protected <code>main</code> with exact <code>tag</code> and <code>expected_commit</code> inputs. It verifies the planned identity, approved commit, and that commit&#39;s ancestry from main, then checks the exact data source into a separate untrusted-source directory. The workflow creates only an ephemeral local annotated tag object. Only the protected-main release tooling executes. The protected <code>form-package-release</code> Environment, commit-pinned Actions, and Cosign v3 sign and immediately verify the canonical index against the exact protected-main workflow identity. The workflow emits a same-run checksum-closed candidate containing the tag object, seven public assets, source/tooling commits, run/attempt, and every asset digest. It has no repository-write, tag-push, Release, public-attestation, or publish authority.</p><p>The local owner deploy entrypoint downloads that exact run/attempt, verifies its outer checksum closure, reconstructs and checks the tag object, verifies the Sigstore bundle and seven-asset semantic closure, repeats remote absence proofs, then uses only the operator machine&#39;s Git/GitHub authority to create the tag, draft, upload, and immutable release. It downloads the public assets again and requires the same release ID, tag, names, sizes, and digests. When repository release immutability is enabled, that final local publication locks the tag and assets. Asset POSTs use the exact absolute <code>https://uploads.github.com/repos/...</code> URL returned by the release API; passing that host through <code>gh api --hostname</code> is invalid because the CLI rewrites it to the nonexistent <code>api.uploads.github.com</code>. The upload subprocess receives the operator token only as its host-scoped <code>GH_ENTERPRISE_TOKEN</code> environment; the token is never placed in argv or retained in the repository.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>For the <code>ObjectBucket</code> tag <code>forms/k-j5rguzldorbhky3lmv2a/v1.0.0</code>, verify the retained canonical index and bundle with:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cosign verify-blob takoform-form-k-j5rguzldorbhky3lmv2a_1.0.0_package-index.json \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --bundle takoform-form-k-j5rguzldorbhky3lmv2a_1.0.0_package-index.sigstore.json \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --certificate-identity \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &#39;https://github.com/tako0614/terraform-provider-takoform/.github/workflows/form-package-release.yml@refs/heads/main&#39; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --certificate-oidc-issuer &#39;https://token.actions.githubusercontent.com&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sha256sum --check --strict SHA256SUMS</span></span></code></pre></div><p>The bundle carries the signature, certificate, and transparency inclusion proof. Air-gapped verification additionally requires a retained, operator-managed Sigstore trusted root from the Public Good Instance; the distribution endpoint is never a trust root.</p><p>Historical publication readback parses the SBOM and provenance as strict I-JSON, rejects non-canonical or duplicate-key bytes and unknown/omitted fields, and recomputes their bindings from the signed package index and retained release manifest. Asset filenames, media types, and checksums alone are not semantic release evidence. Form Package release tests additionally validate generated SBOMs offline against the repository-pinned official SPDX 2.3 JSON Schema from the SPDX <code>v2.3</code> tag.</p><h2 id="append-only-security-revocation" tabindex="-1">Append-only security revocation <a class="header-anchor" href="#append-only-security-revocation" aria-label="Permalink to &quot;Append-only security revocation&quot;">​</a></h2><p>One source file at <code>forms/revocations/&lt;statementVersion&gt;.json</code> records a consecutively sequenced security decision for one exact package digest and FormRef. A matching cumulative <code>forms/revocations/checkpoints/&lt;statementVersion&gt;.json</code> commits every statement from sequence 1 through the current sequence and the previous canonical checkpoint digest. It fixes the effects to block new create/update and activation while retaining referenced bytes for observe/delete. Deprecation is not a revocation.</p><p><code>.github/workflows/form-package-revocation.yml</code> dispatches from protected main, binds the exact statement/checkpoint source to <code>forms/revocations/v&lt;statementVersion&gt;</code>, verifies the complete cumulative source chain, and keyless-signs the checkpoint through the same protected Environment. It emits only a checksum-closed same-run candidate; it cannot create a tag, Release, public attestation, or published asset. The Form Package owner deploy surface verifies that candidate and performs the create-only tag, exact local Release publication, and authoritative readback with operator credentials. CI permits preparing a new statement/checkpoint pair but rejects edits, renames, and deletion of existing source paths.</p><p>Prepare, publish, and independently read back one checkpoint with explicit phase boundaries:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release prepare-revocation \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/revocations/v&lt;statement-version&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;40-character-reviewed-commit&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release publish-revocation \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/revocations/v&lt;statement-version&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-40-character-reviewed-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;candidate-workflow-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;candidate-workflow-run-attempt&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-form-package-release verify-revocation \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag forms/revocations/v&lt;statement-version&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-40-character-reviewed-commit&gt;</span></span></code></pre></div><p>After verifying the Sigstore bundle against the exact <code>@refs/heads/main</code> workflow identity, a host starts only at sequence 1 and durably pins the checkpoint sequence, canonical SHA-256 digest, and cumulative-entry digest. It accepts only the next sequence with a matching <code>previousCheckpointDigest</code> and unchanged cumulative prefix; rollback, gaps, omissions, prefix rewrites, and forks fail closed.</p><h2 id="repository-configuration" tabindex="-1">Repository configuration <a class="header-anchor" href="#repository-configuration" aria-label="Permalink to &quot;Repository configuration&quot;">​</a></h2><p>The source tree and repository settings are both part of the trust boundary:</p><ul><li>the current tag ruleset targets <code>refs/tags/forms/*/sha256-*</code>; the retained Legacy rule targets <code>refs/tags/forms/*/v*</code>. Both restrict creation and prevent deletion and non-fast-forward updates;</li><li><code>form-package-release</code> has required reviewers and only permits signing a candidate from the <code>main</code> branch;</li><li>release immutability is enabled before the first publication; and</li><li>a real tag/release is created only by the local owner deploy entrypoint after maintainer authorization.</li></ul><p>The retired <code>1.0.0</code> and <code>1.0.1</code> Form Packages have immutable tags and retained release evidence. The current <code>forms/retired-package-set.json</code> and <code>admission/v1/published-package-set.json</code> select the exact <code>1.0.1</code> generation; <code>standard-form-conformance published-package-check</code> authenticates that selected set and its production package-publisher trust inputs offline. The retained <code>1.0.0</code> history is not rewritten and is not the selected input to that current check.</p><p>The historical <code>portable-v1</code> ledger retains 34 signed immutable releases. The last admission identity actually published was <code>forms/admissions/v1.0.7</code>; its old experiment asserted a ten-Form <code>portable-standard</code> subset. These fields and tags are Legacy evidence only. Current Form maturity comes from <code>forms/lifecycle.json</code>, while every host independently owns package installation, executable support, activation, principal audience, and any Offering.</p>`,58)])])}const u=a(n,[["render",o]]);export{g as __pageData,u as default};
