import{_ as t,o as a,c as i,ag as s}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"0033 — Edge app assets and SQLite migrations are content-addressed","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0033-edge-app-assets-and-sqlite-migrations-are-content-addressed.md","filePath":"spec/decisions/0033-edge-app-assets-and-sqlite-migrations-are-content-addressed.md"}'),n={name:"spec/decisions/0033-edge-app-assets-and-sqlite-migrations-are-content-addressed.md"};function o(r,e,d,c,l,p){return a(),i("div",null,[...e[0]||(e[0]=[s('<h1 id="_0033-—-edge-app-assets-and-sqlite-migrations-are-content-addressed" tabindex="-1">0033 — Edge app assets and SQLite migrations are content-addressed <a class="header-anchor" href="#_0033-—-edge-app-assets-and-sqlite-migrations-are-content-addressed" aria-label="Permalink to &quot;0033 — Edge app assets and SQLite migrations are content-addressed&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-09</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The first Edge Platform Family milestone can run a worker and provision a SQLite database, but it cannot describe either half of a small edge application that is not executable code: static files served beside the worker, and ordered SQL changes applied to the database. Putting either byte stream directly in a Form would create a second artifact protocol beside the content-addressed transport of <a href="./0012-artifacts-use-content-addressed-upload.html">0012</a>, put raw bytes in provider state, and make an immutable revision&#39;s identity depend on client serialization rather than on the manifest the host commits.</p><p>The two byte streams also do not have the same semantics as a <code>WorkerBundle</code>. Static files are looked up by request path and never linked as modules. SQL migrations are an ordered history: changing an applied file is not a new rendering of the same state, but a rewrite of the database&#39;s past. Reusing the <code>WorkerBundle</code> shape would make one manifest kind mean three different things.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The family adds three unpublished candidate Forms at definition version <code>0.1.0</code>, alongside one optional <code>WorkerVersion.assets</code> object. The candidate lane has never been published, so regenerating these exact candidate identities does not mutate a published FormRef.</p><h3 id="static-assets" tabindex="-1">Static assets <a class="header-anchor" href="#static-assets" aria-label="Permalink to &quot;Static assets&quot;">​</a></h3><p><code>StaticAssetBundle</code> is an immutable <code>revision</code>. Its entire portable desired state is <code>{manifestDigest}</code>. The referenced manifest MUST have kind <code>StaticAssetBundle</code>; it carries <code>files</code> and no module members. File order has no routing meaning. The manifest digest, paths, media types, sizes, and blob digests are artifact evidence. Raw file bytes never enter desired state or provider state.</p><p><code>WorkerVersion.assets</code>, when present, is one closed object whose three members are all required:</p><ul><li><code>bundle</code> is an exact-Form relation to <code>StaticAssetBundle</code>;</li><li><code>runWorkerFirst</code> is a boolean;</li><li><code>notFoundHandling</code> is exactly <code>none</code> or <code>single_page_application</code>.</li></ul><p>Static asset media types are extensible within the published artifact-manifest v1alpha1 normalized lowercase type/subtype grammar and carry no parameters; they are not a closed runtime allowlist. Migration files remain exactly <code>application/sql</code>.</p><p>Absence means no asset lookup. With <code>runWorkerFirst=false</code>, the host looks up the request path first and invokes <code>fetch</code> only when that lookup produces no response. With <code>runWorkerFirst=true</code>, the host invokes <code>fetch</code> first and looks up assets only when the worker response is 404. An asset response wins; if the asset stage also misses, the worker&#39;s 404 is preserved. <code>none</code> makes a missing exact path a miss. <code>single_page_application</code> serves <code>index.html</code> for a missing path, and the host MUST refuse the Worker Version before mutation when the referenced manifest has no <code>index.html</code>. The attachment grants no hidden runtime binding and does not mutate the asset bundle.</p><p>Asset routing takes the runtime URL <code>pathname</code>, ignores its query and fragment, decodes percent escapes once as strict UTF-8, and strips exactly one leading <code>/</code>. Encoded separators, repeated or empty segments, dot segments, backslashes, controls, Unicode noncharacters, malformed escapes, and invalid UTF-8 are invalid paths. They fail closed and never enter SPA fallback; only a valid missing path may resolve to <code>index.html</code>.</p><h3 id="sqlite-migrations" tabindex="-1">SQLite migrations <a class="header-anchor" href="#sqlite-migrations" aria-label="Permalink to &quot;SQLite migrations&quot;">​</a></h3><p><code>SQLiteMigrationSet</code> is an immutable <code>revision</code>. Its entire portable desired state is <code>{manifestDigest}</code>. The referenced manifest MUST have kind <code>MigrationBundle</code>; every file MUST use <code>application/sql</code>, and the <code>files</code> array order is the migration order. The ordered manifest is the identity: changing a path, digest, or position produces a different set. SQL bytes remain blobs in the artifact store and never enter Form desired state or provider state.</p><p><code>SQLiteMigrationApplication</code> is an immutable <code>attachment</code> with exactly two required exact-Form relations: <code>database</code> to <code>SQLiteDatabase</code> and <code>migrationSet</code> to <code>SQLiteMigrationSet</code>. A host keeps a durable ledger in the database, ordered by migration path and blob digest. Before applying anything, it MUST prove that the ledger is an exact prefix of the referenced manifest. An already-applied path with another digest, another order, or absence from the new set is <code>migration_required</code> and mutates nothing. Only the unapplied suffix may run.</p><p>For each suffix entry, execution of that SQL file and insertion of its ledger record are one SQLite transaction. A failed file rolls back both the file and its ledger record; earlier committed entries remain, so retry resumes at the same suffix boundary. Hosts MUST serialize this prefix check and suffix apply per database and re-check it while holding that serialization boundary. The attachment is Ready only when the durable ledger equals the referenced ordered set. Multiple append-only applications may coexist: after a later superset is applied, an older shorter application remains stored but is not Ready until it is re-applied against its intended set.</p><p>Deleting <code>SQLiteMigrationApplication</code> detaches management only. It MUST NOT execute down migrations, delete ledger records, reinterpret the schema, or delete the database. Ordinary relation protection separately prevents deleting the database or migration set while a live application references it. What happens after the attachment is removed is the database&#39;s own deletion policy, not migration rollback.</p><p>Neither migration Form declares a typed output. Readiness plus the exact relations state the portable result. A resultant schema digest would depend on SQLite introspection and normalization rules not declared by this family and would pretend to be portable evidence it is not.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>WorkerBundle</code>, <code>StaticAssetBundle</code>, and <code>MigrationBundle</code> stay three closed artifact meanings even though they use one transport.</li><li>Provider local-file authoring may keep local paths, sizes, and digests as evidence, but uploads bytes before resource apply and sends only <code>manifestDigest</code> to the host.</li><li>A new migration is an appended manifest entry and a new <code>SQLiteMigrationSet</code> revision. An applied file is never edited in place.</li><li>The reference host can prove manifest kind closure, SQL media type, SPA index presence, and the prefix/suffix ledger state machine. It deliberately has no SQLite execution engine; a real backend transaction and a real asset-serving runtime remain publication evidence, not facts inferred from local tests.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Put files or SQL strings directly in desired state.</strong> Rejected because it duplicates artifact identity, leaks bytes into state, and makes large payloads ordinary resource documents.</li><li><strong>Reuse <code>WorkerBundle</code> for every byte set.</strong> Rejected because module linking, path serving, and ordered schema history are different meanings.</li><li><strong>Track only migration filenames.</strong> Rejected because rewriting an applied file under the same name would become invisible.</li><li><strong>Make an application update point to the next set.</strong> Rejected because the attachment&#39;s desired history would move in place. A new exact set and a new immutable application keep the plan reviewable.</li><li><strong>Delete means rollback.</strong> Rejected because arbitrary SQL has no portable inverse and attachment teardown must not become implicit destructive data mutation.</li></ul>',24)])])}const u=t(n,[["render",o]]);export{m as __pageData,u as default};
