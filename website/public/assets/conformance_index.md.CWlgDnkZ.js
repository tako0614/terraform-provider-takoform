import{_ as t,o as a,c as n,ag as o}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"Conformance","description":"","frontmatter":{},"headers":[],"relativePath":"conformance/index.md","filePath":"conformance/index.md"}'),r={name:"conformance/index.md"};function s(i,e,d,c,h,l){return a(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="conformance" tabindex="-1">Conformance <a class="header-anchor" href="#conformance" aria-label="Permalink to &quot;Conformance&quot;">​</a></h1><p>The conformance evidence in this repository is executable, local, and role-scoped. It proves implementation behavior; it never proves publication, admission, Host Support, or Form maturity.</p><ul><li><code>internal/provider/*_test.go</code> asserts that the provider built from this repository registers exactly the 15 v1beta1 Edge Platform Family resources — and no generic carrier, which is itself asserted (<a href="./../spec/decisions/0035-beta-contracts-ship-in-stable-provider-v2-1.html">decision 0035</a>, <a href="./../spec/decisions/0021-third-party-forms-and-contract-distribution.html">decision 0021</a>) — and covers typed schema behavior, validation, lane negotiation, and the absence of plan-time remote mutation;</li><li><code>examples/resources/</code> contains one formatted HCL example for every registered resource, with no exceptions: every provider resource is derived from a Form, so every one has an example;</li><li><code>cmd/worker-authoring-conformance</code> drives the raw v1beta1 resources and the official <code>worker-app</code> module through real OpenTofu and Terraform CLIs against the deterministic reference host, as one fail-closed matrix.</li></ul><p>Run:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go test ./...</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go vet ./...</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">tofu fmt -check -recursive examples</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/worker-authoring-conformance matrix --opentofu tofu --terraform terraform</span></span></code></pre></div><h2 id="schema-derivation-gate" tabindex="-1">Schema derivation gate <a class="header-anchor" href="#schema-derivation-gate" aria-label="Permalink to &quot;Schema derivation gate&quot;">​</a></h2><p>The provider schema, v1beta1 Form Definitions, and candidate conformance fixtures are all derived from one family catalog declaration, so drift between them is a build failure rather than something a checked-in fixture corpus has to notice later. <code>go run ./cmd/standard-form-conformance verify</code> regenerates nothing: it reads the committed candidate set, re-verifies its bytes, and inspects the actual provider resource schema for every declared Form, including that each field the definition calls immutable really forces replacement.</p><h2 id="withdrawn-corpora" tabindex="-1">Withdrawn corpora <a class="header-anchor" href="#withdrawn-corpora" aria-label="Permalink to &quot;Withdrawn corpora&quot;">​</a></h2><p>The pre-Beta corpora — <code>form-package-v1/</code> (the Legacy v1alpha1 package corpus), <code>portable-host-v1/</code> (the provider-v1 host corpus), <code>portable-host-v2/</code> (the provider-v2 host corpus), and the earlier-withdrawn <code>portable-host-v3</code> — are recorded with the lane each declared in <a href="../release/published-document-lanes.json"><code>../release/published-document-lanes.json</code></a>, and their bytes remain in this repository&#39;s history (decisions 0037 and <a href="./../spec/decisions/0042-the-pre-beta-epochs-are-withdrawn.html">0042</a>). A corpus that cannot resolve what it measures proves nothing by existing, and a withdrawn corpus address is never reused for a different lane.</p><p>Every count this page states about a corpus — the size of a check matrix, of an error taxonomy, of a retryable set — is bound by <code>bun run check:public-surfaces</code> to the array in that corpus&#39;s <code>contract.json</code> that defines it. A number written here beside a number a machine already knows is a defect waiting to recur, so the gate names both values and fails when they part company.</p><h2 id="portable-host-evidence" tabindex="-1">Portable host evidence <a class="header-anchor" href="#portable-host-evidence" aria-label="Permalink to &quot;Portable host evidence&quot;">​</a></h2><p><code>portable-host-v1beta1/</code> is the Host API v1beta1 corpus consumed by the provider v2.1 client lane. It pins the v1beta1 discovery/API paths, the closed 26-code error taxonomy with its exact HTTP status map and 4-code retryable set, the uid/generation/revision identity rules, the closed portable condition-reason vocabulary, and exact Edge Family probe identities (ModuleWorker, EdgeKVNamespace, AtLeastOnceQueue, WorkerVersion, WorkerBundle, StaticAssetBundle, SQLiteDatabase, SQLiteMigrationSet, SQLiteMigrationApplication, WorkerDeployment, WorkerCustomDomain, WorkerEndpoint, WorkerCronTrigger, QueueConsumer) with their registry package digests plus byte-pinned desired-negative fixtures. It pins each of those 14 Forms&#39; DESIRED SCHEMA as byte-digested bytes as well, because the runner materializes every probe spec against the pin rather than against what the host under test serves: a runner that took its defaults from the subject agreed with that subject about every byte it then sent, so a host publishing a default of its own passed a whole run while a real client — materializing the normative default — computed a different <code>specDigest</code> and had every <code>prepare</code> refused. <code>form-definition-exact</code> compares what a host serves against those bytes, and a repository test recomputes them from the installed Definition at the exact pinned FormRef so the pin cannot drift (<a href="./../spec/decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>). The family has one additional current Form: <code>ObjectBucket</code> is the one 15th Form intentionally unprobed by this corpus. Its wire rules remain normative, but causal host evidence for that Form requires a separate exact package fixture; this corpus makes no coverage claim for it. It also pins a byte-digested SECOND Form Definition of the ModuleWorker line, so the lane can install two contracts of one group and kind at once; without it, &quot;a host answers for the exact ref recorded in state&quot; and &quot;a host answers for the kind&quot; are the same behavior and no check can tell them apart (same decision). <code>self-test --contract conformance/portable-host-v1beta1</code> starts a deterministic reference host over the real candidate definitions and drives the complete 116-check matrix over real HTTP: exact discovery and availability, a credential that is REQUIRED — an absent <code>Authorization</code> header and a bearer credential naming nobody are both refused <code>unauthenticated</code> on a read surface and on a mutating one, and the identical requests under a real credential succeed, so a host whose credential lookup fails open cannot pick a tenant the caller never named, validate/prepare with RFC 8785 prepare binding and substitution rejection (a prepare against an existing resource requires the update generation fence, and a fence-less or stale prepare is rejected), uid minting and delete-then-recreate uid change, a replay record that does not outlive the incarnation it reports — the byte-identical create that replays while its resource is present is a NEW create with a new uid once that resource is deleted, and reads back, so <code>terraform destroy</code> followed by <code>terraform apply</code> of an unchanged configuration converges; while the delete&#39;s own record, which reports no live incarnation, still replays its 204 and does not remove the replacement that holds the name, and an accepted 202 follows the incarnation its operation committed, generation fences versus revision fences (including a host-side status touch that advances only the revision), a delete fenced on the expected generation — refused unfenced, refused stale, and accepted carrying nothing else, while the representation fence stays honored when a client sends one and is never required, so tearing an aggregate down succeeds under the generation a refresh read before the teardown moved the revision itself, packageDigest as audit-only evidence that never enters identity or queries, one kind name in two namespaced groups, revision-role update rejection, cross-resource relation resolution before mutation — every reference a Form derives from its desired schema, not only typed bindings — pinned by target uid, with <code>dependency_in_use</code> on the deletion of any referenced target, <code>ExternalChange</code> when a target is destroyed and recreated under the same name — which a spec-identical re-apply repairs, re-pinning every relation while generation stands still and only the revision moves — and binding contracts verified against allowedTargetForms, the target Form&#39;s providedInterfaces, and the source role, the exact identity a host answers for — two definition versions of one group and kind installed at once and answering independently on availability, the Form Definition surface, and the support profile, a resource answered only under the exact ref it was created under and <code>resource_not_found</code> under any other, and a relation refused before mutation when its target does not satisfy the contract the reference annotates, whether that is an exact Form identity or a required Interface, with the stored pin recording the target&#39;s exact FormRef beside its uid — import validated exactly like apply and DISTINGUISHABLE from a create: the <code>nativeId</code> an adoption names is a claim the host records on the resource and holds exclusively within the caller&#39;s tenant, so a second live resource adopting one native identity is refused <code>import_conflict</code> before any mutation — from any space of that tenant, and under any FORM KIND, because a backend object has one identity whatever Form was pointed at it and a host indexing claims by <code>(tenant, kind, nativeId)</code> lets a queue and a KV namespace manage one object between them; never across tenants, where a refusal would report what a stranger manages, while inside the second tenant&#39;s own plane the same claim binds in full. A resource already adopted cannot be re-pointed at another native identity, and the claim is released when its holder is deleted and by nothing else — an ordinary update withdraws none of it. Both paths onto a first claim are driven, because the claim is recorded by the first import whichever way the resource arrived: the ordinary <code>terraform import</code> onto a resource this host CREATED, and the adoption that brought one into being. Everything else the lane asks of an import — a minted uid, generation 1, the whole validation gauntlet — a plain create also satisfies, and <code>terraform import</code> against such a host mints a new backend object and orphans the one being adopted (<a href="./../spec/decisions/0011-resource-identity-generation-and-revision.html">decision 0011</a>; these are also the corpus&#39;s only organic producers of <code>import_conflict</code>, which was otherwise reachable only through the error probe), cross-resource semantics the Forms declare in prose but only a host can enforce (WorkerDeployment weights summing to exactly 10000 — short and long are both refused, and a real two-version split at 1000/9000 is accepted, so a host that admits only one weighted entry has no traffic split and fails; one active deployment per worker, with a replacement accepted once the live one is released, so the rule is about what is live rather than a flag nothing clears; a cron trigger or queue consumer refused with <code>unsupported_capability</code> until some WorkerVersion declares the matching handler; a WorkerEndpoint answered with a complete HTTPS address the host assigned rather than the author, in canonical form — lowercase, no trailing root dot, and a url built from exactly that hostname — the same address still published, under the same uid, after a host-side status refresh, a promotion of the worker it serves, and a re-read, and a second endpoint against one worker refused; and <code>status.outputs</code> present with exactly the declared members for the one Form that declares an outputSchema and omitted for every Form that declares none), 202 Operation polling with Retry-After plus terminal replay and cancel — with every fence, binding resolution, and blob requirement re-verified at commit time rather than at accept time, and the commit bound to the exact incarnation the mutation was accepted for, so a target removed out of band and re-created under the same name — under the same contract or under the other definition version, at the same revision — terminates the operation <code>uid_mismatch</code> and survives it, while one that simply vanished terminates it <code>resource_not_found</code> — the content-addressed artifact upload/commit flow feeding a WorkerBundle apply including its manifest reject list and commit-time size binding, the closed SpaceID grammar, support profiles free of price/SKU/region/quota, concurrent mutation of unrelated resources, and idempotency isolation across principals, tenants, and Spaces. It also drives the operability rules of <a href="./../spec/decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a>: a namespaced group served as two ordinary path segments on the resource, form-definition, and support routes, with no request target percent-encoding a slash; an operation id and an upload id answered <code>operation_not_found</code> and <code>artifact_missing</code> for every other principal and tenant while the owner&#39;s record survives untouched; and a content address that is a name rather than an entitlement — another tenant reads neither the manifest nor the blob, a second principal of the HOLDING tenant reads both, and the other tenant that uploads the same bytes is told they are missing for IT before committing the identical, physically deduplicated artifact. The same holding rule governs USING an address rather than only reading one: a bundle whose desired state references a manifest the caller&#39;s tenant does not hold is refused <code>artifact_missing</code> before any mutation, on apply and on import, storing nothing, while a second principal of the holding tenant references it successfully and the other tenant references it once it has supplied the bytes itself. The plane those three surround is held to the same boundary by <a href="./../spec/decisions/0028-the-resource-plane-is-tenant-isolated.html">decision 0028</a>: two tenants create one <code>{space, kind, name}</code> and get two resources with two uids, neither reads, observes, updates, or deletes the other&#39;s — answered <code>resource_not_found</code>, indistinguishably from a name nobody created, message included — a reference resolves only inside the referring tenant even when the name matches exactly, a <code>prepareDigest</code> minted by one tenant is not spendable by another — while a second PRINCIPAL of the minting tenant spends the same review successfully, because the boundary is the tenant — and one <code>Idempotency-Key</code> from two tenants is two operations rather than a replay, with the second tenant&#39;s resource read back so an answered key cannot stand in for an executed one. Every one of those refusals is paired with the permissive half it would otherwise be satisfied by refusing: the second tenant UPDATES and DELETES resources of its own while the first tenant&#39;s identically-named resource does not move; the holder of a resource a stranger reached for can still update and delete it, so a host that quarantines the record fails; and the relation check reaches a successful apply and reads the stored pin by what it protects — the first tenant&#39;s identically named target deletes freely, the second tenant&#39;s source stays Ready when it does, and the second tenant&#39;s own target is refused <code>dependency_in_use</code>. The one claim in the lane that stops at the tenant WITHOUT the tenant being in its address is the <code>WorkerCustomDomain</code> hostname, and it is measured in the direction that costs a mistake: a second tenant standing up its own aggregate and claiming the hostname the first tenant serves is ACCEPTED, both claims stay live under two uids, releasing one leaves the other where it was, and a third claim inside the second tenant is still refused — naming that tenant&#39;s own holder and no one else&#39;s. <code>import</code> is the one surface whose absent answer is a SUCCESS, and it is measured as one: a second tenant adopting the name the first tenant holds is answered the 201 it would be answered for a name nobody holds anywhere — same status, same ETag, same document but for the minted uid and the name — while the holder&#39;s uid, generation, and revision do not move, and the fenced form of the same adoption is refused <code>resource_not_found</code>. Nine of the ten are enumerated by SURFACE rather than by intent: every route that takes a resource name is listed against the check measuring it, and that list is bound to the published route block and to the required-check list, so a name-addressed endpoint cannot be added without one; the tenth is the permissive half of all of them. All ten are black box and all ten need the alternate-TENANT credential, which is why the runner requires it. The two attachment claims of <a href="./../spec/decisions/0026-attachment-claims-are-canonical-and-acyclic.html">decision 0026</a> are driven on all three surfaces that record names, not on <code>apply</code> alone: a colliding hostname and a cycle-closing consumer are both refused through <code>import</code> and accepted through it once the state that made them wrong is gone, and an accepted <code>202</code> that was legal when it was accepted terminates <code>invalid_argument</code> at commit when a synchronous request took the hostname, or closed the loop, while it was pending — committing nothing and leaving the synchronous resource alive. The A-label rule is driven as the refusal it is: a U-label hostname is rejected at <code>validate</code> and at <code>prepare</code> while the name is free, so nothing but the Form&#39;s grammar can be refusing it, and the A-label of the same name is accepted and stored byte-for-byte. Probing every stable error uses the runner-only <code>Takoform-Conformance-Probe</code> header (<code>error:&lt;code&gt;</code>, <code>async</code>, <code>touch-status</code>, <code>external-change</code>); it is disposable-adapter transport, never a production surface. <code>external-change</code> performs one delete as an out-of-band backend change so a runner can reach the one state relation protection otherwise makes unreachable. As with the other lanes, a passing local report is implementation evidence for the runner and reference host only: it is explicitly <code>publicationReady: false</code> and is never publication, admission, host support, or Form maturity evidence.</p><p>The executable runner is <code>go run ./cmd/portable-host-conformance</code>. Its <code>self-test</code> command starts a disposable deterministic host and drives the actual HTTP discovery, Form availability, preview, apply, read, observe, refresh, import, delete, and read-only Interface endpoints. It rejects a host that accepts a preview plan for different spec bytes, advances a generation on an exact idempotent replay, ignores a stale fence, changes an exact response/ETag on replay, or exposes a required Interface before Ready or after delete.</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/portable-host-conformance self-test</span></span></code></pre></div><p>The matrix also exercises all stable error envelopes over HTTP. Errors such as permission or backend failure have no provider-neutral way to induce, so a disposable external conformance endpoint uses the runner-only <code>Takoform-Conformance-Probe-Error</code> header. The same disposable adapter accepts the closed authorization probes <code>Takoform-Conformance-Probe-Authorization: credential-revoked</code>, <code>permission-revoked</code>, and <code>policy-revoked</code>, and must process each current denial in the normal authentication/authorization path before any replay lookup. After every denial, the runner repeats the unprobed request and requires the original cached success byte-for-byte, proving that a denial did not overwrite or poison the success record.</p><p>Plan binding has two explicitly separated evidence classes. Valid substitutions of desired spec, Space, and an existing Resource generation are pure black-box HTTP checks. The remaining one-field substitutions would ordinarily be rejected by route, envelope, or exact-Form validation before reaching plan binding, so the disposable adapter accepts the closed-enum <code>Takoform-Conformance-Probe-Plan-Binding</code> header. It passes that request directly to the same canonical plan-binding function used by normal apply and returns <code>Takoform-Conformance-Probe-Plan-Binding-Result: rejected</code> with <code>invalid_argument</code> only when that function rejects it. If the function accepts the substitution, the adapter returns HTTP 204 with <code>accepted-no-mutation</code> and must not execute a fence, mutate state, or record a replay. Unknown values fail as <code>invalid_argument</code>, and authentication and authorization run before this instrumentation. The report records pure black-box and instrumented-adapter inputs separately.</p><p>The disposable adapter also accepts <code>Takoform-Conformance-Probe-Raw-JSON: duplicate-error-code</code> and returns the contract&#39;s exact malformed error envelope. The runner must reject it during complete raw-document I-JSON validation, before decoding stable error semantics. Unknown raw-probe values fail as <code>invalid_argument</code>.</p><p>All four probe headers are disposable-adapter transport, are not part of the host API, and must not be implemented or enabled on production. The first two token variables must name distinct authenticated principals in the same tenant. The third must represent the first principal in another tenant. All three credentials must be distinct and authorized for the complete test lifecycle in both exact runner Spaces. For the cross-tenant isolation probe only, both tenant contexts must address the same disposable Resource namespace; an independently executed create then deterministically collides instead of returning the first tenant&#39;s cached success. Run the same matrix against such an endpoint with:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/portable-host-conformance run \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --endpoint https://disposable-host.example \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --token-env TAKOFORM_CONFORMANCE_TOKEN \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --alternate-token-env TAKOFORM_CONFORMANCE_ALTERNATE_TOKEN \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --alternate-tenant-token-env TAKOFORM_CONFORMANCE_ALTERNATE_TENANT_TOKEN</span></span></code></pre></div><p>Both reports are explicitly <code>publicationReady: false</code>. The local reference report proves the executable runner, not an external host. Admission still requires a signed report from the host workflow that actually executed its backend lifecycle. Desired-stage negative fixtures are host request evidence; observed-stage rejection is provider/host-response evidence because the portable API intentionally has no operation for injecting observed state.</p><p>The required sequence includes create collision, mutable update, stale update/delete/observe/refresh rejection, exact replay, retry-code semantics, the fixed stable-code/HTTP mapping, the exact <code>1..9223372036854775807</code> decimal generation range, and Interface readiness. It also rejects plan substitution field by field for desired spec, Resource identity/name, Space, generation, every exact FormRef field, and package digest. Name instrumentation uses the matching alternate Resource URL, while generation uses a real update after the Resource has advanced, so neither can pass because of a create fence or URL/body mismatch. Replay evidence proves that another principal cannot address the first principal&#39;s cached success, that the same principal in another tenant cannot address it, and that current authentication, permission, and policy denial each precede replay lookup. The same Resource name and exact Form are then created and read in <code>runnerInput.alternateSpace</code> with the primary Space&#39;s Idempotency-Key; neither Resource state nor replay records may cross the Space boundary.</p><p>Before lifecycle mutation, raw adversarial requests additionally prove that unknown top-level envelope fields, unknown metadata fields, authority-shaped desired fields, duplicate <code>metadata.space</code>, duplicate <code>metadata.resourceVersion</code>, duplicate <code>spec</code>, and invalid UTF-8 fail closed. The duplicate cases are rejected by shared complete-document I-JSON validation before typed decoding. The report also records the three replay isolation dimensions, the three current denial codes, and preservation of the original successful replay after every denial.</p><p><code>interfaceDeclarations.checks</code> is an exact executed subset of <code>requiredRunnerChecks</code>, and the report repeats that exact list as Interface evidence. Over real HTTP the runner rejects unknown, duplicate, and partially paired Interface query parameters; requires an explicit Space without substituting another one; exercises omitted-version success only for a unique visible declaration; creates a second Ready Resource in the same Space to require <code>interface_instance_ambiguous</code>; and re-reads an unchanged projection after POST, PUT, PATCH, and DELETE are rejected on both portable Interface routes. The visible declaration must equal the exact Form descriptor, satisfy its <code>documentSchema</code>, contain no portable authority fields, appear only with a Ready Resource, and disappear after deletion. When present, <code>resourceUri</code> must pass the shared credential-free HTTPS grammar; the adversarial runner rejects userinfo, query, fragment, plaintext HTTP, and Unicode-hostname substitutions.</p><p>The unprobed <code>ObjectBucket</code> Form has one required descriptor at one version in its retained fixture, so this runner does not claim that it induced multi-version identity ambiguity, optional-descriptor omission, or activation rejection on a feature-disabled host. Its wire rules remain normative, and the stable ambiguity error envelopes are exercised, but causal host evidence for those cases needs a separate exact package fixture.</p><p>It treats drift only as validated observed evidence and defines no drift operation or portable audit endpoint.</p><h2 id="runtime-abi-evidence" tabindex="-1">Runtime ABI evidence <a class="header-anchor" href="#runtime-abi-evidence" aria-label="Permalink to &quot;Runtime ABI evidence&quot;">​</a></h2><p><code>runtime-abi-v1/</code> measures a different subject from every corpus above. Those drive a control plane; this one drives a <strong>runtime</strong>. The Host API v1beta1 lane can prove that a host advertises <code>worker.runtime@1.0.0</code> at the exact pinned digest and refuses a <code>WorkerVersion</code> that names a handler the ABI does not define, and <a href="./../spec/host-api/v1beta1.html#what-the-lane-proves-and-what-stays-a-host-obligation"><code>../spec/host-api/v1beta1.md</code></a> lists what it cannot prove, because proving it means executing the module rather than driving the API. This corpus executes it (<a href="./../spec/decisions/0023-the-runtime-abi-is-measured-separately-from-the-control-plane.html">decision 0023</a>).</p><p>The corpus is digest-pinned the same way <code>portable-host-v1beta1/</code> is: <code>manifest.json</code> carries the sha256 of <code>contract.json</code>, and <code>contract.json</code> additionally pins the sha256 of every module byte it ships. It states, as data, the exact <code>worker.runtime</code> InterfaceRef it measures, the closed handler vocabulary, the closed loadable media-type set, the portable globals floor, the deployment an operator reproduces — bundle, declared handlers, vars, sensitive variable, <code>edge.kv</code>, <code>edge.queue</code> and <code>worker.service</code> bindings, cron expression, queue, and the SECOND worker the service binding addresses — and for each of the 18 required checks its name, what it proves, the bundle it needs, and the request/expected-observation pairs that decide it. It pins a second InterfaceRef beside the runtime ABI&#39;s, <code>worker.service@1.0.0</code>, because two of those checks are claims about that contract&#39;s delivery model and a corpus naming a contract it does not pin can go on measuring one that no longer exists.</p><p>Five checks drive the module loader: a bundle whose bytes load and report the handler set THOSE BYTES export, a module media type outside the closed set, a <code>mainModule</code> the bundle does not carry, bytes that are not a compilable module, and a declared handler the module does not export — the last being exactly the obligation the Host API lane states it cannot prove for arbitrary bytes. Thirteen drive a deployed worker over HTTP: the three-argument handler signature, a returned <code>Response</code>, an uncaught throw becoming a completed host-generated 500 rather than a hung request, <code>env</code> projecting exactly the declared names, the globals floor, a byte round trip through the <code>edge.kv</code> binding, a request body answered chunk by chunk before the next chunk is sent, a response body whose chunks arrive separated in time, <code>ctx.waitUntil</code> holding the isolate open while a rejected task leaves the sent response alone, a <code>scheduled</code> invocation from the cron attachment, and a queue batch delivered to the <code>queue</code> handler with the producer&#39;s exact bytes. Two more take the streaming pair one worker further along: the same request-body and response-body observations, made THROUGH the <code>module-worker.service</code> binding into a SECOND worker, which is what holds the projection to the streaming model <code>worker.service@1.0.0</code> states.</p><p>That second worker has to be distinguishable from the first, and stating that it exists is not enough. While the peer ran the caller&#39;s own byte-pinned bundle, a host that answered <code>env.PEER.fetch(...)</code> out of the caller&#39;s fetch handler produced the same accounting, the same chunk timing and the same status as the dispatch it never performed, so both checks passed a runtime with no cross-worker projection at all — a required check no incorrect runtime can fail. The peer therefore runs its own bundle, <code>conformance-peer</code>, whose module declares an identity the caller&#39;s bytes do not contain and stamps it on every observation it emits; the runner credits a service check only for stamped answers. The loader enforces both halves of that: the identity must be derivable from the peer module an operator deploys, and it must appear in no other bundle of the corpus, so an answer the caller could have produced is refused. Which checks require it is pinned in the runner beside the required check list, so dropping the marker fails verification rather than quietly restoring the defect.</p><p>The response-stream procedure also holds the response HEAD to the body that follows it. <code>worker.service</code> spells an unknown length as a null <code>contentLength</code> precisely because the call completes at the head, where a body still being generated has no byte count; the two ways a host can refuse to say so are both refused here. A host that BUFFERS the body to learn a count delivers chunks the producer separated in time all at once and fails the separation. A host that INVENTS one is held to it: the runner reads the body to its end and requires a declared length to be the length delivered.</p><p>Every handler the ABI declares is measured, and the loader ENFORCES that rather than the corpus asserting it: a handler in <code>handlerVocabulary</code> with no check whose <code>operation</code> names it — or with only an <code>unmeasured</code> entry, which is what <code>tail</code> used to have — fails corpus verification by name. That is why <code>tail</code> left the ABI rather than staying as an entry nothing could reach (<a href="./../spec/decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>).</p><p>The bundles carry module bytes and the handlers those bytes genuinely export, and loading the corpus recomputes every stated outcome from the bytes. A bundle that claims an export its module does not have is refused, and so is a check expecting a failure the bytes cannot cause: a required check no correct runtime can pass, and one no incorrect runtime can fail, are the same defect.</p><p>Three checks read back an observation the runtime stored for them — the <code>edge.kv</code> round trip, the queue delivery and the <code>ctx.waitUntil</code> marker — and the deployment they are measured against outlives the run, <code>edge.kv</code> namespace and all. A pinned constant cannot correlate a run and a per-run value cannot be pinned, so what the corpus pins is the correlation TEMPLATE: <code>runCorrelation</code> states the placeholder and the token width, each of the three checks states a template such as <code>kv-round-trip-{run}</code>, and the runner mints one unpredictable token per run, substitutes it into the value it sends, and derives the observation it expects by the same substitution. The corpus bytes never move, the token is never in them, and the report states the token a run used so a failure stays diagnosable afterwards. Pinning those values as constants instead produced both halves of the same defect at once: on a second run a runtime whose <code>put</code> stored nothing and whose queue delivered nothing passed on the first run&#39;s leftovers, and a conforming runtime failed because the <code>waitUntil</code> marker was already there before its deferred task had run.</p><p><code>self-test</code> runs the whole matrix against an in-process stand-in shipped with this repository, so the corpus is exercised on every <code>bun run check</code>. The stand-in has no JavaScript engine and reimplements each corpus module&#39;s behaviour in Go; what keeps it honest is that it is constructed from the deployment description and the module bytes and nothing else, so it never sees a check or an expected observation. The peer&#39;s identity is part of that: the stand-in reads it out of the main module it was handed, the way it reads the exported handler set, so a stand-in wired to answer service calls from the caller stamps nothing and fails the two service checks exactly as a real host doing the same thing would. A self-test report says this in the document itself: its <code>classification</code> is <code>in-process-fake-runtime-self-test</code> and its <code>proves</code> member states that it proves the corpus and nothing about any runtime.</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/runtime-conformance verify</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/runtime-conformance self-test</span></span></code></pre></div><p>Measuring a real runtime means deploying the corpus bundle exactly as the contract&#39;s <code>deployment</code> states and pointing the runner at it. <code>loadModule</code> decides its outcome before any traffic arrives, so the load lane is measured through a disposable adapter over the runtime&#39;s own module loader (<code>takoform.runtime-abi-loader@v1</code>); the adapter is not part of the ABI and must never be exposed by a production deployment. Without one the load lane is reported <code>unmeasured</code> and the run <code>partial</code>, rather than counting an unmeasurable half as evidence.</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/runtime-conformance run \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --endpoint https://conformance-worker.example \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --loader-endpoint https://disposable-loader.example/load \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --token-env TAKOFORM_RUNTIME_CONFORMANCE_TOKEN \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --loader-token-env TAKOFORM_RUNTIME_LOADER_TOKEN</span></span></code></pre></div><p>Both reports are explicitly <code>publicationReady: false</code>, as in every other lane. Publication blocker V3-008 closes on a passed <code>deployed-runtime-conformance-run</code> with its load lane measured, against a runtime this repository does not own; a self-test never closes it, and each report repeats that sentence in its own <code>blockerEvidence</code> member.</p>`,39)])])}const m=t(r,[["render",s]]);export{u as __pageData,m as default};
