import{_ as o,o as a,c as t,ag as i}from"./chunks/framework.cyF4XceO.js";const h=JSON.parse('{"title":"Takoform portable specification","description":"","frontmatter":{},"headers":[],"relativePath":"spec/overview.md","filePath":"spec/overview.md"}'),r={name:"spec/overview.md"};function n(s,e,c,d,l,p){return a(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="takoform-portable-specification" tabindex="-1">Takoform portable specification <a class="header-anchor" href="#takoform-portable-specification" aria-label="Permalink to &quot;Takoform portable specification&quot;">​</a></h1><p>This directory is the portable specification surface for Takoform. It contains the <strong>Specification 1.1 candidate</strong> and the literal Host API v1 source candidate. Host API v1 is a separate, unpublished protocol identity; publishing Specification 1.1 does not publish or promote it. The numbered release remains open until its exact committed normative source snapshot closes. Takoform defines a small desired-state boundary between infrastructure-as-code clients and resource hosts. It is not an industry standards body, certification authority, or guarantee of backend portability.</p><p>Requirement keywords, conformance classes, and what a passing check does and does not prove are defined in <a href="./conformance.html"><code>conformance.md</code></a>. How the API group, Forms, packages, and the provider are versioned is in <a href="./versioning.html"><code>versioning.md</code></a>. Current project positioning and the Proposal → Experimental → Stable → Legacy lifecycle are defined in <a href="./project-lifecycle.html"><code>project-lifecycle.md</code></a>. The exact boundary between portable workload semantics and host/profile/operator concerns is <a href="./portability-boundary.html"><code>portability-boundary.md</code></a>.</p><h2 id="product-contract-map" tabindex="-1">Product contract map <a class="header-anchor" href="#product-contract-map" aria-label="Permalink to &quot;Product contract map&quot;">​</a></h2><p>Takoform has five public contract interfaces:</p><ol><li><strong>Exact Form and Package data.</strong><a href="./form-definition/"><code>form-definition/</code></a> defines immutable <code>FormRef</code> and the desired/observed/output shape. <a href="./form-package/"><code>form-package/</code></a> binds one exact definition and its data-only fixtures into immutable package bytes under the current <code>packages.forms.takoform.com/v1alpha5</code> envelope. <a href="./core/"><code>core/</code></a> defines the family-neutral compilation of admitted package semantic closures into one immutable exact-identity Snapshot.</li><li><strong>Desired Resource lifecycle.</strong><a href="./host-api/"><code>host-api/</code></a> defines discovery, exact Form availability, preview/apply, read/import/observe/refresh/delete, fencing, and portable errors on the <code>forms.takoform.com/v1</code> wire, reached through <code>/.well-known/takoform/v1</code>. The host chooses implementation and placement.</li><li><strong>Form Families and their contract surfaces.</strong><a href="./form-families.html"><code>form-families.md</code></a> defines namespaced Form Family groups; current families use versionless groups such as <code>edge.forms.takoform.com</code>. <a href="./host-api/v1.html"><code>host-api/v1.md</code></a> defines the Host API channel with UID/generation/revision resource identity, long-running Operations, and Host Support Profiles.</li><li><strong>Interface, Binding, and artifact contracts.</strong><a href="./interface-contract/"><code>interface-contract/</code></a> defines exact digest-bound Interface contracts, <a href="./binding-contract/"><code>binding-contract/</code></a> defines typed Binding contracts, <a href="./artifact-transport/"><code>artifact-transport/</code></a> defines content-addressed artifact upload, and <a href="./standard-services/"><code>standard-services/</code></a> defines Host-resolved sealed-slot access by opaque <code>standards.takoform.com/v1</code> reverse-DNS protocol identity.</li><li><strong>Trust, lifecycle, version, and release identity.</strong><a href="./trust/"><code>trust/</code></a> defines immutable publisher evidence and revocation; <a href="./project-lifecycle.html"><code>project-lifecycle.md</code></a> separates Form maturity from Host Support and availability; <a href="./versioning.html"><code>versioning.md</code></a> keeps provider, API, Form, and package compatibility independent. <a href="./../release/"><code>release/</code></a> binds artifacts to those exact identities without changing the contracts above.</li></ol><p><a href="./schemas/"><code>schemas/</code></a>, <a href="./conformance.html"><code>conformance.md</code></a>, and <a href="./decisions/"><code>decisions/</code></a> support those interfaces with structural minima, executable evidence language, and decision rationale. They are not additional product interfaces. The generated current inventory is <a href="./../forms/"><code>../forms/README.md</code></a>, host discovery validation is <a href="schemas/host-discovery-v1.schema.json"><code>schemas/host-discovery-v1.schema.json</code></a>, and the local evidence map is <a href="./../conformance/"><code>../conformance/README.md</code></a>.</p><h2 id="current-status" tabindex="-1">Current status <a class="header-anchor" href="#current-status" aria-label="Permalink to &quot;Current status&quot;">​</a></h2><p>Specification 1.1 is the first numbered release. Identity 1.0 was never published, is withdrawn, and is never reused. The separately generated five-class compatibility report at <a href="../release/specification-compatibility.json"><code>../release/specification-compatibility.json</code></a> binds raw source bytes to owning ledgers and migration dispositions and byte-pins the literal Host API v1 candidate. It is compatibility evidence only, not publication evidence, a release asset, or a prerequisite. Current Forms, packages, and Host API v1 remain unpublished candidates; no <code>/v1.1</code> or v2 lane/schema/tag/receipt is created.</p><h2 id="c1-c2-and-c3-release-boundaries" tabindex="-1">C1, C2, and C3 release boundaries <a class="header-anchor" href="#c1-c2-and-c3-release-boundaries" aria-label="Permalink to &quot;C1, C2, and C3 release boundaries&quot;">​</a></h2><p>The W09 workflow keeps three commits distinct:</p><ul><li><strong>C1 — normative freeze:</strong> the normative <code>spec/</code> tree and executable validation tooling are frozen; every publication-evidence field remains <code>null</code>.</li><li><strong>C2 — source evidence:</strong> an evidence-only change records one exact committed Specification source snapshot and its static projection. Candidate corpus, reference conformance, Provider, Host, and compatibility-report data remain outside this evidence record.</li><li><strong>C3 — publication receipt:</strong> a later authoritative receipt appends one immutable Specification 1.1 entry to the numbered ledger and its projections; it carries only the source-snapshot prerequisite and live publication readback.</li></ul><p>The compatibility report is generated and checked separately. It is never a C2 or C3 asset, prerequisite, or release identity.</p><p>The FormRef, Form Definition, package-index, revocation, and cumulative revocation-checkpoint schemas, the RFC 8785/I-JSON library, the closed local verifier, the positive/negative corpus, the protected keyless Sigstore release lane, and the signed append-only checkpoint delivery lane are implemented.</p><p>The neutral Core tracer compiles zero families, synthetic external groups, and the generated current package set through artifact bytes without importing an official family. It remains an internal pre-extraction implementation; it is not a public SDK or independent module release.</p><p>Current Form design work uses eight versionless namespaced Form Family groups (<a href="./form-families.html"><code>form-families.md</code></a>) and 31 exact Experimental <code>0.x</code> FormRefs. Edge contains 16 and has no current <code>ObjectBucket</code>; packages use <code>packages.forms.takoform.com/v1alpha5</code>. Package publication, Form maturity, Host API version, Specification release, and Provider release are independent. A repository implementation or local passing gate is not Form publication, Host Support, activation, or live availability.</p><p>Two pre-Beta epochs preceded this one and were <strong>withdrawn</strong> while Takoform is pre-Stable (<a href="./decisions/0042-the-pre-beta-epochs-are-withdrawn.html">decision 0042</a>). Decision <a href="./decisions/0004-takoform-is-an-experimental-specification.html"><code>0004</code></a> made the previously published <code>forms.takoform.com/v1alpha1</code> line Legacy after it was labelled <code>standard</code> without sufficient independent implementation and operational evidence; decision <a href="./decisions/0006-v1alpha2-restarts-form-lines.html"><code>0006</code></a> restarted selected kinds in the distinct v1alpha2 epoch. Both epochs&#39; served identities — wire lanes, schema addresses, corpus and candidate documents — are recorded as retired in <a href="../release/published-document-lanes.json"><code>../release/published-document-lanes.json</code></a> and <a href="../release/public-schema-identities.json"><code>../release/public-schema-identities.json</code></a>, so a withdrawn address can never quietly answer again meaning something else. Their bytes stay verifiable in this repository&#39;s git history and release tags; the <code>formpackage</code> verifier deliberately keeps every epoch&#39;s schema for that purpose. Historical <code>standard</code> and <code>portable-standard</code> fields in those immutable bytes do not define a current approved subset, and nothing derives current approval or admission from that history.</p><p>The literal Host API v1 candidate uses wire <code>forms.takoform.com/v1</code>, reached through <code>/.well-known/takoform/v1</code> with API root <code>/apis/forms.takoform.com/v1</code>. The Specification release assertion remains open while its committed evidence pointers are null. The Host API group is a protocol compatibility identity independent of every Form group and Form maturity; Specification 1.1 publication does not publish or promote this Host API candidate. The current package envelope is <code>packages.forms.takoform.com/v1alpha5</code>; Interface and Binding refs remain <code>interfaces.takoform.com/v1alpha1</code> and <code>bindings.takoform.com/v1alpha2</code>. The Terraform provider identity is <code>registry.terraform.io/tako0614/takoform</code>; current Provider <code>v3.0.0</code> is Registry-published for the 31 current Forms, while <code>v2.1.1</code> on retained Host API <code>forms.takoform.com/v1beta1</code> is immutable history. Both are independent from all of these current API identities. The <code>release/version.json</code> descriptor remains <code>candidate-only</code> metadata by design after owner publication.</p><p>Provider 3 is a separate Registry-published, non-normative reference implementation. It may implement the exact current <code>0.x</code> Forms without defining their semantics or blocking Specification 1.1. Publishing Specification 1.1 does not promote those Forms to <code>1.0.0</code>; publishing the separate Host API v1 candidate would not do so either. A future Stable Form requires an explicit per-Form decision.</p><h2 id="normative-consistency-audit" tabindex="-1">Normative consistency audit <a class="header-anchor" href="#normative-consistency-audit" aria-label="Permalink to &quot;Normative consistency audit&quot;">​</a></h2><p><code>go test ./spec</code> is the cross-specification contradiction gate. It does not repeat the Form Package verifier, provider schema tests, or portable-host runner. Instead, it joins their machine-readable inputs and fails when:</p><ul><li>host operations, mutation fences, idempotency, or the stable error taxonomy disagree with the portable-host conformance contract;</li><li>the portable API identity, provider candidate version, or canonical provider FQN diverges between release, schema, trust, and conformance locks; or</li><li>a normative active Form, package, schema, or host contract leaks a concrete backend vocabulary such as Cloudflare/Workers configuration.</li></ul><p>The complete repository gate, <code>bun run check</code>, runs this audit together with the deeper package-byte, provider-schema, and generated-surface verifiers. Passing it remains local evidence only; it does not prove Registry publication, Host Support, Form maturity, production activation, or interoperability.</p>',23)])])}const f=o(r,[["render",n]]);export{h as __pageData,f as default};
