import{_ as t,o as a,c as r,ag as i}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"0042 — The pre-Beta epochs are withdrawn","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0042-the-pre-beta-epochs-are-withdrawn.md","filePath":"spec/decisions/0042-the-pre-beta-epochs-are-withdrawn.md"}'),o={name:"spec/decisions/0042-the-pre-beta-epochs-are-withdrawn.md"};function s(n,e,d,h,c,l){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="_0042-—-the-pre-beta-epochs-are-withdrawn" tabindex="-1">0042 — The pre-Beta epochs are withdrawn <a class="header-anchor" href="#_0042-—-the-pre-beta-epochs-are-withdrawn" aria-label="Permalink to &quot;0042 — The pre-Beta epochs are withdrawn&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-22</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The repository carried three generations at once: the Legacy <code>forms.takoform.com/v1alpha1</code> epoch (34 published Form Package identities, the admission evidence trees, the provider-v1 lane), the <code>forms.takoform.com/v1alpha2</code> epoch (nine retained candidates, the provider-v2 lane, the v1alpha3 identities its provider carried), and the current Beta generation. The first two were retained as frozen history under the rule that published identities are never overwritten.</p><p>Retention is not free. The retained generations were most of the repository&#39;s surface — candidate trees, release inventories, admission ledgers, lifecycle registries, conformance corpora, schemas, provider code paths, publication gates — and every one of those surfaces had to keep meaning what it meant, which the gates could only partially check. Measured in this change set: two retained documents had already changed meaning undetected, the word <code>current</code> named two different generations depending on which file pinned it, and the retained machinery (admission checkpoints, lifecycle registry, publication truth, Registry readback tooling, the Legacy package release train) existed only to keep asserting facts about identities no new work may use.</p><p><a href="./0037-immutability-begins-at-stable.html">Decision 0037</a> settled the authority question: <strong>immutability begins at Stable.</strong> A pre-Stable identity may be withdrawn, provided the withdrawal is recorded — never silently, and never by reusing the address for something else. Takoform is Experimental; both pre-Beta epochs are below that line.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Both pre-Beta epochs are withdrawn.</strong> The Legacy v1alpha1 epoch, the v1alpha2 epoch, the v1alpha3 identities that epoch&#39;s provider served, and every tree that existed only to retain them: candidate sets, release inventories and plans, the lifecycle registry, admission evidence and its identity ledger, the withdrawn lanes&#39; schemas, operation tables, normative documents, conformance corpora, provider resources and data sources, and the publication-truth machinery derived from the admission evidence.</p><p><strong>What a withdrawal is.</strong> Every served identity moves to the <code>retired</code> list of the ledger that owns it — document addresses in <a href="../../release/published-document-lanes.json"><code>../../release/published-document-lanes.json</code></a>, schema <code>$id</code>s in <a href="../../release/public-schema-identities.json"><code>../../release/public-schema-identities.json</code></a> — with the bytes it had and the reason. A retired identity is never reused: the ledgers fail closed when a retired address answers again or a retired <code>$id</code> reappears under different bytes. The bytes themselves stay in this repository&#39;s git history and in the immutable <code>forms/*</code> release tags.</p><p><strong>What is deliberately kept:</strong></p><ul><li>The <code>formpackage</code> verifier keeps embedded copies of every epoch&#39;s schemas, so the package bytes retained in history and tags stay verifiable forever. A withdrawal removes service, not the ability to read what was served.</li><li>The revocation delivery lane (<code>.github/workflows/form-package-revocation.yml</code> and its deploy phases) stays standing: published bytes remain revocable after their epoch is withdrawn.</li><li>The published provider releases <code>v1.0.3</code>, <code>v2.0.0</code>, and <code>v2.1.1</code> are immutable Registry history and are not touched. <code>v2.1.1</code> remains the current published provider; the nine v1alpha2 resources it carries are simply without successors in this repository.</li><li>The keyless package-publisher identity (<code>.github/workflows/form-package-release.yml@refs/heads/main</code>) stays pinned in the trust profile. The workflow file itself is removed — its plan source was withdrawn, and <a href="./0041-form-packages-publish-with-the-provider-release.html">decision 0041</a> moved package publication onto the provider release train — but the identity is path-addressed, so the rebuilt publication step recreates it at the same path and historical signatures keep verifying.</li></ul><p><strong>What this forces next.</strong> The provider built from this repository exposes only the 15 Family resources, so the next published release MUST be a major, <code>3.0.0</code>; <a href="./../../release/migrations/v2-to-v3.html"><code>../../release/migrations/v2-to-v3.md</code></a> is the migration contract for users of the nine. The signed Registry readback lane was epoch-bound tooling and is retired with it; the <code>v2.1.1</code> readback closure is retained in <code>release/provider-release-identities.json</code>, and the <code>3.0.0</code> release must bring a readback lane matched to its own surface before it can be called Registry-verified.</p><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p>The withdrawal is recorded where machines look, not only where people read:</p><ul><li>both ledgers hold the retired identities and refuse their reuse, and the ledger checks walk committed history so an entry cannot be deleted from both the tree and the ledger in one edit;</li><li>the lane and envelope tables in <code>scripts/check-public-surfaces.mjs</code> keep a row per withdrawn identity, require each row to state why it was withdrawn, and fail when a withdrawn identity still names bytes on disk;</li><li>the current-lane residue check keeps rejecting withdrawn-lane identifiers in current implementation code, so the withdrawal cannot quietly reverse.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>One generation remains: <code>forms.takoform.com/v1beta1</code>, <code>edge.forms.takoform.com/v1beta1</code>, <code>packages.forms.takoform.com/v1alpha4</code>, provider <code>2.1.1</code> published and <code>3.0.0</code> next. The word <code>current</code> has one referent again. The cost is that recovering a withdrawn epoch&#39;s contract now requires git history rather than the working tree — which is the point: the tree states what is, history states what was.</p>',17)])])}const u=t(o,[["render",s]]);export{m as __pageData,u as default};
