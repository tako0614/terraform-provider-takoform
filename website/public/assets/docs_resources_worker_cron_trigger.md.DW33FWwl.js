import{_ as o,o as r,c as t,a2 as a}from"./chunks/framework.BTLSZHNB.js";const p=JSON.parse('{"title":"takoform_worker_cron_trigger","description":"Worker Cron Trigger (edge.forms.takoform.com/v1alpha1, role attachment).","frontmatter":{"page_title":"takoform_worker_cron_trigger Resource - takoform","subcategory":"Edge Platform Family","description":"Worker Cron Trigger (edge.forms.takoform.com/v1alpha1, role attachment)."},"headers":[],"relativePath":"docs/resources/worker_cron_trigger.md","filePath":"docs/resources/worker_cron_trigger.md"}'),i={name:"docs/resources/worker_cron_trigger.md"};function n(c,e,s,d,l,h){return r(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="takoform-worker-cron-trigger" tabindex="-1">takoform_worker_cron_trigger <a class="header-anchor" href="#takoform-worker-cron-trigger" aria-label="Permalink to &quot;takoform_worker_cron_trigger&quot;">​</a></h1><p>Attaches one cron schedule to a Module Worker, invoking its scheduled handler at each match. Schedules are interpreted in UTC only; there is no timezone field, so two hosts can never fire the same trigger at different instants. The accepted grammar is exactly five single-value fields separated by single spaces: minute is a literal 0-59 and hour a literal 0-23, day-of-month is <code>*</code> or 1-31, month is <code>*</code> or 1-12, and day-of-week is <code>*</code> or 0-6. Ranges, lists, steps such as <code>*/5</code>, names, and <code>*</code> in the minute or hour field are all rejected, so the most frequent representable schedule is once per day at one fixed UTC time. Hourly and sub-hourly schedules are not expressible and need a future grammar revision, which is a new definition version of this Form.</p><p>This is an <code>attachment</code> resource: it connects a parent to inward activation (routes, domains, schedules, queue consumption). Deleting the attachment never deletes the parent.</p><p>This resource speaks the Host API v1alpha3 lane and requires provider v2.1.0 or later (source candidate; not yet published). The configured host selects and operates the concrete backend; no attribute names a vendor, target, credential, price, or implementation. See the <a href="../../examples/resources/takoform_worker_cron_trigger/resource.tf">complete example</a>.</p><h2 id="arguments" tabindex="-1">Arguments <a class="header-anchor" href="#arguments" aria-label="Permalink to &quot;Arguments&quot;">​</a></h2><ul><li><code>name</code> (String, required, forces replacement) — Portable resource name (<code>metadata.name</code>).</li><li><code>worker</code> (String, required, forces replacement) — Module Worker whose scheduled handler this trigger invokes. Set the name of the target <code>ModuleWorker</code> resource.</li><li><code>cron</code> (String, required) — UTC cron schedule in the enforced closed grammar: exactly five single-value fields separated by single spaces — minute <code>0</code>-<code>59</code> and hour <code>0</code>-<code>23</code> are literal numbers (<code>*</code> is not accepted there), day-of-month is <code>*</code> or <code>1</code>-<code>31</code>, month is <code>*</code> or <code>1</code>-<code>12</code>, and day-of-week is <code>*</code> or <code>0</code>-<code>6</code>. Ranges, lists, steps (such as <code>*/5</code>), and names are not accepted.</li><li><code>space</code> (String, optional, forces replacement) — Exact opaque SpaceID; overrides the provider default.</li><li><code>create_timeout</code> / <code>update_timeout</code> / <code>delete_timeout</code> (String, optional) — Go durations bounding each operation (defaults <code>20m</code> / <code>20m</code> / <code>30m</code>).</li></ul><h2 id="read-only-attributes" tabindex="-1">Read-only attributes <a class="header-anchor" href="#read-only-attributes" aria-label="Permalink to &quot;Read-only attributes&quot;">​</a></h2><ul><li><code>uid</code> — host-issued immutable resource identity; delete and re-create yields a new UID.</li><li><code>generation</code> — desired-state generation; increments only when the portable desired spec changes. Updates fence on it.</li><li><code>revision</code> — representation revision; increments whenever the representation changes — a spec-changing update, new status, or new outputs. Deletes fence on it via <code>If-Match</code>.</li><li><code>ready</code> — true when the closed <code>Ready</code> condition reports <code>True</code>.</li><li><code>outputs_json</code> — JSON-serialized <code>status.outputs</code> document (<code>&quot;{}&quot;</code> when empty).</li><li><code>form_api_version</code>, <code>form_kind</code>, <code>form_definition_version</code>, <code>form_schema_digest</code> — the exact immutable FormRef this state is bound to; reads dispatch on it.</li><li><code>form_package_digest</code> — audit-only package provenance; never part of resource identity, queries, or fences.</li><li><code>relation_drift_reason</code> — internal recovery only: <code>ExternalChange</code> or <code>DependencyMissing</code> while the host reports that a resource this one references was replaced or removed out of band, null otherwise. A refresh reports the break as a warning and keeps the resource in state; the next plan then proposes an in-place re-apply of the same desired state, which is all a host needs to re-resolve and re-pin every reference. It is provider-side recovery bookkeeping — no portable wire member carries it — and configurations must not depend on it.</li></ul><h2 id="import" tabindex="-1">Import <a class="header-anchor" href="#import" aria-label="Permalink to &quot;Import&quot;">​</a></h2><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_worker_cron_trigger.example NAME</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_worker_cron_trigger.example SPACE/NAME</span></span></code></pre></div>`,10)])])}const m=o(i,[["render",n]]);export{p as __pageData,m as default};
