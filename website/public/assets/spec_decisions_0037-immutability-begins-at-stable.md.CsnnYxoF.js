import{_ as t,o as a,c as i,ag as n}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"0037 — Immutability begins at Stable; a pre-Stable identity may be withdrawn","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0037-immutability-begins-at-stable.md","filePath":"spec/decisions/0037-immutability-begins-at-stable.md"}'),s={name:"spec/decisions/0037-immutability-begins-at-stable.md"};function r(o,e,d,l,c,h){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="_0037-—-immutability-begins-at-stable-a-pre-stable-identity-may-be-withdrawn" tabindex="-1">0037 — Immutability begins at Stable; a pre-Stable identity may be withdrawn <a class="header-anchor" href="#_0037-—-immutability-begins-at-stable-a-pre-stable-identity-may-be-withdrawn" aria-label="Permalink to &quot;0037 — Immutability begins at Stable; a pre-Stable identity may be withdrawn&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-22</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><a href="./../versioning.html"><code>versioning.md</code></a> promised that &quot;every released <code>0.x</code> identity remains immutable&quot;. Read as &quot;never overwritten&quot; that rule is right and stays. Read as &quot;served forever&quot; it committed a pre-Stable project to carrying every generation it had ever produced, and that is what it was being read as.</p><p>The cost is visible. Four Host API lanes, four Form groups, four package envelopes and three provider lines are alive at once; 169 published documents declare ten different lanes; the same <code>vN</code> shape appears on seven independent axes so that the word &quot;v3&quot; does not identify anything without saying which axis it belongs to. None of that is an accident of naming — it is the retention rule working exactly as written, applied to material that never earned it.</p><p>The repository&#39;s own records say the material never earned it:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>forms/candidates/v1alpha2/candidate-set.json   publicationStatus: &quot;unpublished&quot;</span></span>
<span class="line"><span>forms/lifecycle.json                           currentForms: []</span></span>
<span class="line"><span>forms/README.md   &quot;none is published, Experimental, Stable, centrally approved,</span></span>
<span class="line"><span>                   or guaranteed commercially available&quot;</span></span></code></pre></div><p><code>currentForms</code> has been empty in every revision since the file was created. The nine retained <code>forms.takoform.com/v1alpha2</code> Forms are Proposal-derived candidates that never reached Experimental, and Stable-grade permanence was being spent on them.</p><p>Three facts decide how much a withdrawal actually costs, and all three were measured rather than assumed:</p><ul><li><strong>A released provider does not fetch what would be withdrawn.</strong><code>formpackage/schema.go</code> embeds the schemas with <code>go:embed</code>; nothing in <code>internal/provider</code>, <code>internal/client</code>, or <code>internal/clientv3</code> requests <code>https://forms.takoform.com/schemas/...</code> at runtime. An installed provider is self-contained.</li><li><strong>The bytes are not destroyed.</strong> They stay in this repository&#39;s history and under 78 immutable <code>forms/*</code> tags. Removing something from the working tree and the served site is not the same act as destroying it.</li><li><strong>Nothing needs to be unpublished from the Terraform Registry.</strong> Provider <code>v1.0.3</code>, <code>v2.0.0</code> and <code>v2.1.1</code> are immutable there, keep working, and are unaffected.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Permanence begins at Stable.</strong> A Stable identity is served for as long as the project exists, under the rules already written. A Proposal or Experimental identity carries no such promise and MAY be withdrawn.</p><p><strong>&quot;Never overwritten&quot; is unchanged and has no exceptions.</strong> While an identity is served it means exactly what it meant when it was published. A breaking correction mints a new identity. This is the rule that makes an exact FormRef worth recording in client state, and withdrawal does not touch it.</p><p><strong>A withdrawal is a recorded act, never a silence.</strong> The identity moves to the <code>retired</code> list of the ledger that published it, keeping the bytes and the lane it was published with, and saying why. The ledger therefore still answers what that address meant after it has stopped answering. Three properties follow, and each is enforced rather than described:</p><ul><li>a writer will not drop a published address on its own — regenerating from whatever happens to be on disk is the laundering these ledgers exist to prevent, so <code>sync:published-document-lanes</code> refuses and names what it would have forgotten;</li><li>a withdrawn address may not be reused. Publishing something else at it fails by name, because the danger a reader faces is not a <code>404</code> but a URL they know that quietly means something new;</li><li>a withdrawal may not restate history. The retired record must carry the same bytes the identity was served under, so retirement cannot launder a change that &quot;never overwritten&quot; would have refused.</li></ul><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p><code>release/published-document-lanes.json</code> and <code>release/public-schema-identities.json</code> each gain a <code>retired</code> list, and their checks gain the three refusals above. <code>scripts/deploy.mjs</code> passes the retired set into <code>enforceAppendOnlyPublicSchemaIdentities</code> at both call sites, so the full-history append-only walk admits a recorded withdrawal and nothing else.</p><p>Both were driven backwards before being trusted: an unrecorded removal fails, a <code>--write</code> that would forget an address fails and names it, a recorded withdrawal passes, a withdrawn address that is published again fails, a withdrawal under different bytes fails, and an identity listed as both served and retired fails.</p><p><a href="./0036-artifact-names-state-the-lane-they-describe.html">Decision 0036</a> is unaffected. It governs what a name says, not how long the thing it names is served, and the corpus-naming gate it added still applies to whatever remains.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>Retiring the pre-Stable generations becomes possible, which is the point: the project can carry one live generation instead of four, and &quot;which one is current&quot; stops being a question a reader has to answer by inference.</p><p>What this decision does not do is decide which generations go. It makes withdrawal expressible and auditable; each withdrawal is still its own reviewed change, and each has to say in its <code>retiredBecause</code> why the address stopped answering.</p><p>The rule is asymmetric on purpose. Reaching Stable is a commitment to serve an identity indefinitely, and that commitment should be made once, deliberately, about contracts that earned it — not by default, in advance, about every candidate the project ever built.</p>`,23)])])}const u=t(s,[["render",r]]);export{m as __pageData,u as default};
