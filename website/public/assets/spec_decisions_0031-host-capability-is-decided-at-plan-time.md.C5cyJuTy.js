import{_ as a,o as t,c as o,a2 as i}from"./chunks/framework.Bbwa0s72.js";const u=JSON.parse('{"title":"0031 — Host capability is decided at plan time","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0031-host-capability-is-decided-at-plan-time.md","filePath":"spec/decisions/0031-host-capability-is-decided-at-plan-time.md"}'),s={name:"spec/decisions/0031-host-capability-is-decided-at-plan-time.md"};function n(r,e,d,l,c,h){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="_0031-—-host-capability-is-decided-at-plan-time" tabindex="-1">0031 — Host capability is decided at plan time <a class="header-anchor" href="#_0031-—-host-capability-is-decided-at-plan-time" aria-label="Permalink to &quot;0031 — Host capability is decided at plan time&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-09</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A conforming host may implement <code>WorkerVersion</code> and the <code>edge.kv</code> binding while implementing none of bucket, SQLite, or queue. Nothing about that is unusual: <a href="./0013-v1alpha3-lane-ships-in-provider-v2-1.html">0013</a> made Host Support Profiles a readable API surface precisely so a host can say which exact FormRefs, Interfaces, and Bindings it carries, and <a href="./0016-the-worker-aggregate-has-one-active-deployment.html">0016</a> and <a href="./0024-a-worker-is-reachable-at-a-host-assigned-address.html">0024</a> both name <code>unsupported_capability</code> (422) as the refusal a host raises for a capability it does not offer.</p><p>The provider never read any of it. An author wrote a bucket binding, planned cleanly, and discovered at APPLY — after the bundle bytes had been uploaded and part of the aggregate created — that the host had no buckets. The information was published, in a document the provider already had a client for, and the plan simply did not ask.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The provider reads the Host Support Profile surface during <code>ModifyPlan</code> and decides capability there, transparently, for every v1alpha3 resource.</p><p><strong>Transparently, not through a data source.</strong> <code>data &quot;takoform_host_support&quot;</code> was the alternative. It was rejected because a data source REPORTS and a plan has to DECIDE: an author who never writes the data block keeps discovering the unsupported binding at apply, which is the whole defect, and an author who does write it has to restate every Form&#39;s own requirements as <code>precondition</code> blocks — the same facts a third time, in HCL, by hand, on every worker. Both sides of the comparison are already declared: the catalog Form names its Interfaces, its Bindings, its enums and its ranges, and the profile names what the host implements. Comparing two declarations the provider already holds needs no configuration at all.</p><p><strong>What the plan decides</strong>, per resource:</p><ul><li>is this exact FormRef supported, and does the profile&#39;s <code>operations</code> cover the whole lifecycle the resource needs (create, read, delete, and update where the Form declares one);</li><li>is each Interface the Form states in <code>providedInterfaces</code> implemented, read from <code>{api}/support/interfaces/{name}/{version}</code>;</li><li>is each Binding contract the configuration actually USES implemented, read from <code>{api}/support/bindings/{name}/{version}</code> — only the ones used, so a KV-only worker is never refused for a bucket contract it does not declare;</li><li>does every planned closed-enum value fall inside <code>supportedEnums</code>, and every planned integer inside <code>supportedRanges</code>;</li><li>does every planned collection stay inside the ceiling the profile publishes for it, read by the profile schema&#39;s own lowerCamelCase convention (<code>maximumHandlers</code> bounds <code>handlers</code>, <code>maximumVersions</code> bounds a deployment&#39;s <code>versions</code>, <code>maximumRequiredSensitiveVars</code> bounds the secret slots a version asks for);</li><li>does the artifact this plan would commit stay inside <code>maximumBundleBytes</code>, checked before a byte is uploaded.</li></ul><p>Whether endpoint assignment is offered, whether a queue is offered, and whether a secret slot is offered all fall out of the first rule applied to the resource that needs it, rather than being separate special cases.</p><p><strong>A host that says nothing is not a host that says no.</strong> The published profile schema lets a profile omit everything but identity and operations, so reading omission as denial would refuse conforming hosts. Only an explicit refusal — <code>form_unknown</code> or <code>resource_not_found</code> on a support route — is an error. An unreadable support surface is a WARNING that names what could not be decided, and apply remains the backstop it already is. A provider must not refuse an apply because it failed to ASK.</p><p><strong>Three planes, kept apart, and said so.</strong> Form semantics belong to Takoform: what a <code>WorkerVersion</code> means and which values its fields admit are properties of the exact FormRef, identical on every host. Host capability belongs to the Host Support Profile: which of those a THIS host implements. Capacity, price, region, and SLA belong to a Service Offering and are not in this API at all — the published profile schema admits none of them, and the provider would ignore one if a host sent it. No host-specific value enters a Form&#39;s desired state as a result of any of this: the plan either proceeds with the desired state the author wrote, or refuses it.</p><p>Each profile is read at most once per provider configuration and cached, because a profile is a static statement about one host and a plan asks the same questions once per resource.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>terraform plan</code> now makes a small number of read-only host requests, bounded by the existing 30-second plan preview deadline. A slow or absent support surface costs a warning, never a failed plan.</li><li>An author on a partial host sees the refusal before any artifact is uploaded and before any resource is created, naming the exact Form or contract and the fact that the same configuration applies unchanged on a host that implements it.</li><li>The client gains <code>GetInterfaceSupport</code> and <code>GetBindingSupport</code>; the Form support read already existed and was unused.</li><li>A host that publishes a ceiling gets it honoured with no client change, because the ceiling names are read by convention from the field&#39;s own wire name rather than from a list in the provider.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>A <code>data &quot;takoform_host_support&quot;</code> source.</strong> See above: it reports where the plan has to decide, and it makes correctness opt-in per configuration.</li><li><strong>Refuse whatever the profile does not explicitly permit.</strong> Rejected because the published profile schema makes <code>supportedEnums</code>, <code>supportedRanges</code>, <code>supportedBindings</code>, and <code>limits</code> all optional. A host that publishes only identity and operations is conforming, and treating its silence as denial would make the provider unusable against it.</li><li><strong>Read the profile once at Configure and hold it for the whole run.</strong> Rejected as premature: Configure runs before the plan knows which kinds are in play, so it would have to read every profile a build knows about on every command, including <code>terraform output</code>. Reading lazily and caching costs the same requests for a plan that uses them and none for a command that does not.</li><li><strong>Check capability at apply, but earlier in the apply.</strong> Rejected because a plan the author approved is the artifact a review reads. A refusal that only ever appears after approval has already cost the review its meaning.</li></ul>',18)])])}const m=a(s,[["render",n]]);export{u as __pageData,m as default};
