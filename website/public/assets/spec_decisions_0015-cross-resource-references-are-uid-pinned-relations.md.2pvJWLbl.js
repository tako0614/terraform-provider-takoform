import{_ as t,o as a,c as r,a2 as o}from"./chunks/framework.Bbwa0s72.js";const m=JSON.parse('{"title":"0015 — Cross-resource references are UID-pinned relations","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0015-cross-resource-references-are-uid-pinned-relations.md","filePath":"spec/decisions/0015-cross-resource-references-are-uid-pinned-relations.md"}'),n={name:"spec/decisions/0015-cross-resource-references-are-uid-pinned-relations.md"};function i(s,e,d,c,l,h){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="_0015-—-cross-resource-references-are-uid-pinned-relations" tabindex="-1">0015 — Cross-resource references are UID-pinned relations <a class="header-anchor" href="#_0015-—-cross-resource-references-are-uid-pinned-relations" aria-label="Permalink to &quot;0015 — Cross-resource references are UID-pinned relations&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A v1alpha3 host recognised a cross-resource reference by finding an object under a member literally named <code>resource</code>, which is the shape a typed Binding instance happens to have. Every other reference a Form declares was therefore invisible: a Worker Version could name a worker or a bundle that does not exist, a Deployment could weight a Version belonging to a different worker, a Consumer could target a missing queue, and deleting any of those targets was not blocked.</p><p>The reference itself carried only <code>{kind, name}</code>. A bare kind forces a host to guess which installed Form a name means once two Form Families declare the same kind, and a name alone cannot distinguish one resource from a different resource that later reused the name — which is exactly what happens when a target is deleted and re-created out of band.</p><p>Decision <a href="./0014-published-schemas-are-structural-minima.html">0014</a> settled where a new invariant may live: data the published schema already admits, then Definition content, then code plus conformance. It authorises <em>deriving</em> relation metadata from the desired schema instead of minting a new Definition member. It does not decide what a reference is, how it is resolved, what a host stores, what protects a referenced resource from deletion, what happens when a target&#39;s identity changes underneath a source, or which stable errors any of that produces. Those are normative contract decisions, and this repository&#39;s <code>AGENTS.md</code> requires a decision record for them.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>A <strong>relation</strong> is one reference from a resource&#39;s desired spec to another resource in the same space. The rules below are normative; the operative text lives in <a href="./../host-api/v1alpha3.html"><code>../host-api/v1alpha3.md</code></a> and <a href="./../binding-contract/"><code>../binding-contract/README.md</code></a>.</p><ol><li><strong>Shape.</strong> A reference MUST be the closed three-member object <code>{apiVersion, kind, name}</code>. <code>apiVersion</code> and <code>kind</code> MUST be <code>const</code> in the declaring Form&#39;s <code>desiredSchema</code>; <code>name</code> follows the portable resource-name grammar. All three members are required. A reference carries no space member, so a cross-space reference is unrepresentable rather than refused.</li><li><strong>Derivation.</strong> Relations MUST be derived from the desired schema and MUST NOT be declared beside it. Every closed object requiring exactly those three members with the first two <code>const</code> is a relation, identified by its JSON Pointer with <code>*</code> standing for an array element. A binding list additionally carries the <code>x-takoform-binding</code> annotation naming its contract; the exact digest-bound BindingRef stays the Definition&#39;s own <code>acceptedBindings</code> entry, so the digest keeps one source of truth (decision 0014).</li><li><strong>Resolution before mutation.</strong> On <code>apply</code> and on <code>import</code>, before ANY mutation, a host MUST resolve every relation present in the materialized spec and MUST verify that the resolved resource&#39;s Form has exactly the referenced group and kind.</li><li><strong>UID pinning.</strong> A host MUST store the resolved target&#39;s <code>metadata.uid</code> alongside the source, not only the name. A name is a label the client chose and can reuse; the UID is the identity of one incarnation.</li><li><strong>Absent target.</strong> A relation naming a resource that does not exist MUST fail <code>resource_not_found</code> (404) before any mutation, and the message MUST name the relation pointer.</li><li><strong>Deletion protection.</strong> Deleting a resource that any stored relation references by UID MUST fail <code>dependency_in_use</code> (409). This covers every relation, not only typed bindings. A self-reference does not block a resource&#39;s own deletion, and an accepted (202) delete MUST re-run the scan at commit time.</li><li><strong>Incarnation change.</strong> When a stored relation&#39;s target resolves to a different UID, the source MUST report <code>Ready=False</code> with <code>ExternalChange</code>; when it resolves to nothing, with <code>DependencyMissing</code>. Both MUST carry a <code>hostReason</code> naming the relation pointer and both UIDs. A host MUST NOT re-bind automatically, and a read MUST NOT heal the condition.</li><li><strong>Binding verification order.</strong> A binding relation MUST be verified before any mutation, in this order: (1) the source Definition&#39;s <code>acceptedBindings</code> carries the annotated contract, else <code>invalid_argument</code> (400); (2) the host has installed that contract at exactly the accepted <code>schemaDigest</code>, else <code>unsupported_capability</code> (422); (3) the source Form&#39;s role equals the Binding&#39;s <code>sourceRole</code>, else <code>invalid_argument</code>; (4) the resolved target&#39;s exact Form appears in <code>allowedTargetForms</code>, else <code>invalid_argument</code>; (5) the target Form declares the Binding&#39;s <code>targetInterface</code> in <code>providedInterfaces</code>, else <code>invalid_argument</code>. Rule 2 is about what this host can do at all, which is why it alone is a capability failure.</li><li><strong>Recovery.</strong> Because a host never re-binds, the remedy MUST be an apply, and that apply MUST stay reachable. Every accepted mutation re-resolves and re-pins every relation, including one whose spec is byte-identical to the stored spec. Re-pinning MUST NOT move <code>metadata.generation</code>, because desired state did not change, and it moves <code>metadata.revision</code>, because the representation did. A client MUST report the broken relation without failing its refresh — the plan that repairs the resource is computed from the refreshed state — and MUST offer an apply: a spec-identical one for a Form that declares <code>update</code>, and a REPLACEMENT for a Form that declares none, since a host refuses every apply to an existing resource of such a Form.</li><li><strong>An accepted mutation is bound to the incarnation it was accepted for.</strong> A host that answers <code>202</code> MUST record the target&#39;s exact Form identity and <code>metadata.uid</code> beside the fence, and at commit time MUST resolve through that record instead of re-deriving a target from the name the request addressed. When the name is held by a different incarnation the operation MUST terminate <code>uid_mismatch</code> (409); when nothing holds it, <code>resource_not_found</code> (404). Neither commits anything. A name is reusable and a re-created resource starts at revision 1, so the fence an accepted delete was admitted under is satisfied by the very replacement that proves the resource it was accepted for is gone. A create is fenced against the free NAME rather than against an incarnation, so it pins none and its own fence decides at commit. This is rule 4 — identity is pinned to what was actually resolved, never re-derived from a name later — stated about the operation&#39;s own target rather than about a relation&#39;s.</li></ol><p>Semantics a schema cannot state stay in the host under decision 0014, and are proven by the required conformance checks <code>relation-target-missing-rejected</code>, <code>relation-target-deletion-blocked</code>, <code>relation-incarnation-change-detected</code>, <code>relation-reapply-repins</code>, <code>binding-contract-verified</code>, and <code>async-commit-binds-the-accepted-identity</code>.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Fourteen relations across the Edge Platform Family are validated by one rule instead of one special case per binding list. Enforcing that rule surfaced a contract inconsistency no schema could: the service binding allowed a <code>ModuleWorker</code> target while <code>worker.service</code> was declared on <code>WorkerVersion</code>, so no service binding could ever have been verified.</li><li>A host keeps a reverse index from target UID to holders. That index, not a binding scan, is what <code>dependency_in_use</code> is computed from.</li><li>The desired spec still carries the NAME. Desired state stays what an author wrote; the UID lives in host-owned records, so a spec is portable between hosts that issue different UIDs.</li><li>An operation id names a mutation to ONE resource for as long as it exists. A host that stores accepted mutations keeps the target&#39;s exact ref and uid on the operation record, which costs two fields and is the only thing standing between a slow delete and a resource that took the same name afterwards.</li><li>A resource pinned to a replaced target is visibly broken and recoverable, not quietly wrong. The provider records the reported reason in a computed <code>relation_drift_reason</code> attribute so a plan can propose the repairing apply; that attribute is provider-side recovery bookkeeping and is deliberately absent from the portable wire spec.</li><li>A <code>revision</code>-role resource recovers only by replacement. That follows from the role — a revision is immutable by definition — and clients must plan it rather than attempt an update the host will refuse.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Declare relations as a new Form Definition member.</strong> Rejected twice over. The published Form Definition schema admits no such member and its bytes are immutable (decision 0014), so it would require minting a replacement identity; and it would make the declared list and the desired schema two sources of truth for the same facts, free to disagree, with the schema still deciding what a valid spec looks like.</li><li><strong>Pin relations by name only.</strong> Rejected because a name is reusable. A target deleted and re-created under the same name would silently adopt every source that referenced the old one, and no client would ever learn that the resource its author named is gone. Storing the UID is what makes the difference observable at all.</li><li><strong>Re-bind automatically when the target&#39;s UID changes.</strong> Rejected because it turns a destroyed backend resource into a silent adoption: the source would start pointing at a resource its author never named, inheriting whatever state that resource holds — another tenant&#39;s queue, an empty replacement of a populated namespace — while reporting itself healthy. The failure it would hide is precisely the one worth reporting.</li><li><strong>Fail the read while a relation is broken.</strong> Rejected because a read is Terraform&#39;s refresh, and a failed refresh aborts the plan that would repair the resource. It is a remedy that removes its own path.</li><li><strong>Scan only binding-shaped members, as before.</strong> Rejected because the reference shape, not the member name, is what a Form declares; the narrower rule protected typed bindings and left every plain reference unresolved, unprotected, and undetected.</li></ul>',14)])])}const p=t(n,[["render",i]]);export{m as __pageData,p as default};
