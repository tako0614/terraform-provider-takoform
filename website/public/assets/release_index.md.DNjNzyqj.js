import{_ as t,o as a,c as i,ag as s}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"Provider release boundary","description":"","frontmatter":{},"headers":[],"relativePath":"release/index.md","filePath":"release/index.md"}'),o={name:"release/index.md"};function r(n,e,d,c,l,p){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="provider-release-boundary" tabindex="-1">Provider release boundary <a class="header-anchor" href="#provider-release-boundary" aria-label="Permalink to &quot;Provider release boundary&quot;">​</a></h1><p><code>release/version.json</code> is the independent Takoform provider version source. It does not inherit a Takosumi package or release version.</p><p>The provider-specific trust lane is pinned by the release trust profile (internal decision identifier D-08) in <a href="./../spec/trust/"><code>../spec/trust/</code></a>. Form Packages use a separate keyless trust lane and never reuse this provider GPG key. Its release and revocation delivery boundary is documented in <a href="./form-packages.html"><code>form-packages.md</code></a>.</p><p>The repository can build deterministic, unsigned candidate evidence:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go -C ./cmd/provider-release run . verify-source</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go -C ./cmd/provider-release run . verify-reproducible</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go -C ./cmd/provider-release run . build --output ../takoform-provider-candidate</span></span></code></pre></div><p><code>build</code> refuses a dirty source tree, a missing or mismatched annotated tag, an unverified tag signature, a different Go toolchain, or an existing output path. The explicit <code>--allow-dirty-candidate</code> and <code>--allow-untagged-candidate</code> flags are for local non-publishable evidence only. Any such exception is recorded in the manifest and keeps <code>publicationReady=false</code>.</p><p>Provider <code>v2.1.1</code> is the current Registry-published release. Provider <code>v2.0.0</code> is the published compatibility predecessor and Provider <code>v1.0.3</code> is the published Legacy <code>v1</code> release. Their signed tags, immutable GitHub Releases, and canonical Terraform Registry listings exist.</p><p><code>release/version.json</code> now describes the stable provider release target <code>v2.1.1</code>, tag <code>v2.1.1</code>, carrying Host API <code>forms.takoform.com/v1beta1</code>. Its <code>publicationStatus</code> remains <code>candidate-only</code> as descriptor metadata: a source descriptor, local gate, or candidate build does not itself establish signed-release or Registry publication. The retained owner-flow readback evidence below establishes that the Provider 2.1.1 bytes are published; the descriptor value remains <code>candidate-only</code> by design.</p><p>Provider v2.1.1 embeds exactly 15 Experimental <code>0.1.0</code> Forms in <code>edge.forms.takoform.com/v1beta1</code>. Their exact FormRefs and definition/package digests are locked independently in <a href="provider-form-identities.json"><code>provider-form-identities.json</code></a>, even while the <code>packages.forms.takoform.com/v1alpha4</code> artifacts remain unpublished. Open package/public-service and later Stable/GA evidence obligations do not turn this stable provider version into a prerelease (<a href="./../spec/decisions/0035-beta-contracts-ship-in-stable-provider-v2-1.html">decision 0035</a>).</p><h2 id="abandoned-v2-1-0-candidate-and-v2-1-1-forward-repair" tabindex="-1">Abandoned v2.1.0 candidate and v2.1.1 forward repair <a class="header-anchor" href="#abandoned-v2-1-0-candidate-and-v2-1-1-forward-repair" aria-label="Permalink to &quot;Abandoned v2.1.0 candidate and v2.1.1 forward repair&quot;">​</a></h2><p>The signed annotated <code>v2.1.0</code> tag is a permanently abandoned, unpublished candidate. Its exact tag object is <code>5ec38b5485c774650065e8317f8024ad40270393</code>, and it peels to source commit <code>9add889446740d406a4c9ca93b74137c9b014fca</code>. Candidate workflow run <code>31379384964</code> failed before artifacts because Bun was missing. No GitHub Release or Terraform Registry version exists for <code>v2.1.0</code>; never move, delete, recreate, or reuse that tag.</p><p><code>v2.1.1</code> is the patch forward repair from the reviewed main commit. It carries the exact same 15 Beta FormRefs, schema/package digests, Host API <code>forms.takoform.com/v1beta1</code>, and family <code>edge.forms.takoform.com/v1beta1</code>; only provider release identity and tooling changed. The current <code>provider-form-identities.json</code> entry is re-keyed to <code>v2.1.1</code> without changing any nested Form identity or digest. The exact Provider 2.1.1 Registry readback is now retained in <code>release/provider-release-identities.json</code>; the descriptor remains <code>candidate-only</code> metadata by design and no publication claim is made for a future version without its own readback.</p><p><code>release/provider-release-identities.json</code> retains the exact six-file signed Registry readback closure for current provider releases as canonical base64. The closure binds the provider release commit, readback tooling commit, Terraform and OpenTofu direct installs, one provider binary digest, the workflow certificate identity, transparency-log proof, and original checksum manifest. <code>scripts/publication-truth.mjs</code> reconstructs and validates those exact bytes before public documentation can call the current provider Registry-published. This distribution evidence grants no Form maturity, Host Support, activation, placement, or commercial authority.</p><h2 id="provider-v1-0-3-relationaldatabase-schema-inputs" tabindex="-1">Provider v1.0.3 RelationalDatabase schema inputs <a class="header-anchor" href="#provider-v1-0-3-relationaldatabase-schema-inputs" aria-label="Permalink to &quot;Provider v1.0.3 RelationalDatabase schema inputs&quot;">​</a></h2><p>Provider <code>v1.0.3</code> exposes the existing optional <code>schema_url</code>, <code>schema_sha256</code>, and <code>schema_format</code> fields of the immutable <code>RelationalDatabase@3.0.0</code> Form as typed HCL attributes. It does not mint or replace a Form identity. Hosts remain responsible for fetching the exact digest-bound schema bundle and converging it before reporting the Resource Ready.</p><h2 id="provider-v1-0-2-interface-url-addition" tabindex="-1">Provider v1.0.2 Interface URL addition <a class="header-anchor" href="#provider-v1-0-2-interface-url-addition" aria-label="Permalink to &quot;Provider v1.0.2 Interface URL addition&quot;">​</a></h2><p>Provider <code>v1.0.2</code> adds the optional computed <code>resource_uri</code> attribute to the read-only <code>takoform_interface</code> data source. The host remains authoritative for the URI; the provider does not infer a provider-specific hostname or copy the URI into portable Resource desired state. Existing provider-v1 Resource state and every immutable Form identity remain unchanged.</p><h2 id="provider-v1-0-1-resource-transition" tabindex="-1">Provider v1.0.1 resource transition <a class="header-anchor" href="#provider-v1-0-1-resource-transition" aria-label="Permalink to &quot;Provider v1.0.1 resource transition&quot;">​</a></h2><p>Provider <code>v1.0.1</code> removes the active <code>takoform_http_service</code> resource and adds <code>takoform_edge_worker</code> for the distinct <code>EdgeWorker@3.0.0</code> Form identity. It also removes the writable <code>takoform_interface</code> resource: portable Form Definitions may declare non-secret Interface descriptors and the provider retains the read-only <code>takoform_interface</code> data source, but Interface records, bindings, write fencing, authorization, and lifecycle belong to the host. Published <code>HttpService@1.0.0</code> bytes and provider <code>v0.2.1</code> remain immutable.</p><p>The compatibility break is broader than those two removed resource types. Provider <code>v0.2.1</code> and published <code>v1.0.1</code> each compile 34 Form identities. They share 33 kind names, but all 33 shared exact FormRefs/packages changed; there are zero unchanged exact identities. Common names, artifacts, connections, and semantic field contracts also changed. Every existing v0.2.1 Form resource, not only <code>takoform_http_service</code>, must therefore stay pinned until an operator performs an explicit create/cutover/destroy migration.</p><p>Provider v1 gives every Form resource schema version <code>1</code>, stores the exact FormRef/package identity in state, and handles schema version <code>0</code> only with a diagnostic that returns no transformed state and makes no Resource lifecycle request. This makes a direct v0.2.1 state load fail before lifecycle code can query the new exact identity, receive <code>404</code>, and erase old state as though the old Resource had disappeared. Provider configuration may already have performed host discovery; this fence covers Resource lifecycle requests. Existing writable <code>takoform_interface</code> state also remains manageable only while pinned to v0.2.1. If the host has adopted the Interface record, destroying it through v0.2.1 can delete that remote record: verify host ownership and binding continuity, then remove only the Terraform state entry. Destroy through v0.2.1 only when the remote Interface itself is intentionally being retired. An automatic transformation or v1 import target would falsely claim a portable resource/data migration that does not exist.</p><p>Some v0.2.1 OpenTofu state may use the retired <code>registry.opentofu.org/tako0614/takoform</code> identity. That address is not an alias for <code>registry.terraform.io/tako0614/takoform</code>; when it is actually present, normalize it with an explicit, reviewed <code>tofu state replace-provider</code> while the resource version remains pinned to v0.2.1. Address replacement does not migrate any Form resource. The full inventory, backup, address-normalization, and per-resource cutover procedure is <a href="./migrations/v0.2.1-to-v1.0.1.html"><code>migrations/v0.2.1-to-v1.0.1.md</code></a>, backed by the machine-readable <a href="migrations/v0.2.1-to-v1.0.1.json"><code>migration audit</code></a>.</p><p>This is the first stable provider compatibility line, not a claim that any Form epoch graduated. The <code>forms.takoform.com/v1alpha1</code> Form epoch is now frozen Legacy; the retained provider-v2 epoch uses <code>forms.takoform.com/v1alpha2</code> inside <code>packages.forms.takoform.com/v1alpha3</code> after an individual lifecycle transition, and is superseded for new design work by the namespaced Form Families lane. The retained nine are still unpublished candidates. The outer Host API wire independently uses <code>forms.takoform.com/v1alpha2</code> behind <code>/.well-known/takoform/v1alpha2</code> for provider v2. The frozen provider-v1 lane retains <code>forms.takoform.com/v1alpha1</code> behind <code>/.well-known/takoform</code>; one discovery document never advertises both epochs. Provider and Form Definition versions remain independent; current Form Package artifacts are content-addressed while retained Legacy package locators preserve their published identities. Admission generations are a separate historical evidence stream. Published <code>EdgeWorker@1.0.0</code> and <code>EdgeWorker@1.0.1</code> identities cannot be reset, so the provider-neutral definition uses the independent <code>EdgeWorker@3.0.0</code> identity. The intermediate <code>2.0.0</code> release source remains unmodified; tightening its artifact URL grammar required a new Form major. The exact contract is in <a href="./../spec/versioning.html"><code>../spec/versioning.md</code></a>.</p><p>Every candidate contains:</p><ul><li>five deterministic provider archives, one for each configured platform;</li><li><code>SHA256SUMS</code>, whose signed Registry closure contains the five provider ZIP archives and the exact Registry metadata manifest;</li><li>the detached OpenPGP signature for <code>SHA256SUMS</code>;</li><li><code>manifest.json</code> with archive and binary digests, source commit, embedded version evidence, and publication blockers;</li><li>five archive-specific SPDX 2.3 documents generated from the exact Go module graph;</li><li>one RFC 8785 canonical in-toto Statement v1 with SLSA Provenance v1; and</li><li>the provenance statement&#39;s detached OpenPGP signature.</li></ul><p>The public GitHub Release inventory is exactly 15 assets and is deliberately broader than the signed Registry checksum manifest. Before provenance is added, the five archives, five SPDX documents, manifest, <code>SHA256SUMS</code>, and its signature form the exact 13-asset payload closure. The canonical provenance statement binds all 13 by name, byte size, and SHA-256, plus the source commit, release tag, tooling commit, workflow identity, run and attempt, request ID, and annotated tag-object OID and SHA-256. It excludes itself and its signature to avoid self-reference. Its detached signature MUST verify with the same pinned provider OpenPGP key that authenticates <code>SHA256SUMS</code>; a GitHub artifact attestation is not a substitute.</p><p>Per-archive SPDX documents and source-bound provenance remain immutable release evidence and must not be listed in <code>SHA256SUMS</code>. The Registry metadata manifest is not an installable package, but the public Registry ingress contract requires its digest in the signed checksum file. Both Terraform and OpenTofu use the same six-entry checksum target set.</p><p>Providers <code>v0.1.1</code>, <code>v0.1.2</code>, and the then-corrective <code>v0.1.3</code> describe historical pre-v1 layouts only. <code>v0.1.1</code> included SPDX evidence in <code>SHA256SUMS</code>; <code>v0.1.2</code> omitted the required Registry manifest; and <code>v0.1.3</code> corrected that historical checksum closure without replacing a published byte. None of those layouts defines the current provider <code>v1.0.3</code> release. The v1 release is governed by the 15-asset provenance closure above and MUST NOT overwrite or inherit the identity of any historical version.</p><p>The signed annotated <code>v1.0.0</code> tag is immutable but never became a GitHub Release or Terraform Registry version. Its candidate workflow failed before publication because the embedded provenance generator did not parse. <code>v1.0.0</code> is permanently abandoned: never move, delete, or reuse that tag. Provider <code>v1.0.1</code> is the forward repair, and workflow-embedded Node programs are now parsed in the portable gate while this provenance generator is also executed against its exact 13-subject contract.</p><p><code>release/version.json</code> also pins the supported CLI/FQN matrix. Release CI must exercise Terraform <code>1.15.8</code> and OpenTofu <code>1.12.3</code> with the same canonical identity, <code>registry.terraform.io/tako0614/takoform</code>. Both must expose the same schema and complete lifecycle evidence for the exact embedded structural candidate set.</p><p>Provider publication is an independent distribution action. The <code>v*</code> workflow runs <code>candidate-publication-check</code>, which requires <code>publicationStatus: candidate-only</code> and the unchanged structural inventory. For v2.1.1 the owner gate additionally locks all 15 Terraform resource schemas, exact Beta FormRefs/digests, fake/reference-host v1beta1 conformance, provider state compatibility, and no-overwrite public identities. Publishing that exact binary, checksums, SBOM, provenance, and signatures does not change any Form&#39;s lifecycle state, install a Form Package, establish Host Support, or grant host activation authority. Provider SemVer has no admission-generation coupling.</p><p>The normal <code>matrix</code> command intentionally uses a locally built provider binary through <code>dev_overrides</code>; it is a pre-publication regression gate and is not Registry evidence. After the first authorized publication, capture the post-publication readback with:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/provider-lifecycle-conformance render-registry-matrix \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --opentofu tofu --terraform terraform \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &gt; /tmp/provider-lifecycle-matrix.json</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/provider-registry-readback \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --matrix /tmp/provider-lifecycle-matrix.json \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --provider-release-commit &quot;$(git rev-list -n 1 &quot;$(jq -r .tag release/version.json)&quot;)&quot; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --output /tmp/provider-readback.json</span></span></code></pre></div><p>During Terraform Registry propagation, a maintainer can diagnose one exact CLI execution without weakening the dual-CLI matrix gate:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/provider-lifecycle-conformance render-registry-report \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --cli /absolute/path/to/terraform</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/provider-lifecycle-conformance render-registry-report \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --cli /absolute/path/to/tofu</span></span></code></pre></div><p>Each command still performs the complete provider lifecycle through <code>direct {}</code> and validates the resulting report, but the single-CLI report is non-publishable diagnostic evidence. Only <code>render-registry-matrix</code> proves that both reviewed CLIs install the canonical FQN and expose identical bytes, schema, and lifecycle.</p><p>The matrix mode pins the exact descriptor version in generated configuration, runs <code>init</code> with only <code>direct {}</code>, locates and hashes the downloaded provider binary, and repeats the complete lifecycle. Its report carries <code>installationSource: direct-registry-install</code>; the historical readback validator rejects otherwise-valid matrices carrying <code>local-dev-override</code>. The matrix is still not self-authenticating: it becomes usable only when an externally signed, canonical <code>takoform.provider-registry-readback@v1</code> document binds its digest, installed binary/schema digests, CLI/FQN identities, provider tag, and source commit.</p><h2 id="historical-admission-evidence" tabindex="-1">Historical admission evidence <a class="header-anchor" href="#historical-admission-evidence" aria-label="Permalink to &quot;Historical admission evidence&quot;">​</a></h2><p>Takoform no longer has a central candidate set, admission assembly workflow, admission publisher, or set-wide promotion lane. The provider Registry readback and the all-34 provider reports remain useful compatibility evidence, but they do not vote on Form maturity and are not inputs to a current central approval process.</p><p>Published admission identities are a closed historical namespace. Versions <code>1.0.1</code>, <code>1.0.2</code>, <code>1.0.3</code>, <code>1.0.4</code>, <code>1.0.6</code>, and <code>1.0.7</code> are pinned by exact annotated tag object, commit, retained tree, and set digest in <a href="../admission/admission-identities.json"><code>../admission/admission-identities.json</code></a>. Version <code>1.0.5</code> is permanently <code>reserved-abandoned</code>. No new admission checkpoint is assigned.</p><p>The history-only gates are:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/standard-form-conformance legacy-published-package-check</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">go run ./cmd/standard-form-conformance legacy-admission-evidence-check</span></span></code></pre></div><p>They prove the immutable Legacy publication inventory and historical Git identity ledger. They do not rerun old <code>portable-standard</code> assertions as current conformance, support, availability, or activation claims. Provider versions remain independent of Form versions and of these retired historical identities.</p><p>The provider build tool never signs, uploads, creates a GitHub Release, or publishes to a Registry/mirror. The only operator entrypoint is the repository deploy command. First dispatch the protected signed-tag lane with the exact descriptor tag and current protected-main commit:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-provider-release prepare \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag &lt;descriptor-tag-from-release/version.json&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;40-character-protected-main-commit&gt;</span></span></code></pre></div><p>The entrypoint records the workflow run and stops. Its read-only preflight job has no protected Environment, write token, or signing key and is the only job that executes Go, the candidate provider, or either Terraform-compatible CLI. Only canonical descriptor/build/SBOM/provenance/lifecycle digests cross the artifact boundary. The protected signing job starts from a fresh exact checkout, performs only static JSON/hash/Git/Registry-absence checks, imports the <code>provider-release</code> Environment key, and exports a checksum-closed public signed-tag object without repository write credentials. The signed message binds the protected-main commit, complete preflight checksum inventory, and exact Actions run/attempt. No local human signing key is required.</p><p>After the Environment-approved run succeeds, an admin maintainer consumes that exact signed-tag run through the same owner entrypoint:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-provider-release tag \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag &lt;descriptor-tag-from-release/version.json&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-40-character-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;signed-tag-workflow-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;signed-tag-workflow-attempt&gt;</span></span></code></pre></div><p>The verifier closes both inventories, reconstructs the exact public tag object with <code>git mktag</code>, checks its expected object id and peeled target, imports only the pinned public key in a temporary keyring, verifies the signer fingerprint, and refuses to replace an existing local tag ref. The final push uses the maintainer&#39;s existing admin authentication to cross the restricted tag-creation ruleset; the Actions job itself cannot bypass that rule. The entrypoint then dispatches the prepare-only <code>release.yml</code> at that exact immutable signed tag ref, with the same tag and peeled source commit. It never dispatches this candidate workflow from mutable <code>main</code>; its workflow identity is exactly <code>.github/workflows/release.yml@refs/tags/&lt;tag&gt;</code>. It prints that exact run URL and stops again. Its read-only build job verifies the signed tag with the public key, runs the same non-publishing GoReleaser command twice, requires the five final archive names and bytes to match after a source-mtime perturbation, extracts the exact final Linux amd64 provider, and runs both supported CLI lifecycles against those extracted bytes. It then validates every final Syft document against the repository-pinned official SPDX 2.3 schema, and closes the five provider archives plus Registry metadata manifest under the checksum file. A fresh protected signing job executes no provider or repository Go code: it statically rechecks the tag, inventory, Registry absence, and checksums, imports the same Environment key, adds the detached checksum signature, builds the RFC 8785 canonical provenance over that exact 13-subject payload, and adds the provenance statement&#39;s detached GPG signature. It emits a same-run, checksum-closed local-publication candidate with exactly 15 public assets. Neither job has tag, GitHub Release, public-attestation, or publication authority.</p><p>After that second run succeeds, publish only its exact run/attempt:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-provider-release publish \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag &lt;descriptor-tag-from-release/version.json&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-40-character-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;provider-release-candidate-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;provider-release-candidate-run-attempt&gt;</span></span></code></pre></div><p>The owner deploy entrypoint verifies the outer checksum closure and detached GPG signature, then uses only the operator machine&#39;s GitHub authority to create a draft, upload the exact fifteen assets, compare their public API digests, publish that same draft, and require an immutable exact-ID/tag readback. A dispatch, tag push, or candidate artifact is not publication success.</p><p>The provider candidate metadata is exact recursively key-sorted, two-space pretty JSON with exactly one trailing LF, matching the <code>jq -S</code> workflow output. Compact JSON, a missing or extra LF, different indentation, unsorted keys, and duplicate keys are rejected before any publication mutation.</p><p>If the exact signed annotated tag already exists locally and remotely but no GitHub Release or Registry version exists, normal <code>publish</code> remains intentionally unusable. Complete only that exact partial identity with:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-provider-release recover-tag-only \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag &lt;descriptor-tag-from-release/version.json&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-release-commit &lt;signed-tag-peeled-release-commit-E&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-tag-object &lt;exact-annotated-signed-tag-object&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-recovery-commit &lt;current-reviewed-protected-main-commit-F&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;original-provider-candidate-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;original-provider-candidate-run-attempt&gt;</span></span></code></pre></div><p>This phase requires <code>E</code> to be an ancestor of <code>F</code>, and the exact <code>E..F</code> diff may contain only the reviewed recovery implementation, its tests, and this release documentation. It requires current protected <code>main</code> to equal <code>F</code>; the exact local and remote annotated tag object must still peel to <code>E</code> and verify with the pinned provider key; the GitHub Release and Registry version must still be absent; and the successful candidate run must have exact head <code>E</code>, branch the descriptor tag, run, attempt, checksums, 15 assets, GPG signatures, and provenance. The owner gate and the same recovery fence run immediately before draft creation and again immediately before publication. Recovery never moves, deletes, or recreates the tag.</p><p>If that phase stops after retaining one exact draft, resume only the named identity:</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-provider-release recover-draft \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag &lt;descriptor-tag-from-release/version.json&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-release-commit &lt;signed-tag-peeled-release-commit-E&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-tag-object &lt;exact-annotated-signed-tag-object&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-recovery-commit &lt;current-reviewed-protected-main-commit-F&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --release-id &lt;exact-retained-github-release-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;original-provider-candidate-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;original-provider-candidate-run-attempt&gt;</span></span></code></pre></div><p><code>recover-draft</code> re-verifies the same candidate, tag, Registry absence, owner gate, and <code>E..F</code> recovery fence. It accepts only the exact draft id, tag, name, body, target commitish, upload/assets endpoints, and already-uploaded subset; it uploads only missing assets, rereads the complete draft, repeats the fence, and publishes that same draft. Any competing, public, unknown, duplicate, or drifted identity fails closed without deletion or blind retry.</p><p>Both recovery phases require exclusive single-writer operator authority from draft creation or resumption through immutable publication. GitHub&#39;s REST API has no atomic precondition spanning release metadata and all assets, so the strongest available closure is used: an immediate authoritative empty-draft read after creation, a complete exact-draft reread before publication, a PATCH that restates the full tag/name/body/target/prerelease identity, an immutable exact Release readback and download, and one final pinned signed-tag check before reporting <code>VERIFIED</code>.</p><p>After the immutable GitHub Release exists and the public Registry has indexed it, dispatch the signed direct-install readback and then verify that exact run:</p><p>For the ordinary lane, protected <code>main</code> may still equal the signed tag&#39;s peeled commit. After an exact recovery, <code>--expected-commit</code> is instead the current reviewed protected-main source/tooling commit <code>F</code>. The immutable provider release provenance and provider commit remain the tag&#39;s peeled commit <code>E</code>. The readback workflow and local verifier require <code>E</code> to be an ancestor of <code>F</code> and preserve those two bindings separately; they never relabel the released provider bytes as having been built from <code>F</code>.</p><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-provider-release readback \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag &lt;descriptor-tag-from-release/version.json&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;current-reviewed-protected-main-source-commit&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">bun run deploy -- takoform-provider-release verify \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --tag &lt;descriptor-tag-from-release/version.json&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --expected-commit &lt;same-current-reviewed-protected-main-source-commit&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-id &lt;registry-readback-workflow-run-id&gt; \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --run-attempt &lt;registry-readback-workflow-attempt&gt;</span></span></code></pre></div><p>The readback workflow installs the exact public version directly through both OpenTofu and Terraform, requires one provider binary digest, signs the Registry readback, and emits the exact six-file candidate used by admission. The verify phase requires that workflow attempt to have completed successfully and closes the downloaded artifact inventory and checksums; it never republishes the provider.</p><p>Repository configuration is part of the trust boundary, not a claim made by this tree. The workflow references the <code>provider-release</code> GitHub Environment, but publication remains blocked until maintainers verify required reviewers on that Environment plus protected <code>main</code> and restricted <code>v*</code> tag creation rules.</p><p>The release verifier is an isolated Go module under <code>cmd/provider-release</code>. Its schema/attestation dependencies are not provider runtime dependencies and do not appear in the provider module graph or provider SBOM.</p><p>The approved provider signing fingerprint is <code>3510E75E05BBCC303B92D77934FC18AC897FB709</code>; its public key is pinned under <code>release/keys/</code>. The private key and passphrase remain outside every repository and are available to Actions only as <code>GPG_PRIVATE_KEY</code> and <code>PASSPHRASE</code>. The HCP organization <code>takoform</code> has claimed the public Terraform Registry namespace <code>tako0614</code>; its GitHub App installation is limited to <code>tako0614/terraform-provider-takoform</code>. Registry key ID <code>34FC18AC897FB709</code> is registered and matches the full pinned fingerprint above. Providers <code>v0.1.1</code> and <code>v0.1.2</code> remain immutable GitHub Releases. Terraform Registry rejected the first because its checksum manifest projected SPDX evidence as provider packages, and rejected the second because the required Registry metadata manifest was absent from <code>SHA256SUMS</code>. Existing version paths must never be overwritten. At that historical point the corrected six-entry candidate was <code>v0.1.3</code>, and direct Terraform/OpenTofu Registry install evidence remained post-publication; the coordinated Form <code>1.0.1</code> candidate therefore used provider <code>0.1.3</code>. Those retained facts do not define the then-current published <code>v1.0.1</code> release or its 15-asset provenance closure.</p><p>Key rotation is additive and review-gated: create a distinct repo-external key, change the pinned fingerprint/public key in one reviewed commit, register that public key with the Terraform Registry before tagging, and retain old public keys for verification of historical releases. Never reuse the Takoform key for the separately owned Takosumi legacy/admin provider.</p>`,68)])])}const u=t(o,[["render",r]]);export{m as __pageData,u as default};
