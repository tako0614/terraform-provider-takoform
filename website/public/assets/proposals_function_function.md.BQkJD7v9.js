import{_ as t,o as n,c as o,ag as a}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"Function — takoform_function","description":"","frontmatter":{},"headers":[],"relativePath":"proposals/function/function.md","filePath":"proposals/function/function.md"}'),i={name:"proposals/function/function.md"};function r(s,e,c,d,l,h){return n(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="function-—-takoform-function" tabindex="-1">Function — <code>takoform_function</code> <a class="header-anchor" href="#function-—-takoform-function" aria-label="Permalink to &quot;Function — `takoform_function`&quot;">​</a></h1><blockquote><p>Historical/deferred candidate (English-only). This non-Edge Form is not in the current official Edge16 corpus or Current navigation.</p></blockquote><h2 id="workload-and-consumer" tabindex="-1">Workload and consumer <a class="header-anchor" href="#workload-and-consumer" aria-label="Permalink to &quot;Workload and consumer&quot;">​</a></h2><p>An application team runs event-invoked code as one regional function on a proven cloud function service: upload an artifact, declare a handler, and the platform runs one handler invocation per event. Consumers address the function by its logical identity: deployments, endpoints, and future source-family attachments all point at this resource, never at a specific build.</p><h2 id="role" tabindex="-1">Role <a class="header-anchor" href="#role" aria-label="Permalink to &quot;Role&quot;">​</a></h2><p><code>identity</code>. The Form carries no desired fields: everything that can change — code, configuration, resource bounds, traffic — lives on revision and deployment resources around it.</p><h2 id="observable-semantics" tabindex="-1">Observable semantics <a class="header-anchor" href="#observable-semantics" aria-label="Permalink to &quot;Observable semantics&quot;">​</a></h2><p>The identity fixes the regional function ABI, and says exactly what that ABI is: the exact Interface contract <code>function.runtime@1.0.0</code>, authored the way <code>worker.runtime@1.1.0</code> fixes the ModuleWorker ABI (<a href="./../../spec/decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>). It is a JavaScript ES-module function runtime, deliberately distinct from <code>worker.runtime</code>:</p><ul><li>The unit of execution is one invocation. The main module&#39;s declared handler is a named export invoked as <code>handler(event, context)</code>; an execution environment handles ONE invocation at a time, so concurrency comes only from parallel environments, never from interleaving within one.</li><li>The event vocabulary is closed and every event names its kind. This version defines exactly the <code>http</code> event, delivered by <a href="./function-endpoint.html">FunctionEndpoint</a>; an event kind no attachment can activate does not enter the contract until the attachment ships beside it, in a new exact version — the same rule decision 0019 fixes for the worker.</li><li>The wall-clock budget is the invoking version&#39;s declared timeout, a regional budget measured in minutes rather than the colocated-request budget of an edge fetch event; <code>context</code> reports the invocation id and the remaining budget.</li><li>Nothing is promised about proximity. The contract carries no colocation guarantee between caller and execution — the edge family&#39;s model — and no region field: placement is a host decision, not portable state.</li><li>Work the handler does not await is not guaranteed to run. There is no <code>waitUntil</code>; the environment may be suspended the moment the handler settles.</li><li>Environment reuse across invocations is permitted and never promised; durable state lives behind the revision&#39;s external services.</li><li>The contract also fixes the environment projection (vars, sensitive slots, and per-protocol standard-service members, nothing else portable), exception handling, the minimum JavaScript and Web API surface, and module loading.</li></ul><p>There is no compatibility date and no compatibility flag, for the reason the Edge family records: a date names no behavior without a registry saying what each date changes. A runtime revision is a new exact Interface version — and, if it changes what a Form desires, a new Form version. A different language runtime is not a version of this contract at all: it is a different Form line, exactly as a WASI function is to <a href="./../edge/module-worker.html">ModuleWorker</a>.</p><h2 id="why-this-is-one-form" tabindex="-1">Why this is one Form <a class="header-anchor" href="#why-this-is-one-form" aria-label="Permalink to &quot;Why this is one Form&quot;">​</a></h2><p>Every consumer of the family programs against exactly this ABI. Splitting the identity from the ABI — the withdrawn epoch&#39;s open <code>runtime</code> token — would let two hosts attach the same deployments to observably different runtimes.</p><h2 id="what-would-require-a-separate-form" tabindex="-1">What would require a separate Form <a class="header-anchor" href="#what-would-require-a-separate-form" aria-label="Permalink to &quot;What would require a separate Form&quot;">​</a></h2><p>A different language runtime is a different Form line. Cross-family event sources — queue-, topic-, or schedule-driven invocation — are attachments declared in the source&#39;s own family, never members here; the Edge family&#39;s <code>QueueConsumer</code> stays worker-targeted, and scheduled invocation belongs to the separate deferred Schedule candidate family. A source-specific object event would require its own future contract; there is no current <code>ObjectBucket</code> Form. Function-to-function invocation would be its own <code>function.invoke</code> Interface and binding contract, proposed when that work starts.</p><h2 id="provided-interfaces" tabindex="-1">Provided Interfaces <a class="header-anchor" href="#provided-interfaces" aria-label="Permalink to &quot;Provided Interfaces&quot;">​</a></h2><p><code>function.runtime@1.0.0</code> — the runtime ABI a conforming host provides to this function&#39;s code. It is held by the identity because the identity is what a host implements; a Function Version is the code that fills it.</p><h2 id="accepted-bindings" tabindex="-1">Accepted Bindings <a class="header-anchor" href="#accepted-bindings" aria-label="Permalink to &quot;Accepted Bindings&quot;">​</a></h2><p>None. Capability bindings belong to revision resources (decision 0010), and <a href="./function-version.html">FunctionVersion</a> states why the MVP revision accepts none either.</p><h2 id="lifecycle-risks" tabindex="-1">Lifecycle risks <a class="header-anchor" href="#lifecycle-risks" aria-label="Permalink to &quot;Lifecycle risks&quot;">​</a></h2><p>Deleting the identity while versions, deployments, or attachments reference it must fail with <code>dependency_in_use</code>. Delete/recreate mints a new UID (decision 0011); stale references never rebind silently.</p><h2 id="prior-art" tabindex="-1">Prior art <a class="header-anchor" href="#prior-art" aria-label="Permalink to &quot;Prior art&quot;">​</a></h2><p>The regional function service every major cloud offers — uploaded artifact, declared handler, per-invocation lifecycle — retained without vendor, account, or commercial surface. The withdrawn v1alpha2 <code>EdgeWorker</code> candidate is prior art in part; its open <code>runtime</code> token is the defect this identity replaces by fixing one exact runtime contract per Form line.</p>',22)])])}const f=t(i,[["render",r]]);export{m as __pageData,f as default};
