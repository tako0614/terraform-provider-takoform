import{_ as t,o as a,c as i,a2 as o}from"./chunks/framework.DzNHXt93.js";const m=JSON.parse('{"title":"0021 — Third-party Forms and contract distribution wait for a trust model","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0021-third-party-forms-and-contract-distribution.md","filePath":"spec/decisions/0021-third-party-forms-and-contract-distribution.md"}'),n={name:"spec/decisions/0021-third-party-forms-and-contract-distribution.md"};function r(s,e,d,c,h,l){return a(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="_0021-—-third-party-forms-and-contract-distribution-wait-for-a-trust-model" tabindex="-1">0021 — Third-party Forms and contract distribution wait for a trust model <a class="header-anchor" href="#_0021-—-third-party-forms-and-contract-distribution-wait-for-a-trust-model" aria-label="Permalink to &quot;0021 — Third-party Forms and contract distribution wait for a trust model&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Two surfaces of the v1alpha3 lane promised third-party reach that nothing in the lane could back.</p><p>The provider&#39;s generic carrier, <code>takoform_resource</code>, accepted any exact FormRef as four configured strings plus one JSON <code>spec_json</code>, and applied it. It validated the FormRef grammar and that <code>spec_json</code> parsed as an object. It fetched no Form Definition, compiled no desired schema, materialized no portable defaults, learned no role, and never checked that the Definition a host serves for that ref hashes to the <code>schemaDigest</code> the configuration pinned. &quot;Carry any third-party Form by exact reference&quot; therefore meant &quot;accept an exact reference as a string&quot;: the first real feedback arrived from the host at apply time, and the digest — the whole point of an exact ref — was a value nobody compared anything to.</p><p>Earning that promise turns out to be impossible against the lane as published, and the reason is not effort. <code>schemaDigest</code> binds the RFC 8785 canonical <strong>Form Definition</strong> bytes (<a href="./../versioning.html"><code>../versioning.md</code></a>), and a Form Definition&#39;s required members include <code>title</code>, <code>role</code>, and <code>lifecycleCapabilities</code> (<a href="/schemas/v1alpha3/form-definition.schema.json"><code>../schemas/form-definition-v1alpha3.schema.json</code></a>). The only surface a host exposes for a Definition is the <code>form-definition</code> operation, whose response is <code>host-api-wire-v1alpha3.schema.json#/$defs/formDefinitionResponse</code>: an <code>additionalProperties: false</code> envelope carrying exactly <code>identity</code>, <code>displayName</code>, <code>description</code>, and <code>desiredSchema</code>. <code>role</code> is not in it, and neither is anything else the digest covers. No other v1alpha3 surface carries a role either — a Host Support Profile carries <code>formRef</code>, <code>operations</code>, supported enums, ranges, bindings, and limits, and no role.</p><p>So a client that does not already hold the Definition can never reconstruct the bytes the pinned digest names, and can never read the Form&#39;s role. Both wire documents are published and immutable (<a href="./../publication-freeze.html"><code>../publication-freeze.md</code></a>, <a href="./0014-published-schemas-are-structural-minima.html">decision 0014</a>); adding a member to either is a byte change the append-only ledger rejects, and the only alternative is minting a replacement identity, which 0014 spends its whole argument against.</p><p>What remains implementable is compiling the served <code>desiredSchema</code> and validating <code>spec_json</code> against it, materializing that schema&#39;s <code>default</code>s, and calling the host&#39;s <code>validate</code> at plan time. Every one of those rests on a document whose correspondence to the pinned digest is unverifiable, served by the same host that will later apply the spec. A carrier doing them would report schema errors in the plan and look like it had verified the Form — which is exactly the failure mode where a partial trust model is worse than none.</p><p>Interface and Binding contracts have the mirror-image problem, one layer up. They are distributed as bare Definition documents under <code>interfaces/candidates/v1alpha1/</code> and <code>bindings/candidates/v1alpha1/</code>, with no package envelope: no canonical package digest, no publisher signature, no revocation feed, no fixture closure, none of the properties <a href="./0010-exact-interface-and-binding-contracts.html">decision 0010</a> promised when it said &quot;Interface Packages follow the Form Package rules&quot;. Meanwhile 0010&#39;s own opening sentence, this repository&#39;s glossary, and <a href="./../interface-contract/"><code>../interface-contract/</code></a> all describe an Interface as &quot;independently published&quot; — a claim no artifact, verifier, or release lane supports.</p><p>An envelope cannot be added here either, and again the obstacle is an identity rather than the work. A Form Package index is <a href="/schemas/v1alpha4/package-index.schema.json"><code>../schemas/package-index-v1alpha4.schema.json</code></a>: <code>additionalProperties: false</code>, <code>kind</code> fixed to <code>FormPackage</code>, and <code>formRef</code> required. It cannot carry an InterfaceRef or a BindingRef, so an Interface or Binding Package needs its own index identity. That is a mint, and this decision does not make one.</p><p>These are MUST-level provider and specification semantics, so this repository&#39;s <code>AGENTS.md</code> requires a decision record.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Takoform ships what it can verify and says plainly what it cannot. Two rules, one per surface.</p><h3 id="_1-the-v1alpha3-provider-lane-exposes-only-typed-form-resources" tabindex="-1">1. The v1alpha3 provider lane exposes only typed Form resources <a class="header-anchor" href="#_1-the-v1alpha3-provider-lane-exposes-only-typed-form-resources" aria-label="Permalink to &quot;1. The v1alpha3 provider lane exposes only typed Form resources&quot;">​</a></h3><p><code>takoform_resource</code> is withdrawn from the provider v2.1 surface. It never shipped in a released provider: v2.1.0 is an unpublished source candidate under the <a href="./../publication-freeze.html">publication freeze</a>, so nothing that exists is broken by its removal.</p><p>The lane&#39;s registered resource set is now exactly the typed Edge Platform Family resources, one per catalog Form. Each compiles its Form&#39;s declarations into the build: the desired schema as typed attributes, the portable defaults as framework defaults, the role as its replacement and update semantics, and the exact FormRef as a compiled-in constant its state is bound to. That is what the removed carrier could not have, and the exactness of the set is itself enforced — by <code>internal/standardforms</code>, by the counting tests in <code>internal/provider</code>, and by the exact-set gates in <code>scripts/check-public-surfaces.mjs</code>.</p><p>Supporting a third-party Form means adding it to a build, not naming it in a configuration. The provider MUST NOT expose a resource whose Form identity is supplied by configuration until a client can verify that identity, which requires at minimum that a host be able to serve the canonical Form Definition bytes the <code>schemaDigest</code> covers. That is a new wire surface and therefore a new schema identity; it belongs to the next coherent generation of identities that 0014 describes, not to this change set.</p><p>Nothing else about the lane moves. The exact-identity JSON import ID, the UID-mismatch rule, and pending-operation resumption of <a href="./0017-provider-state-survives-form-evolution-and-interruption.html">decision 0017</a> are unchanged for the typed resources; only 0017&#39;s carrier-specific clause lapses with the carrier.</p><h3 id="_2-interface-and-binding-contracts-are-repository-distributed-documents" tabindex="-1">2. Interface and Binding contracts are repository-distributed documents <a class="header-anchor" href="#_2-interface-and-binding-contracts-are-repository-distributed-documents" aria-label="Permalink to &quot;2. Interface and Binding contracts are repository-distributed documents&quot;">​</a></h3><p>Interface Definitions and Binding Definitions are digest-bound documents distributed <strong>with this repository</strong>, under <code>interfaces/candidates/v1alpha1/</code> and <code>bindings/candidates/v1alpha1/</code>. They are NOT independently installable third-party artifacts, and no Interface Package or Binding Package envelope identity exists, is specified, or is published.</p><p>What the exact ref does and does not carry follows directly, and the owning specifications now say it in those words:</p><ul><li>an <code>InterfaceRef</code> or <code>BindingRef</code> <code>schemaDigest</code> binds the canonical Definition bytes, so a document that reaches a consumer intact is verifiable <strong>as bytes</strong>;</li><li>it carries no statement about where those bytes came from. There is no package digest, no publisher identity, no signature, no revocation feed, and no fixture closure over payload files, because there is no package;</li><li>a host therefore obtains these contracts the way it obtains any part of this specification, and an operator who installs one from anywhere else has exactly the assurance they arranged themselves.</li></ul><p><code>spec/interface-contract/README.md</code> and <code>spec/binding-contract/README.md</code> state this as their distribution rule, and the word &quot;published&quot; is removed from the descriptions of what an Interface is, in those documents and in <code>CONTEXT.md</code>.</p><p>When an envelope is built, it mints <code>packages.interfaces.takoform.com</code> and <code>packages.bindings.takoform.com</code> index identities together with whatever else the next generation needs, and it satisfies the existing Form Package verifier rules — one package, one definition, canonical index digest, data-only payload, closed file inventory, fixture closure — rather than a parallel set.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A user with a third-party v1alpha3 Form has no provider path today. That is the honest state: the previous path did not verify the Form, so what it offered was the ability to type an identity, not to rely on one.</li><li>The provider surface is smaller and every resource in it is derived from a Form. <code>internal/standardforms</code> no longer hand-authors any resource document or example, so &quot;every published surface comes from a catalog declaration&quot; is now true without exception.</li><li>One conformance obligation is retired, not weakened: the lane&#39;s required checks are unchanged, because the reason the carrier cannot verify a Form is a published wire schema&#39;s closed shape, not a behavior any host could get right or wrong. Adding a check would test the JSON Schema, not the host.</li><li>The specification stops claiming Interface publication it cannot perform. A reader who wants the trust properties of a Form Package now learns from the Interface and Binding documents themselves that those properties do not exist yet, instead of inferring them from the word &quot;published&quot;.</li><li>Restoring third-party reach has a named prerequisite — a host surface serving the canonical Form Definition bytes — so the next generation of schema identities has one more member to carry, and the work is a design decision rather than an implementation backlog item.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Earn the carrier&#39;s promise: fetch the Definition, verify its digest against the pinned <code>schemaDigest</code>, compile the desired schema locally, materialize the Form&#39;s defaults, read its role, and call <code>validate</code> at plan time.</strong> Rejected because three of those six are unimplementable against the published lane and the other three are misleading without them. The Form Definition response is closed to <code>identity</code>, <code>displayName</code>, <code>description</code>, and <code>desiredSchema</code>, so the canonical Definition bytes are unavailable, the digest comparison has no left-hand side, and the role has no source. What would have shipped is plan-time validation against an unverified schema plus defaults materialized from it — a plan that renders per-field diagnostics and a resource that appears to have checked its Form, over a document the pinned digest never covered. The cost of doing it anyway is the worst kind: a trust model that reports success.</li><li><strong>Keep the carrier exactly as it is and document the gap.</strong> This is the status quo, and its own reference document already carried three disclaimers — no local schema compilation, write the Form&#39;s defaults explicitly, nothing about the trust model changes on import. Rejected because a resource that requires an exact <code>schemaDigest</code>, forces replacement on it, and records it in state states by construction that the digest means something. Documentation does not undo an argument the surface makes.</li><li><strong>Add the missing members to <code>formDefinitionResponse</code>, or mint a replacement wire schema.</strong> Rejected on 0014&#39;s terms. The v1alpha3 wire schema is published and immutable, the append-only ledger and the deploy no-overwrite guard both reject a byte change, and minting one identity per blocker leaves a trail of superseded documents served forever. The surface this needs is real and is recorded above as a prerequisite for the next generation.</li><li><strong>Ship an Interface Package envelope now.</strong> Rejected because it cannot be built without minting an index schema identity: <code>package-index-v1alpha4</code> is <code>additionalProperties: false</code> with <code>kind</code> const <code>FormPackage</code> and <code>formRef</code> required, so it cannot carry an InterfaceRef, and no unpublished index identity exists to extend. Reusing the Form Package index by pretending an Interface is a Form would put a non-Form document behind a <code>FormPackage</code> claim, which is worse than having no envelope.</li><li><strong>Leave the Interface and Binding documents saying &quot;independently published&quot;.</strong> Rejected because it is the same defect as the carrier at the specification layer: a word that promises a trust property no artifact carries. A reader deciding whether to install a third-party Interface is entitled to know that installing one is not a thing this project has defined.</li></ul>',28)])])}const u=t(n,[["render",r]]);export{m as __pageData,u as default};
