import{_ as t,o as a,c as o,ag as r}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"0043 — A Form Family targets a popular primitive whose APIs are vendor-locked; de-facto standards are integrated, not respecified","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0043-forms-target-popular-vendor-locked-primitives.md","filePath":"spec/decisions/0043-forms-target-popular-vendor-locked-primitives.md"}'),s={name:"spec/decisions/0043-forms-target-popular-vendor-locked-primitives.md"};function i(d,e,n,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="_0043-—-a-form-family-targets-a-popular-primitive-whose-apis-are-vendor-locked-de-facto-standards-are-integrated-not-respecified" tabindex="-1">0043 — A Form Family targets a popular primitive whose APIs are vendor-locked; de-facto standards are integrated, not respecified <a class="header-anchor" href="#_0043-—-a-form-family-targets-a-popular-primitive-whose-apis-are-vendor-locked-de-facto-standards-are-integrated-not-respecified" aria-label="Permalink to &quot;0043 — A Form Family targets a popular primitive whose APIs are vendor-locked; de-facto standards are integrated, not respecified&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-23</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>One family exists, and its shapes all come from one platform: the Edge Platform Family fixes the Workers model. The withdrawn v1alpha2 epoch had covered more ground — relational databases, container services, key/value stores, vector indexes — but as least-common-denominator contracts, which <a href="./0008-forms-preserve-service-shape.html">decision 0008</a> rejected: a Form fixes the complete shape of one proven primitive, and what is exchangeable is the host, never the meaning.</p><p>Widening the lineup again therefore needs a selection rule, or the same drift recurs: either every cloud product grows a Form (unbounded, and most would be LCD contracts again), or Forms accrete wherever the maintainer happens to work (which is how one platform&#39;s shapes became the whole catalog).</p><p>The rule falls out of asking where a host-neutral desired-state contract adds value at all. A service category can be popular and portable already — object storage has the S3-compatible API, relational databases have the PostgreSQL wire protocol, caches have the Redis protocol, mail submission has SMTP. There a portability layer already exists, an ecosystem of clients speaks it, and a Takoform respecification would compete with a standard instead of adding one. The categories that actually lock applications in are the popular managed services whose provisioning and service APIs are vendor-specific: serverless containers, document/KV tables, schedulers, vector indexes, workflow engines — offered by every major cloud, portable to none of them.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A Form Family is minted only for a service category that is (a) popularly offered as a managed service across major providers and (b) has no de-facto standard API — the category where a host-neutral contract is the only portability there is.</strong></p><p><strong>A category with a de-facto standard API is never respecified as a Form. Takoform integrates with it instead</strong>: a Form&#39;s runtime reaches an S3-compatible bucket, a PostgreSQL database, a Redis cache, or an SMTP submission endpoint as an <strong>external standard-service binding</strong> — the standard protocol is the contract, and the host resolves the endpoint and credential the way it already resolves sensitive slots, so neither ever enters portable desired state. That binding contract is its own reviewed spec change; this record fixes the direction.</p><p>Under this rule the v1 lineup is:</p><table tabindex="0"><thead><tr><th>Family group</th><th>Fixed shape</th><th>Replaces (withdrawn)</th></tr></thead><tbody><tr><td><code>edge.forms.takoform.com</code></td><td>the Workers platform model (existing, 15 Forms)</td><td>—</td></tr><tr><td><code>container.forms.takoform.com</code></td><td>OCI-image service with immutable revisions and traffic splitting (the Cloud Run/Knative shape)</td><td><code>ContainerService</code></td></tr><tr><td><code>table.forms.takoform.com</code></td><td>document/KV table with declared keys and secondary indexes (the DynamoDB shape)</td><td><code>KeyValueStore</code>, <code>StatefulEntity</code> in part</td></tr><tr><td><code>schedule.forms.takoform.com</code></td><td>standalone cron invoking a declared target with a retry policy (the Cloud Scheduler shape)</td><td><code>Schedule</code></td></tr><tr><td><code>vector.forms.takoform.com</code></td><td>fixed-dimension vector index with declared metric and namespaces</td><td><code>VectorIndex</code></td></tr></tbody></table><p><code>workflow</code> (the Step Functions category) satisfies the rule and is recorded as a candidate; its state-language surface is large enough to be its own design effort. An <code>actor</code> family is deliberately not minted now: its proven shape is one vendor&#39;s, and the Edge family&#39;s SQLite/queue primitives cover most of its current use.</p><p>What is deliberately <strong>excluded</strong> by the rule: object storage beyond the Edge family&#39;s own bucket (S3-compatible is the standard), relational databases (PostgreSQL is the standard — the earlier <code>postgres</code> family idea dies here), caches (Redis), mail (SMTP). Each is an integration target, not a Form.</p><p>The same principle is adopted ecosystem-wide: takoserver builds on de-facto standards where they exist and defines contracts only where the category is vendor-locked. That obligation lives with takoserver; this record states the shared principle.</p><h2 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h2><p>Minting a family is a reviewed spec change, and this record is what the review holds it against: a proposed family must name the popular category, show the absence of a de-facto standard API, and name the one proven shape it fixes (decision 0008). The external standard-service binding contract arrives as its own spec change with its own conformance surface; until it lands, nothing claims it exists.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The lineup grows by four families and a recorded candidate, every one of them justified by the same sentence, and the catalog stops being one platform&#39;s silhouette. Where the industry already solved portability, Takoform rides the standard instead of fighting it — which is also what keeps the spec small enough to graduate.</p>',18)])])}const f=t(s,[["render",i]]);export{m as __pageData,f as default};
