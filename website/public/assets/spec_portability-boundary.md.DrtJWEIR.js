import{_ as t,o as a,c as r,a2 as o}from"./chunks/framework.Bbwa0s72.js";const h=JSON.parse('{"title":"Portable Form boundary","description":"","frontmatter":{},"headers":[],"relativePath":"spec/portability-boundary.md","filePath":"spec/portability-boundary.md"}'),i={name:"spec/portability-boundary.md"};function n(s,e,d,c,l,p){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="portable-form-boundary" tabindex="-1">Portable Form boundary <a class="header-anchor" href="#portable-form-boundary" aria-label="Permalink to &quot;Portable Form boundary&quot;">​</a></h1><p>This document defines what may enter a current Takoform Form. The governing rule, decided in <a href="./decisions/0008-forms-preserve-service-shape.html">decision 0008</a>:</p><blockquote><p>A Form is the smallest <strong>semantically complete</strong> desired-state contract that independent hosts can implement with the same application-visible behavior. A field or semantic rule must not be removed merely because one host can choose it privately. If two conforming hosts could make observably different choices after a field or rule is omitted, the Form is incomplete.</p></blockquote><p>Takoform does not define generic resources reduced to the least common denominator of several clouds. A Form fixes the application-visible semantics of one proven service primitive — its execution ABI, client API, data model, consistency, delivery guarantees, failure and retry behavior, update, delete, and migration units, and runtime interface — completely. Only the provider- or host-specific facts are outside the contract: account, region, SKU, price, credentials, native IDs, and internal implementation.</p><p>Implementations with different semantics are separate Forms sharing a family, never one Form with a selector token. A host supports a Form by implementing that shape with the same meaning; no host must implement every Form.</p><h2 id="ownership-layers" tabindex="-1">Ownership layers <a class="header-anchor" href="#ownership-layers" aria-label="Permalink to &quot;Ownership layers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Layer</th><th>Owns</th><th>Examples</th></tr></thead><tbody><tr><td>Form</td><td>complete application-visible semantics of one service shape</td><td>module-worker ABI, KV consistency, queue delivery guarantee, SQLite semantics</td></tr><tr><td>Interface contract</td><td>exact operation surface a resource exposes</td><td>operations, input/output types, errors, pagination, consistency</td></tr><tr><td>Binding contract</td><td>typed capability projected into a consumer</td><td>worker KV binding: runtime API + permission, no credentials</td></tr><tr><td>Host Support Profile</td><td>whether and within what limits a host implements an exact ref</td><td>supported compatibility dates, flags, metrics, dimension ceilings</td></tr><tr><td>Adapter profile</td><td>translation to one concrete backend or ecosystem</td><td>Cloudflare API mapping, S3 compatibility, workerd hosting</td></tr><tr><td>Operator policy</td><td>realized placement and operating decisions</td><td>region, replicas, capacity assignment</td></tr><tr><td>Service Offering</td><td>commercial availability</td><td>quota, size classes, SLA, price, billing</td></tr></tbody></table><h2 id="where-a-difference-lives" tabindex="-1">Where a difference lives <a class="header-anchor" href="#where-a-difference-lives" aria-label="Permalink to &quot;Where a difference lives&quot;">​</a></h2><table tabindex="0"><thead><tr><th>The difference changes…</th><th>Placement</th></tr></thead><tbody><tr><td>execution ABI, client API, consistency, persistence, delivery guarantee</td><td>a separate Form</td></tr><tr><td>the unit of update, delete, migration, or replacement</td><td>a separate Form or a separate Resource</td></tr><tr><td>how one resource connects to another</td><td>a typed Binding contract</td></tr><tr><td>orthogonal inward activation (route, trigger, consumer, domain)</td><td>an attachment Resource</td></tr><tr><td>an immutable implementation snapshot (code, assets, migrations)</td><td>a revision Resource</td></tr><tr><td>independently updated operating rules (CORS, retention, lock, lifecycle)</td><td>a policy Resource</td></tr><tr><td>what a host can accept (versions, features, ceilings)</td><td>a Host Support Profile</td></tr><tr><td>minimum execution requirements (CPU, memory)</td><td>a Form or revision resource requirement</td></tr><tr><td>SKU, price, actual allocation, placement, vendor IDs</td><td>Service Offering / host policy</td></tr><tr><td>the concrete translation to one vendor API</td><td>an adapter profile</td></tr></tbody></table><p>Resources share one Form because the same program can use them under the same assumptions — not because they belong to the same category. A JavaScript module worker, a WASI function, and an OCI container are three Forms. An eventually consistent global KV, a per-key linearizable KV, and a Redis-compatible cache are three Forms. SQLite, PostgreSQL, and MySQL databases are three Forms.</p><h2 id="free-tokens-do-not-carry-semantics" tabindex="-1">Free tokens do not carry semantics <a class="header-anchor" href="#free-tokens-do-not-carry-semantics" aria-label="Permalink to &quot;Free tokens do not carry semantics&quot;">​</a></h2><p>Open value tokens such as <code>runtime</code>, <code>engine</code>, <code>persistence</code>, <code>consistency</code>, <code>ordering</code>, or <code>projection</code> are prohibited as semantic fields in current Forms. A token a host may interpret freely turns one desired document into different systems on different hosts.</p><ul><li>A token that changes API or ABI → a separate Form.</li><li>A token that changes consistency or persistence → a separate Form.</li><li>A token that changes a binding API → a typed Binding contract.</li><li>An internal algorithm choice with identical API, storage model, and lifecycle (for example a vector distance metric) → a closed enum.</li><li>Future extension values → explicit namespaced extensions, marked non-portable and excluded from the portable core.</li></ul><h2 id="field-admission-test" tabindex="-1">Field admission test <a class="header-anchor" href="#field-admission-test" aria-label="Permalink to &quot;Field admission test&quot;">​</a></h2><p>A field belongs in a Form only when all of the following are true:</p><ol><li>a consumer needs the value to distinguish workload meaning, not merely a preferred implementation;</li><li>two independent hosts must give the value the same observable semantics;</li><li>drift, update, replacement, import, and delete behavior can be tested from the portable lifecycle;</li><li>the field does not select a vendor product, account, target, credential, price, placement, or operator-private policy;</li><li>absence of the field cannot be silently interpreted as one particular provider&#39;s default;</li><li>removing the field would let two conforming hosts choose observably different behavior.</li></ol><p>Interfaces are exact contracts, not open descriptors: an Interface Definition fixes operations, types, errors, consistency, and pagination, and is bound by digest (<a href="./decisions/0010-exact-interface-and-binding-contracts.html">decision 0010</a>). Endpoints, credentials, authorization, and protocol compatibility remain host-owned; S3-compatible access to an <code>ObjectBucket</code> is adapter material, not a desired field.</p><h2 id="current-candidate-authoring-rule" tabindex="-1">Current-candidate authoring rule <a class="header-anchor" href="#current-candidate-authoring-rule" aria-label="Permalink to &quot;Current-candidate authoring rule&quot;">​</a></h2><p>The v1alpha1 Legacy catalog and the v1alpha2 provider-v2 candidate line are prior art only. A current candidate MUST be authored from its Proposal and this boundary. A generator MUST NOT copy a Legacy or retained Definition or fixture tree and change only API version, status, or SemVer. Reusing the shared schema vocabulary and retaining a semantic field after explicit review are allowed; inheriting an old field set is not.</p><p>The retained v1alpha2 candidates under <a href="/forms/candidates/v1alpha2/candidate-set.json"><code>forms/candidates/v1alpha2</code></a> remain frozen provider-v2 preview source generated from <code>internal/currentformcatalog</code>. New family-based candidates are generated from their family catalog sources into family-scoped candidate trees; the catalog tests pin every reviewed field set, reject known substrate and operator fields, and reject open semantic tokens. Reproducibility checks prove the tracked package bytes still come from that source.</p>',20)])])}const u=t(i,[["render",n]]);export{h as __pageData,u as default};
