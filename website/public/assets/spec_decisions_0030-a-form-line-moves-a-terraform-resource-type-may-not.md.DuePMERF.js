import{_ as t,o as a,c as r,a2 as i}from"./chunks/framework.BTLSZHNB.js";const u=JSON.parse('{"title":"0030 — A Form line moves; a Terraform resource type may not","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0030-a-form-line-moves-a-terraform-resource-type-may-not.md","filePath":"spec/decisions/0030-a-form-line-moves-a-terraform-resource-type-may-not.md"}'),o={name:"spec/decisions/0030-a-form-line-moves-a-terraform-resource-type-may-not.md"};function n(s,e,d,c,h,l){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="_0030-—-a-form-line-moves-a-terraform-resource-type-may-not" tabindex="-1">0030 — A Form line moves; a Terraform resource type may not <a class="header-anchor" href="#_0030-—-a-form-line-moves-a-terraform-resource-type-may-not" aria-label="Permalink to &quot;0030 — A Form line moves; a Terraform resource type may not&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-09</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Decision <a href="./0017-provider-state-survives-form-evolution-and-interruption.html">0017</a> settled what a moving Form line costs persisted STATE: the provider dispatches on the exact FormRef recorded in state and carries one codec per supported identity, so a resource created under an earlier definition version stays addressable as itself. Decision <a href="./0011-resource-identity-generation-and-revision.html">0011</a> settled that the Form semantic identity of a resource is its exact FormRef.</p><p>Neither settles what a moving Form line costs the Terraform SCHEMA, and the schema cannot do what the codec does. There is exactly one <code>takoform_worker_version</code> schema in a provider build. Every resource of that type is decoded through it, every configuration is written against it, and the configuration is source a user maintains and a reviewer reads. A codec absorbs a definition change invisibly; a schema change is felt by every author at once.</p><p>The rule was implicit, which meant it was decided per change by whoever made one.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The rule is written down, and the mechanical half of it is enforced.</p><p><strong>The same Terraform resource type is kept</strong> when every existing attribute keeps exactly its meaning and the change is one of:</p><ul><li>adding an Optional attribute;</li><li>adding a Computed attribute, a declared output (<a href="./0025-declared-outputs-are-a-typed-contract.html">0025</a>) included;</li><li>relaxing validation;</li><li>adding an enum value that breaks neither an existing host nor existing state.</li></ul><p><strong>A new Terraform resource type is required</strong> for:</p><ul><li>removing an attribute;</li><li>changing an attribute&#39;s type;</li><li>making an attribute Required;</li><li>changing a declared output&#39;s type;</li><li>changing the Form&#39;s lifecycle role;</li><li>changing the identity or the replacement unit;</li><li>any other semantic break.</li></ul><p>A Form that breaks <code>takoform_worker_version</code>&#39;s schema becomes <code>takoform_worker_version_v2</code>, or a different Form kind. Both types then exist in one build: the old one keeps serving the state written under it through its own codec, and the new one is what new configurations write. Removing the old type is a provider MAJOR, governed by &quot;Provider versions are independent&quot; in <a href="./../versioning.html"><code>../versioning.md</code></a>.</p><p>Enforcement is split, because the rule is:</p><ol><li><strong>A committed surface floor.</strong> <code>internal/provider/testdata/v3-schema-baseline.json</code> records every v1alpha3 resource type, its schema version, and each attribute&#39;s type and Required/Optional/Computed/Sensitive flags. A test derives the current surface from the live schemas and refuses any difference that is not on the allowed list — naming the <code>_v2</code> type that would have to be minted instead. It is a FLOOR, not a snapshot: an additive change needs no edit and passes. The baseline is hand-committed and never rewritten by a gate, because a baseline a gate rewrites agrees with every change.</li><li><strong>A schema version and a registered state upgrader on every resource of the lane.</strong> The schema version is what lets a resource type outlive a change to its own state layout, and a state upgrader is how state written at an earlier version becomes readable at the current one. Version 1 is the first numbered layout; the upgrade from version 0 is a whole-value carry, declared with a prior schema so the framework decodes the old value rather than a hand-written parse losing an attribute. The lane ships in provider v2.1.0, an unpublished source candidate, so no user state exists at version 0 outside development — registering the mechanism now is what makes it available on the day the upgrade is not an identity.</li><li><strong>Review.</strong> Whether an attribute that kept its name, type, and flags kept its MEANING is not decidable by a test. The rule states it and review carries it.</li></ol><p>The behavioural half is covered end to end against a synthetic second definition version of one Form: a NEW resource is created under the current identity while state recorded under the OLD one is refreshed, imported by its exact FormRef, read for the outputs that definition published, and deleted — all through ONE Terraform resource type.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A Form line may advance additively without a provider surface change, which is what <a href="./0017-provider-state-survives-form-evolution-and-interruption.html">0017</a> promised and this makes checkable.</li><li>A breaking Form major now has a named cost in the provider — a second resource type and a migration an author performs deliberately — rather than an implicit one discovered by users.</li><li>The baseline is a file a reviewer reads. A change that edits it is a change that is claiming something about compatibility, and the diff says which.</li><li>Every v1alpha3 resource carries schema version 1 from this decision onward. Fresh state is written at version 1; there is no user state at version 0 to migrate.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Pin the schema exactly and require a re-pin for every change.</strong> Rejected because it makes the additive case — the common, permitted one — cost a file edit and a review argument, and because a gate that must be re-pinned constantly stops being read.</li><li><strong>Derive the rule from the Form&#39;s own SemVer.</strong> A Form minor is additive in the DESIRED contract, which is not the same claim as additive in the Terraform surface: the provider adds envelope attributes, recovery attributes, and typed outputs that no Form version describes. The surface has to be measured directly.</li><li><strong>Version the resource type name on every Form definition version (<code>..._v0_1_0</code>).</strong> Rejected because it makes every author&#39;s configuration churn on changes that cost them nothing, and it defeats the codec machinery that exists precisely so one type can serve several identities.</li><li><strong>Leave the schema version at 0 and add an upgrader when one is first needed.</strong> Rejected because the first time a state layout has to move is the worst time to discover the mechanism is absent, and because the cost of registering it while the lane is an unpublished candidate is zero.</li></ul>',20)])])}const p=t(o,[["render",n]]);export{u as __pageData,p as default};
