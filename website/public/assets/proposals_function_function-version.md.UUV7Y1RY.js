import{_ as t,o,c as a,ag as n}from"./chunks/framework.cyF4XceO.js";const m=JSON.parse('{"title":"FunctionVersion — takoform_function_version","description":"","frontmatter":{},"headers":[],"relativePath":"proposals/function/function-version.md","filePath":"proposals/function/function-version.md"}'),i={name:"proposals/function/function-version.md"};function r(s,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="functionversion-—-takoform-function-version" tabindex="-1">FunctionVersion — <code>takoform_function_version</code> <a class="header-anchor" href="#functionversion-—-takoform-function-version" aria-label="Permalink to &quot;FunctionVersion — `takoform_function_version`&quot;">​</a></h1><h2 id="workload-and-consumer" tabindex="-1">Workload and consumer <a class="header-anchor" href="#workload-and-consumer" aria-label="Permalink to &quot;Workload and consumer&quot;">​</a></h2><p>A team ships one executable snapshot of a function: which artifact runs, which export handles events, its non-secret vars, its sealed slots, and the resource bounds it runs under. Deployments select among versions; nothing ever edits a version in place.</p><h2 id="role" tabindex="-1">Role <a class="header-anchor" href="#role" aria-label="Permalink to &quot;Role&quot;">​</a></h2><p><code>revision</code>. Every field is immutable; a change is a new Function Version.</p><h2 id="observable-semantics" tabindex="-1">Observable semantics <a class="header-anchor" href="#observable-semantics" aria-label="Permalink to &quot;Observable semantics&quot;">​</a></h2><p><code>function</code> is the immutable reference pinning this version to its owning <a href="./function.html">Function</a> identity, resolved and uid-pinned like every relation and carrying <code>x-takoform-target-formrefs</code> — exactly the Edge family&#39;s <code>WorkerVersion.worker</code> shape, and for the same reason: without the parent reference a host cannot say which function&#39;s aggregate a version belongs to.</p><p>The runtime a version runs on is not a field of this Form. It is the exact <code>function.runtime@1.0.0</code> contract the Function identity provides, so there is no <code>compatibilityDate</code> and no <code>compatibilityFlags</code> (<a href="./../../spec/decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>).</p><p><code>artifact</code> is one committed manifest digest under the content-addressed artifact transport (<a href="./../../spec/decisions/0012-artifacts-use-content-addressed-upload.html">decision 0012</a>, <a href="./../../spec/artifact-transport/">spec/artifact-transport</a>). The family adds a <code>FunctionBundle</code> manifest kind: a main module plus digest-pinned modules, whose closed media-type policy is what <code>function.runtime</code> loads plus the source maps a bundle carries without importing. Before any mutation a host resolves the manifest and holds it to the artifact contract: an uncommitted digest is <code>artifact_missing</code>, and a manifest of another kind or shape is <code>artifact_invalid</code>. There is no separate bundle Form: the manifest digest is already the build&#39;s whole content identity and the transport deduplicates shared bytes, so a standalone bundle resource would add a name without adding identity.</p><p><code>handler</code> names the export of the main module the host invokes; the runtime contract requires that the declared export exist and be callable. <code>vars</code> is a bounded data-only JSON map projected as environment members. <code>requiredSensitiveVars</code> declares only the names of sealed values the host must supply, under the same permitted vocabulary the Edge family records (the Form Package data-only policy forbids the token <code>secret</code> in field names).</p><p><code>externalServices</code> is carried from birth (<a href="./../../spec/decisions/0045-external-standard-services-are-sealed-slots.html">decision 0045</a>, <a href="./../../spec/standard-services/">spec/standard-services</a>): each entry is exactly <code>{&quot;name&quot;: &quot;DB&quot;, &quot;service&quot;: {&quot;apiVersion&quot;: &quot;standards.takoform.com/v1alpha1&quot;, &quot;protocol&quot;: &quot;postgresql&quot;}}</code>, with the closed protocol vocabulary <code>s3-compatible</code>, <code>postgresql</code>, <code>redis</code>, <code>smtp</code>. The host satisfies a slot with connection material; endpoints and credentials never enter portable state. The version projects vars, sensitive names, and slot projections into ONE process-environment namespace, and because slot projections are derived member names (<code>DB_URL</code>, <code>MEDIA_ENDPOINT</code>, …), uniqueness is enforced over the PROJECTED names, not only the declared union: a var literally named <code>DB_URL</code> collides with a <code>postgresql</code> slot named <code>DB</code>, and a host refuses the collision before mutation.</p><p><code>memoryMiB</code> is the bound of usable memory: exceeding it fails the invocation observably. <code>timeoutSeconds</code> is the wall-clock budget per invocation: on expiry the invocation is terminated and reported failed. <code>maxConcurrency</code> bounds simultaneous invocations of this version: excess invocations are refused observably as throttling, never queued indefinitely. These bounds change what an application and its callers observe and are therefore desired state; the admitted ranges are Host Support Profile facts.</p><h2 id="why-this-is-one-form" tabindex="-1">Why this is one Form <a class="header-anchor" href="#why-this-is-one-form" aria-label="Permalink to &quot;Why this is one Form&quot;">​</a></h2><p>Code, handler, environment, sealed slots, and resource bounds must travel as one immutable unit, or rollback cannot be exact: re-activating an old version must restore exactly the snapshot it was verified with.</p><h2 id="what-would-require-a-separate-form" tabindex="-1">What would require a separate Form <a class="header-anchor" href="#what-would-require-a-separate-form" aria-label="Permalink to &quot;What would require a separate Form&quot;">​</a></h2><p>Mutable in-place configuration or a per-environment overlay model breaks the immutable-snapshot shape. A version whose artifact is an OCI image is the container family&#39;s <code>ContainerRevision</code>, not a variant of this Form.</p><h2 id="provided-interfaces" tabindex="-1">Provided Interfaces <a class="header-anchor" href="#provided-interfaces" aria-label="Permalink to &quot;Provided Interfaces&quot;">​</a></h2><p>None. The runtime contract belongs to the <a href="./function.html">Function</a> identity.</p><h2 id="accepted-bindings" tabindex="-1">Accepted Bindings <a class="header-anchor" href="#accepted-bindings" aria-label="Permalink to &quot;Accepted Bindings&quot;">​</a></h2><p>None in the MVP, and the absence is honest rather than an oversight. Current Edge Interfaces such as <code>edge.kv</code>, <code>edge.sql</code>, and <code>edge.queue</code> state the JavaScript surface their <code>module-worker.*</code> bindings project into <code>worker.runtime</code>&#39;s environment. The retained v1beta1 <code>edge.objects</code> Interface is historical and is not a current Function capability. Projecting any typed binding into this family&#39;s runtime requires either a stated projection into <code>function.runtime</code> or a wire-level realization of the Interface, and the binding contracts define neither today. The MVP reaches external state through the standard-service slots above; typed cross-family bindings arrive when that realization is specified as its own reviewed contract. This proposal does not invent it.</p><h2 id="lifecycle-risks" tabindex="-1">Lifecycle risks <a class="header-anchor" href="#lifecycle-risks" aria-label="Permalink to &quot;Lifecycle risks&quot;">​</a></h2><p>Creating a version whose manifest is uncommitted or invalid must fail (<code>artifact_missing</code> / <code>artifact_invalid</code>) before mutation, and a referenced manifest and its blobs stay readable for as long as the version exists. Deleting a version still weighted by a deployment must fail with <code>dependency_in_use</code>. A REQUIRED external-service slot a host cannot satisfy is refused at plan time with <code>unsupported_capability</code>, and an unsatisfied slot makes the resource not Ready. Import must reproduce the exact snapshot, digest and handler included.</p><h2 id="prior-art" tabindex="-1">Prior art <a class="header-anchor" href="#prior-art" aria-label="Permalink to &quot;Prior art&quot;">​</a></h2><p>The published function version of a proven cloud function service — artifact, handler string, environment, memory and timeout — with its sealed slots and external services made explicit contract. The withdrawn v1alpha2 <code>EdgeWorker</code> candidate is prior art in part.</p>',24)])])}const p=t(i,[["render",r]]);export{m as __pageData,p as default};
