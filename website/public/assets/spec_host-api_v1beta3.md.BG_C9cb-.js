import{_ as t,o as a,c as o,ag as n}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"Host API v1beta3","description":"","frontmatter":{},"headers":[],"relativePath":"spec/host-api/v1beta3.md","filePath":"spec/host-api/v1beta3.md"}'),i={name:"spec/host-api/v1beta3.md"};function s(r,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="host-api-v1beta3" tabindex="-1">Host API v1beta3 <a class="header-anchor" href="#host-api-v1beta3" aria-label="Permalink to &quot;Host API v1beta3&quot;">​</a></h1><p><code>forms.takoform.com/v1beta3</code> is the current Host API lane. It was minted for protocol reasons (<a href="./../decisions/0039-a-lane-is-minted-for-one-of-two-reasons.html">decision 0039</a>), and the reason is stated in <a href="./../decisions/0048-the-protocol-states-mechanisms-not-forms.html">decision 0048</a>: <strong>this document states MECHANISMS and names no Form kind of any family.</strong></p><p>Its predecessor named Edge Form kinds sixty-six times, in sections about one family&#39;s cardinality rules, its assigned address, and its migration ledger — which is why a family could not gain a Form without the protocol changing. Here a Definition declares which mechanism it instantiates and a host reads that, so a family adds a Form, or a rule of a shape this lane already knows, without this document moving. A rule of a NEW shape still needs a reviewed protocol change, because a new shape is a new thing every host must be able to enforce.</p><p>The wire is otherwise its predecessor&#39;s, which is what makes the mechanisms the whole of the difference. The v1beta2 and v1beta1 lanes remain served history at its own discovery path; nothing here restates what its documents meant.</p><p>The lane carries namespaced FormRef groups, UID/generation/revision resource identity, long-running Operations, content-addressed artifact upload, and Host Support Profiles. This document is normatively self-contained: every error code it may answer, every fence it may demand, and every query key it may read is defined here or in the machine tables it names.</p><p>The wire schema is <a href="/schemas/v1beta2/host-api-wire.schema.json"><code>../schemas/host-api-wire-v1beta2.schema.json</code></a>; the machine operation table is <a href="operations-v1beta2.json"><code>operations-v1beta2.json</code></a>.</p><h2 id="discovery" tabindex="-1">Discovery <a class="header-anchor" href="#discovery" aria-label="Permalink to &quot;Discovery&quot;">​</a></h2><p><code>GET /.well-known/takoform/v1beta3</code> returns a document validating against <a href="/schemas/v1beta2/host-discovery.schema.json"><code>../schemas/host-discovery-v1beta2.schema.json</code></a>: <code>api_versions</code> is exactly <code>[&quot;forms.takoform.com/v1beta3&quot;]</code>; the features <code>service_forms</code>, <code>exact_form_ref</code>, <code>optimistic_concurrency</code>, <code>idempotent_lifecycle</code>, <code>operations</code>, <code>artifact_upload</code>, and <code>support_profiles</code> are all required and true; <code>endpoints.api</code> is same-origin with path <code>/apis/forms.takoform.com/v1beta3</code>. Each lane has its own discovery path; a v1alpha2 client can never select this lane accidentally.</p><p>Every advertised endpoint path is compared in its escaped form and MUST carry no percent-encoding at all. A client rejects the discovery document otherwise (<a href="./../decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a>): an escaped path describes a shape this lane does not have, and comparing the decoded path instead would let <code>%2Fv1beta3</code> pass as <code>/v1beta3</code>.</p><p>A client negotiates each lane independently, under a deadline of its own that is short and separate from its resource-operation deadline. Nothing about one lane&#39;s outcome may make another lane&#39;s resources unusable, and each resource type reports its own lane&#39;s negotiation error.</p><h2 id="shared-wire-rules" tabindex="-1">Shared wire rules <a class="header-anchor" href="#shared-wire-rules" aria-label="Permalink to &quot;Shared wire rules&quot;">​</a></h2><p>These rules predate this lane and are restated here so the lane&#39;s contract is closed under this document alone.</p><p><strong>Same-origin endpoints.</strong> JSON Schema cannot compare URL origins. After schema validation, an implementation MUST parse and normalize every advertised URL and MUST reject any endpoint whose scheme, host, and effective port do not equal those of <code>endpoints.api</code>. Userinfo, query, and fragment components are forbidden. Plain HTTP is allowed only for a loopback development origin. A provider sends bearer credentials only to same-origin advertised URLs.</p><p><strong>Space identity.</strong> <code>SpaceID</code> is the portable scope identity used by Resource bodies, query parameters, idempotency scope, provider configuration, and provider state. It is an opaque, case-sensitive string, not a <code>PatternName</code>. A valid <code>SpaceID</code> is valid UTF-8 containing 1 through 255 Unicode code points; does not start or end with a Unicode <code>White_Space</code> code point or <code>U+FEFF</code>; contains no Unicode <code>Cc</code> control code point (which includes <code>U+007F</code>); and contains no <code>/</code>. The normative code-point sets are encoded explicitly in <a href="/schemas/v1beta2/host-api-wire.schema.json"><code>../schemas/host-api-wire-v1beta2.schema.json</code></a> under <code>$defs.spaceId</code>. Embedded non-control whitespace is data and is valid. Every participant MUST preserve the exact decoded value: no trimming, Unicode normalization, or case folding. URL percent-encoding MAY represent that value on the wire, but decoding MUST recover the same code-point sequence. Import uses either <code>NAME</code> with a configured default or <code>SPACE/NAME</code>; forbidding <code>/</code> in <code>SpaceID</code> makes that split unambiguous.</p><p><strong>I-JSON raw-first decoding.</strong> Every Host API request and response document MUST be UTF-8 I-JSON. Before typed decoding, an implementation MUST validate the complete raw document, including nested objects, and reject invalid UTF-8, duplicate object member names, invalid Unicode, non-I-JSON numbers, excessive nesting, and trailing data. A request with a duplicate member or an unknown envelope/metadata field fails as <code>invalid_argument</code> / HTTP 400 before mutation. The wire schema&#39;s <code>x-takoform-equals</code> annotations (the envelope <code>apiVersion</code> and <code>kind</code> mirroring <code>/form/formRef/*</code>) are enforced at the same typed-validation stage, before any fence: a mirrored member that disagrees is <code>invalid_argument</code> naming both pointers. A response with duplicate members is protocol-invalid before any typed or stable-error semantics are applied. The same raw-first rule applies when decoding this repository&#39;s digest-pinned conformance contract; a duplicate member is verification failure, never last-member-wins.</p><p><strong>Idempotency-key namespacing and replay fingerprinting.</strong> Every MUTATING fenced request carries a deterministic <code>Idempotency-Key</code> (observe, the fenced non-mutation, carries none), and a retry reuses the same key. The key is not a bearer capability and never a global cache address: a host MUST namespace a replay record by its authenticated tenant/security domain, authenticated principal, exact Space, and <code>Idempotency-Key</code>, and MUST authenticate and authorize the current request before consulting or returning that record. A same-key request from another principal or tenant is an independent request, and a principal whose credential or permission has since been revoked receives the current <code>unauthenticated</code>, <code>permission_denied</code>, or <code>policy_denied</code> result rather than a cached success. The replay fingerprint also binds the method, exact request target, generation preconditions, and exact request body bytes; reusing a key for different request bytes fails as <code>invalid_argument</code>. A key is 1 through 255 visible ASCII characters (0x21–0x7E); anything else is <code>invalid_argument</code>. A same-key, same-fingerprint request that arrives while the first is STILL EXECUTING is not replayed and not run twice: the host answers <code>resource_busy</code> (retryable) until the first execution records its outcome. A replay record survives at least as long as the incarnation it reports and, for an accepted 202, at least as long as its operation record; a host MAY expire older records and says nothing when it does — an expired key simply executes as new.</p><h2 id="path-shape" tabindex="-1">Path shape <a class="header-anchor" href="#path-shape" aria-label="Permalink to &quot;Path shape&quot;">​</a></h2><p>A namespaced Form group travels as <strong>two ordinary path segments</strong> — the group name, then the group version — wherever a URL template names a group:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{api}/resources/{formGroup}/{formVersion}/{kind}/{name}</span></span>
<span class="line"><span>{api}/form-definitions/{formGroup}/{formVersion}/{kind}</span></span>
<span class="line"><span>{api}/support/forms/{formGroup}/{formVersion}/{kind}/{definitionVersion}</span></span></code></pre></div><p>So <code>edge.forms.takoform.com/v1beta3</code> travels as <code>edge.forms.takoform.com/v1beta3</code>. <strong>No path segment ever percent-encodes a slash.</strong> Proxies, gateways, and web frameworks disagree about whether <code>%2F</code> inside a path segment is passed through, decoded, rejected, or normalized, so a lane that required it could not be placed behind ordinary infrastructure at all (<a href="./../decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a>). A host rejoins the two segments into the exact apiVersion; the FormRef <code>apiVersion</code> string is unchanged everywhere else — request bodies, responses, and the <code>group</code> query key still carry <code>edge.forms.takoform.com/v1beta3</code> verbatim. This is the required conformance check <code>namespaced-group-travels-as-two-path-segments</code>.</p><h2 id="addressing-a-resource" tabindex="-1">Addressing a resource <a class="header-anchor" href="#addressing-a-resource" aria-label="Permalink to &quot;Addressing a resource&quot;">​</a></h2><p>The lane&#39;s resources are addressed by five facts: the authenticated tenant, the Space, and the exact FormRef&#39;s group, kind, and name. Three of them travel as path segments (<code>{formGroup}/{formVersion}/{kind}/{name}</code>); the remaining exactness travels differently by direction, and the split is normative:</p><ul><li><strong>Requests with a body</strong> (<code>validate</code>, <code>prepare</code>, <code>apply</code>, <code>import</code>) carry the Space as <code>metadata.space</code> and the whole exact FormRef as <code>form.formRef</code> inside the document. Nothing exact travels in the query.</li><li><strong>Requests without a body</strong> (<code>read</code>, <code>observe</code>, <code>delete</code>, and the form-definition read) carry <code>space</code>, <code>definitionVersion</code>, and <code>schemaDigest</code> as query parameters, exactly as <a href="operations-v1beta2.json"><code>operations-v1beta2.json</code></a> lists per operation. A listed required parameter that is absent or malformed is <code>invalid_argument</code>; a well-formed exact query naming a ref the resource is not bound to is <code>resource_not_found</code>, indistinguishably from a name nobody created. <code>observe</code> carries an empty body; only its fence header and query address it.</li><li><code>definitionVersion</code> values never carry SemVer build metadata (an installed definition version is forbidden to have any: the digest already distinguishes builds), and a <code>+</code> that does appear in a query value MUST travel percent-encoded as <code>%2B</code>; a host decodes the query before matching.</li></ul><p>The machine table&#39;s <code>exactFormQuery</code> flag marks every operation addressed this way, and <code>exactFormQueryMeaning</code> in the same file is its definition.</p><h2 id="resource-identity" tabindex="-1">Resource identity <a class="header-anchor" href="#resource-identity" aria-label="Permalink to &quot;Resource identity&quot;">​</a></h2><p>A portable resource name matches <code>^[a-z]([a-z0-9-]{0,61}[a-z0-9])?$</code>: 1–63 lowercase characters, starting with a letter, never ending in a hyphen — so every name is also a valid DNS label, and the hostname grammars of this family never diverge from the names beneath them. Names are stable wire identities; nobody trims, folds, or normalizes one.</p><ul><li><p><code>metadata.name</code> is the only name; Form desired schemas do not carry a <code>name</code> field.</p></li><li><p><code>metadata.uid</code> is host-issued and immutable. Delete followed by re-create of the same name yields a new UID. A client MAY fence any mutation with <code>expectedUid</code>; mismatch fails <code>uid_mismatch</code> (409).</p></li><li><p><code>metadata.generation</code> increments only when the portable desired spec changes. Desired-state mutations fence on the expected generation (<code>Takoform-Expected-Generation</code> header or <code>expectedGeneration</code> body field); a stale fence fails <code>generation_conflict</code> (412).</p></li><li><p><code>metadata.revision</code> increments whenever the representation — including status, conditions, and outputs — changes. The strong ETag is the quoted revision; <code>If-Match</code> fences on it and a stale value fails <code>revision_conflict</code> (412).</p></li><li><p><strong>A <code>delete</code> fences on the expected generation, and MUST NOT be required to fence on the revision.</strong> A delete withdraws desired state, so it is a desired-state mutation and carries the same fence every other one carries: the <code>Takoform-Expected-Generation</code> header, REQUIRED, with a stale value failing <code>generation_conflict</code> (412) and an absent one <code>invalid_argument</code> (400). A client MAY additionally send <code>If-Match</code>, and a host that is given one MUST honor it and answer <code>revision_conflict</code> (412) when the representation moved — but a host MUST NOT require it, and MUST NOT refuse an otherwise valid delete because the revision moved. The revision moves for reasons the deleting client did not cause: a host-side status change, and the derived rendering below. Removing an aggregate means removing its dependents first, so a client&#39;s own teardown moves the revision of the resource it is about to delete next, after the plan that computed the teardown read it — and requiring the representation fence would refuse a destroy on a change the destroy itself made, with no repair available, because the next dependent moves it again. The generation moves only when a client changes a desired spec, which is the only &quot;it changed under me&quot; a deleter can act on; the incarnation question is <code>expectedUid</code>&#39;s, and an accepted delete resolves through the recorded uid before it evaluates any fence. These are the required conformance checks <code>delete-generation-fence</code> and <code>delete-fence-survives-derived-rendering</code>, and they are decided by <a href="./../decisions/0011-resource-identity-generation-and-revision.html">decision 0011</a>.</p></li><li><p>A host that renders any part of a representation from OTHER resources MUST advance that resource&#39;s <code>metadata.revision</code> when the rendering changes, and MUST NOT move its <code>metadata.generation</code>: no desired spec changed. This lane has two such renderings — relation drift and derived readiness — so creating, re-weighting, or deleting a resource whose selection another resource&#39;s readiness follows moves that resource&#39;s revision, and a target that is deleted or replaced moves the revision of every source pinned to it. Serving the changed representation under the old revision would make the ETag a validator that reports &quot;unchanged&quot; about a change, and <code>If-Match</code> a fence on a representation the client never saw. After an accepted mutation a host recomputes the rendering of every resource that mutation can affect and advances only those that actually changed, so an idempotent re-apply moves nothing anywhere. This is a required conformance check (<code>dependent-revision-advances-with-rendering</code>), and it is decided by <a href="./../decisions/0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a>. It is also exactly why a delete does not fence on the revision, above: the rendering a teardown moves is the rendering of what the teardown deletes next.</p></li><li><p><code>status.observedGeneration</code> names the desired generation the status reflects; <code>status.conditions</code> uses the closed types <code>Ready</code>, <code>Reconciling</code>, <code>Degraded</code>, <code>Drifted</code>, <code>Blocked</code>, <code>Deleting</code> with closed portable reasons and an optional non-portable <code>hostReason</code>. The condition model is exact: <code>Ready</code> is present in EVERY status; every other type appears only while its state holds; a type appears at most once per status; and the valid type-reason pairs are closed —</p><table tabindex="0"><thead><tr><th>Type</th><th>Valid reasons</th><th>Present when</th></tr></thead><tbody><tr><td><code>Ready</code></td><td><code>Available</code>, <code>Provisioning</code>, <code>UnsupportedCapability</code>, <code>ExternalChange</code>, <code>DependencyMissing</code></td><td>always; status True/False/Unknown says whether the resource serves</td></tr><tr><td><code>Reconciling</code></td><td><code>Provisioning</code></td><td>the host is actively converging this resource toward its desired generation</td></tr><tr><td><code>Degraded</code></td><td><code>ExternalChange</code>, <code>DependencyMissing</code></td><td>the resource serves, worse than desired</td></tr><tr><td><code>Drifted</code></td><td><code>ExternalChange</code></td><td>observed backend state departed from what the host materialized</td></tr><tr><td><code>Blocked</code></td><td><code>UnsupportedCapability</code>, <code>DependencyMissing</code></td><td>convergence cannot proceed without an outside change</td></tr><tr><td><code>Deleting</code></td><td><code>Provisioning</code></td><td>an accepted delete has not reached its terminal state</td></tr></tbody></table><p>A reason outside its type&#39;s row, a missing <code>Ready</code>, or a duplicated type is protocol-invalid. A client surfaces the WHOLE condition list, not a boolean derived from it: the reason is what says why a resource is not ready, and a client that discards it forces its operator to leave the tool to find out (<a href="./../decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a>). Conditions are host-rendered state that changes without any desired spec changing, so a client MUST NOT carry a previous value forward as if it were still true.</p></li><li><p><strong>A replay record does not outlive the incarnation it reports.</strong> A host MUST NOT answer a request from a recorded idempotency replay once the incarnation that recording reports as LIVE no longer exists; such a record is RETIRED and the request is executed as the new request it is. A record is bound to the <code>metadata.uid</code> its recorded answer carries — a create, an update, an adoption, and an observe all report one. A record whose answer reports no live incarnation is bound to nothing and MUST keep replaying: that is a successful delete, whose <code>204</code> reports the incarnation gone, and every refusal. An accepted <code>202</code> is bound to its Operation and follows that operation&#39;s outcome. Retirement is by INCARNATION, never by name: a later resource taking the same name retires nothing, and a host MAY still expire records by age or capacity. Both halves matter. Without retirement a <code>destroy</code> followed by an <code>apply</code> of an unchanged configuration is answered the old <code>201</code> forever and never converges, because a create&#39;s prepare binds the create markers so the re-create derives a byte-identical key and fingerprint; retire a delete&#39;s own record and the retried delete of a lost response is EXECUTED against whichever incarnation holds the name now. These are the required conformance checks <code>apply-idempotency-replay</code> and <code>replay-record-retires-with-its-incarnation</code>, and they are decided by <a href="./../decisions/0011-resource-identity-generation-and-revision.html">decision 0011</a>.</p></li><li><p>The Form semantic identity is the exact FormRef. The package digest used at installation is audit evidence (<code>form.packageDigest</code>, optional in responses); it never enters queries, fences, or state identity, and a host that installed the same FormRef from a different legitimate package MUST read and delete the same resources.</p></li><li><p>An exact FormRef query is answered about the WHOLE identity or not at all. A host MUST fail <code>form_unknown</code> (404) when the <code>definitionVersion</code> or <code>schemaDigest</code> it was given names a definition it does not have, on <code>{api}/forms</code>, on <code>{api}/form-definitions/…</code>, and on every resource route — it MUST NOT fall back to matching the group and kind. A client dispatches on the exact FormRef recorded in its state precisely so that a resource created under an earlier definition version stays addressable as itself (<a href="./../decisions/0017-provider-state-survives-form-evolution-and-interruption.html">decision 0017</a>), and a host that matched by kind would answer every such request about a different contract, successfully, with nothing downstream able to detect it. This is the required conformance check <code>exact-form-ref-fails-closed-on-unknown-definition</code>.</p></li></ul><h3 id="an-adoption-claims-the-native-identity-it-names" tabindex="-1">An adoption claims the native identity it names <a class="header-anchor" href="#an-adoption-claims-the-native-identity-it-names" aria-label="Permalink to &quot;An adoption claims the native identity it names&quot;">​</a></h3><p>The rules of this section are decided by <a href="./../decisions/0011-resource-identity-generation-and-revision.html">decision 0011</a>.</p><p><code>import</code> carries a <code>nativeId</code> — compared byte-exact, with no trimming, case folding, or Unicode normalization anywhere in the claim index — the host&#39;s own name for a backend object that already exists. It is a CLAIM, not a hint.</p><ul><li><strong>A host RECORDS the <code>nativeId</code> of every adoption on the resource it adopts, and holds it exclusively within the caller&#39;s tenant.</strong> At most one live resource of one tenant holds one native identity. An adoption naming an identity another live resource of that tenant already holds fails <code>import_conflict</code> (409) before any mutation and stores nothing.</li><li><strong>A recorded claim is immutable.</strong> An <code>import</code> addressing an EXISTING resource of the caller under a different <code>nativeId</code> fails <code>import_conflict</code> (409) before any mutation. Re-importing that resource under the identity it already holds is not a conflict: it is the ordinary fenced import and answers the same incarnation. A resource this host created holds no claim, so the FIRST <code>import</code> naming it records one rather than changing one — and that import is the ordinary one, <code>terraform import</code> onto an address a configuration already manages.</li><li><strong>The claim is released with its holder, and by nothing else.</strong> Once the resource holding an identity is deleted, that identity is adoptable again — otherwise a <code>destroy</code> would leave a backend object permanently unimportable. An ordinary <code>apply</code> on the holder withdraws nothing: a resource that survives an update keeps the object it was adopted onto.</li><li><strong>The claim spans every space of one tenant, and every Form kind, and stops at the tenant.</strong> Spaces partition one tenant&#39;s resources and a backend object does not partition with them, so two spaces adopting one object is the same duplication as two resources in one space. Neither is an object partitioned by the Form that adopted it: it has one identity, so two resources of one tenant may not both name it whatever their kinds, and a host keying the claim by <code>(tenant, kind, nativeId)</code> is holding a narrower claim than this one. It stops at the tenant for the reason every refusal in <a href="#tenant-isolation">Tenant isolation</a> is shaped the way it is: a host-wide claim would answer &quot;somebody already manages that&quot; to a caller who cannot see the holder, which is a membership oracle over every identifier a stranger can guess. Whose account an object lives in is authority this lane does not model. Inside each tenant the claim binds in full, for every tenant.</li></ul><p>These are the required conformance checks <code>import-claims-its-native-identity</code> and <code>import-records-its-native-identity</code>, with the tenant edge driven by <code>resource-import-is-tenant-isolated</code>. They exist because every other thing this lane asks of <code>import</code> — a minted uid, generation <code>1</code>, the full validation gauntlet, the claim scan of <a href="./../decisions/0026-attachment-claims-are-canonical-and-acyclic.html">decision 0026</a> — a plain create also satisfies. A host whose <code>/import</code> created would pass the rest of the corpus while <code>terraform import</code> against it minted a NEW backend object and orphaned the one being adopted. Each is measured at the scope written above and not at a convenient narrowing of it: the rival that may not adopt is a different Form kind from the holder, the release is measured by that other kind, and the first claim is driven onto a resource the host created as well as onto one adoption brought into being.</p><p><strong>The <code>nativeId</code> never appears in a response, and no rule above asks a portable author to know one.</strong> A native identifier is host detail, outside the Form contract (<a href="./../portability-boundary.html">portability boundary</a>), and the published wire documents are closed (<a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a>), so what is observable is not the identifier but what a host holding it can no longer do. What this lane therefore does NOT prove is that the object named existed before the call, or that the host did not also mint a fresh one beside it: no portable surface reports either, and a black-box runner cannot name an object it knows a host already has without depending on that host&#39;s identifier format. Publication blocker <strong>V3-014</strong> records that remainder.</p><h3 id="what-the-availability-booleans-mean" tabindex="-1">What the availability booleans mean <a class="header-anchor" href="#what-the-availability-booleans-mean" aria-label="Permalink to &quot;What the availability booleans mean&quot;">​</a></h3><p>The <code>/forms</code> answer&#39;s members are truth-conditioned, not vibes:</p><ul><li><code>definitionKnown</code> — the host holds the exact Definition bytes the <code>schemaDigest</code> names and can serve them from the form-definition route.</li><li><code>installed</code> — code compiled against that exact contract exists in this host. Implies <code>definitionKnown</code>.</li><li><code>executable</code> — that code can actually run here now (its runtime present, its own dependencies loadable). Implies <code>installed</code>.</li><li><code>activated</code> — the host&#39;s operator policy allows NEW resources of this exact Form for this tenant. Never implied by the previous three; deactivation refuses creates while every existing resource keeps its whole lifecycle.</li><li><code>availableToPrincipal</code> — the requesting principal may address this exact Form at all; false hides nothing else in the row.</li></ul><p><code>form_not_installed</code> is <code>definitionKnown &amp;&amp; !installed</code> at a lifecycle route; <code>form_unavailable</code> is <code>installed</code> with transiently absent capacity. There is no <code>deprecated</code> member in this lane: a deprecation surface arrives with the deprecation exercise, as its own reviewed change, with a source of truth.</p><h3 id="the-installed-set-is-keyed-by-the-exact-identity" tabindex="-1">The installed set is keyed by the exact identity <a class="header-anchor" href="#the-installed-set-is-keyed-by-the-exact-identity" aria-label="Permalink to &quot;The installed set is keyed by the exact identity&quot;">​</a></h3><p>The rules of this section are decided by <a href="./../decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>.</p><ul><li>A host&#39;s installed set is keyed by the whole <code>{apiVersion, kind, definitionVersion, schemaDigest}</code> tuple, never by group and kind. Two definition versions of one group and kind MUST be installable at the same time and MUST answer independently on <code>{api}/forms</code>, on <code>{api}/form-definitions/…</code>, and on <code>{api}/support/forms/…</code>. A host MUST NOT install two Definitions that agree on group, kind, and <code>definitionVersion</code> and differ on <code>schemaDigest</code>: a definition version names one set of bytes, and the support path resolves an exact identity from that version alone. A <code>definitionVersion</code> from one contract combined with a <code>schemaDigest</code> from another names no installed Definition and fails <code>form_unknown</code> (404) like any other unknown identity. This is the required conformance check <code>two-definition-versions-answer-independently</code>.</li><li><strong>The Form Definition surface answers with the Definition&#39;s own bytes.</strong> For an exact FormRef, <code>desiredSchema</code> MUST be the <code>desiredSchema</code> of the installed Definition whose canonical bytes that <code>schemaDigest</code> addresses — every declared default, bound, pattern, and enum, unchanged. Echoing the requested identity is not enough: a host that answered a pinned FormRef with a schema of its own would hand every client a different portable default, and a client that materialized it would compute a <code>specDigest</code> no other client computes, with <code>prepare</code> refusing what the author actually wrote and nothing on either side able to say why. A client CANNOT re-derive the served document from the pinned digest — the digest covers the whole Definition and this surface serves a subset of it — so the conformance corpus pins the desired schema of every Form it drives as bytes, and <code>form-definition-exact</code> compares what a host serves against them. The runner materializes its probe specs from the PINNED schema, never from the served one; measuring a host against defaults that host supplied measures nothing.</li><li>A resource RECORDS the exact FormRef it was created under. That ref is written at create, carried forward unchanged by every update and import, and is the only identity the resource is answered about. A <code>read</code>, <code>observe</code>, <code>apply</code>, <code>import</code>, or <code>delete</code> naming any other exact ref addresses no resource and fails <code>resource_not_found</code> (404) — the Form may well be installed; what is absent is a resource of that name under that contract. A response MUST NOT rewrite an older resource&#39;s recorded ref to a newer one. This is the required conformance check <code>resource-answers-only-under-its-recorded-form-ref</code>.</li><li>A resource NAME stays unique within one tenant, space, group, and kind. The definition version decides what a request is answered about, never where a resource lives: a reference is <code>{apiVersion, kind, name}</code> and carries no definition version, so two same-named resources of one kind under different contracts would leave every reference to that name unresolvable. A create therefore still conflicts with a name taken under another contract of the same kind — and never with a name taken by another TENANT, which is a different address entirely (<a href="#tenant-isolation">Tenant isolation</a>).</li></ul><h2 id="tenant-isolation" tabindex="-1">Tenant isolation <a class="header-anchor" href="#tenant-isolation" aria-label="Permalink to &quot;Tenant isolation&quot;">​</a></h2><p>The rules of this section are decided by <a href="./../decisions/0028-the-resource-plane-is-tenant-isolated.html">decision 0028</a>.</p><p>A host&#39;s <strong>internal resource address MUST include the tenant, the space, the <code>apiVersion</code>, the <code>kind</code>, and the <code>name</code>.</strong> The tenant is the AUTHENTICATED tenant of the request: no path segment, query key, reference, or <code>metadata</code> member names one, so it comes from the credential and from nowhere else. It is recorded when a resource is created and carried forward by every update and import, exactly like the recorded exact FormRef.</p><p>A request that carries no credential, or one naming nobody, therefore has no tenant and MUST be refused <code>unauthenticated</code> (401) — never <code>permission_denied</code> (403), because the caller has not been identified and there is nothing yet to deny. This is the required conformance check <code>unauthenticated-request-refused</code>, driven with an ABSENT <code>Authorization</code> header and with a well-formed bearer credential naming nobody, on a read surface and on a mutating one. Everything below is downstream of it: a host whose credential lookup fails open has picked a tenant the caller never named, and every rule in this section is then about a boundary that is not there.</p><p>Everything else follows from the address.</p><ul><li>Two tenants MAY create one <code>{space, apiVersion, kind, name}</code>. They get two resources with two host-issued uids, two generations, and two revisions. Neither create conflicts with the other, because a name is taken <strong>within one tenant</strong> — a host that let one tenant&#39;s name choice deny another&#39;s would make the whole name space a shared, first-come resource and a membership oracle besides.</li><li>Every surface that takes a resource name addresses the caller&#39;s own tenant: <code>read</code>, <code>observe</code>, <code>prepare</code>, <code>apply</code>, <code>import</code>, and <code>delete</code>. A request naming another tenant&#39;s resource is answered exactly as one naming a resource that was never created. Where that answer is a refusal it is <code>resource_not_found</code> (404), and it MUST be <strong>indistinguishable</strong> from the refusal for a name nobody created — the same code, the same status, and a message that discloses nothing about the other tenant. <code>permission_denied</code> (403) is the wrong answer and is forbidden here: it would confirm that a resource of that name exists somewhere on the host, which is the fact the boundary exists to withhold, to any caller who can guess a name.</li><li><strong>&quot;Its own tenant&quot; is a permission as well as a limit.</strong> Every tenant MUST be able to create, read, observe, update, import, and delete resources in its own plane, and a resource MUST stay operable by its holder after a caller from another tenant has been refused. A host that is create-and-read-only outside one privileged tenant, and a host that quarantines a record a stranger reached for, both refuse everything the paragraph above requires them to refuse — and both have taken a resource from the tenant that owns it. Required conformance checks: <code>each-tenant-mutates-its-own-plane</code>, and the holder&#39;s own leg of <code>resource-update-is-tenant-isolated</code> and <code>resource-delete-is-tenant-isolated</code>.</li><li><code>import</code> is the one surface whose absent answer is not a refusal, so the rule is stated for it exactly. An adoption under <code>If-None-Match: *</code> of a name the caller does not hold MINTS a resource at generation 1, and a name held only by another tenant is not held by the caller — so that adoption MUST succeed, and MUST answer what the same adoption of a name nobody holds anywhere answers: the same status, the same ETag, and a document differing in nothing but the host-issued uid and the name. An adoption carrying an update generation fence names an existing resource instead, so against a name only another tenant holds it fails <code>resource_not_found</code> (404) like any other absent target. A host that refused the create-intent adoption with <code>generation_conflict</code> would be answering &quot;that name is taken&quot; about a tenant the caller cannot see; one that took the update path would write the caller&#39;s desired state over a stranger&#39;s live resource, which — followed by a delete — destroys it through nothing but a name.</li><li><code>validate</code> carries a name and resolves none: it answers diagnostics about the document it was handed and reads no stored resource, so it has no tenant-dependent answer to give.</li><li><strong>Relations resolve only within the same tenant, and the stored pin is the uid they resolved to.</strong> A reference is <code>{apiVersion, kind, name}</code> and carries neither a tenant nor a space, so it is resolved inside the referring resource&#39;s own scope. A name that only another tenant holds is an absent target and fails <code>resource_not_found</code> (404) like any other. Resolving correctly and PINNING the other tenant&#39;s uid is the same defect arriving one step later, and it is the step everything downstream reads: deletion protection, drift, and every Worker aggregate rule are computed from the pin, so a foreign pin protects one tenant&#39;s resource from its own owner and makes another tenant&#39;s source follow a resource its author never named. A host that resolved a reference across tenants would bind one tenant&#39;s desired state to another tenant&#39;s resource and pin its uid into stored state, which is the substitution <a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a> closes for incarnations, committed across a security boundary rather than a naming one. Deletion protection, drift, and every Worker aggregate rule read those pinned relations, so all of them stop at the tenant too.</li><li>A <code>prepareDigest</code> is minted for one tenant and is spendable by that tenant alone. An apply presenting a review minted in another tenant fails <code>invalid_argument</code> (400) before any mutation — the ordinary prepare-binding refusal, because the resource being addressed is the caller&#39;s own and what is untrue is the review. A create binding pins no uid and no generation, so without the tenant two tenants preparing one name in one space would hold the same digest, and a review — a value clients log, print in plans, and pass between processes — would be a bearer token over another tenant&#39;s plane. The boundary is the TENANT and not the principal: a second principal of the minting tenant MUST be able to spend the same review, because planning under one identity and applying under another is ordinary operation and a host enforcing the rule more tightly than it is written is a host two deployments differ on.</li><li>An <code>Idempotency-Key</code> names one operation <strong>of one tenant</strong>. The same key from two tenants is two operations, and each tenant replays only its own recorded response. Answering a second tenant with the first tenant&#39;s recorded success would hand it another tenant&#39;s uid, generation, and ETag while performing no mutation at all — and so would answering it a fresh-looking <code>201</code> that stores nothing, which is why the check reads the second tenant&#39;s resource back.</li><li>Anything a host renders from OTHER resources — the relation drift and Worker readiness of <a href="#resource-identity">Resource identity</a> — is computed from, and advances the revision of, resources of the mutating tenant only. That pass writes, so a host-wide one would move another tenant&#39;s ETag.</li></ul><p>There is <strong>one deliberate exception</strong>, and it is the one <a href="./../decisions/0026-attachment-claims-are-canonical-and-acyclic.html">decision 0026</a> already decided: a claimed value is unique across every space <strong>of one tenant</strong>, because spaces partition one tenant&#39;s resources and a claimed namespace does not partition with them (<a href="#claimed-values">Claimed values</a>). That rule drops the space and keeps the tenant. It does not reach past the tenant, and no rule in this lane does.</p><p>Ten required conformance checks measure this, all of them black box, all of them driven with two configured tenants&#39; credentials: <code>resource-address-is-tenant-scoped</code>, <code>resource-read-is-tenant-isolated</code>, <code>resource-observe-is-tenant-isolated</code>, <code>resource-update-is-tenant-isolated</code>, <code>resource-import-is-tenant-isolated</code>, <code>resource-delete-is-tenant-isolated</code>, <code>relation-resolution-is-tenant-scoped</code>, <code>prepare-is-tenant-scoped</code>, <code>idempotency-is-tenant-scoped</code>, and <code>each-tenant-mutates-its-own-plane</code>. A runner that cannot authenticate as two tenants can measure none of them, which is why the lane&#39;s runner REQUIRES an alternate-tenant credential alongside the same-tenant alternate principal rather than treating it as optional.</p><p>Nine of the ten are enumerated by SURFACE and not by intent. Every route of <a href="#lifecycle">Lifecycle</a> that takes a resource name is listed against the check that measures it, and the list is bound to the published route block, to the reference host&#39;s router, and to the required-check list by tests, so a name-addressed endpoint cannot be added to this lane without one. An enumeration by intent is what left <code>observe</code> and <code>import</code> out of the first version of this section: both take a name, one returns a whole representation and one mutates, and a host that scoped <code>read</code>, <code>apply</code> and <code>delete</code> while resolving either host-wide satisfied every check there was. The tenth is not a surface but the permissive half of all of them, so it is listed against <code>PUT</code> and <code>DELETE</code> beside the two boundary checks it completes.</p><p>Two facts are host obligations this lane does NOT prove, because a black-box runner cannot observe them:</p><ul><li>that the tenant is part of the ADDRESS rather than a filter applied somewhere late. The checks measure the consequences, which is what a third-party host can be held to; a host that reaches the same answers by other means passes, and is conforming.</li><li>that a host-wide derived-rendering pass would cross the tenant. Constructing the case needs one tenant&#39;s resource to render from another&#39;s, and relation resolution refuses to build it — so there is no request sequence that distinguishes a scoped pass from an unscoped one.</li></ul><p>Publication blocker <strong>V3-012</strong> records this item; the reference host&#39;s own obligations are proved by host-side tests in <code>internal/portableconformancev3/tenant_isolation_test.go</code>.</p><h2 id="lifecycle" tabindex="-1">Lifecycle <a class="header-anchor" href="#lifecycle" aria-label="Permalink to &quot;Lifecycle&quot;">​</a></h2><p>Endpoints under <code>/apis/forms.takoform.com/v1beta3</code>, keyed by group and kind so one kind name can exist in many groups. <code>{formGroup}/{formVersion}</code> is the two-segment group of <a href="#path-shape">Path shape</a>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET    {api}/forms                       enumeration; six optional filter keys</span></span>
<span class="line"><span>GET    {api}/form-definitions/{formGroup}/{formVersion}/{kind}?space&amp;definitionVersion&amp;schemaDigest</span></span>
<span class="line"><span>POST   {api}/resources/validate         diagnostics only, no mutation, no digest</span></span>
<span class="line"><span>POST   {api}/resources/prepare          short-lived prepare digest for one exact spec</span></span>
<span class="line"><span>PUT    {api}/resources/{formGroup}/{formVersion}/{kind}/{name}    apply; carries review.prepareDigest</span></span>
<span class="line"><span>GET    {api}/resources/{formGroup}/{formVersion}/{kind}/{name}</span></span>
<span class="line"><span>POST   {api}/resources/{formGroup}/{formVersion}/{kind}/{name}/import</span></span>
<span class="line"><span>POST   {api}/resources/{formGroup}/{formVersion}/{kind}/{name}/observe</span></span>
<span class="line"><span>DELETE {api}/resources/{formGroup}/{formVersion}/{kind}/{name}</span></span></code></pre></div><p><code>GET {api}/forms</code> enumerates every installed exact Form the principal may see. Each of its six query keys (<code>group</code>, <code>version</code>, <code>kind</code>, <code>definitionVersion</code>, <code>schemaDigest</code>, <code>space</code>) is optional and narrows the answer; a malformed value is <code>invalid_argument</code>; all six present is the exact-availability probe, whose answer carries zero entries or one. The response never blends tenants and never lists a Form the principal could not address (<a href="#tenant-isolation">Tenant isolation</a>).</p><p><code>observe</code> is the lane&#39;s only fenced read-only re-observation. There is no <code>refresh</code> operation: v1alpha2 carried both under one contract, which meant two spellings of one behavior and therefore two ways for hosts to differ. A v1beta2 Form never declares the <code>refresh</code> capability and a v1beta2 host serves no <code>/refresh</code> route.</p><p><code>validate</code> reports diagnostics without mutating and without minting a digest; a client MUST NOT describe provider apply-time preparation as &quot;reviewed in plan&quot;. <code>prepare</code> binds the exact spec, identity, and fences to a <code>prepareDigest</code>; substitution after prepare fails <code>invalid_argument</code> before mutation. A prepare digest is valid at least while the generation it fenced is current: a host MUST NOT expire it earlier by clock, restart, or capacity, and MUST refuse it once that generation moves. Expiry and substitution answer the same <code>invalid_argument</code> with a message that says which — a client repairs both the same way, by preparing again.</p><p><code>delete</code> carries an empty body, an <code>Idempotency-Key</code>, and the expected generation, which is its only required precondition (<a href="#resource-identity">Resource identity</a>). <code>observe</code> carries an empty body and NO <code>Idempotency-Key</code>: it mutates nothing, so there is no operation to replay, and a recorded answer would only be a stale representation the ETag rules exist to prevent. Every observe executes.</p><h3 id="the-fence-matrix" tabindex="-1">The fence matrix <a class="header-anchor" href="#the-fence-matrix" aria-label="Permalink to &quot;The fence matrix&quot;">​</a></h3><p>One table governs how a generation fence travels, and <a href="operations-v1beta2.json"><code>operations-v1beta2.json</code></a> carries it as data (<code>fences</code>):</p><table tabindex="0"><thead><tr><th>Operation</th><th>Transport</th><th>Absent</th><th>Stale</th></tr></thead><tbody><tr><td>update (apply on an existing resource)</td><td><code>Takoform-Expected-Generation</code> header, or <code>expectedGeneration</code> in the body</td><td><code>invalid_argument</code></td><td><code>generation_conflict</code></td></tr><tr><td>prepare on an existing resource</td><td>header only — the prepare request shape carries no fence member</td><td><code>invalid_argument</code></td><td><code>generation_conflict</code>; a fence naming an absent resource is <code>resource_not_found</code></td></tr><tr><td>observe</td><td>header only</td><td><code>invalid_argument</code></td><td><code>generation_conflict</code></td></tr><tr><td>delete</td><td>header only</td><td><code>invalid_argument</code></td><td><code>generation_conflict</code></td></tr><tr><td>create (apply minting a resource)</td><td><code>If-None-Match: *</code></td><td><code>invalid_argument</code></td><td>an existing resource answers <code>generation_conflict</code></td></tr></tbody></table><p>When an apply carries both the header and the body field they MUST be equal; disagreement is <code>invalid_argument</code> before either is evaluated. A create carrying <code>If-None-Match</code> together with a generation fence is <code>invalid_argument</code>: the two assert contradictory beliefs about existence.</p><p>Role rules are wire-enforced: an update to a <code>revision</code>-role resource fails <code>invalid_argument</code>; deleting a resource any live relation references fails <code>dependency_in_use</code> (409); deleting a <code>deployment</code>-role resource any live dependent needs fails the same way (<a href="./../decisions/0016-the-worker-aggregate-has-one-active-deployment.html">decision 0016</a>). The <code>policy</code> role has no wire rules in this lane: the first policy-role Form brings them, and until then no code claims a policy surface exists.</p><h2 id="cross-resource-relations" tabindex="-1">Cross-resource relations <a class="header-anchor" href="#cross-resource-relations" aria-label="Permalink to &quot;Cross-resource relations&quot;">​</a></h2><p>The rules of this section are decided by <a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a>.</p><p>A <strong>relation</strong> is one reference from a resource&#39;s desired spec to another resource. Its wire shape is the closed three-member object</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example.forms.takoform.com/v1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ExampleStore&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cache&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>where <code>apiVersion</code> and <code>kind</code> are <code>const</code> in the referring Form&#39;s <code>desiredSchema</code> and <code>name</code> follows the portable resource-name grammar. Both constants are required. A reference carrying only <code>{kind, name}</code> cannot address two Form Families at once and forces a host to guess which installed Form a bare kind means; a group-qualified reference makes the target Form exact.</p><p>Relations are <strong>derived from the Form Definition&#39;s <code>desiredSchema</code>, never declared</strong>. Every reference already states its target group and kind as schema constants, so a separate relation list on the Definition would be a second source of truth for facts the schema already carries — and the published Form Definition schema admits no such member (<a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a>). A host derives the relation set from the same <code>desiredSchema</code> it serves: every closed object that requires AT LEAST <code>apiVersion</code>, <code>kind</code>, and <code>name</code> with the first two <code>const</code> is a relation, identified by its JSON Pointer with <code>*</code> standing for an array element (<code>/owner</code>, <code>/versions/*/revision</code>, <code>/bindings/*/resource</code>). A reference MAY require additional members — a resource that selects a named export of its target names that export beside the reference — and then the reference node carries the <code>x-takoform-relation-extras</code> annotation listing exactly those member names; extra members select WITHIN the pinned target and never weaken the pin. A closed reference-shaped object with extra members and no annotation is a Definition defect a package verifier refuses, never a silently un-derived relation. A binding-list property additionally carries the <code>x-takoform-binding</code> annotation naming the Binding contract that governs the references inside it; the exact digest-bound BindingRef is the Definition&#39;s own <code>acceptedBindings</code> entry of that name.</p><h3 id="what-a-relation-requires-of-its-target" tabindex="-1">What a relation requires of its target <a class="header-anchor" href="#what-a-relation-requires-of-its-target" aria-label="Permalink to &quot;What a relation requires of its target&quot;">​</a></h3><p>The rules of this section are decided by <a href="./../decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>.</p><p>A group and a kind say WHICH resource a reference names. They say nothing about what that resource must still satisfy, so a target whose Definition later moved to an incompatible version would keep satisfying every reference to it. Every reference-shaped node in a <code>desiredSchema</code> therefore carries exactly one target contract, as an annotation on the reference itself — data the published Form Definition schema already admits, exactly like <code>x-takoform-binding</code> (<a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a>):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-takoform-target-formrefs&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example.forms.takoform.com/v1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ExampleArtifact&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;definitionVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sha256:...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>when the relation depends on the target&#39;s exact desired contract, or</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-takoform-required-interface&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;interfaces.takoform.com/v1alpha1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;worker.runtime&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sha256:...&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>when it depends only on an Interface the target provides. A reference carrying both, or neither, is refused when relations are derived: a host that cannot say what a relation requires cannot verify it.</p><p>Which one is correct is decided by the dependency, not by preference, and by nothing else: <code>x-takoform-target-formrefs</code> states the requirement exactly when the source — or the host acting for it — reads a member of the target&#39;s desired spec or enforces a rule stated over the target Form itself; <code>x-takoform-required-interface</code> states it exactly when the source needs only behavior a contract fixes, which any Form providing that Interface would serve. No list of which references use which lives here: each Definition&#39;s own annotations are the enumeration, and a reader asking &quot;why this one&quot; reads the criterion against the reference&#39;s semantics.</p><p>A binding-list reference MUST require exactly the Interface its Binding Definition names as <code>targetInterface</code>. The Binding Definition stays the authority and the annotation is its projection onto the reference, so the two cannot become two sources of truth; a binding relation is verified against its Binding Definition first, in the order below, and against the annotation second.</p><h3 id="resolution-and-uid-pinning" tabindex="-1">Resolution and UID pinning <a class="header-anchor" href="#resolution-and-uid-pinning" aria-label="Permalink to &quot;Resolution and UID pinning&quot;">​</a></h3><p>On <code>apply</code> and <code>import</code>, for every derived relation present in the materialized spec, a host MUST, before any mutation (<a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a>):</p><ul><li>resolve <code>(space, apiVersion, kind, name)</code> to a stored resource. Absent fails <code>resource_not_found</code> (404) and the message MUST name the relation pointer. Cross-space references are unrepresentable — a reference carries no space.</li><li>verify that the resolved resource&#39;s Form has exactly the referenced group and kind, failing <code>invalid_argument</code> (400) on mismatch. The resolved resource&#39;s own RECORDED exact ref names that Form; a well-formed spec cannot reach this, because the schema pins both constants, but a host that resolved by name alone can, and must refuse rather than bind the wrong Form.</li><li>verify the target contract the reference annotates (<a href="./../decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>): the target&#39;s recorded exact ref is one of the listed <code>x-takoform-target-formrefs</code>, or the target Form&#39;s Definition declares the annotated <code>x-takoform-required-interface</code> in <code>providedInterfaces</code> at exactly that digest. A target that satisfies neither fails <code>invalid_argument</code> (400) — the request is well formed and states something untrue about the resource it points at — and the message MUST name the relation pointer and what was required. These are the required conformance checks <code>relation-target-form-ref-verified</code> and <code>relation-target-interface-verified</code>.</li><li>verify the binding contract when the relation is a binding (below).</li><li>record the resolution as <code>{pointer, relation, targetAPIVersion, targetKind, targetName, targetUID, targetFormRef, bindingRef?}</code> alongside the resource, where <code>pointer</code> is the concrete instance pointer and <code>relation</code> is the derived pointer it came from.</li></ul><p>A host MUST store the <strong>target UID</strong>, not only the name. A name is a label the client chose and can reuse; the UID is the identity of one incarnation. It MUST also store the <strong>target&#39;s exact FormRef</strong>: the UID pins which incarnation the source is bound to, and the ref pins what contract that incarnation satisfies. This is the required conformance check <code>relation-pin-records-target-form-ref</code>.</p><h3 id="incarnation-change" tabindex="-1">Incarnation change <a class="header-anchor" href="#incarnation-change" aria-label="Permalink to &quot;Incarnation change&quot;">​</a></h3><p>When a resource is read or observed and a stored relation&#39;s target now resolves to a <em>different</em> UID, to a different exact FormRef, or to nothing, the source reports <code>Ready=False</code> with</p><ul><li><code>ExternalChange</code> — the target name resolves to a different incarnation, or the same incarnation under a different exact contract;</li><li><code>DependencyMissing</code> — the target no longer exists;</li></ul><p>and a <code>hostReason</code> naming the relation pointer, both UIDs, and both exact FormRefs. A host MUST NOT re-bind the relation automatically: re-resolving the name would make a delete and re-create of the target invisible and silently point the source at a resource its author never named. The source stays pinned until it is re-applied, and re-reading it does not heal the condition. A moved CONTRACT is reported through this same condition rather than a parallel one: it is the same fact — the source is pinned to something that is no longer there — with the same remedy.</p><h3 id="recovery" tabindex="-1">Recovery <a class="header-anchor" href="#recovery" aria-label="Permalink to &quot;Recovery&quot;">​</a></h3><p>The remedy is an apply, and it MUST stay reachable (<a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a>).</p><ul><li><strong>What the host reports.</strong> The condition above, on <code>read</code> and <code>observe</code>, for as long as the stored relation does not resolve to its pinned UID. Reporting it is not an error outcome: the resource is still readable, still deletable, and still carries its desired spec.</li><li><strong>What re-pins.</strong> Every ACCEPTED mutation re-resolves every relation and stores the UIDs it resolved to, including a <code>apply</code> whose spec is byte-identical to the stored one. Re-pinning is host-owned bookkeeping, not desired state: it MUST NOT move <code>metadata.generation</code>, and it moves <code>metadata.revision</code> exactly when it changes the representation the host serves — which it does, because the Ready condition stops reporting the drift. A second identical apply then moves nothing at all.</li><li><strong>What a client must do.</strong> Report the break without failing the read, and offer an apply. A client that fails its refresh on this condition removes its own remedy, because the plan that repairs the resource is computed from the refreshed state. A Form that declares <code>update</code> recovers with a spec-identical apply. A Form that declares none — every <code>revision</code>-role Form — has no such apply at all: a host refuses every apply to the existing resource, so its only recovery is REPLACEMENT, and a client MUST plan one. A <code>DependencyMissing</code> target must exist again before either apply can succeed.</li></ul><p>A client sees the same fact — an incarnation changed — in two places, and the two answers differ because the remedies do. A relation whose TARGET moved is a warning with a proposed apply: the resource itself is still the one state names, and an accepted apply re-pins the reference. A resource whose OWN <code>metadata.uid</code> no longer matches what state records is an error the operator must resolve, and a client MUST keep the resource in state while reporting it: no apply converts one incarnation into another, and dropping the resource would make the next apply fence on <code>If-None-Match: *</code> against the resource that does exist, with no plan left that repairs anything (<a href="./../decisions/0017-provider-state-survives-form-evolution-and-interruption.html">decision 0017</a>).</p><h3 id="dependency-protection" tabindex="-1">Dependency protection <a class="header-anchor" href="#dependency-protection" aria-label="Permalink to &quot;Dependency protection&quot;">​</a></h3><p>Deleting a resource that any stored relation references by UID fails <code>dependency_in_use</code> (409). This covers every relation, not only typed bindings: a Worker Bundle a Worker Version executes, a Worker Version a Worker Deployment weights, a Module Worker an attachment activates, a queue a consumer drains, and a dead-letter queue are all live dependencies. A resource that references itself does not block its own deletion. An accepted (202) delete re-runs the scan at commit time against the incarnation it was accepted for, so a resource that acquired a holder while the operation was pending survives, and a resource that merely took the deleted one&#39;s name is never touched.</p><h3 id="binding-verification" tabindex="-1">Binding verification <a class="header-anchor" href="#binding-verification" aria-label="Permalink to &quot;Binding verification&quot;">​</a></h3><p>A binding relation is verified, never assumed. Before any mutation a host MUST check, in this order:</p><ol><li>the source Form Definition&#39;s <code>acceptedBindings</code> carries the Binding contract the desired schema annotates — otherwise <code>invalid_argument</code> (400);</li><li>the host has installed that contract at exactly the accepted <code>schemaDigest</code> — otherwise <code>unsupported_capability</code> (422), because the capability itself is unavailable rather than the request malformed;</li><li>the source Form&#39;s <code>role</code> equals the Binding Definition&#39;s <code>sourceRole</code> — otherwise <code>invalid_argument</code> (400);</li><li>the resolved target&#39;s exact Form (group and kind) appears in the Binding Definition&#39;s <code>allowedTargetForms</code> — otherwise <code>invalid_argument</code> (400);</li><li>the target Form&#39;s Definition declares the Binding&#39;s <code>targetInterface</code> in <code>providedInterfaces</code> — otherwise <code>invalid_argument</code> (400). A binding projects an Interface, so a target that provides none cannot be bound;</li><li>source and target share a space, which the wire guarantees: a reference carries no space member.</li></ol><p>Rules 3 through 5 are about the target or holder the client chose and are therefore argument failures; rule 2 is about what this host can do at all.</p><h2 id="declared-cross-resource-rules" tabindex="-1">Declared cross-resource rules <a class="header-anchor" href="#declared-cross-resource-rules" aria-label="Permalink to &quot;Declared cross-resource rules&quot;">​</a></h2><p>Some rules a desired-state schema cannot express. A schema cannot count the resources pointing at one target, read a relation of a resource it does not contain, add a column of weights, know which entrypoints a referenced revision exports, or reach across sibling properties. Those rules live in the host under <a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a>.</p><p>The previous lane wrote them out one Form at a time. It said that a worker carries one active deployment, that a queue carries one consumer, that a database carries one live migration application — each a paragraph naming a Form kind, in a document that is supposed to be the protocol. Its own aggregate chapter admitted the shape of the problem: <em>hosted here while that family is this lane&#39;s only family</em>. The consequence was mechanical and it happened: a family that gained two Forms could not gain them without editing the protocol.</p><p><strong>This lane states the MECHANISMS. A family states which of them apply to which of its references, in its own Definitions.</strong> A host reads a Definition and enforces what it declares, without knowing what any Form is for. The lane therefore names no Form kind, and a check enforces that it does not.</p><p>Every rule below is decided against the UID a reference RESOLVED to, never against the name a spec spells.</p><h3 id="exclusive-holds" tabindex="-1">Exclusive holds <a class="header-anchor" href="#exclusive-holds" aria-label="Permalink to &quot;Exclusive holds&quot;">​</a></h3><p>A reference may declare <code>x-takoform-exclusive</code>: <strong>at most one LIVE resource of this Form kind may hold the target this reference resolves to.</strong> A second one fails <code>invalid_argument</code> (400) before any mutation — the request is well formed and what is untrue is what it says about the target it points at.</p><p>The annotation is an object. Its optional <code>keyedBy</code> member is a JSON Pointer to a sibling property of the same desired spec, and it joins the target in the key:</p><table tabindex="0"><thead><tr><th><code>keyedBy</code></th><th>The key is</th><th>Two holders conflict when</th></tr></thead><tbody><tr><td>absent</td><td>the resolved target</td><td>they resolve the same target</td></tr><tr><td>a pointer</td><td>the target and that property&#39;s value</td><td>they resolve the same target AND carry the same value</td></tr></tbody></table><p>A conflict is decided over LIVE resources only. A resource an accepted delete has removed holds nothing, so the target it held is free — a rule that outlived its holder would make a destroyed-and-recreated target permanently unmanageable.</p><p>The rule is per exact Form kind, not per target: two resources of DIFFERENT kinds resolving one target are not a conflict. That is what lets one target carry an exclusive holder of one kind and any number of resources of another.</p><h3 id="summed-members" tabindex="-1">Summed members <a class="header-anchor" href="#summed-members" aria-label="Permalink to &quot;Summed members&quot;">​</a></h3><p>An object-list property may declare <code>x-takoform-sum</code>: the named integer member of its elements MUST total exactly the stated value. A list that does not fails <code>invalid_argument</code> (400) before any mutation.</p><p>A schema can bound each element and cannot add a column. This is the whole of what the annotation adds, and it adds it as data rather than as a sentence about one Form&#39;s traffic weights.</p><h3 id="claimed-values" tabindex="-1">Claimed values <a class="header-anchor" href="#claimed-values" aria-label="Permalink to &quot;Claimed values&quot;">​</a></h3><p>A property may declare <code>x-takoform-claim</code>: its value is held by <strong>at most one live resource per tenant</strong>, across every space, compared on the CANONICAL form the property&#39;s own schema defines. A second claimant fails <code>invalid_argument</code> (400) before any mutation, and releasing the holder makes the claim representable again.</p><p>Canonicalization happens before comparison and before storage, so two spellings of one value are one claim rather than two. Which spellings are equal is the property&#39;s schema to say; that the comparison happens on the canonical form is this lane&#39;s.</p><h3 id="host-assigned-outputs" tabindex="-1">Host-assigned outputs <a class="header-anchor" href="#host-assigned-outputs" aria-label="Permalink to &quot;Host-assigned outputs&quot;">​</a></h3><p>A declared output member may carry <code>x-takoform-host-assigned</code>: the host mints the value, it is immutable for the lifetime of the resource&#39;s UID, and no desired property may state it. A consumer may store it; a portable configuration never parses it, asserts a suffix of it, or reconstructs it from a resource name.</p><h3 id="activation-entrypoints" tabindex="-1">Activation entrypoints <a class="header-anchor" href="#activation-entrypoints" aria-label="Permalink to &quot;Activation entrypoints&quot;">​</a></h3><p>A reference may declare <code>x-takoform-required-entrypoint</code>: the entrypoint of the target&#39;s runtime Interface that this resource&#39;s inward activation invokes. Activation is admitted against the target&#39;s ACTIVE selecting resource — not against any stored revision — and EVERY revision that selection weights MUST provide the annotated entrypoint, because an event served by any weighted revision has to find it.</p><p>An absent selection, or a weighted revision that does not provide the entrypoint, fails <code>unsupported_capability</code> (422) before any mutation and the message names what is missing. A stored revision is a history entry, not a running one: gating on it would admit an activation against code no selection serves.</p><p>A resource that SELECTS a named export rather than activating one states the same requirement through its own property, named by <code>x-takoform-relation-extras</code>, and is gated on it wherever a selection exists to contradict it. It differs in the absent-selection case, and the difference is structural rather than a concession: a revision may declare a binding to the selector its own identity serves, so identity, revision, and selection are ordinarily created in ONE apply and no order exists in which the selection comes first. Such a resource STORES with <code>Ready=False</code> / <code>Provisioning</code>, and the selection that lands next is what makes it serve.</p><h3 id="dependents-and-reverse-validation" tabindex="-1">Dependents and reverse validation <a class="header-anchor" href="#dependents-and-reverse-validation" aria-label="Permalink to &quot;Dependents and reverse validation&quot;">​</a></h3><p>The dependents of a selecting resource are DERIVED, not listed: they are the live resources whose annotated entrypoint that selection must keep serving, and the live bindings that resolve to it. A change that would stop serving a dependent&#39;s entrypoint, and a delete of a selection a dependent still needs, both fail <code>dependency_in_use</code> (409) naming what depends on it.</p><p>Nothing here enumerates a Form kind. A family that adds an activation adds an annotated reference, and its dependents are counted by the same walk that counted every other one.</p><h2 id="external-standard-services" tabindex="-1">External standard services <a class="header-anchor" href="#external-standard-services" aria-label="Permalink to &quot;External standard services&quot;">​</a></h2><p>A revision-role Form MAY embed sealed external-service slots (<a href="./../standard-services/"><code>../standard-services/README.md</code></a>, <a href="./../decisions/0045-external-standard-services-are-sealed-slots.html">decision 0045</a>). The lane adds the rules only a wire contract can carry:</p><ul><li><strong>Derivation.</strong> The array property embedding the slots carries the <code>x-takoform-standard-services</code> annotation, exactly parallel to <code>x-takoform-binding</code>, so a host holding only the Definition derives every slot it must enforce. An un-annotated slot-shaped array is a Definition defect a package verifier refuses.</li><li><strong>Slot shape.</strong> <code>{name, service, required?}</code>: <code>name</code> matches <code>^[A-Z][A-Z0-9_]*$</code> (64 max) — the SCREAMING_SNAKE grammar <code>requiredSensitiveVars</code> already uses, because the projection mints <code>NAME_</code>-prefixed members and a lowercase or <code>$</code>-bearing name would make those unpronounceable; <code>service</code> validates against the StandardServiceRef schema; <code>required</code> is an optional boolean defaulting to true. An optional slot the host does not satisfy projects nothing and blocks nothing.</li><li><strong>The namespace closure.</strong> Uniqueness is enforced over the PROJECTED closure, not the declared names: the union of vars keys, sensitive-value names, binding names, and every member each slot projects (<code>MEDIA_ENDPOINT</code>, <code>MEDIA_ACCESS_KEY_ID</code>, …) must be collision-free, and a host refuses the collision before mutation exactly as it refuses the declared-name collision — a var named <code>MEDIA_ENDPOINT</code> beside an <code>s3-compatible</code> slot named <code>MEDIA</code> is <code>invalid_argument</code>.</li><li><strong>Plan surface.</strong> <code>validate</code> never depends on the tenant, so slot satisfiability is answered by the <code>StandardServiceSupport</code> profile and enforced at <code>prepare</code>: an unsatisfiable REQUIRED slot is <code>unsupported_capability</code> there, and again at <code>apply</code> where prepare could not know. An unsatisfied REQUIRED slot discovered later renders <code>Ready=False</code> / <code>DependencyMissing</code>.</li><li><strong>Sealing.</strong> Projected values never appear in desired state, observed state, outputs, diagnostics, or provider state; the observed document MAY state that a slot is satisfied and MUST NOT state with what.</li></ul><p>Adopting a slot-projecting runtime is a runtime-contract revision: a family whose runtime Interface pins its environment membership (the Edge family&#39;s <code>worker.runtime</code> does) mints the next Interface version to admit the projected members, per <a href="./../decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>.</p><h2 id="lifecycle-capabilities" tabindex="-1">Lifecycle capabilities <a class="header-anchor" href="#lifecycle-capabilities" aria-label="Permalink to &quot;Lifecycle capabilities&quot;">​</a></h2><p><code>lifecycleCapabilities</code> is a claim about what a host can actually be asked to do, so it is derived from the Form&#39;s own declared fields rather than from its role. The base set every Form declares is exactly <code>create</code>, <code>read</code>, <code>delete</code>, <code>import</code>, <code>observe</code>. <code>update</code> is added only when the Form has at least one mutable desired field — a field that is not immutable, on a Form whose role is not <code>revision</code>. A Form with no field at all, or whose every field is immutable, therefore declares no <code>update</code>.</p><p>The rule is enforced on the wire in both directions. A host MUST refuse a spec-CHANGING apply to an existing resource whose Form Definition omits <code>update</code>, failing <code>invalid_argument</code> before any mutation; a client MUST plan a replacement rather than an in-place change for such a Form.</p><h2 id="portable-defaults" tabindex="-1">Portable defaults <a class="header-anchor" href="#portable-defaults" aria-label="Permalink to &quot;Portable defaults&quot;">​</a></h2><p>An optional desired property carries its portable meaning in its own schema: the Form Definition declares a JSON Schema <code>default</code> on that property inside <code>desiredSchema</code>. That default is normative, not advisory.</p><p>A host MUST materialize every declared top-level default into the desired spec at the entry point of <code>validate</code>, <code>prepare</code>, <code>apply</code>, and <code>import</code> — before the spec is validated, digested, stored, or echoed. Materialization fills only absent properties; a property that is present keeps its written value, even when that value equals the default. It follows that:</p><ul><li>the effective spec IS the wire spec: there is no second, host-private document a client cannot see;</li><li><code>specDigest</code> is the digest of the MATERIALIZED spec;</li><li>omitting a defaulted property and writing its default are one desired state: same <code>specDigest</code>, same <code>metadata.generation</code>, no update;</li><li>a client that sends an unmaterialized spec MUST accept the materialized echo from <code>prepare</code>, <code>apply</code>, <code>import</code>, and <code>read</code> as its own desired state.</li></ul><p>An optional property with no declared default is only legitimate when its ABSENCE is itself the portable semantics, and the property&#39;s <code>description</code> must then state what the absent case does.</p><h2 id="declared-outputs" tabindex="-1">Declared outputs <a class="header-anchor" href="#declared-outputs" aria-label="Permalink to &quot;Declared outputs&quot;">​</a></h2><p>The rules of this section are decided by <a href="./../decisions/0025-declared-outputs-are-a-typed-contract.html">decision 0025</a>.</p><p>A Form Definition MAY declare an <code>outputSchema</code>: the closed Draft 2020-12 contract of the host-computed values it publishes. Every declared member is required and the object carries <code>additionalProperties: false</code>, because an output a host may omit forces every consumer to invent a fallback, and an undeclared member is a value the contract never described.</p><p>A host is held to it in both directions, which is the rule the wire schema already states through <code>x-takoform-requiredWhen</code> and <code>x-takoform-omittedWhen</code>:</p><ul><li>for a Form that declares an <code>outputSchema</code>, <code>status.outputs</code> is PRESENT, validates against that schema, and carries exactly its members;</li><li>for a Form that declares none, <code>status.outputs</code> is OMITTED — not an empty object.</li></ul><p>This is the required conformance check <code>form-declared-outputs-are-exact</code>, driven across a Form that declares outputs and Forms that declare none, because a host returning an empty document everywhere would satisfy only the first half. Each returned value is measured against the declared schema — its type, its anchored pattern, its bounds — rather than against an assumption about it: a check that only asked for a non-empty string would accept <code>hostname: &quot;not a hostname&quot;</code> and reject an integer output the authoring model permits. The conformance corpus pins the whole declared schema for that reason, since no wire surface serves one.</p><p>An output is not desired state. It carries no default, no immutability, and no cross-resource reference: those describe what an author asks for, and an output is what the host answers. A change to an output moves <code>metadata.revision</code> and never <code>metadata.generation</code>, like every other representation change.</p><p>The <code>outputSchema</code> is not served on the wire. The form-definition response is a closed document carrying identity, display name, description, and <code>desiredSchema</code>; <code>displayName</code> is exactly the installed Definition&#39;s <code>title</code> and is always present — a host neither invents nor omits it. The bytes are immutable (<a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a>), so a client learns a Form&#39;s output contract from the Form Package it installs.</p><p>The Terraform and OpenTofu provider projects each declared output as a typed computed attribute, and retains <code>outputs_json</code> — carrying the WHOLE document, unnarrowed — as the way to reach an output no schema describes. It writes null for a declared output only where no representation exists — the state an accepted-but-unfinished mutation leaves behind — and fails an ordinary create, update, or refresh whose response omits a declared output or returns it with the wrong type, naming the output and what was wrong.</p><h2 id="long-running-operations" tabindex="-1">Long-running operations <a class="header-anchor" href="#long-running-operations" aria-label="Permalink to &quot;Long-running operations&quot;">​</a></h2><p>Create, update, delete, import, and artifact commit MAY return <code>202 Accepted</code> with an Operation envelope (<a href="/schemas/operations/v1alpha2/operation.schema.json"><code>../schemas/operation-v1alpha2.schema.json</code></a>) instead of the terminal representation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET  {api}/operations/{id}</span></span>
<span class="line"><span>POST {api}/operations/{id}/cancel</span></span></code></pre></div><p>Clients poll with <code>Retry-After</code> when present, otherwise exponential backoff with full jitter, under an overall deadline. The contract guarantees only that an operation ID is stable, that the host keeps it addressable at <code>GET {api}/operations/{id}</code> while the operation record exists (afterwards the closed outcome is <code>operation_not_found</code>), and that a terminal operation replays its terminal state instead of re-executing. A terminal Operation carries exactly one of <code>result</code> or <code>error</code> — the schema enforces the exactly, so a done record with neither, or both, is protocol-invalid.</p><p>Cancel has exactly three outcomes, and the machine table carries them as data. The request body is empty. A safely stoppable running operation is cancelled: the answer is <code>200</code> with the operation now terminal, <code>done: true</code> and <code>error.code: operation_cancelled</code> — cancellation is a terminal FAILURE of the operation, never a fourth state. An already-terminal operation replays its terminal record unchanged, also <code>200</code>. An operation past its safe stopping point answers HTTP <code>409</code> <code>operation_cancelled</code> with <code>retryable: false</code> and keeps running to its own terminal state; nothing about it changed. <code>operation_not_found</code> (404) covers an id the tenant does not hold; <code>deadline_exceeded</code> (504) reports a host-side deadline on the underlying work, never on the poll.</p><p>A <code>202</code> accepts a mutation to <strong>one resource</strong>, and the commit is bound to that one (<a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a>). A host records the target&#39;s exact <code>formRef</code> and <code>metadata.uid</code> beside the fence the mutation was accepted under, and at commit time resolves through that record rather than re-deriving a target from the name the request addressed. A name is unique per kind and reusable, and a re-created resource starts at generation 1 and at revision 1, so whichever counter a fence names, a target removed out of band and re-created — under the same contract or under another definition version of it — presents a NEW incarnation behind the same address that satisfies the fence the original was accepted under. When the name is held by a different incarnation the operation terminates <code>uid_mismatch</code> (409); when nothing holds it, <code>resource_not_found</code> (404). Neither commits anything, and neither is retryable: no wait turns one incarnation back into another. A create is fenced against the free name rather than against an incarnation, so it pins none and <code>If-None-Match: *</code> decides at commit as it always did. This is the required conformance check <code>async-commit-binds-the-accepted-identity</code>.</p><p>An operation id is a resumption handle, <strong>not a capability</strong>. A host records the authenticated tenant and principal that the mutation was accepted from, and answers <code>GET {api}/operations/{id}</code> and <code>POST {api}/operations/{id}/cancel</code> from any other tenant or principal with <code>operation_not_found</code> (404) — the same answer an id that never existed gets. <code>permission_denied</code> would confirm that the id names a real operation, which is the one fact a stranger holding a guessed or leaked id is trying to learn (<a href="./../decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a>). This is the required conformance check <code>operation-bound-to-its-creating-principal</code>.</p><p>&quot;While the record exists&quot; is a retention window, not an ordering: a host MUST NOT drop a terminal operation merely because the system moved on. As long as the record is retained, the ID stays addressable and its terminal state replays byte-identically after the resource it names has been mutated again and after other operations have been accepted and settled. This is the required conformance check <code>operation-resumable-after-settlement</code>, and it is what makes a persisted operation ID a usable handle rather than one whose value depends on timing.</p><p>A client that persists the ID resumes from it (<a href="./../decisions/0017-provider-state-survives-form-evolution-and-interruption.html">decision 0017</a>): the Terraform/OpenTofu provider records it as <code>pending_operation_id</code> and consults it BEFORE it reads the resource, because on a host that commits the resource only when the operation commits, a <code>resource_not_found</code> during that window means &quot;not yet&quot;, not &quot;deleted&quot;. A terminal success is verified against the exact identity before anything is adopted, and an expired record defers to an exact resource read rather than being read as failure.</p><h2 id="artifacts" tabindex="-1">Artifacts <a class="header-anchor" href="#artifacts" aria-label="Permalink to &quot;Artifacts&quot;">​</a></h2><p>The content-addressed upload API is <a href="./../artifact-transport/"><code>../artifact-transport/</code></a>. The artifact endpoints share the lane&#39;s auth, idempotency, and error taxonomy.</p><p>The desired state of an artifact-backed revision resource is the <strong>manifest digest and nothing else</strong>: the committed artifact manifest describes the bytes, so the manifest and the desired spec are never two spellings of the same facts. Which manifest kind a given Form requires is that Form&#39;s family to state (<a href="./../form-families.html"><code>../form-families.md</code></a>); a host MUST resolve the referenced manifest before it mutates anything, on apply and on import alike, and fail closed when</p><ul><li>the digest names no committed manifest the caller&#39;s tenant holds — <code>artifact_missing</code> (404). Resolution is the same per-tenant question the artifact read surfaces ask, so a manifest another tenant committed is answered exactly as an uncommitted digest is;</li><li>the stored manifest&#39;s RFC 8785 canonical digest differs from the referenced digest, its document is undecodable, or its <code>kind</code> is not the kind the Form requires — <code>artifact_invalid</code> (400);</li><li>the manifest violates any rule of <a href="./../artifact-transport/"><code>../artifact-transport/</code></a>, including its per-kind exclusivity, its media-type policy, entry-count or aggregate-byte ceilings, and the host&#39;s published <code>limits</code> — <code>artifact_invalid</code> (400).</li></ul><p>A committed manifest and its blobs MUST remain readable while any resource references the manifest. Abandoning an unrelated upload session, or garbage-collecting staged blobs, MUST NOT make a referenced artifact unresolvable.</p><h3 id="a-bundle-s-modules-are-what-the-runtime-can-load" tabindex="-1">A bundle&#39;s modules are what the runtime can load <a class="header-anchor" href="#a-bundle-s-modules-are-what-the-runtime-can-load" aria-label="Permalink to &quot;A bundle&#39;s modules are what the runtime can load&quot;">​</a></h3><p>A manifest whose entries are a module graph splits its media types into a LOADABLE set the runtime contract imports and an AUXILIARY set the bundle carries and the graph never imports. WHICH types fall on each side is the runtime contract&#39;s to state and the family&#39;s to reference (<a href="./../decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>); that the split exists, and what a host does with it, is this lane&#39;s.</p><p>A host MUST refuse such a manifest whose <code>mainModule</code> names an auxiliary module, before commit and before any mutation that references the manifest, with <code>artifact_invalid</code> (400) — and MUST NOT refuse a bundle merely for CARRYING one. A published manifest schema states the union in one enum and cannot relate <code>mainModule</code> to the media type of the module it names, so the split is host-enforced under <a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a> and proved by the required conformance check <code>bundle-main-module-is-loadable</code>. The corresponding runtime obligation — an import resolving to an auxiliary module fails <code>unsupported_media_type</code> — is behavior no desired-state runner observes, and stays a host obligation with the rest of the ABI.</p><h3 id="a-form-s-own-artifact-semantics-are-its-family-s" tabindex="-1">A Form&#39;s own artifact semantics are its family&#39;s <a class="header-anchor" href="#a-form-s-own-artifact-semantics-are-its-family-s" aria-label="Permalink to &quot;A Form&#39;s own artifact semantics are its family&#39;s&quot;">​</a></h3><p>What an artifact MEANS to the Form that references it — which manifest kind it must be, what its entries may contain, what the host does with them, and what makes the referencing resource Ready — is stated by the family that defines that Form, not here. The previous lane wrote two such rulebooks into the protocol document, and they were the clearest case of the problem this lane exists to fix: a reader of the protocol had to scroll past one family&#39;s asset-fallback order and another&#39;s migration ledger to reach the next protocol rule.</p><p>What stays here is the obligation every artifact reference carries, whatever it references: <strong>a host MUST resolve the referenced manifest and hold it to the contract the referring Form states, before it mutates anything, on apply and on import alike.</strong> A reference whose manifest does not resolve, or resolves to something the Form does not accept, fails before mutation — never after, and never as a readiness condition on a resource that was already stored.</p><p>The Edge Platform Family&#39;s own artifact semantics are in <a href="./../form-families.html"><code>../form-families.md</code></a>.</p><h3 id="upload-sessions-are-owned" tabindex="-1">Upload sessions are owned <a class="header-anchor" href="#upload-sessions-are-owned" aria-label="Permalink to &quot;Upload sessions are owned&quot;">​</a></h3><p>An upload id is a handle bound to the tenant and principal that started the session, exactly as an operation id is. Session identity is per-(tenant, manifest digest): a start request for a manifest the tenant already has an open session for answers that session&#39;s id again (200), and a start for already-committed bytes answers the committed state rather than a new session. An open session survives at least an hour of inactivity before a host may reclaim it; a blob PUT into a session already committed is <code>invalid_argument</code>. Continuing it (<code>PUT {api}/artifacts/uploads/{uploadId}/blobs/{sha256}</code>), committing it, and abandoning it from any other tenant or principal all fail <code>artifact_missing</code> (404). Abandoning an id the caller does not hold fails the same way whether it never existed or belongs to someone else: answering <code>204</code> for one and <code>404</code> for the other would make the difference observable, which is the disclosure the rule exists to prevent. This is the required conformance check <code>upload-session-bound-to-its-creating-principal</code>.</p><h3 id="a-content-address-is-not-a-capability" tabindex="-1">A content address is not a capability <a class="header-anchor" href="#a-content-address-is-not-a-capability" aria-label="Permalink to &quot;A content address is not a capability&quot;">​</a></h3><p>A digest names bytes; it does not entitle a caller to them (<a href="./../decisions/0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a>). <code>GET {api}/artifacts/{manifestDigest}</code> and <code>HEAD {api}/artifacts/blobs/{sha256}</code> answer only a caller whose TENANT already holds that address — by having uploaded the bytes, or by having committed a manifest that names them. Any other tenant is answered <code>artifact_missing</code> (404) for a manifest and <code>404</code> for a blob, even for a digest that exists. It follows that <code>missingBlobs</code> is answered per tenant and not per byte store: reporting a blob as already present because a different tenant uploaded it would hand over bytes on the strength of a digest.</p><p>A host MAY still deduplicate physically — one stored copy per content address, however many tenants hold it. Two tenants that upload the same bytes and commit the same manifest see the same immutable identity, and neither one&#39;s abandon or garbage collection may take the bytes away from the other. This is the required conformance check <code>artifact-digest-is-not-a-capability</code>.</p><p>The rule governs <strong>using</strong> an address as well as reading one. Wherever a host resolves a manifest or a blob on behalf of a request — above all the referenced manifest of a bundle-shaped desired state — it asks the same per-tenant holding question, so a caller who merely learns a digest cannot apply or import their way to another tenant&#39;s artifact. The refusal is the ordinary <code>artifact_missing</code> (404), raised before any mutation on apply and import alike and re-raised when a 202 commits, and it MUST NOT distinguish &quot;exists but is not yours&quot; from &quot;does not exist&quot;: a distinguishable refusal is the existence oracle the read rule removes. Holding is the tenant&#39;s, so a manifest one principal uploaded is referenceable by another principal of the same tenant, and a tenant that supplies the same bytes itself references the same immutable identity like any other holder. This is the required conformance check <code>manifest-reference-is-not-a-capability</code>.</p><h2 id="support-profiles" tabindex="-1">Support profiles <a class="header-anchor" href="#support-profiles" aria-label="Permalink to &quot;Support profiles&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET {api}/support/forms</span></span>
<span class="line"><span>GET {api}/support/forms/{formGroup}/{formVersion}/{kind}/{definitionVersion}</span></span>
<span class="line"><span>GET {api}/support/interfaces/{name}/{version}</span></span>
<span class="line"><span>GET {api}/support/bindings/{name}/{version}</span></span>
<span class="line"><span>GET {api}/support/standard-services/{protocol}</span></span></code></pre></div><p>Responses validate against <a href="/schemas/support/v1alpha2/host-support-profile.schema.json"><code>../schemas/host-support-profile-v1alpha2.schema.json</code></a>. A profile declares supported exact refs, closed capability subsets (<code>supportedEnums</code>), inclusive ranges (<code>supportedRanges</code>), supported binding contracts, and numeric limits. A profile&#39;s <code>operations</code> member is the subset of the Form&#39;s declared <code>lifecycleCapabilities</code> this host serves: narrowing is lawful (a host serving no <code>import</code> refuses it <code>unsupported_capability</code> at prepare, <a href="./../decisions/0031-host-capability-is-decided-at-plan-time.html">decision 0031</a>); widening is not — an operation the Definition does not declare never appears. <code>supportedEnums</code>, <code>supportedRanges</code>, and <code>limits</code> keys are RFC 6901 JSON Pointers into the desired document (<code>/assets/notFoundHandling</code>, <code>/versions/-/weight</code> with <code>-</code> addressing every element), so nested and array-element members are addressable; every <code>limits</code> value stays within 2^53−1 so the document remains I-JSON.</p><p>A <code>StandardServiceSupport</code> profile answers the standard-services route: its <code>serviceRef</code> names the protocol and <code>satisfiable</code> says whether this host will satisfy slots of that protocol for the requesting tenant — never with what. Satisfiability is tenant wiring, so the answer is tenant-scoped, <code>validate</code> never depends on it, and the plan-time refusal of an unsatisfiable REQUIRED slot lives in <code>prepare</code> (<code>unsupported_capability</code>), the earliest surface that knows the tenant (<a href="./../standard-services/"><code>../standard-services/README.md</code></a>). Artifact-backed Forms publish <code>maximumBundleBytes</code> together with <code>maximumBundleFiles</code>, at the portable floors their family states. Price, SKU, region, quota, and commercial policy MUST NOT appear; those remain Service Offering data outside this API.</p><p>A host that supports a Form providing a runtime ABI contract MUST advertise that contract at the exact <code>schemaDigest</code> the Form&#39;s <code>providedInterfaces</code> names, and MUST advertise any enum derived from it as exactly the vocabulary that contract defines. It MUST NOT advertise a compatibility date range or a compatibility flag enum: runtime behavior is stated by implementing an exact contract, not by a token no registry interprets (<a href="./../decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>). A revision declaring an entrypoint the runtime contract does not define MUST be refused before any mutation, and so MUST a revision declaring an entrypoint the artifact it references does not provide: loading fails that revision before any traffic arrives, so storing it would leave the activation gate above admitting an activation against an entrypoint that does not exist. The first is decidable from the spec alone and is refused on <code>validate</code>, <code>prepare</code>, and <code>apply</code> alike; the second needs the bundle relation, the committed manifest, and the module bytes, so it is refused before any mutation on <code>apply</code> and <code>import</code> alike and re-raised when a 202 commits — <code>invalid_argument</code> (400) in both cases, because the request is well formed and states something untrue about what will run. These are the required conformance checks <code>module-worker-runtime-contract-advertised</code>, <code>undeclared-runtime-handler-rejected</code>, and <code>declared-handler-not-exported-rejected</code>.</p><h3 id="what-the-lane-proves-and-what-stays-a-host-obligation" tabindex="-1">What the lane proves, and what stays a host obligation <a class="header-anchor" href="#what-the-lane-proves-and-what-stays-a-host-obligation" aria-label="Permalink to &quot;What the lane proves, and what stays a host obligation&quot;">​</a></h3><p><code>worker.runtime@1.0.0</code> fixes far more than a black-box lifecycle runner can observe, so the split is stated rather than implied.</p><p>Proven by required checks:</p><ul><li>the host advertises the runtime contract at the EXACT pinned <code>schemaDigest</code>, and supports the exact Form line that provides it;</li><li>any entrypoint enum it advertises is exactly that contract&#39;s vocabulary, and it advertises no <code>compatibilityDate</code> range or <code>compatibilityFlags</code> enum;</li><li>a version declaring a handler outside the vocabulary is refused, and one declaring only defined handlers is still accepted;</li><li>a version declaring a handler its referenced module does not export is refused against a corpus-pinned bundle, and one declaring only exported handlers is still accepted;</li><li>every inward-activation attachment is gated on the handler its events invoke, in both directions;</li><li>a host-assigned output is answered with a complete address the host minted, in canonical form and with every published member built from one hostname; that address is still the same address, under the same UID, after a host-side status refresh, a promotion of the worker it serves, and a re-read; and a second endpoint against one worker is refused.</li></ul><p>Obligations a conforming host MUST meet that this lane does NOT prove, because proving them means executing the module rather than driving the Host API:</p><ul><li>the default export&#39;s shape, the three-argument handler signatures, and that a returned promise is awaited;</li><li><code>handler_not_exported</code> for an ARBITRARY module — the lane drives bundles whose exports the corpus pinned, so what it proves is that a host refuses the version when it knows, not that it derives the export set from any bytes an author uploads;</li><li>request and response bodies streaming rather than buffering;</li><li><code>env</code> carrying exactly the declared names and nothing else portable;</li><li><code>ctx.waitUntil</code> holding the isolate open, and a rejected task never changing an already-sent response;</li><li>an uncaught throw becoming a host-generated 500 in <code>fetch</code> and a failed invocation in <code>scheduled</code> and <code>queue</code>;</li><li>the <code>globals</code> floor, the loadable module media types, and that an import resolving to an auxiliary module fails <code>unsupported_media_type</code> rather than linking source-map evidence into the graph;</li><li>that a request to a host-assigned published address actually ARRIVES at the resource. The lane drives desired state and sends no traffic, so what it proves is that a host answers with a complete address it minted and keeps that address stable across a promotion — not that anything is listening on it. Nor can it prove the refusal branch: a black-box runner cannot take the address-assignment capability away from the host under test, so <code>unsupported_capability</code> (422) for a host that cannot assign one is stated normatively and left to that host to honor.</li></ul><p>Nothing the ABI declares is unmeasurable any more. <code>worker.runtime@1.0.0</code> used to declare a <code>tail</code> handler no resource in this family could activate, so no run could observe it and two hosts could implement it differently with nothing able to detect the divergence; the handler was removed rather than left in a published-shaped contract nothing reaches, and the runtime corpus now REFUSES a declared handler with no check measuring it, so the property is enforced rather than described. <code>tail</code> returns with the attachment that makes it observable and a new exact runtime Interface version, never as a bare handler (<a href="./../decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>).</p><p>Those are stated normatively in the contract&#39;s own descriptions and proven by its behavior fixtures, which a runtime conformance run executes against a real isolate (<a href="./../interface-contract/"><code>../interface-contract/</code></a>). That run is <a href="../../conformance/runtime-abi-v1/contract.json"><code>../../conformance/runtime-abi-v1/</code></a>, whose runner drives a worker deployed from its own byte-pinned bundle and a disposable adapter over the runtime&#39;s module loader; it is a separate corpus, runner, and command from this lane precisely because the subjects differ (<a href="./../decisions/0023-the-runtime-abi-is-measured-separately-from-the-control-plane.html">decision 0023</a>). A host that passes this lane has not thereby proven it implements the ABI; it has proven it says which ABI it implements and holds desired state to it.</p><p>The same split covers the four data-plane contracts — <code>edge.kv</code>, <code>edge.objects</code>, <code>edge.sql</code>, and <code>edge.queue</code> — and <code>worker.service</code>, whose whole delivery model is a claim about bytes moving between two workers: that neither body is buffered, that an absent body is distinguishable from an empty one and from one whose length the writer does not yet know, that a length the head DOES declare is one the bytes keep, that backpressure and cancellation propagate, that a request abort and a response abort are different observable outcomes, that a callee exception arrives as a complete host-generated 500 rather than a hung call, and that a call which could not be dispatched fails rather than answering with a status. <code>edge.sql</code> separately fixes its safe binary64 value corridor, canonical encoded BLOBs, rollback-only queries, one-statement runtime boundary, admin-only schema migrations, and materialize-before-commit transactions. This lane drives desired state and never moves a byte of application data (<a href="./../decisions/0020-the-edge-interfaces-state-their-data-and-delivery-model.html">decision 0020</a>, <a href="./../decisions/0034-edge-sql-uses-safe-wire-values-and-rollback-only-queries.html">decision 0034</a>).</p><p>Proven by required checks:</p><ul><li>the host advertises each of the four contracts at the EXACT pinned <code>schemaDigest</code>, which is what turns &quot;this host has a KV store&quot; into a statement about a consistency model, a value model, an error vocabulary, and a set of limits (<code>edge-interface-contracts-advertised</code>);</li><li>a schedule expression that is a shape rather than a schedule is refused, and the sub-hourly schedules the grammar exists for are accepted (<code>cron-grammar-enforced</code>);</li><li>a second holder of an exclusively held target is refused, and the same holder is accepted once the first is gone (<code>queue-single-consumer-enforced</code>);</li><li>a claimed value written in any spelling of one name is stored under the canonical one, and a second attachment claiming it — in either spelling — is refused while the first lives and accepted once it is gone (<code>custom-domain-hostname-canonicalized</code>, <code>custom-domain-hostname-claim-unique</code>);</li><li>a hostname another TENANT serves is claimable, both claims stay live under two uids, releasing one leaves the other alone, and inside either tenant a second claim is still refused without naming anyone else&#39;s resource (<code>custom-domain-hostname-claim-stops-at-the-tenant</code>);</li><li>a resource whose dead-letter destination resolves to its own target, or closes a cycle through another consumer&#39;s, is refused, and an acyclic destination is accepted, including one that shares a destination with another chain (<code>dead-letter-cycle-rejected</code>).</li></ul><p>Obligations a conforming host MUST meet that this lane does NOT prove, because proving them means exercising the data plane rather than driving the Host API:</p><ul><li><strong>Static-asset routing.</strong> That request paths resolve to the exact bytes in the referenced asset manifest, that a family&#39;s declared stage order is honoured, and that a declared fallback serves those bytes rather than a host-owned document. The reference host proves the exact relation and refuses an SPA bundle without that path; it serves no HTTP application traffic.</li><li><strong>SQLite migration execution.</strong> That each SQL file and its <code>(path, digest)</code> ledger insertion commit in one real SQLite transaction, that a failed file leaves neither schema effects nor a ledger record, and that concurrent applications serialize on the same database. The reference host proves the ordered prefix/suffix state machine and never pretends its in-memory ledger executed SQL.</li><li><strong><code>edge.kv</code> convergence.</strong> That a write eventually becomes visible at every location. One client cannot observe cross-location convergence at all, which is why the contract&#39;s deterministic fixtures assert only facts no write has to converge for. The obligation is that convergence happens, that a host&#39;s own convergence target is stated in its Host Support Profile, and that the contract&#39;s other reading — read-your-writes — is NOT provided and must not be relied on.</li><li><strong>The encoded-bytes value model.</strong> That <code>maxValueBytes</code> and <code>maxMessageBytes</code> bound the DECODED length, that <code>maxKeyBytes</code> bounds a key&#39;s UTF-8 encoding, and that undecodable base64 is refused.</li><li><strong><code>edge.objects</code> streaming, ranges, conditionals, and multipart.</strong> That a body is never buffered into a JSON member, that a ranged <code>get</code> returns exactly the requested subrange of one object version, that a failed precondition changes nothing, and that a multipart upload assembles its parts atomically or not at all. The contract&#39;s fixtures state the first three as traces a runtime conformance run executes; multipart cannot be a static trace, because its steps depend on part etags the host mints while the trace runs.</li><li><strong><code>edge.sql</code> value and effect boundaries.</strong> That safe finite binary64 numbers, UTF-8 text, null, and canonical encoded BLOBs round-trip unchanged; unsafe input and output fail <code>numeric_out_of_range</code> without rounding; a runtime call refuses multiple statements, transaction-control SQL, and schema migration; <code>query</code> materializes inside a transaction it always rolls back with <code>rowsWritten: 0</code>, even when the statement transiently writes; and a transaction materializes every bounded result before committing all effects or none. The Interface fixtures state the deterministic wire/refusal half; proving persistent effects requires a real SQLite data plane (<a href="./../decisions/0034-edge-sql-uses-safe-wire-values-and-rollback-only-queries.html">decision 0034</a>).</li><li><strong><code>edge.queue</code> delivery.</strong> That a <code>messageId</code> is stable across redeliveries, that <code>attempts</code> counts them, that the first delivery does not count toward <code>maxRetries</code>, that an uncaught handler exception retries every message not already acknowledged, and that a dead-lettered message arrives as a new message with <code>attempts</code> starting again at 1. All of it needs a consumer and the passage of time, neither of which a desired-state runner has.</li></ul><p>Those are stated normatively in each contract&#39;s own descriptions and proven by its behavior fixtures wherever a fixture can prove them.</p><h2 id="errors" tabindex="-1">Errors <a class="header-anchor" href="#errors" aria-label="Permalink to &quot;Errors&quot;">​</a></h2><p>The taxonomy is closed, and this document defines every code in it. Nothing is inherited: a code either has its trigger stated here or it is not in the lane. <code>retryable: true</code> is lawful only on <code>resource_busy</code>, <code>backend_unavailable</code>, <code>rate_limited</code>, and <code>deadline_exceeded</code>; <code>Retry-After</code> is honored when present; every other code carries <code>retryable: false</code>.</p><table tabindex="0"><thead><tr><th>Code</th><th style="text-align:right;">HTTP</th><th>A host answers it exactly when</th></tr></thead><tbody><tr><td><code>invalid_argument</code></td><td style="text-align:right;">400</td><td>the request fails validation before any fence or mutation: malformed or missing query/header/body members, duplicate JSON members, a fence pair that disagrees, a spec the exact Form&#39;s desired schema refuses, a review echo that does not match, or an expired/substituted prepare digest</td></tr><tr><td><code>artifact_invalid</code></td><td style="text-align:right;">400</td><td>uploaded or committed bytes violate the artifact manifest they were declared under</td></tr><tr><td><code>unauthenticated</code></td><td style="text-align:right;">401</td><td>the request carries no credential, or one that names nobody</td></tr><tr><td><code>permission_denied</code></td><td style="text-align:right;">403</td><td>the authenticated principal lacks authority for this operation on this resource</td></tr><tr><td><code>policy_denied</code></td><td style="text-align:right;">403</td><td>the principal has the authority, and a host-side policy refuses the act anyway; <code>hostCode</code>/<code>details</code> MAY name the policy, portable state never does</td></tr><tr><td><code>form_unknown</code></td><td style="text-align:right;">404</td><td>the exact FormRef names a group/kind/version this host has never installed</td></tr><tr><td><code>resource_not_found</code></td><td style="text-align:right;">404</td><td>the addressed resource does not exist under the exact ref for this tenant — indistinguishably from never-created, foreign-tenant, and wrong-exact-ref</td></tr><tr><td><code>operation_not_found</code></td><td style="text-align:right;">404</td><td>the operation id names no record the principal&#39;s tenant holds</td></tr><tr><td><code>artifact_missing</code></td><td style="text-align:right;">404</td><td>a referenced content address names bytes the caller&#39;s tenant has not committed</td></tr><tr><td><code>form_not_installed</code></td><td style="text-align:right;">409</td><td>the exact FormRef is known but not installed as executable code — <code>definitionKnown</code> true, <code>installed</code> false in the availability answer</td></tr><tr><td><code>resource_busy</code></td><td style="text-align:right;">409</td><td>the resource exists and cannot answer now because the host is mid-way through another mutation of IT (a concurrent apply, a commit in flight, an index backfilling); transient by definition, hence retryable</td></tr><tr><td><code>import_conflict</code></td><td style="text-align:right;">409</td><td>an adoption names a <code>nativeId</code> another live resource of the tenant already holds</td></tr><tr><td><code>operation_cancelled</code></td><td style="text-align:right;">409</td><td>a cancel arrived past the operation&#39;s safe stopping point (the HTTP refusal), or — inside a terminal operation record — the operation ended because it was cancelled</td></tr><tr><td><code>dependency_in_use</code></td><td style="text-align:right;">409</td><td>deletion is refused because a live relation, binding, dead-letter target, subscription, or dependent resource still references the target</td></tr><tr><td><code>migration_required</code></td><td style="text-align:right;">409</td><td>the request addresses state a newer contract wrote and this host will not silently reinterpret</td></tr><tr><td><code>uid_mismatch</code></td><td style="text-align:right;">409</td><td>a fence or commit named a uid and the name is now held by a different incarnation</td></tr><tr><td><code>unsupported_capability</code></td><td style="text-align:right;">422</td><td>the exact Form, operation, declared binding, external-service slot, or entrypoint requirement is one this host does not serve for this tenant; answered at the earliest surface that can know it (prepare, or apply where prepare could not)</td></tr><tr><td><code>revision_conflict</code></td><td style="text-align:right;">412</td><td>a supplied ETag/revision fence does not match the current revision</td></tr><tr><td><code>generation_conflict</code></td><td style="text-align:right;">412</td><td>a supplied generation fence does not match the current generation, or a create found the name occupied</td></tr><tr><td><code>rate_limited</code></td><td style="text-align:right;">429</td><td>the tenant exceeded a host-side rate policy; retryable, <code>Retry-After</code> states when</td></tr><tr><td><code>backend_unavailable</code></td><td style="text-align:right;">503</td><td>the host&#39;s backend cannot be reached; nothing about the request is wrong</td></tr><tr><td><code>form_unavailable</code></td><td style="text-align:right;">503</td><td>the exact Form is installed but its backing capacity is temporarily not serving — <code>installed</code> true, availability transiently false</td></tr><tr><td><code>internal_error</code></td><td style="text-align:right;">500</td><td>the host failed; the request may or may not have taken effect, so a mutation is never blindly retried</td></tr><tr><td><code>deadline_exceeded</code></td><td style="text-align:right;">504</td><td>the host gave up waiting on its own downstream work; completion state is unknown, so a mutation is never blindly retried</td></tr></tbody></table><p>The v1alpha2 codes <code>resource_version_conflict</code>, <code>interface_identity_ambiguous</code>, and <code>interface_instance_ambiguous</code> do not exist in this lane: version conflicts split into the revision/generation pair, and the v1alpha2 interface projection is replaced by exact Interface contracts (<a href="./../interface-contract/"><code>../interface-contract/</code></a>). The v1beta1 codes <code>form_identity_conflict</code> and <code>deletion_protected</code> do not exist in this lane either: the first was never given a trigger, and the second claimed a policy surface no Form provides. A withdrawn code is never reused meaning something else.</p><p>An error body always carries <code>code</code>, <code>message</code>, <code>requestId</code>, and <code>retryable</code>, MAY carry <code>hostCode</code> and <code>details</code>, and never carries a credential, a price, or another tenant&#39;s existence.</p>`,195)])])}const m=t(i,[["render",s]]);export{u as __pageData,m as default};
