import{_ as a,o as t,c as i,a2 as s}from"./chunks/framework.Bbwa0s72.js";const u=JSON.parse('{"title":"Release trust profile","description":"","frontmatter":{},"headers":[],"relativePath":"spec/trust/index.md","filePath":"spec/trust/index.md"}'),r={name:"spec/trust/index.md"};function n(o,e,d,c,l,p){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="release-trust-profile" tabindex="-1">Release trust profile <a class="header-anchor" href="#release-trust-profile" aria-label="Permalink to &quot;Release trust profile&quot;">​</a></h1><p>This directory records the D-08 trust decision — the owning engineering policy&#39;s package/provider trust decision, internal identifier D-08 — for Takoform provider and Form Package artifacts. Requirement keywords are used as described in <a href="./../conformance.html"><code>../conformance.md</code></a>. It does not make either artifact publishable. The machine-readable authority is <a href="profile.json"><code>profile.json</code></a>; implementation and live distribution evidence must satisfy it before a release descriptor can become publishable.</p><h2 id="separate-trust-lanes" tabindex="-1">Separate trust lanes <a class="header-anchor" href="#separate-trust-lanes" aria-label="Permalink to &quot;Separate trust lanes&quot;">​</a></h2><p>The Terraform/OpenTofu provider and Form Packages use separate trust lanes.</p><p>The provider follows the Terraform Registry contract:</p><ul><li>a signed immutable <code>v*</code> tag selects the source commit;</li><li>deterministic GitHub Release assets are identified by SHA-256;</li><li>the archive-plus-Registry-manifest checksum file is signed with the RSA OpenPGP key whose full fingerprint is pinned in <code>release/version.json</code> and <code>profile.json</code>;</li><li>five SPDX 2.3 documents cover the exact provider archives separately and are never projected as Registry provider packages;</li><li>one RFC 8785 canonical in-toto Statement v1 with SLSA Provenance v1 exact-closes the 13 pre-provenance assets by name, byte size, and SHA-256; its detached OpenPGP signature is authenticated by the same pinned provider key;</li><li>an existing version MUST NOT be overwritten;</li><li>the <code>tako0614</code> public namespace and pinned key ID <code>34FC18AC897FB709</code> are registered; provider <code>v1.0.1</code> is published, and its retained authenticated readback proves direct canonical Registry installation through both OpenTofu and Terraform.</li></ul><p>Form Packages do not reuse the provider GPG key. The standard Takoform publisher uses a keyless Sigstore blob signature and bundle bound to the exact repository, protected workflow, and <code>forms/*/v*</code> tag. The bundle MUST carry a transparency-log inclusion proof so an operator can verify retained artifacts without trusting the distribution endpoint at verification time. A third-party publisher has no ambient trust: an operator MUST install a separate publisher policy for its exact issuer and source identity.</p><h2 id="canonical-package-identity" tabindex="-1">Canonical package identity <a class="header-anchor" href="#canonical-package-identity" aria-label="Permalink to &quot;Canonical package identity&quot;">​</a></h2><p>Form Definition and package-index JSON use the JSON Canonicalization Scheme in RFC 8785. Inputs are UTF-8 I-JSON and reject duplicate object names, invalid Unicode, non-finite numbers, and negative zero. The exact identity is <code>sha256:&lt;lowercase hex&gt;</code> over the RFC 8785 bytes of the package index. The index lists the digest, media type, and size of every data payload.</p><p>An archive is only a transport container; archive metadata is not the semantic package identity. A verifier MUST validate the signed canonical index and every referenced payload before exposing a definition. It rejects unlisted files, path traversal, links, devices, executable files, credentials, operator configuration, target/capacity data, prices, billing fields, and executable validation or adapter code.</p><h2 id="provenance-and-publication" tabindex="-1">Provenance and publication <a class="header-anchor" href="#provenance-and-publication" aria-label="Permalink to &quot;Provenance and publication&quot;">​</a></h2><p>The provider&#39;s public GitHub Release inventory contains exactly 15 assets. The 13 provenance subjects are five provider archives, their five SPDX documents, the provider manifest, <code>SHA256SUMS</code>, and its detached OpenPGP signature. The remaining two assets are the canonical provenance statement and its detached OpenPGP signature. To avoid an impossible self-reference, neither provenance asset is a subject of the statement. The statement additionally binds the source commit, release tag, tooling commit, workflow identity, workflow run and attempt, request ID, and exact annotated tag-object OID and SHA-256. A verifier MUST reject an omitted, extra, renamed, resized, or redigested subject, a non-canonical statement, an incomplete build binding, or a provenance signature that does not verify with the pinned provider key. GitHub artifact attestation is not provider release authority in this profile.</p><p>The package index receives a Sigstore v0.3 bundle before a draft release can become public. The release also contains an in-toto Statement v1 with SLSA Provenance v1 and an SPDX 2.3 data-artifact SBOM. The provenance binds the exact package artifacts to their source commit and protected build workflow. The protected preparation artifact separately binds the exact release inventory, source/tooling commits, and workflow run/attempt before local publication.</p><p>The implemented distribution lane is an immutable GitHub Release. Current v1alpha2 packages use <code>forms/&lt;release-id&gt;/sha256-&lt;hex&gt;</code>; retained Legacy v1alpha1 releases keep <code>forms/&lt;release-id&gt;/v&lt;semver&gt;</code>. The release ID is reversible lowercase unpadded base32 of the exact FormRef Kind. A protected-main <code>workflow_dispatch</code> accepts the exact planned tag and approved commit, verifies their canonical release-plan binding and main ancestry, creates only an ephemeral local tag object, and prepares the assets without repository-write authority. It uses the protected <code>form-package-release</code> Environment, commit-pinned Actions, Cosign v3 keyless blob signing, immediate identity/transparency verification, and a checksum-closed same-run artifact. The owner repository&#39;s local deploy entrypoint verifies that exact candidate, then creates the tag and performs draft/upload/publish/immutable readback with operator credentials. A connected or air-gapped mirror copies the exact release assets only after signature, transparency proof, provenance, and digest validation. Installation is an operator action; a customer request path never fetches a package or executable extension.</p><p>The FormRef, Form Definition, package-index and revocation schemas, RFC 8785/I-JSON implementation, closed local verifier, positive/negative corpus, release builder, keyless Sigstore workflow, and append-only revocation delivery lane now exist. The retired <code>1.0.1</code> packages have real immutable releases; their exact release closures and signed indexes are retained under <code>admission/v1</code> with a TUF-authenticated production root and a digest-pinned, version-bound historical aggregate package publisher policy. All 34 retained Legacy Form Packages also have signed immutable releases, and provider <code>v1.0.1</code> has retained authenticated direct-Registry readback. No revocation statement has been released. The historical admission closure is source-retained, offline-authenticated lifecycle evidence, not another package or activation release and not a current maturity authority. Remote host distribution/install, host-side publisher-policy enforcement, activation, and revocation consumption remain host/operator work.</p><p>The generated all-34 source inventory, definition <code>status: standard</code>, and the exact-ten v4 <code>portable-standard</code> closure are immutable Legacy facts. Published tag <code>forms/admissions/v1.0.7</code> pins the final historical v4 closure. The retained Takosumi host closure proves that those exact ten identities passed that host&#39;s historical lifecycle evidence. It does not define a current approved subset or make the other 24 lower-maturity Forms. Current Form maturity, Host Support, and availability are separate as defined by <a href="./../project-lifecycle.html"><code>../project-lifecycle.md</code></a>.</p><p>Provider distribution and the retained historical admission evidence used an explicit two-phase authority split. The first phase published signed provider <code>v1.0.1</code>; installability did not promote or activate any Form. After both supported CLIs installed that exact version from the canonical Registry FQN, independent protected workflows produced signed host reports, all-34 provider reports, one canonical Registry readback, and 10 admission-evidence subjects. Reviewed source retains those exact candidates, package-release readbacks, and the resulting historical set. The history verifier pins published admission tag objects, commits, retained trees, and set digests from the closed identity ledger. It publishes no aggregate archive or GitHub Release and performs no controller promotion. Provider GPG authority, independent package publisher identity, and host, provider, Registry-readback, and admission-evidence identities remain distinct.</p><p>The direct Registry provider is untrusted executable code. It runs only in a read-only job with no protected Environment, OIDC token, attestation, or repository-write permission. Only its canonical matrix and readback cross the job boundary. A distinct protected job verifies the same-run artifact and signs the canonical readback, producing a non-published checksum-closed candidate. Those candidates and the later signed admission-evidence subjects are retained as historical source. The central assembly command and signing workflow have been removed. The history verifier authenticates published Git identities; it does not reinterpret old evidence under current lifecycle rules. There is no current publication job, approval controller, or set-wide stability transition.</p><h2 id="offline-legacy-admission-evidence-verification" tabindex="-1">Offline Legacy admission-evidence verification <a class="header-anchor" href="#offline-legacy-admission-evidence-verification" aria-label="Permalink to &quot;Offline Legacy admission-evidence verification&quot;">​</a></h2><p>The repository retains the old offline verifier formats so historical bytes remain explainable. The current authority gate does not rebuild a central candidate or rerun its semantic approval: it verifies the exact Legacy package publication snapshot, then checks the closed admission identity ledger against historical annotated tag objects, commits, retained trees, and set digests.</p><p>The retained admission directory must contain these reviewed source inputs:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>admission/v1/trust/offline-sigstore-pins.json</span></span>
<span class="line"><span>admission/v1/trust/trusted-root.json</span></span>
<span class="line"><span>admission/v1/trust/publisher-policy.json</span></span>
<span class="line"><span>admission/v1/trust/host-report-policy.json</span></span>
<span class="line"><span>admission/v1/trust/provider-report-policy.json</span></span>
<span class="line"><span>admission/v1/trust/package-index-policy.json</span></span>
<span class="line"><span>admission/v1/trust/registry-readback-policy.json</span></span>
<span class="line"><span>admission/v1/registry/provider-readback.json</span></span>
<span class="line"><span>admission/v1/registry/provider-readback.sigstore.json</span></span>
<span class="line"><span>admission/v1/registry/provider-lifecycle-matrix.json</span></span>
<span class="line"><span>admission/v1/packages/&lt;slug&gt;/evidence.json</span></span>
<span class="line"><span>admission/v1/packages/&lt;slug&gt;/evidence.sigstore.json</span></span>
<span class="line"><span>admission/v1/packages/&lt;slug&gt;/host-report.json</span></span>
<span class="line"><span>admission/v1/packages/&lt;slug&gt;/host-report.sigstore.json</span></span>
<span class="line"><span>admission/v1/packages/&lt;slug&gt;/provider-report.json</span></span>
<span class="line"><span>admission/v1/packages/&lt;slug&gt;/provider-report.sigstore.json</span></span>
<span class="line"><span>admission/v1/releases/&lt;release-id&gt;/&lt;version&gt;/release-manifest.json</span></span>
<span class="line"><span>admission/v1/releases/&lt;release-id&gt;/&lt;version&gt;/&lt;five exact release assets&gt;</span></span></code></pre></div><p>All retained identities and readback bytes are reviewed source. Historical <code>admission/v1</code> deliberately omits <code>registry/provider-readback.sigstore.json</code>; that incomplete historical lane is not reusable as evidence for another identity. The retained v4 lane required the signed Registry candidate to be retained in source before evidence assembly. The offline gate authenticates that retained bundle like every other subject; it neither creates an activation archive, publishes a release, nor promotes current Form maturity.</p><p>The <code>takoform.offline-sigstore-pins@v2</code> manifest binds the exact trusted-root and five role-specific publisher-policy byte sets by canonical <code>sha256:&lt;lowercase-hex&gt;</code> digest. Each strict publisher policy pins one exact Fulcio OIDC issuer, certificate identity, and Sigstore v0.3 media type. The five <code>(issuer, certificate identity)</code> pairs must be mutually distinct, so an admission-evidence publisher, host runner, provider runner, package publisher, or Registry-readback/admission authority cannot silently substitute for another role. The verifier accepts only keyless blob message signatures over the exact retained subject SHA-256, requires a verified Rekor inclusion proof and signed integrated time, validates the Fulcio chain and exact identity, and requires a verified certificate-transparency SCT. It reads only retained regular files below <code>admission/v1</code>; parent-directory symlinks and network lookups are rejected by construction.</p><p>The historical <code>admission/v1</code> set uses <code>takoform.standard-admission-set@v2</code>; retained <code>admission/v4</code> material uses <code>takoform.standard-admission-set@v3</code>. The earlier v1 formats were an intentionally non-opening pre-release foundation: no real set or trust pins were installed and no provider release could pass them. Therefore v2 was a clean pre-publication contract replacement, not a migration of admitted or customer state, while v3 added the generation-aware historical closure. Test fixtures use an explicit in-process fake subject verifier and are never written under the repository&#39;s <code>admission/</code> path; they do not represent signatures or live evidence.</p><p>Canonical host reports remain <code>takoform.standard-runner-report@v1</code>. Historical provider reports may also use that format; v1 contains only its runner subject and version, exact <code>(FormRef, packageDigest)</code>, <code>passed</code> status, all eight lifecycle booleans, named positive fixture results, and named negative results normalized to <code>invalid_argument</code>. Fixture closure is role-specific and stage-derived: the host report covers the exact <code>desired</code> negatives it can submit through the portable API, while the provider report covers those plus exact <code>observed</code> response negatives. Neither role may claim an <code>output</code> negative until a normative execution contract exists for that stage. The historical standard-admission candidate contract required at least one <code>desired</code> negative, so both the host and provider report closures were non-empty.</p><p>The later historical provider reports use the distinct <code>takoform.standard-provider-runner-report@v2</code> format. It adds the required <code>providerBinarySha256</code> field so the report binds the exact provider executable used by the lifecycle and fixture runs. The retained closure format required all provider reports to carry the same digest and requires that digest and provider version to equal every direct Registry installation readback. The evidence-tooling source commit remains provenance for the report generator; it is not incorrectly equated with the older immutable provider-release commit. <code>profile.json</code> schema v2 locks the host/provider format split and digest algorithm.</p><p>Each canonical report SHA-256 must equal both the admission set entry&#39;s role digest and the corresponding <code>AdmissionEvidence.conformance.*.evidenceDigest</code>. Unknown fields, duplicate/failed fixtures, incomplete lifecycle, identity substitution, and non-portable negative codes fail closed.</p><p>The deterministic package readback does not trust a download URL. The v2 set pins the exact release-manifest bytes; the validator rereads all five assets, checks every size and digest, requires the canonical index, archive, Sigstore bundle, SPDX SBOM, and in-toto provenance names/media types, compares the index to the provider-compiled candidate, verifies the deterministic tar entry order, metadata, payload sizes/digests, and absence of unlisted archive entries, and then authenticates that exact index. The SBOM and provenance are themselves RFC 8785 canonical, strictly decoded evidence: the verifier recomputes the SPDX file closure and package verification code and requires SLSA subjects, source repository, tag, tagged-source commit, distinct protected-main tooling commit, commit-versioned workflow builder, and canonicalization parameters to match the exact retained package release. The historical <code>1.0.1</code> publisher policy evidence pins the retired aggregate <code>standard-form-package-set-release.yml@refs/tags/standard-forms/v1.0.1</code> certificate identity and the same release commit. Unknown, duplicate, omitted, or substituted metadata fails closed. The canonical <code>takoform.provider-registry-readback@v1</code> similarly binds the provider version/tag/commit, current release descriptor, candidate-set and schema digests, both CLI/FQN/binary identities, and the exact direct-install matrix digest. <code>cmd/admission-readback</code> renders this unsigned canonical subject from a validated direct matrix. Historical v1 evidence retains its original publisher identity. Later Registry evidence was signed only by <code>.github/workflows/provider-registry-readback.yml</code>, whose identity is distinct from package, provider-report, host-report, and admission-evidence publishers.</p><h3 id="historical-generation-aware-successor-lane" tabindex="-1">Historical generation-aware successor lane <a class="header-anchor" href="#historical-generation-aware-successor-lane" aria-label="Permalink to &quot;Historical generation-aware successor lane&quot;">​</a></h3><p><code>admission/v3</code> is immutable <code>ga-core-v1</code> history. Its <code>published-package-set@v2</code> authenticates the exact ten per-Form releases, including <code>HttpService@1.0.0</code>, with the independent <code>form-package-release.yml</code> identity. The retained signed report candidates remain evidence of what ran, but do not grant admission and are not reused for the successor.</p><p><code>admission/v4</code> is the retained historical <code>ga-core-v2</code> closure. It selects the exact mixed-version ten-Form subset containing the provider-neutral <code>EdgeWorker@3.0.0</code>. Provider reports use generation <code>portable-v1</code> and close over all 34 Legacy Forms before the historical builder selects the ten exact evidence identities. Host reports use generation <code>ga-core-v2</code> and contain exactly those ten identities. Neither manifest carries a false uniform definition or package version. The exact-ten selection is not reused as a current maturity or Host Support authority.</p><p>The retired assembly command and <code>standard-admission-evidence.yml</code> workflow produced the retained material; neither is a current surface. Authentication now uses <code>legacy-admission-evidence-check</code>, which resolves the closed ledger&#39;s historical admission tags and verifies their exact tag objects, commits, retained trees, and set digests. Package publication authentication remains a separate Legacy evidence check.</p><p>The production Sigstore trusted-root snapshot, the distinct package-index and Registry-readback publisher policies, every retired package-index bundle, and the exact immutable release readbacks are retained and digest-pinned by <code>admission/v1/published-package-set.json</code>. They pass the separate offline <code>published-package-check</code> but grant no admission authority. Exact mutually distinct admission-evidence, host-report, and provider-report policies and the five-role offline pin manifest are retained. The v4 signed host/provider and admission reports, canonical Registry readback, and <code>standard-admission-set.json</code> are retained as historical evidence. The historical canonical Registry matrix/readback for provider <code>v0.1.3</code> are also retained under the retired v1 evidence lane, but grant no current admission authority. The v4 material bound a separately signed readback for exact provider <code>v1.0.1</code> and its two-CLI direct-Registry matrix. Historical admission checkpoint versions were independent from bound Form and provider versions. No such checkpoint is assigned now.</p><h2 id="rotation-and-revocation" tabindex="-1">Rotation and revocation <a class="header-anchor" href="#rotation-and-revocation" aria-label="Permalink to &quot;Rotation and revocation&quot;">​</a></h2><p>Provider key rotation is additive: register and pin a new public key before a new version, retain old public keys for historical verification, and never replace old release bytes. A compromise disables the release Environment, removes its secrets, publishes the OpenPGP revocation to operators, coordinates Registry key removal through the maintainer/support process, and resumes only with a new key and new semver.</p><p>Form Package keyless identity rotation is a reviewed change to the pinned OIDC issuer/repository/protected-main workflow policy. An append-only revocation statement references an exact package digest. The signed subject is a cumulative checkpoint containing every statement from sequence 1 through its current sequence and the previous checkpoint digest. Security revocation blocks new create/update and activation, but referenced package bytes remain available for safe observe/delete or an explicit operator evacuation path. Deprecation is not security revocation, and neither state replaces package bytes in place.</p><p>The delivery sources are <code>forms/revocations/&lt;statementVersion&gt;.json</code> and <code>forms/revocations/checkpoints/&lt;statementVersion&gt;.json</code>, selected by <code>forms/revocations/v&lt;statementVersion&gt;</code>. CI rejects edits, renames, and deletion of existing source paths. A host verifies the checkpoint bundle and exact protected-main publisher identity, requires sequence 1 for its initial pin, then durably stores <code>(sequence, canonical checkpoint digest, cumulative entries digest)</code> and accepts only the next sequence whose <code>previousCheckpointDigest</code> equals that pin and whose retained entry prefix has the same digest. This detects rollback, omission, prefix rewrite, and forks. Runtime enforcement remains separate from delivery and is not yet claimed by this repository.</p>`,38)])])}const m=a(r,[["render",n]]);export{u as __pageData,m as default};
