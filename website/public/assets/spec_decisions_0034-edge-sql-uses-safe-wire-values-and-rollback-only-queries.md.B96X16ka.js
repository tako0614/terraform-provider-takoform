import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.LJP3gfxI.js";const p=JSON.parse('{"title":"0034 — edge.sql uses safe wire values and rollback-only queries","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0034-edge-sql-uses-safe-wire-values-and-rollback-only-queries.md","filePath":"spec/decisions/0034-edge-sql-uses-safe-wire-values-and-rollback-only-queries.md"}'),s={name:"spec/decisions/0034-edge-sql-uses-safe-wire-values-and-rollback-only-queries.md"};function i(r,e,c,d,l,u){return a(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="_0034-—-edge-sql-uses-safe-wire-values-and-rollback-only-queries" tabindex="-1">0034 — edge.sql uses safe wire values and rollback-only queries <a class="header-anchor" href="#_0034-—-edge-sql-uses-safe-wire-values-and-rollback-only-queries" aria-label="Permalink to &quot;0034 — edge.sql uses safe wire values and rollback-only queries&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-10</li><li>Owners: Takoform maintainers</li><li>Supersedes: the <code>edge.sql</code>-specific context, decision, fixtures, and consequences of <a href="./0020-the-edge-interfaces-state-their-data-and-delivery-model.html">decision 0020</a> only</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Decision 0020 corrected an earlier SQL value model by projecting SQLite&#39;s five storage classes as tagged objects. That preserved every 64-bit SQLite INTEGER, but it made SQLite&#39;s private storage choice part of the application API, made a normal JavaScript number and string invalid unless wrapped, invented a second binary envelope beside the family-wide encoded-bytes shape, and retained <code>lastInsertRowId</code> as special statement metadata.</p><p>That is the wrong portability boundary. A Module Worker is authored against JavaScript values. JavaScript has one binary64 <code>Number</code>, not separately observable INTEGER and REAL value types, and <code>BigInt</code> does not cross the JSON operation boundary. Pretending otherwise makes every host preserve a distinction consumers cannot use while still requiring special handling for a 64-bit value they cannot represent safely.</p><p>The old <code>query</code> rule had a second defect. It promised idempotency by requiring a host to classify SQL as reading before executing it. SQLite statements can hide effects behind common-table expressions, functions, pragmas, extensions, and future syntax. A text classifier is therefore part of the database semantics without being SQLite, and two hosts can disagree about the same statement.</p><p>The Edge Platform Family is publication-frozen and every affected Form, Interface, and Binding candidate remains unpublished. This correction can therefore keep the human-facing <code>1.0.0</code> and <code>0.1.0</code> version strings while replacing their exact digests. No published identity is rewritten.</p><p>This decision supersedes only the SQL portions of decision 0020: its paragraph explaining why the earlier untagged model could not carry SQLite, its section &quot;<code>edge.sql</code> values are tagged by storage class&quot;, its SQL fixture model, and the SQL-specific digest consequences derived from them. Decision 0020 remains the record for <code>edge.kv</code>, <code>edge.objects</code>, <code>edge.queue</code>, <code>worker.service</code>, the cron grammar, and the common encoded-bytes representation.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="edgesqlvalue-is-one-portable-wire-union" tabindex="-1">EdgeSqlValue is one portable wire union <a class="header-anchor" href="#edgesqlvalue-is-one-portable-wire-union" aria-label="Permalink to &quot;EdgeSqlValue is one portable wire union&quot;">​</a></h3><p>Every bound parameter and returned column is exactly one of:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>null</span></span>
<span class="line"><span>finite binary64 number with abs(value) &lt;= 9007199254740991</span></span>
<span class="line"><span>UTF-8 string</span></span>
<span class="line"><span>{&quot;encoding&quot;:&quot;base64&quot;,&quot;data&quot;:&quot;...&quot;}</span></span></code></pre></div><p>The object is the common encoded-bytes shape already used by the family. Its <code>data</code> is canonical RFC 4648 section 4 base64: padded, unwrapped, and decoding to the BLOB bytes. A SQL-only <code>{type: ...}</code> tag, boolean, <code>BigInt</code>, <code>ArrayBuffer</code>, typed array, and raw byte string are not EdgeSqlValue wire values. A host adapter may use a backend-native byte type internally, but it decodes on ingress and emits the canonical object on egress.</p><p>All numbers are binary64. SQLite may store one as INTEGER or REAL according to its own affinity and expression rules; the wire does not expose that storage class and a host must not tag, stringify, or otherwise preserve it as a second portable type. The SQL program can still call SQLite functions such as <code>typeof()</code>; what is absent is a storage-class tag in the returned value.</p><p>Non-finite numbers and numbers whose absolute value exceeds <code>Number.MAX_SAFE_INTEGER</code> fail <code>numeric_out_of_range</code>, on both input and output. A host never rounds, clamps, stringifies, substitutes null, or commits a write before discovering that its output cannot be represented. A malformed value or malformed base64 is <code>sql_error</code>. The Interface Definition meta-schema admits lower-snake-case operation error codes including <code>numeric_out_of_range</code>, so the exact code is declared by all three operations rather than collapsed into <code>sql_error</code>.</p><p><code>lastInsertRowId</code> is removed. It is not defined for every insert, is ambiguous with triggers and multi-row statements, does not apply to <code>WITHOUT ROWID</code> tables, and can exceed the portable number corridor. Applications that need an inserted identity use SQL <code>RETURNING</code>, whose value follows the same EdgeSqlValue rules as every other result column.</p><h3 id="limits-are-part-of-the-exact-contract" tabindex="-1">Limits are part of the exact contract <a class="header-anchor" href="#limits-are-part-of-the-exact-contract" aria-label="Permalink to &quot;Limits are part of the exact contract&quot;">​</a></h3><p>The existing limits remain:</p><ul><li><code>maxStatementBytes = 100000</code>;</li><li><code>maxBoundParameters = 100</code>;</li><li><code>maxStatementsPerTransaction = 100</code>;</li><li><code>maxRowsPerStatement = 10000</code>;</li><li><code>maxColumnsPerRow = 100</code>.</li></ul><p>The contract additionally fixes:</p><ul><li><code>maxColumnNameBytes = 128</code>;</li><li><code>maxTextBytesPerValue = 1000000</code>;</li><li><code>maxBlobBytesPerValue = 1000000</code>;</li><li><code>maxRowBytes = 2000000</code>;</li><li><code>maxResultBytesPerCall = 8388608</code>.</li></ul><p>SQL text, column names, and TEXT use their UTF-8 byte length. BLOB uses decoded byte length. The JSON Schema <code>maxLength</code> values are structural ceilings; the byte limits remain normative for multi-byte text. <code>maxRowBytes</code> is the UTF-8 length of the RFC 8785 canonical JSON representation of one returned row, and <code>maxResultBytesPerCall</code> is the same measurement over the complete operation output, including every transaction result. This makes the budget independent of host object ordering and number spelling.</p><p>An input over a structural or byte limit is refused before SQL executes. A row, column, value, number, or combined output over a limit fails the operation before its write transaction commits. The host never truncates rows, columns, text, BLOBs, or a transaction&#39;s result list to fit.</p><h3 id="runtime-sql-is-one-statement-and-cannot-own-migrations" tabindex="-1">Runtime SQL is one statement and cannot own migrations <a class="header-anchor" href="#runtime-sql-is-one-statement-and-cannot-own-migrations" aria-label="Permalink to &quot;Runtime SQL is one statement and cannot own migrations&quot;">​</a></h3><p><code>execute</code> and <code>query</code> accept one statement. Each member of <code>transaction</code> is one statement. A host uses SQLite&#39;s parser boundary: after the prepared statement, only whitespace and comments may remain. A second statement is <code>sql_error</code>; splitting on semicolons is not the contract.</p><p>Every runtime operation refuses transaction-control SQL — <code>BEGIN</code>, <code>COMMIT</code>, <code>END</code>, <code>ROLLBACK</code>, <code>SAVEPOINT</code>, and <code>RELEASE</code> — with <code>sql_error</code>. The operation owns its transaction boundary, so SQL inside it cannot open, close, or nest a different one.</p><p>Every runtime operation also refuses a statement that would change the durable schema, database attachment set, or migration ledger. Durable schema change is owned only by the <code>SQLiteMigrationApplication</code> administrative path, which pins an exact <code>SQLiteMigrationSet</code>, validates its append-only path-and-digest prefix, and commits each migration with its ledger entry. The admin path may execute a migration file according to that contract; it is not an invocation of <code>edge.sql</code> and does not weaken the runtime restrictions.</p><h3 id="the-three-operation-boundaries-are-exact" tabindex="-1">The three operation boundaries are exact <a class="header-anchor" href="#the-three-operation-boundaries-are-exact" aria-label="Permalink to &quot;The three operation boundaries are exact&quot;">​</a></h3><p><code>execute</code> runs one statement and is effectful and non-idempotent. It returns exactly <code>{rows, rowsWritten}</code>. It materializes and validates the complete result before the statement&#39;s implicit transaction commits, so a numeric or output limit failure applies nothing. A transport failure after an effect committed is still an unknown outcome the caller must resolve; the contract does not make an unsafe retry safe.</p><p><code>query</code> runs one statement inside a rollback-only transaction. It fully materializes and validates the result, always rolls the transaction back, then returns exactly <code>{rows, rowsWritten: 0}</code>. A statement may transiently write while producing rows; the API promises zero persistent effects without pre-classifying it as read-only. SQL failure, cancellation, output conversion, or a limit failure takes the same rollback path. This executed boundary, not a text classifier, is what makes <code>query</code> idempotent.</p><p><code>transaction</code> accepts 1 through 100 statements and runs them in order under serializable isolation over one consistent snapshot. It returns exactly one <code>{rows, rowsWritten}</code> result per statement, in order. Every row and result is materialized and validated before commit. Only then do all effects commit together. Any SQL, numeric, materialization, busy, cancellation, or limit failure rolls everything back and returns no partial result list.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>edge.sql@1.0.0</code>, <code>module-worker.sqlite@1.0.0</code>, <code>SQLiteDatabase@0.1.0</code>, and every Form that embeds one of those exact refs receive new digests while retaining their unpublished version strings.</li><li>The dependent closure includes at least <code>SQLiteMigrationApplication</code>, <code>WorkerVersion</code>, and <code>WorkerDeployment</code>; their exact Form and package pins, the provider v3 registry, the portable-host-v3 corpus, and the website projections are regenerated together.</li><li>Interface fixtures use ordinary values, safe-number boundaries, canonical encoded BLOBs, multi-statement refusal, transaction-control refusal, and the admin-only schema boundary. They no longer create schema through runtime SQL or assert <code>lastInsertRowId</code>.</li><li>The Host API conformance lane still proves only that a host advertises the exact Interface digest. A deployed data-plane run must prove rollback-only query effects, transaction atomicity, output bounds, and adapter conversion. The publication blocker for a real SQLite backend remains open.</li><li>None of this authorizes publication, provider release, tag creation, push, deploy, or a production migration.</li></ul>`,33)])])}const m=t(s,[["render",i]]);export{p as __pageData,m as default};
