import{_ as i,o as s,c as a,a2 as t}from"./chunks/framework.LJP3gfxI.js";const g=JSON.parse('{"title":"Typed Binding contracts (bindings.takoform.com/v1alpha1)","description":"","frontmatter":{},"headers":[],"relativePath":"spec/binding-contract/index.md","filePath":"spec/binding-contract/index.md"}'),n={name:"spec/binding-contract/index.md"};function o(r,e,d,c,h,l){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="typed-binding-contracts-bindings-takoform-com-v1alpha1" tabindex="-1">Typed Binding contracts (bindings.takoform.com/v1alpha1) <a class="header-anchor" href="#typed-binding-contracts-bindings-takoform-com-v1alpha1" aria-label="Permalink to &quot;Typed Binding contracts (bindings.takoform.com/v1alpha1)&quot;">​</a></h1><p>A Binding is a digest-bound contract that grants one consumer resource a typed capability on one target resource: the runtime API and the permission arrive together, and credentials never cross the boundary. Bindings replace the generic <code>connections</code> / <code>permissions</code> / <code>projection</code> surface of the retained v1alpha2 lane (<a href="./../decisions/0010-exact-interface-and-binding-contracts.html">decision 0010</a>).</p><h2 id="bindingref" tabindex="-1">BindingRef <a class="header-anchor" href="#bindingref" aria-label="Permalink to &quot;BindingRef&quot;">​</a></h2><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bindings.takoform.com/v1alpha1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;module-worker.edge-kv&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sha256:...&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>name</code> uses the dotted grammar; <code>schemaDigest</code> binds the canonical Binding Definition bytes. The normative shapes are <a href="/schemas/bindings/v1alpha1/binding-ref.schema.json"><code>../schemas/binding-ref-v1alpha1.schema.json</code></a> and <a href="/schemas/bindings/v1alpha1/binding-definition.schema.json"><code>../schemas/binding-definition-v1alpha1.schema.json</code></a>.</p><h2 id="binding-definition" tabindex="-1">Binding Definition <a class="header-anchor" href="#binding-definition" aria-label="Permalink to &quot;Binding Definition&quot;">​</a></h2><p>A Binding Definition fixes, as data only:</p><ul><li><code>sourceRole</code>: the Form role allowed to hold this binding (normally <code>revision</code>; a deployment or identity resource never holds capability bindings);</li><li><code>targetInterface</code>: the exact InterfaceRef the target must provide;</li><li><code>allowedTargetForms[]</code>: exact Form kinds (by group and kind) this binding may point at;</li><li><code>runtimeProjection</code>: the API surface projected into the consumer at run time, described against the target Interface&#39;s operations, including which operations the binding exposes (<code>accessModes</code> when the binding supports restricted modes);</li><li><code>description</code>: the concrete surface the consumer&#39;s code actually calls. A list of operation names is not a runtime API — it does not say what the caller writes, what it passes, what it gets back, what a callee failure looks like, or whether a body streams. Every Binding Definition in the Edge Platform Family therefore states its projection in prose: <code>module-worker.service</code> projects <code>env.NAME.fetch(request) -&gt; Promise&lt;Response&gt;</code>, streaming in both directions, resolving (not rejecting) with the callee&#39;s host-generated 500 when the callee&#39;s handler throws, and rejecting only when the call could not be made; the KV, bucket, SQLite, and queue-producer bindings state their method names, argument and result types, and how each interface error code appears in JavaScript. The meta-schema&#39;s <code>runtimeProjection</code> admits operation names only, so this belongs in <code>description</code> (<a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a>);</li><li><code>bindingNameGrammar</code>: the grammar for instance names declared by the consumer. Worker-family bindings use the JavaScript identifier grammar <code>^[A-Za-z_$][A-Za-z0-9_$]*$</code>; <code>.</code> and <code>-</code> are invalid there;</li><li><code>lifecycle</code>: what happens to the binding when the target is deleted (<code>dependency_in_use</code> refusal is the default).</li></ul><h2 id="binding-instances" tabindex="-1">Binding instances <a class="header-anchor" href="#binding-instances" aria-label="Permalink to &quot;Binding instances&quot;">​</a></h2><p>A consumer resource declares instances as typed data. The wire shape is a <code>name</code> plus an exact <code>resource</code> reference:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CACHE&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;resource&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;edge.forms.takoform.com/v1beta1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;EdgeKVNamespace&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cache&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><code>resource</code> is an exact in-space reference. All three members are required: <code>apiVersion</code> and <code>kind</code> are <code>const</code> in the declaring Form&#39;s desired schema — the group is never &quot;defaulted by the family&quot;, because a reference that omits it can address only one family and forces a host to guess which installed Form a bare kind means — and <code>name</code> is chosen by the author. After resolution the host pins the target UID <em>and the target&#39;s exact FormRef</em> in host-owned records; desired state keeps the name reference. A target deleted and re-created under the same name is a different resource, and the source is NOT re-bound to it; neither is a target whose Form has since moved to a different exact contract (<a href="./../host-api/v1beta1.html"><code>../host-api/v1beta1.md</code></a>, <a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a>, <a href="./../decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>). Re-applying the source is what re-pins it.</li><li>Sensitive material never appears: a binding projects an API, not a credential. Sensitive-variable requirements are declared as named slots (<code>requiredSensitiveVars</code> on a worker version) and satisfied by host/operator-owned bindings.</li><li>The object these names land in is fixed by the consumer&#39;s runtime ABI, not by the Binding: for the worker family, <code>worker.runtime@1.0.0</code> states that <code>env</code> carries exactly the declared binding names, vars keys, and sensitive-variable slots, and nothing else portable (<a href="./../decisions/0019-the-module-worker-abi-is-an-exact-contract.html">decision 0019</a>).</li><li>Cross-space targets are unrepresentable, as in the retained lanes.</li></ul><h2 id="which-binding-a-list-carries" tabindex="-1">Which Binding a list carries <a class="header-anchor" href="#which-binding-a-list-carries" aria-label="Permalink to &quot;Which Binding a list carries&quot;">​</a></h2><p>A Form declares each binding list as a typed array property, and the property itself states its contract through the <code>x-takoform-binding</code> annotation in the Form&#39;s <code>desiredSchema</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kvBindings&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;array&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;items&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;one {name, resource} instance&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;x-takoform-binding&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;module-worker.edge-kv&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The annotation names the contract; the exact digest-bound BindingRef is the Form Definition&#39;s own <code>acceptedBindings</code> entry of that name, so the digest keeps one source of truth. The annotation is what lets a host derive the binding relations of a Form from the <code>desiredSchema</code> it already serves, instead of a separate declared list the published Form Definition schema does not admit (<a href="./../decisions/0014-published-schemas-are-structural-minima.html">decision 0014</a>).</p><p>The <code>resource</code> node INSIDE each instance carries a second annotation, stating what the target must satisfy (<a href="./../decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>). For a binding it is always <code>x-takoform-required-interface</code>, naming exactly the Binding Definition&#39;s own <code>targetInterface</code> at its exact digest:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;resource&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;the closed {apiVersion, kind, name} reference&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;x-takoform-required-interface&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;apiVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;interfaces.takoform.com/v1alpha1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;edge.kv&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;schemaDigest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sha256:...&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A binding IS the projection of an Interface into the consumer&#39;s runtime, so the exact contract the target provides is the whole requirement; pinning the target&#39;s Form as well would refuse a different store implementing the same contract for a reason the binding does not have. The Binding Definition remains the authority for that Interface and the annotation is its projection onto the reference, so the two cannot disagree — a Form whose binding list annotated any other Interface does not render.</p><h2 id="verification-a-host-performs" tabindex="-1">Verification a host performs <a class="header-anchor" href="#verification-a-host-performs" aria-label="Permalink to &quot;Verification a host performs&quot;">​</a></h2><p>A conforming host verifies a declared binding before it mutates anything. Schema validity is never sufficient: a schema states the shape of a reference, never whether the contract behind it can be honored. This order and its failure codes are decided by <a href="./../decisions/0015-cross-resource-references-are-uid-pinned-relations.html">decision 0015</a>. In order:</p><ol><li><strong>Accepted by the source Form.</strong> The source Form Definition&#39;s <code>acceptedBindings</code> must carry the annotated contract. Otherwise <code>invalid_argument</code> (400).</li><li><strong>Installed by the host at the exact digest.</strong> The host must have the Binding Definition whose <code>schemaDigest</code> the source accepts. Otherwise <code>unsupported_capability</code> (422): the host cannot perform the capability at all, so this is not a malformed request.</li><li><strong>Source role matches <code>sourceRole</code>.</strong> Otherwise <code>invalid_argument</code> (400). Outward capability belongs to revision resources; an identity or deployment resource never holds one.</li><li><strong>Target Form is listed in <code>allowedTargetForms</code>.</strong> The resolved target&#39;s exact group and kind must appear there. Otherwise <code>invalid_argument</code> (400).</li><li><strong>Target Form provides <code>targetInterface</code>.</strong> The target Form Definition&#39;s <code>providedInterfaces</code> must declare the exact Interface the Binding requires. Otherwise <code>invalid_argument</code> (400) — a binding projects an Interface, so a target that provides none cannot be bound.</li><li><strong>Same space.</strong> Guaranteed by the wire: a reference carries no space member, so a cross-space binding is unrepresentable rather than refused.</li><li><strong>The reference&#39;s own target contract.</strong> The annotated <code>x-takoform-required-interface</code> is verified last, against the target&#39;s recorded exact ref and its Form&#39;s <code>providedInterfaces</code> (<a href="./../decisions/0022-relations-pin-the-target-contract.html">decision 0022</a>). For a binding it restates rule 5, because the annotation must be exactly the Binding&#39;s <code>targetInterface</code>; the order above is the one clients were told, so the binding-specific refusal is the one they get. Every NON-binding relation in a Form is verified by this rule alone.</li></ol><p>Deleting a resource any live binding — or any other stored relation — references fails <code>dependency_in_use</code> (409). The required conformance checks <code>binding-contract-verified</code>, <code>relation-target-missing-rejected</code>, <code>relation-target-deletion-blocked</code>, <code>relation-incarnation-change-detected</code>, <code>relation-reapply-repins</code>, <code>relation-target-form-ref-verified</code>, <code>relation-target-interface-verified</code>, and <code>relation-pin-records-target-form-ref</code> prove a laxer host fails the lane.</p><h2 id="binding-distribution" tabindex="-1">Binding distribution <a class="header-anchor" href="#binding-distribution" aria-label="Permalink to &quot;Binding distribution&quot;">​</a></h2><p>Binding Definitions are distributed <strong>with this repository</strong>, as digest-bound documents under <code>bindings/candidates/v1alpha1</code>. They are NOT independently installable third-party artifacts, and there is no Binding Package envelope: no such identity is specified, published, or planned for this generation (<a href="./../decisions/0021-third-party-forms-and-contract-distribution.html">decision 0021</a>).</p><p>The rule is the Interface distribution rule word for word (<a href="./../interface-contract/"><code>../interface-contract/</code></a>). A <code>BindingRef</code>&#39;s <code>schemaDigest</code> binds the canonical Definition bytes and nothing else: no package digest, no publisher identity, no signature, no revocation feed, and no closed payload inventory, because there is no package. A host that advertises a Binding at an exact digest in its Host Support Profile is stating which document it implements, not where it obtained it.</p><h2 id="direction-rule" tabindex="-1">Direction rule <a class="header-anchor" href="#direction-rule" aria-label="Permalink to &quot;Direction rule&quot;">​</a></h2><p>Outward capability (this worker <em>uses</em> KV, a bucket, a database, a queue, a service) is a Binding held by the consumer&#39;s revision resource. Inward activation (an HTTP route, custom domain, cron trigger, or queue consumer <em>invokes</em> this worker) is an attachment resource pointing at the identity resource. The two directions never share a mechanism.</p><h2 id="provider-surface" tabindex="-1">Provider surface <a class="header-anchor" href="#provider-surface" aria-label="Permalink to &quot;Provider surface&quot;">​</a></h2><p>The official provider renders each binding type as its own typed list attribute — <code>kv_bindings</code>, <code>bucket_bindings</code>, <code>sqlite_bindings</code>, <code>queue_producer_bindings</code>, <code>service_bindings</code> — whose elements carry <code>name</code> and <code>target_name</code>. There is no generic connection block in the v1beta1 lane. (Decision 0010 names the block concept in the singular; the implemented attribute names are these plural lists.)</p>`,30)])])}const k=i(n,[["render",o]]);export{g as __pageData,k as default};
