import{_ as t,o as r,c as o,ag as a}from"./chunks/framework.DCKC-kU_.js";const m=JSON.parse('{"title":"takoform_edge_worker","description":"Portable request/event application executed from digest-bound artifact bytes near an ingress boundary.","frontmatter":{"page_title":"takoform_edge_worker Resource - takoform","subcategory":"Service Forms","description":"Portable request/event application executed from digest-bound artifact bytes near an ingress boundary."},"headers":[],"relativePath":"docs/resources/edge_worker.md","filePath":"docs/resources/edge_worker.md"}'),i={name:"docs/resources/edge_worker.md"};function s(n,e,d,c,l,u){return r(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="takoform-edge-worker" tabindex="-1">takoform_edge_worker <a class="header-anchor" href="#takoform-edge-worker" aria-label="Permalink to &quot;takoform_edge_worker&quot;">​</a></h1><p>Portable request/event application executed from digest-bound artifact bytes near an ingress boundary.</p><p>The configured host selects and operates the concrete backend. This resource carries desired state only: it never names a target, a credential, a price, or an implementation. See the <a href="../../examples/resources/takoform_edge_worker/resource.tf">complete example</a>.</p><h2 id="arguments" tabindex="-1">Arguments <a class="header-anchor" href="#arguments" aria-label="Permalink to &quot;Arguments&quot;">​</a></h2><ul><li><code>name</code> (String, required, forces replacement) — Resource name.</li><li><code>artifact_url</code> (String, required) — Absolute credential-free HTTPS location any conforming host can fetch; userinfo, query, and fragment are forbidden because this value persists in nonsensitive state.</li><li><code>artifact_sha256</code> (String, required) — Digest binding the URL to exact immutable bytes.</li><li><code>artifact_media_type</code> (String, required) — Lowercase type/subtype describing how the bytes are interpreted.</li><li><code>entrypoint</code> (String, required) — Relative path of the artifact module started by the selected runtime.</li><li><code>runtime</code> (String, required) — Open runtime capability token required by the artifact; each host declares support independently.</li><li><code>runtime_version</code> (String, optional) — Optional runtime compatibility version required by the artifact.</li><li><code>configuration</code> (Map of String, optional) — Non-secret application configuration. Credentials remain host-owned.</li><li><code>connections</code> (List of Object, optional) — Declared references to other Resources, each with <code>name</code>, <code>resource</code>, <code>permissions</code>, and <code>projection</code>. A connection is a request the host validates; it grants nothing by itself.</li><li><code>space</code> (String, optional, forces replacement) — Overrides the provider default.</li></ul><h2 id="read-only-attributes" tabindex="-1">Read-only attributes <a class="header-anchor" href="#read-only-attributes" aria-label="Permalink to &quot;Read-only attributes&quot;">​</a></h2><p><code>form_api_version</code>, <code>form_kind</code>, <code>form_definition_version</code>, <code>form_schema_digest</code>, and <code>form_package_digest</code> bind state to the exact immutable Form identity. <code>id</code> is the provider-synthesized <code>Kind/name</code> identity and <code>resource_version</code> is the host generation fence. <code>drift_status</code>, <code>portability</code>, and <code>outputs</code> are written only after the host&#39;s observed and output documents satisfy this exact Form&#39;s closed schemas, identities, and generation. Undeclared host keys are rejected; backend placement is never provider state.</p><h2 id="declared-runtime-interfaces" tabindex="-1">Declared runtime interfaces <a class="header-anchor" href="#declared-runtime-interfaces" aria-label="Permalink to &quot;Declared runtime interfaces&quot;">​</a></h2><ul><li><code>http.request@1</code> — Portable HTTP request surface exposed by the application. Operations: <code>request</code>.</li></ul><p>A declaration says what exists. It carries no credential and grants no consumer access; the host creates the record and authorizes its use.</p><h2 id="import" tabindex="-1">Import <a class="header-anchor" href="#import" aria-label="Permalink to &quot;Import&quot;">​</a></h2><div class="language-console vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">console</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_edge_worker.example NAME</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terraform import takoform_edge_worker.example SPACE/NAME</span></span></code></pre></div>`,12)])])}const h=t(i,[["render",s]]);export{m as __pageData,h as default};
