import{_ as t,o as d,c as o,a2 as a}from"./chunks/framework.BTLSZHNB.js";const u=JSON.parse('{"title":"Current v1alpha2 Form candidates","description":"","frontmatter":{},"headers":[],"relativePath":"forms/index.md","filePath":"forms/index.md"}'),r={name:"forms/index.md"};function c(n,e,i,s,l,h){return d(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="current-v1alpha2-form-candidates" tabindex="-1">Current v1alpha2 Form candidates <a class="header-anchor" href="#current-v1alpha2-form-candidates" aria-label="Permalink to &quot;Current v1alpha2 Form candidates&quot;">​</a></h1><p>This is the provider-v2 source candidate inventory for the nine Form-backed Resources currently operated by Takosumi Cloud. Every entry is a local Proposal-derived publication candidate under <code>forms.takoform.com/v1alpha2</code>, awaiting an explicit lifecycle transition before Experimental; none is published, Experimental, Stable, centrally approved, or guaranteed commercially available. Each contract describes what a caller wants without naming a target, credential, placement, price, or implementation. A host may publish support and activate an exact FormRef under its own policy.</p><p>The frozen v1alpha1 inventory remains verifiable through <a href="standard-package-set.json"><code>standard-package-set.json</code></a> and immutable release sources, but it is not rendered as the current provider catalog.</p><h2 id="compute-and-application" tabindex="-1">Compute and application <a class="header-anchor" href="#compute-and-application" aria-label="Permalink to &quot;Compute and application&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Kind</th><th>Resource</th><th>Version</th><th>Portable intent</th></tr></thead><tbody><tr><td><code>EdgeWorker</code></td><td><code>takoform_edge_worker</code></td><td><code>0.1.0</code></td><td>Portable request/event application executed from digest-bound artifact bytes near an ingress boundary.</td></tr><tr><td><code>Schedule</code></td><td><code>takoform_schedule</code></td><td><code>0.1.0</code></td><td>Portable cron lifecycle that invokes exactly one connected Resource.</td></tr><tr><td><code>ContainerService</code></td><td><code>takoform_container_service</code></td><td><code>0.1.0</code></td><td>Portable service executed from an immutable OCI image digest.</td></tr><tr><td><code>StatefulEntity</code></td><td><code>takoform_stateful_entity</code></td><td><code>0.1.0</code></td><td>Portable namespace of addressable persistent entities implemented by digest-bound application bytes.</td></tr></tbody></table><h2 id="data-and-storage" tabindex="-1">Data and storage <a class="header-anchor" href="#data-and-storage" aria-label="Permalink to &quot;Data and storage&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Kind</th><th>Resource</th><th>Version</th><th>Portable intent</th></tr></thead><tbody><tr><td><code>RelationalDatabase</code></td><td><code>takoform_relational_database</code></td><td><code>0.1.0</code></td><td>Portable relational database identified by an open engine capability token.</td></tr><tr><td><code>ObjectBucket</code></td><td><code>takoform_object_bucket</code></td><td><code>0.1.0</code></td><td>Portable object storage namespace.</td></tr><tr><td><code>KeyValueStore</code></td><td><code>takoform_key_value_store</code></td><td><code>0.1.0</code></td><td>Portable key/value state with declared consistency and expiry semantics.</td></tr><tr><td><code>Queue</code></td><td><code>takoform_queue</code></td><td><code>0.1.0</code></td><td>Portable asynchronous at-least-once message delivery.</td></tr><tr><td><code>VectorIndex</code></td><td><code>takoform_vector_index</code></td><td><code>0.1.0</code></td><td>Portable vector index with dimensions fixed for the index lifecycle.</td></tr></tbody></table><h2 id="declared-runtime-interfaces" tabindex="-1">Declared runtime interfaces <a class="header-anchor" href="#declared-runtime-interfaces" aria-label="Permalink to &quot;Declared runtime interfaces&quot;">​</a></h2><p>A Form may declare the runtime interfaces its service exposes. The names are author-defined and open: there is no registry, no allowlist, and no central approval. A declaration states what exists and how its non-secret values are filled; the host creates the record, authorizes consumers, and owns its lifecycle.</p><table tabindex="0"><thead><tr><th>Kind</th><th>Interface</th></tr></thead><tbody><tr><td><code>EdgeWorker</code></td><td><code>http.request@1</code> (request)</td></tr><tr><td><code>RelationalDatabase</code></td><td><code>sql.query@1</code> (execute, query, transaction)</td></tr><tr><td><code>ObjectBucket</code></td><td><code>object.storage@1</code> (delete, get, list, put)</td></tr><tr><td><code>KeyValueStore</code></td><td><code>keyvalue.store@1</code> (delete, get, list, put)</td></tr><tr><td><code>Queue</code></td><td><code>queue.messages@1</code> (acknowledge, receive, send)</td></tr><tr><td><code>ContainerService</code></td><td><code>http.request@1</code> (request)</td></tr><tr><td><code>StatefulEntity</code></td><td><code>entity.invoke@1</code> (invoke)</td></tr><tr><td><code>VectorIndex</code></td><td><code>vector.query@1</code> (delete, query, upsert)</td></tr></tbody></table><h2 id="immutable-fields" tabindex="-1">Immutable fields <a class="header-anchor" href="#immutable-fields" aria-label="Permalink to &quot;Immutable fields&quot;">​</a></h2><p>Every Form fixes its <code>/name</code>. A Form that additionally fixes a field states so in its definition, and the provider enforces replacement for exactly those fields; the protocol lifecycle proves both.</p><table tabindex="0"><thead><tr><th>Kind</th><th>Immutable</th></tr></thead><tbody><tr><td><code>EdgeWorker</code></td><td><code>/name</code></td></tr><tr><td><code>RelationalDatabase</code></td><td><code>/databaseName</code>, <code>/engine</code>, <code>/name</code></td></tr><tr><td><code>ObjectBucket</code></td><td><code>/name</code></td></tr><tr><td><code>KeyValueStore</code></td><td><code>/name</code></td></tr><tr><td><code>Queue</code></td><td><code>/name</code></td></tr><tr><td><code>Schedule</code></td><td><code>/name</code></td></tr><tr><td><code>ContainerService</code></td><td><code>/name</code></td></tr><tr><td><code>StatefulEntity</code></td><td><code>/name</code></td></tr><tr><td><code>VectorIndex</code></td><td><code>/dimensions</code>, <code>/name</code></td></tr></tbody></table><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Every entry in this inventory is an unpublished <code>0.1.0</code> candidate. Takosumi Cloud implementation is workload and first-host evidence only; it does not turn a Proposal into a portable standard or authorize publication.</p><p>The earlier ten-package generation is also retired, not erased. Its immutable bytes and admission evidence stay verifiable through <a href="retired-package-set.json"><code>retired-package-set.json</code></a>. Neither retained set may be rewritten, re-signed, promoted, or used to derive a current approved subset. Current lifecycle truth comes only from <a href="lifecycle.json"><code>lifecycle.json</code></a>; Host Support and activation remain separate host-owned facts.</p><h2 id="edge-platform-family-edge-forms-takoform-com-v1alpha1" tabindex="-1">Edge Platform Family (edge.forms.takoform.com/v1alpha1) <a class="header-anchor" href="#edge-platform-family-edge-forms-takoform-com-v1alpha1" aria-label="Permalink to &quot;Edge Platform Family (edge.forms.takoform.com/v1alpha1)&quot;">​</a></h2><p>The first official Form Family fixes the shape of a proven edge developer platform without naming its vendor (spec/form-families.md). Its members are source candidates for the Host API v1alpha3 resource lane; the typed resources require provider v2.1.0 or later (source candidate; not yet published). Roles come from the closed v1alpha3 role enum and decide lifecycle mechanics: revisions are immutable, deployments move traffic, attachments activate inward events.</p><table tabindex="0"><thead><tr><th>Kind</th><th>Resource</th><th>Role</th><th>Version</th><th>Portable intent</th></tr></thead><tbody><tr><td><code>ModuleWorker</code></td><td><code>takoform_module_worker</code></td><td><code>identity</code></td><td><code>0.1.0</code></td><td>Long-lived logical identity of one ES Module Worker application. The Form fixes the ES module worker ABI by identity: handlers are exported module functions receiving typed events and a binding environment. Code, configuration, and bindings live on Worker Version revisions; traffic selection lives on Worker Deployments.</td></tr><tr><td><code>WorkerBundle</code></td><td><code>takoform_worker_bundle</code></td><td><code>revision</code></td><td><code>0.1.0</code></td><td>Immutable content-addressed module bundle of one worker build: a main module plus additional modules, each pinned by size and digest and resolved through the content-addressed artifact upload API (decision 0012). Different bytes are a different bundle.</td></tr><tr><td><code>WorkerVersion</code></td><td><code>takoform_worker_version</code></td><td><code>revision</code></td><td><code>0.1.0</code></td><td>Immutable executable snapshot of one Module Worker: a bundle, a runtime compatibility date, declared handlers, non-secret vars, and the typed capability bindings the code may use. A change is a new Worker Version; traffic moves only through Worker Deployments.</td></tr><tr><td><code>WorkerDeployment</code></td><td><code>takoform_worker_deployment</code></td><td><code>deployment</code></td><td><code>0.1.0</code></td><td>Selects which Worker Versions of one Module Worker serve traffic and in what proportion. Weights are basis points and must sum to exactly 10000 across entries; the sum is host-validated semantics because a schema cannot add weights. Rollback is re-weighting, never mutating a revision.</td></tr><tr><td><code>WorkerCustomDomain</code></td><td><code>takoform_worker_custom_domain</code></td><td><code>attachment</code></td><td><code>0.1.0</code></td><td>Attaches one DNS hostname to a Module Worker so its active deployment serves that hostname over HTTPS. Inward activation is an attachment, never a binding; deleting the attachment detaches the hostname and never deletes the worker.</td></tr><tr><td><code>WorkerCronTrigger</code></td><td><code>takoform_worker_cron_trigger</code></td><td><code>attachment</code></td><td><code>0.1.0</code></td><td>Attaches one cron schedule to a Module Worker, invoking its scheduled handler at each match. Schedules are interpreted in UTC only; there is no timezone field, so two hosts can never fire the same trigger at different instants. The accepted grammar is exactly five single-value fields separated by single spaces: minute is a literal 0-59 and hour a literal 0-23, day-of-month is <code>*</code> or 1-31, month is <code>*</code> or 1-12, and day-of-week is <code>*</code> or 0-6. Ranges, lists, steps such as <code>*/5</code>, names, and <code>*</code> in the minute or hour field are all rejected, so the most frequent representable schedule is once per day at one fixed UTC time. Hourly and sub-hourly schedules are not expressible and need a future grammar revision, which is a new definition version of this Form.</td></tr><tr><td><code>EdgeKVNamespace</code></td><td><code>takoform_edge_kv_namespace</code></td><td><code>identity</code></td><td><code>0.1.0</code></td><td>Globally replicated key/value namespace with eventual consistency, exactly as fixed by the edge.kv Interface. Eventual consistency is the Form&#39;s semantics, not an option: a store with different convergence behavior is a different Form.</td></tr><tr><td><code>ObjectBucket</code></td><td><code>takoform_edge_object_bucket</code></td><td><code>identity</code></td><td><code>0.1.0</code></td><td>Flat-namespace object store with read-after-write consistency, exactly as fixed by the edge.objects Interface. Operating rules such as CORS, lifecycle, and lock are separate policy resources, never desired fields of the bucket identity.</td></tr><tr><td><code>SQLiteDatabase</code></td><td><code>takoform_sqlite_database</code></td><td><code>identity</code></td><td><code>0.1.0</code></td><td>Embedded SQLite database with serializable transactions, exactly as fixed by the edge.sql Interface. SQLite semantics are the identity: a database with different SQL, typing, or isolation behavior is a different Form, never an engine token.</td></tr><tr><td><code>AtLeastOnceQueue</code></td><td><code>takoform_at_least_once_queue</code></td><td><code>identity</code></td><td><code>0.1.0</code></td><td>Message queue with at-least-once delivery and no ordering guarantee, exactly as fixed by the edge.queue Interface. There is no ordering field: a FIFO queue is a different Form.</td></tr><tr><td><code>QueueConsumer</code></td><td><code>takoform_queue_consumer</code></td><td><code>attachment</code></td><td><code>0.1.0</code></td><td>Attaches one Module Worker as the batch consumer of one At-Least-Once Queue, invoking its queue handler with message batches and redelivering failed batches. Consumption is inward activation and therefore an attachment, never a binding.</td></tr></tbody></table><p>A generic <code>takoform_resource</code> carries any third-party v1alpha3 Form by exact FormRef. Family membership grants no maturity: these members are tracked in the family candidate set, a lifecycle record begins only at an Experimental transition, and hosts state their supported subset in their Host Support Profiles.</p>',20)])])}const f=t(r,[["render",c]]);export{u as __pageData,f as default};
