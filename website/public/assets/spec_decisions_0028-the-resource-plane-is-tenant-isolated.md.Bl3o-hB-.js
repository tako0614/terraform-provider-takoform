import{_ as t,o as a,c as n,ag as o}from"./chunks/framework.cyF4XceO.js";const p=JSON.parse('{"title":"0028 — The resource plane is tenant-isolated, by address","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0028-the-resource-plane-is-tenant-isolated.md","filePath":"spec/decisions/0028-the-resource-plane-is-tenant-isolated.md"}'),s={name:"spec/decisions/0028-the-resource-plane-is-tenant-isolated.md"};function r(i,e,d,c,h,l){return a(),n("div",null,[...e[0]||(e[0]=[o('<h1 id="_0028-—-the-resource-plane-is-tenant-isolated-by-address" tabindex="-1">0028 — The resource plane is tenant-isolated, by address <a class="header-anchor" href="#_0028-—-the-resource-plane-is-tenant-isolated-by-address" aria-label="Permalink to &quot;0028 — The resource plane is tenant-isolated, by address&quot;">​</a></h1><ul><li>Status: accepted; amended 2026-08-09 — the matrix is measured in the PERMISSIVE direction as well (see &quot;A boundary has two sides&quot; below)</li><li>Date: 2026-08-09</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Every other boundary in the v1alpha3 lane is stated and measured. An Operation id and an upload session are handles bound to the tenant and principal that created them, and a caller who is not the owner is answered as if they did not exist. A content address is a name and not an entitlement: a manifest or a blob is readable only by a tenant that holds it, and a referenced manifest is resolved against the mutating caller&#39;s tenant on apply, on import, and again when an accepted <code>202</code> commits (<a href="./0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a>). A hostname claim is unique per tenant and deliberately stops there (<a href="./0026-attachment-claims-are-canonical-and-acyclic.html">decision 0026</a>).</p><p>The resource plane itself was not. The reference host&#39;s internal key was <code>space + group + kind + name</code>, with no tenant in it. Each record remembered the tenant that created it, but as a field the hostname scan read — not as part of the address. The host said so in its own comment: a real host partitions by tenant, and this corpus does not measure it.</p><p>So two tenants naming one resource in one space addressed one record. The second tenant&#39;s <code>If-None-Match: *</code> create collided with a resource it could not read; a <code>read</code>, a fenced update, and a fenced delete all reached it; a reference from one tenant resolved to another tenant&#39;s resource whenever the names agreed, and pinned its uid into stored state; a <code>prepareDigest</code> minted by one tenant was byte-identical to the other&#39;s and spendable by it; and the derived-rendering pass that advances revisions after a mutation ran across every tenant in the space.</p><p>Nothing in the lane forbade a host from shipping exactly that. An independent host is about to be written against this contract, and a boundary that is documented but unmeasured is not a boundary — it is a thing the first implementer discovers, or does not.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A host&#39;s internal resource address includes the tenant, the space, the <code>apiVersion</code>, the <code>kind</code>, and the <code>name</code>.</strong> The tenant is the authenticated tenant of the request, recorded at create and carried forward by every update and import, exactly as the recorded exact FormRef is (<a href="./0011-resource-identity-generation-and-revision.html">decision 0011</a>, <a href="./0022-relations-pin-the-target-contract.html">decision 0022</a>).</p><ol><li>Two tenants may create one <code>{space, apiVersion, kind, name}</code> and get two resources with two host-issued uids. A name is taken within one tenant.</li><li>Every resource surface addresses the caller&#39;s own tenant — <code>read</code>, <code>observe</code>, <code>prepare</code>, <code>apply</code>, <code>import</code>, and <code>delete</code>. A request naming another tenant&#39;s resource is answered exactly as one naming a resource that never existed. Where that answer is a refusal it is <code>resource_not_found</code> (404), and it is indistinguishable from the refusal for a name nobody created. <code>validate</code> carries a name and resolves none, so it has no tenant-dependent answer to give.</li><li><code>import</code> is the one surface whose absent answer is not a refusal, so it is stated exactly. An adoption under <code>If-None-Match: *</code> of a name the caller does not hold mints a resource at generation 1, and a name held only by another tenant is not held by the caller: that adoption SUCCEEDS, and answers what the adoption of a name nobody holds anywhere answers — the same status, the same ETag, and a document differing in nothing but the host-issued uid and the name. An adoption carrying an update generation fence names an existing resource, so against such a name it fails <code>resource_not_found</code> (404). A host refusing the create-intent adoption with <code>generation_conflict</code> answers &quot;that name is taken&quot; about a tenant the caller cannot see; one taking the update path writes the caller&#39;s desired state over a stranger&#39;s live resource.</li><li>Relations resolve only within the same tenant. A name only another tenant holds is an absent target.</li><li>A <code>prepareDigest</code> binds the minting tenant and is not spendable outside it. An apply presenting a foreign review fails <code>invalid_argument</code> (400).</li><li>An <code>Idempotency-Key</code> names one operation of one tenant. The same key from two tenants is two operations.</li><li>A pass that renders one resource from others, and advances revisions, is scoped to the mutating tenant.</li><li><strong>One exception, unchanged:</strong> a <code>WorkerCustomDomain</code>&#39;s canonical hostname is unique across every space of one tenant, because DNS does not partition with spaces. That rule drops the space and keeps the tenant. Nothing reaches past the tenant.</li></ol><p>The error code is the closed taxonomy&#39;s <code>resource_not_found</code> for every addressing refusal, and <code>permission_denied</code> is forbidden for them. A foreign tenant&#39;s resource must be indistinguishable from one that does not exist, and a 403 is a disclosure: it says &quot;a resource of that name exists on this host and is not yours&quot; to anyone who can guess a name, which is precisely the membership fact the boundary withholds. This is the same reasoning decision 0018 already applied to Operation ids, upload sessions, and content addresses, applied to the plane those three surround. The taxonomy is closed and no code is added (<a href="./0014-published-schemas-are-structural-minima.html">decision 0014</a>, rung 3: the host enforces it and required conformance checks prove a laxer host fails).</p><p>Ten required conformance checks measure it, all black box: <code>resource-address-is-tenant-scoped</code>, <code>resource-read-is-tenant-isolated</code>, <code>resource-observe-is-tenant-isolated</code>, <code>resource-update-is-tenant-isolated</code>, <code>resource-import-is-tenant-isolated</code>, <code>resource-delete-is-tenant-isolated</code>, <code>relation-resolution-is-tenant-scoped</code>, <code>prepare-is-tenant-scoped</code>, <code>idempotency-is-tenant-scoped</code>, and <code>each-tenant-mutates-its-own-plane</code>. The lane&#39;s runner therefore REQUIRES an alternate-tenant credential: a runner that can authenticate as one tenant can measure none of this.</p><h3 id="a-boundary-has-two-sides" tabindex="-1">A boundary has two sides <a class="header-anchor" href="#a-boundary-has-two-sides" aria-label="Permalink to &quot;A boundary has two sides&quot;">​</a></h3><p><em>Amended 2026-08-09.</em> Rule 2 says every surface addresses the caller&#39;s <strong>own</strong> tenant. The first version of this record measured only the half where the answer is a refusal, and a rule proven only in the direction where a host refuses is half a rule: a host that refuses everything passes it. Four concrete hosts got through, and none of them is exotic.</p><ol><li><strong>The second tenant never wrote anything of its own.</strong> Across the whole corpus it created and read; a host that is create-and-read-only for every tenant but the first — <code>resource_not_found</code> to each update and each delete of the second tenant&#39;s OWN resources — satisfied all nine while breaking rule 2 outright. That is the ordinary shape of a tenant column bolted onto a single-tenant store whose write paths still resolve through the original owner, and the failure is silent, because a 404 for your own resource reads like a resource you never made. <code>each-tenant-mutates-its-own-plane</code> drives an update and a delete inside the second tenant&#39;s plane, against a name the first tenant also holds, and holds the first tenant&#39;s uid, generation, and revision still across both.</li><li><strong>The update and delete boundaries proved non-destruction and not availability.</strong> &quot;The resource survived a foreign attempt&quot; is also true of a host that answers a stranger&#39;s request by locking the record, and an operator who cannot change or destroy what they created has lost it just as completely, only slower. Both checks now end with the HOLDER performing the same operation, under the same fences, successfully. <code>observe</code> and <code>import</code> were already two-sided; these two now match them.</li><li><strong>Relation resolution never reached a successful apply</strong>, so the stored pin was never inspected and a host that gated correctly and then pinned the OTHER tenant&#39;s uid passed. The pin is what deletion protection, drift, and every aggregate rule read afterwards, so a foreign pin means one tenant&#39;s resource is protected from its own owner by a stranger&#39;s reference. The check now lands the apply and reads the pin the only way a black box can — by what it protects: the first tenant&#39;s identically-named resource must delete freely, the second tenant&#39;s source must stay Ready when it does, and the second tenant&#39;s own target must be refused <code>dependency_in_use</code>.</li><li><strong>Idempotency never read the second tenant&#39;s resource back</strong>, so a host that answered a key it had already seen from another tenant by synthesizing a fresh-uid 201 and storing nothing passed — including the byte-identical &quot;replay&quot;, which is the same synthesis twice. The check now reads it back.</li></ol><p>A fifth follows from the same reading and is not about tenants at all: a review is bound to the minting TENANT, and only the foreign spend was driven, so a host binding it to the minting PRINCIPAL was indistinguishable — while refusing the ordinary plan-in-one-identity, apply-in-another split <a href="./0018-the-host-api-is-deployable-behind-ordinary-infrastructure.html">decision 0018</a> assumes. <code>prepare-is-tenant-scoped</code> now spends one review from a second principal of the tenant that minted it, and requires that to succeed.</p><h3 id="the-credential-the-tenant-comes-from" tabindex="-1">The credential the tenant comes from <a class="header-anchor" href="#the-credential-the-tenant-comes-from" aria-label="Permalink to &quot;The credential the tenant comes from&quot;">​</a></h3><p><em>Amended 2026-08-09.</em> Everything above is downstream of one fact this record asserted and the corpus never drove: the tenant is the <strong>authenticated</strong> tenant, and nothing on the wire names one. Every request in the matrix carried one of three valid credentials, and the only <code>unauthenticated</code> (401) the lane ever produced came from the runner-only error probe — so a host whose credential lookup failed OPEN, answering an unauthenticated request as some default tenant, passed the entire matrix while exposing every tenant&#39;s plane to anyone who can reach the port. That is not an exotic bug; it is what &quot;auth is wired in later&quot; looks like. <code>unauthenticated-request-refused</code> drives two shapes, because they fail differently — an ABSENT <code>Authorization</code> header, which a framework may route past authentication entirely, and a well-formed bearer credential naming nobody, which a lookup returning a zero value turns into a valid caller — on a read surface and on a mutating one, and requires <code>unauthenticated</code> for both and never <code>permission_denied</code>: the caller has not been identified, so there is nothing yet to deny. The identical requests under a real credential must succeed, so a host cannot pass by refusing the route.</p><p><strong>The nine are enumerated by surface.</strong> The first version of this record listed seven, and it listed them by intent — read it, change it, delete it, point at it, review it, replay it. An enumeration by intent is complete until the next intent, and two surfaces that take a resource name were already missing from it: <code>observe</code>, which hands back a whole representation under a fence, and <code>import</code>, which mutates. A host scoping <code>GET</code>, <code>PUT</code> and <code>DELETE</code> correctly while resolving either host-wide passed all seven — and leaked the foreign representation, or conflicted with and overwrote foreign state. So the enumeration axis is the surface: every route of the lane that takes a resource name is listed against the check measuring its boundary, and that list is bound by tests to the published route block of <code>../host-api/v1alpha3.md</code>, to the reference host&#39;s router, and to the required-check list. A name-addressed endpoint cannot be added to this lane without a tenant check, which is a stronger guarantee than remembering to think of one.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The reference host keys <code>resources</code> by tenant first, and every store lookup, every store-wide scan, and the derived-rendering pass take a <code>resourceScope</code> value rather than a bare space. A key cannot be built without a tenant, so a host-wide question is not one this code can ask by accident — which is exactly how the previous boundary became unmeasured.</li><li><code>cross-principal-idempotency-isolation</code> changes what it expects from the alternate tenant. It used to expect a <code>generation_conflict</code>, which was the second tenant colliding with the first tenant&#39;s record — the defect, recorded as evidence of isolation. It now expects the two legs to differ: the same-tenant principal still collides, and the second tenant is stopped by the review it carries rather than by a resource it does not have.</li><li>The required check list grows from 93 to 102, and to 103 with <code>each-tenant-mutates-its-own-plane</code> <em>(2026-08-09)</em>.</li><li>The reference host&#39;s resource-plane routes are a table rather than a <code>switch</code>, so the set of name-addressed endpoints is data. Three tests read it: one binds it to the published route block, one to the enumeration, and one binds the enumeration to the required-check list.</li><li>Two obligations stay unproven by the lane and are stated as such in <code>../host-api/v1alpha3.md</code>: that the tenant is in the address rather than in a late filter, and that an unscoped derived-rendering pass would cross the tenant. Neither is reachable black box — the second because relation resolution refuses to build the case. Both are held by host-side tests against the reference host.</li><li>Publication blocker <strong>V3-012</strong> records the item as P0 for every Form.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Answer <code>permission_denied</code> (403) for another tenant&#39;s resource.</strong> Rejected because it is a membership oracle over every name on the host, and because it contradicts the answer decision 0018 already fixed for the three handle-shaped surfaces. Two codes for one class of fact would also let two hosts differ on which they return.</li><li><strong>Measure <code>import</code> by accepting any refusal for another tenant&#39;s name.</strong> Rejected because it would accept the defect. Import&#39;s answer for a name the caller does not hold is an adoption, not a refusal, so &quot;the second tenant was refused&quot; is satisfied by exactly the <code>generation_conflict</code> a host-wide resolver returns — the &quot;that name is taken&quot; oracle. The check therefore requires the adoption to SUCCEED and to be byte-comparable to the adoption of a free name, and holds the fenced form of import to the 404 instead.</li><li><strong>Measure the permissive half by adding one &quot;the second tenant can write&quot; check and leaving the nine as they were.</strong> Rejected as half the correction. The new check answers item 1 above and nothing else: it says the second tenant&#39;s plane is writable, not that a resource stays writable by its owner after a stranger reaches for it, not what a stored relation pinned, and not that an answered create was executed. Each of those is a property of the surface its own check names, so each is stated there.</li><li><strong>Keep the tenant as a field and filter at each call site.</strong> Rejected because that is the state this decision corrects. The tenant WAS a field, and exactly one scan read it; every other lookup was correct only by the accident of a uid being unguessable. A filter is something a future edit forgets, and the failure is silent.</li><li><strong>Scope only the resource key, and leave the prepare binding global.</strong> Rejected because a create binding pins no uid and no generation: two tenants preparing one name in one space would hold one digest, so the review would be a bearer token over another tenant&#39;s plane. It is also the cheapest thing to get wrong, because everything else keeps working.</li><li><strong>Put the tenant on the wire, in <code>metadata</code> or as a query key.</strong> Rejected because a client that can name a tenant can name someone else&#39;s, which turns authentication into a suggestion; and because it would make the tenant part of the portable resource identity, which it is not — the same desired state must apply unchanged in any tenant of any host (<a href="./0003-space-id-is-an-opaque-portable-scope-identity.html">decision 0003</a>).</li><li><strong>Scope the hostname claim to the tenant AND the space, for uniformity.</strong> Rejected because it would make the rule wrong. Two spaces of one tenant claiming one DNS name is one collision with two answers, and decision 0026 decided it; uniformity is not a reason to give a host two answers for one hostname.</li></ul>',23)])])}const m=t(s,[["render",r]]);export{p as __pageData,m as default};
