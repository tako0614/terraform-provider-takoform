import{_ as t,o as a,c as o,a2 as r}from"./chunks/framework.LJP3gfxI.js";const u=JSON.parse('{"title":"0032 — A provider diagnostic states identity, fence, code, and repair","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0032-a-diagnostic-states-identity-fence-code-and-repair.md","filePath":"spec/decisions/0032-a-diagnostic-states-identity-fence-code-and-repair.md"}'),n={name:"spec/decisions/0032-a-diagnostic-states-identity-fence-code-and-repair.md"};function i(s,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="_0032-—-a-provider-diagnostic-states-identity-fence-code-and-repair" tabindex="-1">0032 — A provider diagnostic states identity, fence, code, and repair <a class="header-anchor" href="#_0032-—-a-provider-diagnostic-states-identity-fence-code-and-repair" aria-label="Permalink to &quot;0032 — A provider diagnostic states identity, fence, code, and repair&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-09</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A provider diagnostic is the only thing a practitioner sees when an apply stops, and this lane&#39;s whole vocabulary is what makes one actionable. Decision <a href="./0011-resource-identity-generation-and-revision.html">0011</a> gave every resource a UID, a generation, and a revision, and made desired mutations fence on the generation and deletes on the revision — the delete half of which that record has since amended to the generation, for reasons of its own. Decision <a href="./0017-provider-state-survives-form-evolution-and-interruption.html">0017</a> made the exact FormRef the identity every read, update, and delete addresses, and made <code>pending_operation_id</code> load-bearing. The wire error envelope carries a code from a closed taxonomy, a request id, and a retryable flag.</p><p>An audit of all 135 diagnostic call sites in <code>internal/provider</code> found that almost none of that reached the reader. Twenty-one sites rendered <code>err.Error()</code> and nothing else. <code>Failed to update &lt;Kind&gt;</code> was raised on the one path where a <code>uid_mismatch</code> or a <code>generation_conflict</code> arrives, and neither the expected uid nor the expected generation — both in scope, both just sent as the fence — was in the message. <code>Failed to delete &lt;Kind&gt;</code> fenced on the revision and never named it. No diagnostic anywhere carried a JSON pointer. The typed <code>clientv3.APIError</code>, which already holds the code, the request id, the retryable flag, and the HTTP status, was never destructured by the provider at all: the only <code>errors.As</code> in the package was for <code>AcceptedError</code>.</p><p>One instance had a further defect of its own shape. <code>interface_data_source.go</code> answered <code>&quot;Provider not configured&quot;</code> against a v3-only host, fusing two structurally different states — a provider bug and an endpoint fact — under one title, and discarding the recorded <code>v2Err</code> that says which. The v2 form resources and the v3 resources both already reported the lane and the underlying error; the data source was the one lane-asserting site that did not.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Every error the v1alpha3 lane raises goes through one renderer and carries, when it has them: the host address of the resource (type, space, name), the exact FormRef, a JSON pointer into the portable document, the expected and current UID, the expected and current generation and revision, the operation id, the host request id, a stable error code, whether it is retryable, and one concrete repair. A fact the diagnostic does not have is omitted, never rendered empty.</p><p>Two code vocabularies meet and are kept apart. A HOST error renders its code from the closed v1alpha3 taxonomy verbatim, labelled as the host&#39;s and carrying its HTTP status; that enum is published and this provider does not extend it. A PROVIDER refusal — one made before any request — renders a code from a closed set in its own <code>takoform.provider/</code> namespace, because a client-side refusal is not a host outcome and must not borrow a host code. A response that did not match the closed taxonomy is labelled protocol-invalid and offers no guessed remedy.</p><p>The repair is per stable code and is complete: a test reads <a href="../host-api/operations-v1alpha3.json"><code>../host-api/operations-v1alpha3.json</code></a> and fails if any published code has no repair, or if the provider states a repair for a code that is not published. A diagnostic that names a fault and no next action is the shape this decision exists to remove.</p><p>The Terraform resource ADDRESS stays Terraform&#39;s to print. A provider cannot know it; core prefixes every diagnostic raised during a resource operation with <code>with &lt;type&gt;.&lt;name&gt;, on &lt;file&gt; line &lt;n&gt;</code>. What the provider owns and now always states is the HOST address.</p><p>The lane-unavailable diagnostic is one shape shared by every lane-asserting site, resource and data source alike: it names the lane, the resource type, the recorded per-lane negotiation error, the fact that the provider itself configured normally, and what to change. <code>&quot;Provider not configured&quot;</code> is reserved for a genuinely nil provider data — a provider bug, and labelled as one.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The audited sites now render the fences they send. A <code>generation_conflict</code> on update shows the generation that was sent, and so does one on delete, since the delete fence became the generation (<a href="./0011-resource-identity-generation-and-revision.html">0011</a>, amended 2026-08-09). <code>revision_conflict</code> keeps its repair: the code stays in the published taxonomy and stays reachable for a caller that supplies the optional representation fence, and the repair test is over the published codes rather than over the ones this client happens to provoke.</li><li>The interface data source reports <code>Takoform v1alpha2 lane unavailable</code> with the endpoint&#39;s own reason, symmetric with the v2 resources and the v3 resources.</li><li>The host&#39;s relation-drift <code>hostReason</code> is parsed tolerantly for the pointer and the two uids, which are promoted into the identity block; the raw <code>hostReason</code> is rendered either way, so nothing the host said is lost when a host words it differently.</li><li>The retained v1alpha2 lane (<code>internal/provider/form_resource.go</code>) is out of scope by repository rule and keeps its existing diagnostics. Six thin sites remain there and are recorded rather than fixed.</li><li>Adding a code to the published taxonomy now fails a provider test until a repair is written for it, which is the intended coupling.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Extend the closed host error enum with provider-side codes.</strong> Rejected because that enum is published and closed, and because a reader who greps a code needs to know whether the host said it or the client did.</li><li><strong>Attach every diagnostic to an attribute path instead of naming a pointer.</strong> An attribute path is the right home for a configuration fault and is used for one. It is the wrong home for a fault about the host document — a relation whose target moved is at <code>/kvBindings/0/resource</code> in the portable spec, and the attribute that produced it may not exist in the configuration at all. Both are carried.</li><li><strong>Render <code>err.Error()</code> and let the host&#39;s message carry everything.</strong> This is the status quo. The host&#39;s message is one sentence about one request; it does not know which resource address Terraform is applying, which FormRef state records, or which fence the client chose to send.</li></ul>',16)])])}const f=t(n,[["render",i]]);export{u as __pageData,f as default};
