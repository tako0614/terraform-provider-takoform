import{_ as a,o,c as t,ag as r}from"./chunks/framework.cyF4XceO.js";const p=JSON.parse('{"title":"Schedule — takoform_schedule","description":"","frontmatter":{},"headers":[],"relativePath":"proposals/schedule/schedule.md","filePath":"proposals/schedule/schedule.md"}'),i={name:"proposals/schedule/schedule.md"};function d(n,e,s,c,h,l){return o(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="schedule-—-takoform-schedule" tabindex="-1">Schedule — <code>takoform_schedule</code> <a class="header-anchor" href="#schedule-—-takoform-schedule" aria-label="Permalink to &quot;Schedule — `takoform_schedule`&quot;">​</a></h1><h2 id="workload-and-consumer" tabindex="-1">Workload and consumer <a class="header-anchor" href="#workload-and-consumer" aria-label="Permalink to &quot;Workload and consumer&quot;">​</a></h2><p>A team runs periodic work — nightly exports, digest kicks, cleanup — without owning a worker for the clock: the schedule itself delivers a declared message on a cron cadence, and consumers pull the work from the queue or subscription it lands in. Lifecycle is managed through the provider from birth.</p><h2 id="role" tabindex="-1">Role <a class="header-anchor" href="#role" aria-label="Permalink to &quot;Role&quot;">​</a></h2><p><code>identity</code>. Like SQLiteDatabase, the semantics are the identity — plus a small desired surface: <code>cron</code>, <code>target</code>, <code>retryPolicy</code>, <code>paused</code>. There is no revision or attachment split: the schedule is the whole running thing, so there is nothing a split would hold together.</p><h2 id="observable-semantics" tabindex="-1">Observable semantics <a class="header-anchor" href="#observable-semantics" aria-label="Permalink to &quot;Observable semantics&quot;">​</a></h2><p><code>cron</code> is the portable five-field cron grammar, interpreted in UTC only — the same grammar as the edge WorkerCronTrigger, restated exactly. There is no timezone field, so two conforming hosts can never fire the same schedule at different instants and no schedule skips or repeats an hour for a daylight-saving transition. The five fields are minute <code>0</code>-<code>59</code>, hour <code>0</code>-<code>23</code>, day-of-month <code>1</code>-<code>31</code>, month <code>1</code>-<code>12</code>, and day-of-week <code>0</code>-<code>6</code> with <code>0</code> Sunday. Each field is a comma-separated list of <code>*</code>, a literal, a range <code>low-high</code>, <code>*/step</code>, or <code>low-high/step</code>. Month and day names, and a step on a bare literal such as <code>5/10</code>, are not accepted. When day-of-month and day-of-week are both restricted the schedule fires on a day either selects; when only one is restricted, only that one constrains the day. A host PARSES the expression and refuses a value outside its field&#39;s range, an inverted range, or a step outside <code>1</code>..span before any mutation; the provider runs the same parser at plan time.</p><p><code>target</code> is a closed union with exactly one member: <code>queueMessage</code> (<code>{queue, body, attributes}</code> — send the declared message to a resource providing <code>queue.pull@1.0.0</code>; in MVP a <code>PullQueue</code>) or <code>topicPublish</code> (<code>{topic, body, attributes}</code> — publish it to a resource providing <code>topic.publish@1.0.0</code>; in MVP a <code>Topic</code>). Body and attributes use the queue family&#39;s message shape and bound. Each reference is the closed exact <code>{apiVersion, kind, name}</code> shape carrying its group explicitly, so the cross-family target is representable as-is (<a href="./../../spec/binding-contract/">binding contract</a>), and is UID-pinned: reference-failure semantics follow the Host API v1 relation rules (<code>ExternalChange</code> / <code>DependencyMissing</code>, no automatic re-bind, re-apply re-pins).</p><p>Firing: each matched window fires the target once, at-least-once — one matched window may fire twice, so downstream consumers must be idempotent. A failed attempt (the target refused or was unavailable) is retried under <code>retryPolicy</code>: <code>maxAttempts</code> (1..10, counting the first attempt) tries separated by <code>retryDelaySeconds</code> (0..3600, fixed delay). Attempts for one window are abandoned when the next matched window fires: the schedule never works two windows concurrently and never accumulates a backlog.</p><p>Misfire: if the scheduler was unavailable across a window, that window is SKIPPED, never replayed late. The stated bound: no first attempt for a window begins more than five minutes after its matched minute; a window whose first attempt has not begun by then never fires. There is no catch-up replay of missed windows.</p><p><code>paused</code>: while true, windows continue to match and are permanently skipped; unpausing fires nothing retroactively. Every desired field updates in place — the Form retains no data, so identity is the only thing replacement would lose.</p><h2 id="why-this-is-one-form" tabindex="-1">Why this is one Form <a class="header-anchor" href="#why-this-is-one-form" aria-label="Permalink to &quot;Why this is one Form&quot;">​</a></h2><p>Grammar, target, payload, and retry are one observable fact with one lifecycle. The UTC-only rule is the completeness boundary of decision 0008; splitting the target into an attachment would manufacture a parent with nothing of its own to hold.</p><h2 id="what-would-require-a-separate-form" tabindex="-1">What would require a separate Form <a class="header-anchor" href="#what-would-require-a-separate-form" aria-label="Permalink to &quot;What would require a separate Form&quot;">​</a></h2><p>Timezone-aware or calendar-aware scheduling (<code>TimezoneSchedule</code>); an HTTPS invocation target, deferred until the endpoint-authentication shape is fixed (the same caveat as TopicSubscription push delivery); catch-up or backfill replay of missed windows, which is a different firing contract.</p><h2 id="provided-interfaces" tabindex="-1">Provided Interfaces <a class="header-anchor" href="#provided-interfaces" aria-label="Permalink to &quot;Provided Interfaces&quot;">​</a></h2><p>None.</p><h2 id="accepted-bindings" tabindex="-1">Accepted Bindings <a class="header-anchor" href="#accepted-bindings" aria-label="Permalink to &quot;Accepted Bindings&quot;">​</a></h2><p>None.</p><h2 id="lifecycle-risks" tabindex="-1">Lifecycle risks <a class="header-anchor" href="#lifecycle-risks" aria-label="Permalink to &quot;Lifecycle risks&quot;">​</a></h2><p>Deleting the referenced queue or topic while a Schedule targets it must fail with <code>dependency_in_use</code>. A target deleted and re-created under the same name is a different incarnation: the schedule stays pinned, reports the condition per the Host API v1 relation rules, and only a re-apply re-pins it; while the condition stands, matched windows fire, fail, and are bound by the misfire rule — they are never queued for the repaired target.</p><h2 id="prior-art" tabindex="-1">Prior art <a class="header-anchor" href="#prior-art" aria-label="Permalink to &quot;Prior art&quot;">​</a></h2><p>The standalone cron scheduler offered by every major provider — the &quot;Scheduler&quot; survey row decision 0043 minted this family from. The withdrawn v1alpha2 <code>Schedule</code> kind, which this Form replaces with a complete firing contract. The Edge family&#39;s WorkerCronTrigger for contrast: an attachment invoking a worker&#39;s <code>scheduled</code> handler, sharing this Form&#39;s exact cron grammar, with the worker as the payload instead of a declared message.</p>',23)])])}const m=a(i,[["render",d]]);export{p as __pageData,m as default};
