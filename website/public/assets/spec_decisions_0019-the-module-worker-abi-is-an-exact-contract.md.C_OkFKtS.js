import{_ as t,o,c as a,ag as i}from"./chunks/framework.cyF4XceO.js";const u=JSON.parse('{"title":"0019 — The Module Worker ABI is an exact contract, not a date","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0019-the-module-worker-abi-is-an-exact-contract.md","filePath":"spec/decisions/0019-the-module-worker-abi-is-an-exact-contract.md"}'),n={name:"spec/decisions/0019-the-module-worker-abi-is-an-exact-contract.md"};function r(s,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="_0019-—-the-module-worker-abi-is-an-exact-contract-not-a-date" tabindex="-1">0019 — The Module Worker ABI is an exact contract, not a date <a class="header-anchor" href="#_0019-—-the-module-worker-abi-is-an-exact-contract-not-a-date" aria-label="Permalink to &quot;0019 — The Module Worker ABI is an exact contract, not a date&quot;">​</a></h1><ul><li>Status: accepted</li><li>Date: 2026-08-08</li><li>Owners: Takoform maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>ModuleWorker</code> claimed to fix &quot;the ES Module Worker ABI&quot; by identity, and nothing in the repository said what that ABI was. A conforming host had no statement of the module&#39;s default-export shape, the handler signatures, the request and response types, how bindings appear in <code>env</code>, what <code>waitUntil</code> means, what an uncaught exception does, whether bodies stream, which Web APIs must exist, or which module media types load. <a href="./0008-forms-preserve-service-shape.html">Decision 0008</a> says a Form fixes the complete application-visible semantics of one proven service primitive; for a worker the ABI <em>is</em> those semantics, and it was missing. Two hosts could implement <code>ModuleWorker</code> and run observably different runtimes while both passing every check the lane had.</p><p><code>WorkerVersion</code> made this worse rather than better. It carried <code>compatibilityDate</code> (a required calendar date) and <code>compatibilityFlags</code> (a closed set whose only member was <code>nodejs_compat</code>). A compatibility date selects default runtime behavior only against a registry that states which behavior each date changes. This project publishes no such registry and has no plan to own one. The field therefore promised portability it could not deliver: two conforming hosts reading <code>2026-01-01</code> could legitimately apply different defaults, which is precisely the incompleteness <a href="./../portability-boundary.html"><code>../portability-boundary.md</code></a> forbids and precisely the &quot;free semantic token&quot; shape decision 0008 prohibits. The repository already treated the same spelling as a leaked backend token elsewhere: the v1alpha2 vendor-token guard in <code>spec/contracts_test.go</code> lists <code>compatibility_date</code> and <code>compatibility_flags</code> alongside <code>cloudflare</code> and <code>wrangler</code>.</p><p>The five typed Bindings had a smaller version of the same gap. Each named the Interface operations it granted but never said what the consumer&#39;s code actually calls — no method names, argument types, return types, failure shape, or streaming behavior. A capability without a callable surface is not a runtime API.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="the-abi-is-an-exact-interface-contract-owned-by-moduleworker" tabindex="-1">The ABI is an exact Interface contract owned by <code>ModuleWorker</code> <a class="header-anchor" href="#the-abi-is-an-exact-interface-contract-owned-by-moduleworker" aria-label="Permalink to &quot;The ABI is an exact Interface contract owned by `ModuleWorker`&quot;">​</a></h3><p><code>worker.runtime@1.0.0</code> is a new exact Interface contract in the <code>interfaces.takoform.com/v1alpha1</code> lane, authored in <code>internal/edgeformcatalog</code> beside the other Interface definitions and distributed as a candidate under <code>interfaces/candidates/v1alpha1</code>. It fixes, as data:</p><ul><li><strong><code>loadModule</code></strong> — the module&#39;s default export is a plain object; each handler is an optional own property of it; a handler a version DECLARES must exist there or the version fails before traffic arrives. It also fixes the <strong>loadable</strong> media types (<code>application/javascript+module</code>, <code>text/plain</code>, <code>application/octet-stream</code>, <code>application/wasm</code>) and states that a WASM module&#39;s default export is a compiled <code>WebAssembly.Module</code>, never an instance, so imports stay the application&#39;s choice. Its <code>declaredHandlers</code> enum IS the closed handler vocabulary of the ABI, and it is the single source of truth for it: the <code>WorkerVersion</code> <code>handlers</code> enum is read back out of this contract rather than written a second time. Its <code>modules</code> and <code>auxiliaryModules</code> media-type enums are the single source of truth for the two module classes in the same way (see &quot;The loadable set is the manifest&#39;s set, minus what is never imported&quot; below).</li><li><strong><code>fetch</code></strong> — <code>fetch(request, env, ctx) -&gt; Response | Promise&lt;Response&gt;</code>, with the request/response types, <code>bodyStream</code>, and <code>handlerOutcome</code>.</li><li><strong><code>scheduled</code></strong> — <code>scheduled(event, env, ctx) -&gt; void | Promise&lt;void&gt;</code>; the event carries <code>scheduledTime</code> (UTC milliseconds) and the matched five-field <code>cron</code> expression.</li><li><strong><code>queue</code></strong> — <code>queue(batch, env, ctx) -&gt; void | Promise&lt;void&gt;</code>; the batch carries the queue name and ordered messages with <code>id</code>, <code>timestamp</code>, <code>body</code>, and <code>attempts</code>.</li><li><strong><code>environment</code></strong> — <code>env</code>&#39;s own enumerable properties are exactly the union of the version&#39;s <code>vars</code> keys, <code>requiredSensitiveVars</code> names, and binding names, and nothing else portable; a sensitive-variable slot appears as its host-supplied value.</li><li><strong><code>waitUntil</code></strong> — the host keeps the isolate alive until the registered promise settles; a rejection reaches host diagnostics only, never changes an already-sent response, and never fails a successful invocation.</li><li><strong><code>globals</code></strong> — the portable minimum Web API surface: <code>Request</code>, <code>Response</code>, <code>Headers</code>, <code>URL</code>, <code>URLSearchParams</code>, <code>TextEncoder</code>/<code>TextDecoder</code>, <code>crypto.getRandomValues</code>, <code>crypto.subtle.digest</code>, <code>structuredClone</code>, <code>AbortController</code>/<code>AbortSignal</code>, <code>ReadableStream</code>/<code>WritableStream</code>, the four timers, and <code>fetch</code>.</li></ul><p>Exception handling is fixed across the surface: an uncaught throw or unhandled rejection in <code>fetch</code> is a host-generated 500 response, never a hung request; in <code>scheduled</code> and <code>queue</code> it is a failed invocation, and a failed <code>queue</code> invocation redelivers the batch under the at-least-once delivery <code>edge.queue</code> already states and the retry policy of the invoking <code>QueueConsumer</code>.</p><p>The contract is <strong>provided by <code>ModuleWorker</code></strong>, the identity, because the identity is what a host implements; a <code>WorkerVersion</code> is the code that fills it. The published Interface name grammar admits no hyphen, so the contract is named <code>worker.runtime</code>, matching the family&#39;s other worker-scoped Interface <code>worker.service</code>; the hyphenated <code>module-worker.</code> prefix stays in the Binding namespace, where the grammar allows it.</p><p>Rules that are behavioral rather than structural are stated as normative sentences in the definition&#39;s descriptions and proven by fixtures wherever a fixture can prove them — the placement model of <a href="./0014-published-schemas-are-structural-minima.html">decision 0014</a>. Nothing about this contract required a new schema identity.</p><h3 id="the-loadable-set-is-the-manifest-s-set-minus-what-is-never-imported" tabindex="-1">The loadable set is the manifest&#39;s set, minus what is never imported <a class="header-anchor" href="#the-loadable-set-is-the-manifest-s-set-minus-what-is-never-imported" aria-label="Permalink to &quot;The loadable set is the manifest&#39;s set, minus what is never imported&quot;">​</a></h3><p>The ABI&#39;s first statement of its loadable media types was written beside, not against, the artifact manifest&#39;s — and the two did not agree. The manifest admitted <code>application/source-map+json</code>, which no runtime loads; the ABI claimed <code>application/json</code>, which the published manifest enum never admitted. A bundle could therefore be accepted by one and unusable by the other, and nothing discovered it until deploy: a Worker built out of modules the runtime refuses.</p><p>The reconciled set is one statement in two classes:</p><ul><li><strong>loadable</strong> — <code>application/javascript+module</code>, <code>text/plain</code>, <code>application/octet-stream</code>, <code>application/wasm</code>. What the module graph may import, and the only class a bundle&#39;s <code>mainModule</code> may name.</li><li><strong>auxiliary</strong> — <code>application/source-map+json</code>. What a bundle may carry and the graph never imports. Refused as <code>mainModule</code>, refused as an import target (<code>unsupported_media_type</code>), and never an error merely for being present.</li></ul><p><code>application/json</code> is not supported in this ABI version. Removing it is the narrowing direction decision 0014 permits: the published manifest enum is the union of the two classes above, so the code narrows onto the published bytes rather than past them, and a widening later would be a contract change with a new digest like any other.</p><p>The set has ONE source of truth — <code>internal/currentformmodel</code> — which the runtime contract&#39;s two enums, the host&#39;s manifest validator, and the provider&#39;s authoring allowlist all read rather than restate. A drift gate holds it to the published manifest enum, and the required conformance check <code>bundle-main-module-is-loadable</code> proves the half a schema cannot state: the published enum lists all five media types in one place and cannot relate <code>mainModule</code> to the media type of the module it names, so only a host stops a bundle whose first module is evidence rather than code (<a href="./0014-published-schemas-are-structural-minima.html">decision 0014</a>, and <a href="./0012-artifacts-use-content-addressed-upload.html">decision 0012</a> for the manifest side).</p><h3 id="tail-is-removed-and-every-declared-handler-is-measured" tabindex="-1"><code>tail</code> is removed, and every declared handler is measured <a class="header-anchor" href="#tail-is-removed-and-every-declared-handler-is-measured" aria-label="Permalink to &quot;`tail` is removed, and every declared handler is measured&quot;">​</a></h3><p>The vocabulary above no longer contains <code>tail</code>. It was declared, and nothing in the Edge Platform Family could activate it: the inward-activation attachments are <code>WorkerCustomDomain</code> (<code>fetch</code>), <code>WorkerCronTrigger</code> (<code>scheduled</code>), <code>QueueConsumer</code> (<code>queue</code>), and <code>WorkerEndpoint</code> (<code>fetch</code>). No portable deployment could cause a host to invoke it, so no run could observe it and two hosts could implement it differently with nothing able to detect the divergence — which is the <code>compatibilityDate</code> defect in a different member. <a href="./0023-the-runtime-abi-is-measured-separately-from-the-control-plane.html">Decision 0023</a> recorded the removal as the recommended fix and deferred it on sequencing, because the Interface bytes are the input to the generated Form chain; this is that change. Blocker <strong>V3-011</strong> closes with it.</p><p>Adding a <code>WorkerTailConsumer</code> instead would have made the handler observable and is the wrong move now: it widens the family by a Form nothing else asks for, and it decides the trace model — what an event carries, what sampling means, what a failed consumer does to the traced invocation — inside a change whose subject is the ABI. <strong><code>tail</code> returns together with the attachment that makes it observable and a NEW exact <code>worker.runtime</code> version, never as a bare handler.</strong> Under the rule this decision already states, a runtime revision IS a new exact contract version, so re-adding it is a version bump with a new digest every consumer sees, not a quiet widening.</p><p>What replaces the handler is a property: <strong>every handler the ABI declares is measured.</strong> That is a claim worth nothing as prose, so the runtime corpus ENFORCES it. Its loader refuses a corpus in which a member of <code>handlerVocabulary</code> — which is read out of this contract&#39;s own <code>declaredHandlers</code> enum — has no check whose <code>operation</code> names it, and an <code>unmeasured</code> entry does not discharge one, because recording a handler as unmeasured is exactly what the corpus did for <code>tail</code>. Widening the enum without measuring the addition therefore fails corpus verification by name rather than waiting for review. The <code>unmeasured</code> mechanism itself stays: the load lane still reports itself unmeasured when an operator supplies no module-loader adapter, and a future surface that genuinely cannot be observed can still say so — what it can no longer do is stand in for a handler.</p><h3 id="compatibilitydate-and-compatibilityflags-are-removed" tabindex="-1"><code>compatibilityDate</code> and <code>compatibilityFlags</code> are removed <a class="header-anchor" href="#compatibilitydate-and-compatibilityflags-are-removed" aria-label="Permalink to &quot;`compatibilityDate` and `compatibilityFlags` are removed&quot;">​</a></h3><p>Both fields, and the <code>nodejs_compat</code> enum, are deleted from <code>WorkerVersion</code> in the catalog, the generated Form Definition and fixtures, the provider schema, the generated docs and examples, the conformance probe, and the Host Support Profile (<code>supportedRanges.compatibilityDate</code>, <code>supportedEnums.compatibilityFlags</code>). A conforming host MUST NOT advertise either.</p><p><strong>A runtime revision is a new exact Interface version, and — if it changes what a Form desires — a new Form version. It is never a date and never a flag.</strong> This is stated normatively in <a href="./../form-families.html"><code>../form-families.md</code></a>, in <code>../host-api/v1alpha3.md</code>, and in <a href="./../interface-contract/"><code>../interface-contract/</code></a>.</p><h3 id="bindings-state-the-surface-the-caller-calls" tabindex="-1">Bindings state the surface the caller calls <a class="header-anchor" href="#bindings-state-the-surface-the-caller-calls" aria-label="Permalink to &quot;Bindings state the surface the caller calls&quot;">​</a></h3><p>Every Binding Definition&#39;s <code>description</code> now states the JavaScript surface it projects: the method names on <code>env.NAME</code>, argument and return types, how each Interface error code appears in JavaScript, and whether bodies stream. <code>module-worker.service</code> projects <code>env.NAME.fetch(request) -&gt; Promise&lt;Response&gt;</code>, streaming in both directions, resolving with the callee&#39;s host-generated 500 when the callee&#39;s handler throws — the promise rejects only when the call could not be made at all. The meta-schema&#39;s <code>runtimeProjection</code> member admits operation names only, so the projection prose lives in <code>description</code>, again per decision 0014.</p><h3 id="conformance" tabindex="-1">Conformance <a class="header-anchor" href="#conformance" aria-label="Permalink to &quot;Conformance&quot;">​</a></h3><p>Four required checks join the v1alpha3 runner list:</p><ul><li><code>module-worker-runtime-contract-advertised</code> — the host serves the runtime contract&#39;s support profile at the exact pinned <code>schemaDigest</code> and supports the exact <code>ModuleWorker</code> Form line. Because the ModuleWorker Form Definition&#39;s own digest covers its <code>providedInterfaces</code>, a host serving a definition that omits or moves the runtime ref already fails <code>form-definition-exact</code>.</li><li><code>undeclared-runtime-handler-rejected</code> — a <code>WorkerVersion</code> declaring a handler the runtime contract does not define is refused at both pre-mutation gates (<code>validate</code> and <code>prepare</code>) and stores nothing, while a version declaring only defined handlers is still accepted.</li><li><code>declared-handler-not-exported-rejected</code> — a <code>WorkerVersion</code> declaring a handler the ABI <em>does</em> define, against a bundle whose main module does not export it, is refused before any mutation and stores nothing, while a version declaring only exported handlers is still accepted. <code>loadModule</code> fails such a version with <code>handler_not_exported</code> before traffic arrives, so a host that stored it would go on gating attachments against a handler that does not exist. Unlike the check above, this one is not decidable from the spec: it reads the bundle relation, the committed manifest, and the module&#39;s content address, so it lives on the mutation path beside the other Worker aggregate rules.</li><li><code>bundle-main-module-is-loadable</code> — a <code>WorkerBundle</code> manifest whose <code>mainModule</code> names an auxiliary module is refused, while the same module set with a loadable <code>mainModule</code> commits with the auxiliary module still in it. One module set drives both directions, so the only difference between the refusal and the acceptance is which module <code>mainModule</code> names.</li></ul><p><code>support-profiles-present</code> additionally now fails a host that advertises a <code>compatibilityDate</code> range or a <code>compatibilityFlags</code> enum, or whose <code>handlers</code> enum is not exactly the contract&#39;s vocabulary.</p><p>The corpus states what each pinned module exports, because a check pairing a declaration with a bundle that cannot answer it would fail exactly the hosts that implement the contract — a required check no correct host can pass is worse than a missing one. <code>conformance/portable-host-v3</code> therefore carries two bundles: the probe bundle, whose module exports the whole vocabulary and which every positive control is driven against, and one fetch-only bundle that exists so the refusal above can be driven at all. Loading the corpus fails if either stops being what the lane needs it to be.</p><p>The lane is a Host API runner, so it proves what a host advertises and what it refuses, never what an isolate does. <code>../host-api/v1alpha3.md</code> states the split explicitly: handler signatures, streaming bodies, the exact <code>env</code> property set, <code>waitUntil</code>, exception outcomes, the <code>globals</code> floor, and <code>handler_not_exported</code> for arbitrary bytes remain obligations proven by the contract&#39;s behavior fixtures against a real runtime.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>ModuleWorker</code> and <code>WorkerVersion</code> Form Definition bytes change, so their <code>schemaDigest</code> and package digests move, and the whole generated chain — candidates, provider v3 registry, conformance corpus pins, docs, examples, website mirrors — is regenerated. No published artifact changes: the family lane is unpublished.</li><li>A host can no longer claim &quot;runs module workers&quot; without saying which runtime. The claim is one digest, and the conformance lane reads it back.</li><li>The <code>handlers</code> vocabulary has exactly one source of truth. Widening it is a contract change with a new digest, which every consumer sees, rather than a quiet host decision.</li><li>So does the module media-type set. Four surfaces state it and none of them owns a list: the contract, the manifest schema gate, the host validator, and the authoring allowlist all resolve to one declaration, so a bundle a host commits is a bundle this runtime can load, by construction rather than by review.</li><li>An asset-serving worker and a sub-hourly cron remain unexpressible; those are separate blockers with their own decisions. Nothing here widens them.</li><li><code>WorkerVersion</code> loses its only <code>date-string</code> field. The authoring model keeps the kind, because it describes a shape a future Form may legitimately need; what it must never again describe is a runtime selector.</li><li>The handler vocabulary is three, not four. <code>WorkerVersion</code>&#39;s <code>handlers</code> enum, the runtime corpus&#39;s <code>handlerVocabulary</code>, the Host Support Profile a host advertises, and the reference module every positive control is driven against all follow from the one declaration, so none of them had to be edited twice — and the corpus&#39;s own reference module stopped exporting a handler the ABI no longer defines, because a module claiming an export the contract does not declare is refused at load.</li><li><code>conformance/runtime-abi-v1</code> goes from seventeen checks to eighteen: <code>tail</code>&#39;s unmeasured entry leaves, and two worker-to-worker streaming checks arrive (<a href="./0020-the-edge-interfaces-state-their-data-and-delivery-model.html">decision 0020</a>). Its self-test is now <code>complete</code> rather than <code>partial</code> whenever the load lane is measured, which is the observable difference between a matrix with a hole in it and one without.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep <code>compatibilityDate</code> and build a behavior registry.</strong> This is the only way the field could have meant something: a published, versioned, digest-bound document mapping each date to the exact set of behavior changes it turns on, plus a rule for what a host does with a date it does not know, plus conformance proving two hosts apply the same date identically. That is a second normative product line — its own schema identity, its own publication and freeze policy, its own conformance corpus — owned by a project that deliberately does not own runtime implementations. It is also strictly larger than the problem: dates exist to let ONE vendor evolve ONE runtime without breaking deployed code, whereas Takoform&#39;s job is to make independent hosts agree. Rejected for now, not forever: if the family ever ships a runtime revision that must coexist with its predecessor inside one host, this is the design to revisit, and the exact-contract model composes with it — a registry would map dates to contract versions rather than replacing them.</li><li><strong>Keep the date as an opaque host hint.</strong> Rejected because the v1alpha3 lane forbids open tokens outright (decision 0008): a field whose meaning each host decides privately is the exact thing that makes one Form mean many things. An &quot;advisory&quot; token is worse than an absent one, because authors would write it, hosts would honor it differently, and the divergence would be invisible to every check in the lane.</li><li><strong>Describe the ABI in prose only</strong> — in the proposal, the Form description, or a specification page. Rejected because prose is not held to a digest. A host cannot advertise conformance to a paragraph, <code>form-definition-exact</code> cannot detect a host that read it differently, and the two required checks above would have nothing to compare. The ABI is the contract, so it has to be a contract.</li><li><strong>Put the ABI on <code>WorkerVersion</code> instead of <code>ModuleWorker</code>.</strong> Rejected because a version is code, not an implementation of the runtime: a host implements the ABI once, for the identity every deployment and attachment addresses. Declaring it per revision would also make the ABI something a Terraform author appears to choose.</li><li><strong>Mint a <code>runtimes.takoform.com</code> document kind for the ABI.</strong> Rejected under decision 0014: the published interface-definition meta-schema already admits operations with input/output schemas, closed errors, semantics, limits, fixtures, and descriptions, which is enough to express every rule above. A new identity would have bought vocabulary, not closure.</li></ul>',38)])])}const p=t(n,[["render",r]]);export{u as __pageData,p as default};
