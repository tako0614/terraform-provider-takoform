import{_ as i,o as t,c as a,ag as o}from"./chunks/framework.cyF4XceO.js";const h=JSON.parse('{"title":"0057 — Specification 1.1 freezes compatibility and independent identities","description":"","frontmatter":{},"headers":[],"relativePath":"spec/decisions/0057-specification-1-1-compatibility-and-independent-identities.md","filePath":"spec/decisions/0057-specification-1-1-compatibility-and-independent-identities.md"}'),n={name:"spec/decisions/0057-specification-1-1-compatibility-and-independent-identities.md"};function r(s,e,c,d,l,p){return t(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="_0057-—-specification-1-1-freezes-compatibility-and-independent-identities" tabindex="-1">0057 — Specification 1.1 freezes compatibility and independent identities <a class="header-anchor" href="#_0057-—-specification-1-1-freezes-compatibility-and-independent-identities" aria-label="Permalink to &quot;0057 — Specification 1.1 freezes compatibility and independent identities&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted for the W09 Specification 1.1 release. This decision supersedes the numbered-release wording in decisions 0052, 0053, and 0055 where those records refer to Specification 1.0 or require implementation evidence. Those records remain historical decisions; the current machine authority is <a href="../publication-evidence.json"><code>../publication-evidence.json</code></a>, <a href="../../release/specification-releases.json"><code>../../release/specification-releases.json</code></a>. The generated <a href="../../release/specification-compatibility.json"><code>../../release/specification-compatibility.json</code></a> is a separate compatibility report and is not release authority.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The first numbered Specification release is <strong>1.1</strong>. Identity <code>1.0</code> was never published; it is withdrawn and retained as history and MUST never be reused. A future W10 Core owner may become the writer for future releases, but MUST NOT reissue, rewrite, or retag Specification 1.1.</p><p>The current repository remains the authority for publishing this identity: <code>https://github.com/tako0614/terraform-provider-takoform.git</code>. The transfer of future writer authority is a later operation. C1 does not create an SDK tag or publish the future module, whose eventual Go coordinate is <code>github.com/tako0614/takoform</code>.</p><h3 id="compatibility-and-identity" tabindex="-1">Compatibility and identity <a class="header-anchor" href="#compatibility-and-identity" aria-label="Permalink to &quot;Compatibility and identity&quot;">​</a></h3><p>Specification 1.1 has no Host API, Form-package, or Provider effect: <code>hostApiEffect</code>, <code>formPublicationEffect</code>, and <code>providerEffect</code> are all <code>none</code>. The literal Host API v1 bytes remain a separate unpublished candidate. Every current FormRef, package digest, family identity, Interface, Binding, artifact transport identity, standard-service slot, and Provider identity remain unchanged. Publishing Specification 1.1 does not publish or promote Host API v1. There is no <code>/v1.1</code> Host lane, v2 schema, v2 tag, or v2 receipt.</p><p>The generated compatibility manifest is exactly five classes:</p><ol><li>Form and Package;</li><li>Host API lifecycle;</li><li>Form Family and Host Support;</li><li>Interface, Binding, artifact transport, and standard service; and</li><li>trust, revocation, lifecycle, version, and release identities.</li></ol><p>Every entry has a status (<code>retained</code>, <code>new-independent</code>, <code>unpublished-candidate</code>, or <code>withdrawn-retained</code>), raw source-byte digests, an owning ledger, and an explicit migration disposition. The manifest byte-pins the literal Host API v1 source set. Current candidate Forms, candidate packages, and the literal Host API v1 remain <code>unpublished-candidate</code>. Withdrawn document and schema lanes remain <code>withdrawn-retained</code>; every published Provider identity from 1.0.1 through 3.0.0 remains <code>retained</code>. A manifest is compatibility evidence, not publication evidence, a release asset, a prerequisite, or a publication receipt.</p><h3 id="publisher-trust-and-provenance" tabindex="-1">Publisher, trust, and provenance <a class="header-anchor" href="#publisher-trust-and-provenance" aria-label="Permalink to &quot;Publisher, trust, and provenance&quot;">​</a></h3><p>Official and external publishers are equal at the Form and Package contract boundary. “Official” is a publisher/source designation, not a stronger FormRef or schema rule. An operator explicitly selects the trusted source, issuer, signature/revocation policy, and host support policy. Takoform does not infer trust from a product name, a deployment, a Terraform Provider, or a catalog.</p><p>Provenance is required for admission and publication, but it is deliberately outside FormRef equality. A FormRef is exactly its group, kind, definitionVersion, and schemaDigest; publisher/source/issuer/provenance facts MUST NOT be smuggled into that equality key.</p><p>One closed data-only Form Package contains exactly one Form Definition and its content-addressed package files. It does not contain executable code, a host implementation, a Provider, a generic resource, or a second Form. Package install does not itself grant Host Support, activation, or a Service Offering.</p><h3 id="revocation-and-lifecycle" tabindex="-1">Revocation and lifecycle <a class="header-anchor" href="#revocation-and-lifecycle" aria-label="Permalink to &quot;Revocation and lifecycle&quot;">​</a></h3><p>Package revocation is append-only and applies to the exact package identity. After a valid revocation is observed, create, update, and activation MUST stop. Observe/read, delete, and an explicit operator evacuation path remain available so an operator can inspect and safely remove retained state. A revocation does not rewrite package bytes or erase a Resource.</p><p>Authored, verified, packaged, published, installed, supported, activated, provisioned, client-supported, and offered are independent lifecycle facts. Closing one fact MUST NOT imply another. In particular, Specification release does not publish a package, support a Form, activate a Resource, or establish a commercial offering.</p><h3 id="authoring-provider-and-evolution" tabindex="-1">Authoring, Provider, and evolution <a class="header-anchor" href="#authoring-provider-and-evolution" aria-label="Permalink to &quot;Authoring, Provider, and evolution&quot;">​</a></h3><p>An external author follows the same public authoring path as an official author and MUST import only public Core contracts; internal Provider or repository packages are not part of the path. The future Core module is <code>github.com/tako0614/takoform</code>, but no SDK or CLI publication is prepared in C1.</p><p>Provider 3 remains a typed, non-normative implementation projection. It may support only explicitly compiled typed Forms and MUST NOT add an opaque generic JSON resource as a substitute for typed schema/state contracts.</p><p>Form and package releases evolve independently of the numbered Specification and Provider releases. Decision 0041&#39;s Provider-embedded package rule is historical and superseded for current releases; a package publisher owns its own publication decision and cadence. An existing Form/package evolution mechanism (new definitionVersion and digest, with explicit migration and retained identity rules) is sufficient; changing a Form does not require a Specification or Provider bump when the Host API and package contract remain compatible. No fixed package cadence has been adopted.</p><p>Release cadence is a <strong>Proposal</strong> only. No annual/major cadence, independent Form cadence, or Provider/Host schedule is a publication prerequisite until a later decision records owner, evidence, and migration rules.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The W09 release ledger begins with one 1.1 candidate and an empty <code>releases</code> array. The sole prerequisite is <code>specification-source-snapshot</code>; publication evidence for Forms, the reference Host, and Provider 3 remains independently visible and unprepared. C1 freezes the normative source and executable tooling while the evidence record is empty. C2 records only the exact source-snapshot evidence and its two byte-identical website projections. C3 appends only the authoritative publication receipt and its two byte-identical projections. C4, as the direct child of C3, refreshes only deterministic non-authoritative public truth such as current-generation text, the compatibility report, site status, and website output. The compatibility report never enters C2, C3, the release asset set, or release authority.</p><p>C3 is intentionally not a green final tree because the receipt changes the derived Specification status before those non-authoritative projections are regenerated. C3 and C4 must be reviewed and pushed together without squashing, leaving exact C3 visible in history and the branch head green at C4. C4 must not change normative <code>spec/**</code>, the source evidence, the release ledger, or either C3 ledger projection. A create-only release surface may prepare and publish 1.1 only from one exact reviewed W09 owner commit and must fail closed on a pre-existing 1.1 identity. C1 does not execute that surface. W10 may own future releases but must never reissue, rewrite, or retag 1.1.</p>',26)])])}const f=i(n,[["render",r]]);export{h as __pageData,f as default};
