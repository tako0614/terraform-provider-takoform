{
  "apiVersion": "forms.takoform.com/v1alpha1",
  "kind": "StatefulActorNamespace",
  "definitionVersion": "0.0.0-legacy.1",
  "title": "Legacy Stateful Actor Namespace",
  "description": "Compatibility candidate for namespace-level actor lifecycle. Individual actor instances are runtime state and are not desired resources.",
  "status": "compatibility-candidate",
  "desiredSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "required": ["name", "className"],
    "properties": {
      "name": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": ".*\\S.*" },
      "className": { "type": "string", "pattern": "^[A-Za-z_$][A-Za-z0-9_$]*$" },
      "storageProfile": {
        "type": "string",
        "pattern": "^[A-Za-z][A-Za-z0-9._:-]{0,127}$",
        "default": "durable_sqlite"
      },
      "migrationTag": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": ".*\\S.*" },
      "connections": { "$ref": "#/$defs/connections" }
    },
    "$defs": {
      "connections": {
        "type": "object",
        "propertyNames": {
          "type": "string",
          "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
          "x-takoform-fieldPolicy": "portable-data-only-v1"
        },
        "additionalProperties": { "$ref": "#/$defs/connection" }
      },
      "connection": {
        "type": "object",
        "additionalProperties": false,
        "required": ["resource", "permissions", "projection"],
        "properties": {
          "resource": { "type": "string", "pattern": "^\\S+$" },
          "permissions": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9._:-]{0,127}$" }
          },
          "projection": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9._:-]{0,127}$" }
        }
      }
    }
  },
  "observedSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "immutableFields": ["/name"],
  "lifecycleCapabilities": ["create", "update", "observe", "delete", "import"],
  "conformanceFixtures": [
    { "name": "legacy-current", "desiredPath": "fixtures/desired.json" }
  ]
}
