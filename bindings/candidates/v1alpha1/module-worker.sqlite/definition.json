{
  "apiVersion": "bindings.takoform.com/v1alpha1",
  "kind": "BindingDefinition",
  "name": "module-worker.sqlite",
  "version": "1.0.0",
  "title": "Module Worker SQLite binding",
  "description": "Projects the complete edge.sql runtime API into a Worker Version under one JavaScript-identifier binding name, without exposing credentials or connection material. Runtime surface: env.NAME is an object with the methods execute(sql, params?), query(sql, params?), and transaction(statements), each returning a promise. params is an array of TAGGED edge.sql values bound positionally, so a 64-bit INTEGER (decimal text) and a BLOB (base64) survive the round trip that an untagged JSON scalar would have changed. Every one of the three resolves to the same statement-result shape — rows, rowsWritten, and lastInsertRowId — and transaction resolves to one such result per statement, in statement order, so a SELECT inside a transaction returns its rows without leaving the transaction. Statements apply atomically under serializable isolation, so a rejected transaction has applied nothing at all. Errors reject with an Error whose name is the edge.sql error code; busy is the retryable one, and a caller that means to retry must re-run the whole call.",
  "sourceRole": "revision",
  "targetInterface": {
    "apiVersion": "interfaces.takoform.com/v1alpha1",
    "name": "edge.sql",
    "version": "1.0.0",
    "schemaDigest": "sha256:a1dcc648fb5f9f35e181cab455bbe373810876e322b1bd39ea470e4f11e05b27"
  },
  "allowedTargetForms": [
    {
      "apiVersion": "edge.forms.takoform.com/v1alpha1",
      "kind": "SQLiteDatabase"
    }
  ],
  "bindingNameGrammar": "^[A-Za-z_$][A-Za-z0-9_$]*$",
  "runtimeProjection": {
    "operations": [
      "execute",
      "query",
      "transaction"
    ]
  },
  "lifecycle": {
    "targetDeletion": "refuse_while_bound"
  }
}
